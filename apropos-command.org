Type RET on an entry to view its full documentation.

display-buffer		      M-x ... RET
  Display BUFFER-OR-NAME in some window, without selecting it.
isearch-forward		      <menu-bar> <edit> <i-search> <isearch-forward>, s-f, C-s
  Do incremental search forward.
  Value: t
sort-regexp-fields	      M-x ... RET
  Sort the text in the region lexicographically.
transient-mark-mode	      <menu-bar> <options> <transient-mark-mode>
  Toggle Transient Mark mode.
  Value: t
xref-query-replace-in-results M-x ... RET
  Perform interactive replacement of FROM with TO in all displayed
  xrefs.
5x5			      <menu-bar> <tools> <games> <5x5>
  Play 5x5.
gdb			      <menu-bar> <tools> <gdb>
  Run gdb passing it COMMAND-LINE as arguments.
man			      M-x ... RET
  Get a Un*x manual page and put it in a buffer.
erc			      M-x ... RET
  ERC is a powerful, modular, and extensible IRC client.
eww			      M-x ... RET
  Fetch URL and render the page.
cd			      M-x ... RET
  Make DIR become the current buffer's default directory.
dig			      M-x ... RET
  Query addresses of a DOMAIN using dig.
jdb			      M-x ... RET
  Run jdb with command line COMMAND-LINE in a buffer.
xdb			      M-x ... RET
  Run xdb on program FILE in buffer *gud-FILE*.
mail			      M-x ... RET
  Edit a message to be sent.  Prefix arg means resume editing (don't
  erase).
yank			      <menu-bar> <edit> <paste>, S-<insert>, S-<insertchar>, s-v, C-y
  Reinsert ("paste") the last stretch of killed text.
grep			      <menu-bar> <tools> <grep>
  Run Grep with user-specified COMMAND-ARGS.
info			      <help> i, <f1> i, s-?
  Enter Info, the documentation browser.
  Value: ((youtube-sub-extractor emacs) (link-hint avy emacs) (epkg emacs compat clos…
gnus			      <menu-bar> <tools> <gnus>
  Read network news.
pdb			      M-x ... RET
  Run COMMAND-LINE in the `*gud-FILE*' buffer to debug Python
  programs.
undo			      <menu-bar> <edit> <undo>, C-/, <undo>, s-z, C-_
  Undo some previous changes.
diff			      M-x ... RET
  Find and display the differences between OLD and NEW files.
life			      <menu-bar> <tools> <games> <life>
  Run Conway's Life simulation.
ping			      M-x ... RET
  Ping HOST.
zone			      <menu-bar> <tools> <games> <zone>
  Zone out, completely.
dbx			      M-x ... RET
  Run dbx on program FILE in buffer *gud-FILE*.
ffap			      M-x ... RET
  Find FILENAME, guessing a default from text around point.
pong			      <menu-bar> <tools> <games> <pong>
  Play pong and waste time.
pwd			      M-x ... RET
  Show the current default directory.
sdb			      M-x ... RET
  Run sdb on program FILE in buffer *gud-FILE*.
irc			      M-x ... RET
  Connect to all servers in `rcirc-server-alist'.
occur			      M-s o
  Show all lines in the current buffer containing a match for REGEXP.
snake			      <menu-bar> <tools> <games> <snake>
  Play the Snake game.
align			      M-x ... RET
  Attempt to align a region based on a set of alignment rules.
eglot			      <menu-bar> <tools> <eglot>
  Start LSP server in support of PROJECT's buffers under
  MANAGED-MAJOR-MODE.
lgrep			      M-x ... RET
  Run grep, searching for REGEXP in FILES in directory DIR.
rgrep			      <menu-bar> <tools> <rgrep>
  Recursively grep for REGEXP in FILES in directory tree rooted at
  DIR.
rmail			      M-x ... RET
  Read and edit incoming mail.
shell			      <menu-bar> <tools> <shell-commands> <interactive-shell>
  Run an inferior shell, with I/O through BUFFER (which defaults to
  `*shell*').
dired			      <menu-bar> <file> <dired>, s-D
  "Edit" directory DIRNAME--delete, rename, print, etc. some files in
  it.
rsh			      M-x ... RET
  Open a network login connection to host named HOST (a string).
term			      M-x ... RET
  Start a terminal-emulator in a new buffer.
ielm			      <menu-bar> <emacs-lisp> <Interactive Expression Evaluation>
  Interactively evaluate Emacs Lisp expressions.
esup			      M-x ... RET
  Profile the startup time of Emacs in the background.
mkdir			      M-x ... RET
  Create the directory DIR and optionally any nonexistent parent dirs.
woman			      M-x ... RET
  Browse UN*X man page for TOPIC (Without using external Man program).
dirs			      M-x ... RET
  Resync the buffer's idea of the current directory stack.
chmod			      M-x ... RET
  Set mode bits of file named FILENAME to MODE (an integer).
sql-ms			      M-x ... RET
  Run osql by Microsoft as an inferior process.
tetris			      <menu-bar> <tools> <games> <tetris>
  Play the Tetris game.
c-mode			      M-x ... RET
  Major mode for editing C code.
gomoku			      <menu-bar> <tools> <games> <gomoku>
  Start a Gomoku game between you and Emacs.
repeat			      M-x ... RET
  Repeat most recently executed command.
tumme			      M-x ... RET
  Make a thumbnail buffer for all images in DIR and display it.
debug			      M-x ... RET
  Enter debugger.  \<debugger-mode-map>`\[debugger-continue]' returns
  from the debugger.
eshell			      M-x ... RET
  Create an interactive Eshell buffer.
ialign			      M-x ... RET
  Interactively align region BEG END using regexp read from
  minibuffer.
locate			      M-x ... RET
  Run the program `locate', putting results in `*Locate*' buffer.
proced			      M-x ... RET
  Generate a listing of UNIX system processes.
setenv			      M-x ... RET
  Set the value of the environment variable named VARIABLE to VALUE.
widen			      M-x ... RET
  Remove restrictions (narrowing) from current buffer.
getenv			      M-x ... RET
  Get the value of environment variable VARIABLE.
magit			      M-x ... RET
  An alias for `magit-status' for better discoverability.
perldb			      M-x ... RET
  Debug a perl program with gud.
vc-dir			      M-x ... RET
  Show the VC status for "interesting" files in and below DIR.
d2-mode			      M-x ... RET
  (not documented)
diary			      M-x ... RET
  Generate the diary window for ARG days starting with the current
  date.
ediff			      M-x ... RET
  Run Ediff on a pair of files, FILE-A and FILE-B.
edirs			      M-x ... RET
  Run Ediff on directories DIR1 and DIR2, comparing files.
grugru			      M-x ... RET
  Rotate thing on point, if it is in `grugru-*-grugru-alist'.
telnet			      M-x ... RET
  Open a network login connection to host named HOST (a string).
mines			      M-x ... RET
  Play the minesweeper game.
sh-mode			      M-x ... RET
  Major mode for editing shell scripts.
tabify			      M-x ... RET
  Convert multiple spaces in region to tabs when possible.
whois			      M-x ... RET
  Send SEARCH-STRING to server defined by the `whois-server-name'
  variable.
bubbles			      <menu-bar> <tools> <games> <bubbles>
  Play Bubbles game.
cookie			      M-x ... RET
  Return a random phrase from PHRASE-FILE.
erc-tls			      M-x ... RET
  ERC is a powerful, modular, and extensible IRC client.
ert			      M-x ... RET
  Run the tests specified by SELECTOR and display the results in a
  buffer.
finger			      M-x ... RET
  Finger USER on HOST.
ispell			      s-:
  Interactively check a region or buffer for spelling errors.
rcirc			      M-x ... RET
  Connect to all servers in `rcirc-server-alist'.
sql-db2			      M-x ... RET
  Run db2 by IBM as an inferior process.
compile			      <menu-bar> <tools> <compile>
  Compile the program including the current buffer.  Default: run
  `make'.
gud-gdb			      M-x ... RET
  Run gdb passing it COMMAND-LINE as arguments.
ibuffer			      M-x ... RET
  Begin using Ibuffer to edit a list of buffers.
ps-mode			      M-x ... RET
  Major mode for editing PostScript with GNU Emacs.
vc-pull			      M-x ... RET
  Update the current fileset or branch.
vc-push			      <menu-bar> <tools> <vc> <vc-push>
  Push the current branch.
apropos			      <menu-bar> <help-menu> <search-documentation> <find-any-object-by-name>
  Show all meaningful Lisp symbols whose names match PATTERN.
ignore			      <right-divider> <mouse-1>, <bottom-divider> <mouse-1>, <left-edge> <mouse-1>, <top-left-corner> <mouse-1>, <top-edge> <mouse-1>, <top-right-corner> <mouse-1>, <right-edge> <mouse-1>, <bottom-right-corner> <mouse-1>, <bottom-edge> <mouse-1>, <bottom-left-corner> <mouse-1>, <touch-end>, C-M-<drag-mouse-1>
  Do nothing and return nil.
m2-mode			      M-x ... RET
  This is a mode intended to support program development in Modula-2.
rzgrep			      M-x ... RET
  Recursively grep for REGEXP in gzipped FILES in tree rooted at DIR.
up-list			      <menu-bar> <emacs-lisp> <Navigation> <Up List>
  Move forward out of one level of parentheses.
zrgrep			      M-x ... RET
  Recursively grep for REGEXP in gzipped FILES in tree rooted at DIR.
epatch			      M-x ... RET
  Query for a file name, and then run Ediff by patching that file.
fortune			      M-x ... RET
  Display a fortune cookie.
newline			      RET
  Insert a newline, and move to left margin of the new line.
so-long			      M-x ... RET
  Invoke `so-long-action' and run `so-long-hook'.
tab-new			      <menu-bar> <file> <make-tab>
  Create a new tab ARG positions to the right.
tldr			      M-x ... RET
  Lookup TLDR docs.
vc-diff			      <menu-bar> <tools> <vc> <vc-diff>
  Display diffs between file revisions.
acm-hide		      M-x ... RET
  (not documented)
url-mail		      M-x ... RET
  (not documented)
blackbox		      <menu-bar> <tools> <games> <black-box>
  Play blackbox.
bln-mode		      M-x ... RET
  Toggle binary line navigation mode.
  Value: nil
bs-show			      M-x ... RET
  Make a menu of buffers so you can manipulate buffers or the buffer
  list.
dcl-mode		      M-x ... RET
  Major mode for editing DCL-files.
ediff3			      M-x ... RET
  Run Ediff on three files, FILE-A, FILE-B, and FILE-C.
edirs3			      M-x ... RET
  Run Ediff on directories DIR1, DIR2, and DIR3, comparing files.
version			      M-x ... RET
  Display the version of Emacs that is running in this session.
xml-mode		      M-x ... RET
  Major mode for editing XML.
acm-mode		      M-x ... RET
  LSP Bridge mode.
  Value: nil
calendar		      <menu-bar> <tools> <calendar>
  Display a three-month Gregorian calendar.
cua-mode		      <menu-bar> <options> <cua-mode>, <menu-bar> <options> <cua-emulation-mode>
  Toggle Common User Access style editing (CUA mode).
  Value: nil
f90-mode		      M-x ... RET
  Major mode for editing Fortran 90,95 code in free format.
guiler			      M-x ... RET
  Run guiler on program FILE in buffer `*gud-FILE*'.
imenu			      M-g i
  Jump to a place in the buffer chosen using a buffer menu or mouse
  menu.
mml-mode		      M-x ... RET
  Minor mode for editing MML.
  Value: nil
msb-mode		      M-x ... RET
  Toggle Msb mode.
  Value: nil
org-todo		      M-x ... RET
  Change the TODO state of an item.
ses-mode		      M-x ... RET
  Major mode for Simple Emacs Spreadsheet.
sql-mode		      M-x ... RET
  Major mode to edit SQL.
tcl-mode		      M-x ... RET
  Major mode for editing Tcl code.
ido-mode		      M-x ... RET
  Toggle Ido mode on or off.
  Value: nil
org-mode		      M-x ... RET
  Outline-based notes management and organizer, alias
org-yank		      M-x ... RET
  Yank.  If the kill is a subtree, treat it specially.
run-dig			      M-x ... RET
  Look up DNS information for HOST (name or IP address).
rxt-mode		      M-x ... RET
  Regex translation utilities.
asm-mode		      M-x ... RET
  Major mode for editing typical assembler code.
awk-mode		      M-x ... RET
  Major mode for editing AWK code.
c++-mode		      M-x ... RET
  Major mode for editing C++ code.
css-mode		      M-x ... RET
  Major mode to edit Cascading Style Sheets (CSS).
find-tag		      M-x ... RET
  Find tag (in current tags table) whose name contains TAGNAME.
how-many		      M-x ... RET
  Print and return number of matches for REGEXP following point.
recenter		      M-x ... RET
  Center point in selected window and maybe redisplay frame.
vc-merge		      M-x ... RET
  Perform a version control merge operation.
yank-pop		      M-y
  Replace just-yanked stretch of killed text with a different stretch.
org-goto		      M-x ... RET
  Look up a different location in the current file, keeping current
  visibility.
tab-list		      M-x ... RET
  Display a list of named window configurations.
tar-mode		      M-x ... RET
  Major mode for viewing a tar file as a dired-like listing of its
  contents.
webjump			      M-x ... RET
  Jumps to a Web site from a programmable hotlist.
2C-split		      M-x ... RET
  Split a two-column text at point, into two buffers in two-column
  minor mode.
c-guess			      M-x ... RET
  Guess the style in the region up to `c-guess-region-max', and
  install it.
calc-undo		      M-x ... RET
  (not documented)
ebuffers		      M-x ... RET
  Run Ediff on a pair of buffers, BUFFER-A and BUFFER-B.
eldoc			      M-x ... RET
  Document thing at point.
idl-mode		      M-x ... RET
  Major mode for editing CORBA's IDL, PSDL and CIDL code.
calc			      <menu-bar> <tools> <calc>
  The Emacs Calculator.  Full documentation is listed under
  `calc-mode'.
find-file		      <menu-bar> <file> <new-file>, <open>
  Edit file FILENAME.
hexl-mode		      M-x ... RET
  \<hexl-mode-map>A mode for editing binary files in hex dump format.
nxml-mode		      M-x ... RET
  Major mode for editing XML.
org-cycle		      M-x ... RET
  TAB-action and visibility cycling for Org mode.
org-lint		      M-x ... RET
  Check current Org buffer for syntax mistakes.
perl-mode		      M-x ... RET
  Major mode for editing Perl code.
solitaire		      <menu-bar> <tools> <games> <solitaire>
  Play Solitaire.
vc-ediff		      M-x ... RET
  Display diffs between file revisions using ediff.
vera-mode		      M-x ... RET
  Major mode for editing Vera code.
vhdl-mode		      M-x ... RET
  Major mode for editing VHDL code.
view-mode		      M-x ... RET
  Toggle View mode, a minor mode for viewing text but not editing it.
  Value: nil
Info-mode		      M-x ... RET
  Info mode provides commands for browsing through the Info
  documentation tree.
TeX-mode		      M-x ... RET
  Major mode for editing files of input for TeX, LaTeX, or SliTeX.
copy-file		      M-x ... RET
  Copy FILE to NEWNAME.  Both args must be strings.
deadgrep		      M-x ... RET
  Start a ripgrep search for SEARCH-TERM in DIRECTORY.
dns-mode		      M-x ... RET
  Major mode for viewing and editing DNS master files.
html-mode		      M-x ... RET
  Major mode based on SGML mode for editing HTML documents.
icon-mode		      M-x ... RET
  Major mode for editing Icon code.
mail-mode		      M-x ... RET
  Major mode for editing mail to be sent.
mpv-quit		      M-x ... RET
  Exit the current mpv process.
next-line		      <down>, C-n
  Move cursor vertically down ARG lines.
remember		      M-x ... RET
  Remember an arbitrary piece of data.
run-lisp		      M-x ... RET
  Run an inferior Lisp process, input and output via buffer
  `*inferior-lisp*'.
sql-mysql		      M-x ... RET
  Run mysql by TcX as an inferior process.
sql-solid		      M-x ... RET
  Run solsql by Solid as an inferior process.
tab-move		      M-x ... RET
  Move the current tab ARG positions to the right.
tex-mode		      M-x ... RET
  Major mode for editing files of input for TeX, LaTeX, or SliTeX.
todo-show		      M-x ... RET
  Visit a todo file and display one of its categories.
Info-up			      M-x ... RET
  Go to the superior node of this node.
appt-add		      M-x ... RET
  Add an appointment for today at TIME with message MSG.
battery			      M-x ... RET
  Display battery status information in the echo area.
conf-mode		      M-x ... RET
  Mode for Unix and Windows Conf files and Java properties.
dig-mode		      M-x ... RET
  Major mode for displaying dig output.
ftp			      M-x ... RET
  Run `ftp-program' to connect to HOST.
goto-line		      <menu-bar> <edit> <goto> <go-to-line>, M-g M-g, M-g g, s-l
  Go to LINE, counting from line 1 at beginning of buffer.
mh-rmail		      M-x ... RET
  Incorporate new mail with MH.
pcre-mode		      M-x ... RET
  Use emulated PCRE syntax for regexps wherever possible.
  Value: nil
sgml-mode		      M-x ... RET
  Major mode for editing SGML documents.
vc-update		      <menu-bar> <tools> <vc> <vc-update>
  Update the current fileset or branch.
bat-mode		      M-x ... RET
  Major mode for editing DOS/Windows batch files.
diff-mode		      M-x ... RET
  Major mode for viewing/editing context diffs.
fido-mode		      M-x ... RET
  An enhanced `icomplete-mode' that emulates `ido-mode'.
  Value: t
java-mode		      M-x ... RET
  Major mode for editing Java code.
kill-line		      <deleteline>, C-k
  Kill the rest of the current line; if no nonblanks there, kill thru
  newline.
mh-nmail		      M-x ... RET
  Check for new mail in inbox folder.
objc-mode		      M-x ... RET
  Major mode for editing Objective C code.
org-occur		      M-x ... RET
  Make a compact tree showing all matches of REGEXP.
pike-mode		      M-x ... RET
  Major mode for editing Pike code.
rfc-mode		      M-x ... RET
  Major mode to browse and read RFC documents.
speedbar		      M-x ... RET
  Enable or disable speedbar.  Positive ARG means turn on, negative
  turn off.
tab-close		      <menu-bar> <file> <close-tab>
  Close the tab specified by its absolute position TAB-NUMBER.
untabify		      M-x ... RET
  Convert all tabs in region to multiple spaces, preserving columns.
vc-ignore		      <menu-bar> <tools> <vc> <vc-ignore>
  Ignore FILE under the VCS of DIRECTORY.
down-list		      ESC C-<down>, C-M-d, C-M-<down>
  Move forward down one level of parentheses.
free-keys		      M-x ... RET
  Display free keys in current buffer.
mark-sexp		      C-M-SPC, C-M-@
  Set mark ARG sexps from point.
mpv-play		      M-x ... RET
  Start an mpv process playing the file at PATH.
tab-next		      M-x ... RET
  Switch to ARGth next tab.
zoom-mode		      M-x ... RET
  Perform `zoom' automatically as the selected window changes.
  Value: t
5x5-crack		      M-x ... RET
  Attempt to find a solution for 5x5.
butterfly		      M-x ... RET
  Use butterflies to flip the desired bit on the drive platter.
erevision		      M-x ... RET
  Run Ediff by comparing versions of a file.
gfm-mode		      M-x ... RET
  Major mode for editing GitHub Flavored Markdown files.
list-tags		      M-x ... RET
  Display list of tags in file FILE.
mark-word		      M-@
  Set mark ARG words away from point.
next-file		      M-x ... RET
  Select next file among files in current tags table.
org-timer		      M-x ... RET
  Insert a H:MM:SS string from the timer into the buffer.
quickrun		      M-x ... RET
  Run commands quickly for current buffer.
rst-mode		      M-x ... RET
  Major mode for editing reStructuredText documents.
snmp-mode		      M-x ... RET
  Major mode for editing SNMP MIBs.
tab-group		      M-x ... RET
  Add the tab specified by its absolute position TAB-NUMBER to
  GROUP-NAME.
array-mode		      M-x ... RET
  Major mode for editing arrays.
cperl-mode		      M-x ... RET
  Major mode for editing Perl code.
ebuffers3		      M-x ... RET
  Run Ediff on three buffers, BUFFER-A, BUFFER-B, and BUFFER-C.
elpl-mode		      M-x ... RET
  Major mode for interactively evaluating Emacs Lisp expressions.
ffap-next		      M-x ... RET
  Search buffer for next file or URL, and run ffap.
goto-char		      <menu-bar> <edit> <goto> <go-to-pos>, M-g c
  Set point to POSITION, a number or marker.
handwrite		      M-x ... RET
  Turn the buffer into a "handwritten" document.
join-line		      M-x ... RET
  Join this line to previous and fix up whitespace at join.
left-word		      M-<left>, C-<left>
  Move point N words to the left (to the right if N is negative).
scroll-up		      M-x ... RET
  Scroll text of selected window upward ARG lines.
tab-last		      M-x ... RET
  Switch to the last tab or ARGth tab from the end of the tab bar.
texi2info		      M-x ... RET
  Convert the current buffer (written in Texinfo code) into an Info
  file.
LaTeX-mode		      M-x ... RET
  Major mode for editing files of input for LaTeX.
benchmark		      M-x ... RET
  Print the time taken for REPETITIONS executions of FORM.
kill-sexp		      C-M-k
  Kill the sexp (balanced expression) following point.
latex-mode		      M-x ... RET
  Major mode for editing files of input for LaTeX.
left-char		      <left>
  Move point N characters to the left (to the right if N is negative).
lisp-mode		      M-x ... RET
  Major mode for editing programs in Common Lisp and other similar
  Lisps.
make-frame		      s-n
  Return a newly created frame displaying the current buffer.
org-agenda		      M-x ... RET
  Dispatch agenda commands to collect entries to the agenda buffer.
org-refile		      M-x ... RET
  Move the entry or entries at point to another heading.
read-color		      M-x ... RET
  Read a color name or RGB triplet.
rmail-mime		      M-x ... RET
  Toggle the display of a MIME message.
rmail-mode		      M-x ... RET
  Rmail Mode is used by \<rmail-mode-map>\[rmail] for editing Rmail
  files.
sql-ingres		      M-x ... RET
  Run sql by Ingres as an inferior process.
sql-linter		      M-x ... RET
  Run inl by RELEX as an inferior process.
sql-oracle		      M-x ... RET
  Run sqlplus by Oracle as an inferior process.
sql-sqlite		      M-x ... RET
  Run sqlite as an inferior process.
sql-sybase		      M-x ... RET
  Run isql by Sybase as an inferior process.
time-stamp		      M-x ... RET
  Update any time stamp string(s) in the buffer.
undo-only		      M-x ... RET
  Undo some previous changes.
view-file		      M-x ... RET
  View FILE in View mode, returning to previous buffer when done.
where-is		      <help> w, <f1> w
  Print message listing key sequences that invoke the command
  DEFINITION.
zone-mode		      M-x ... RET
  Major mode for viewing and editing DNS master files.
browse-url		      M-x ... RET
  Open URL using a configurable method.
browse-web		      <menu-bar> <tools> <browse-web>
  Fetch URL and render the page.
dired-diff		      M-x ... RET
  Compare file at point with FILE using `diff'.
erc-select		      M-x ... RET
  ERC is a powerful, modular, and extensible IRC client.
eval-defun		      C-M-x
  Evaluate top-level form around point and instrument it if EDEBUG-IT
  is non-nil.
ffap-menu		      M-x ... RET
  Put up a menu of files and URLs mentioned in this buffer.
find-grep		      M-x ... RET
  Run grep via find, with user-specified args COMMAND-ARGS.
gnus-mode		      M-x ... RET
  Base mode from which all other gnus modes derive.
grep-find		      M-x ... RET
  Run grep via find, with user-specified args COMMAND-ARGS.
holidays		      M-x ... RET
  Display the holidays for last month, this month, and next month.
image-crop		      M-x ... RET
  Crop the image under point.
keep-lines		      M-x ... RET
  Delete all lines except those containing matches for REGEXP.
kill-emacs		      M-x ... RET
  Exit the Emacs job and kill it.
load-theme		      M-x ... RET
  Load Custom theme named THEME from its file and possibly enable it.
mail-send		      M-x ... RET
  Send the message in the current buffer.
next-error		      M-g M-n, M-g n
  Visit next `next-error' message and corresponding source code.
next-match		      M-x ... RET
  Visit next `next-error' message and corresponding source code.
re-builder		      <menu-bar> <emacs-lisp> <Construct Regexp>
  Construct a regexp interactively.
recompile		      M-x ... RET
  Re-compile the program including the current buffer.
run-python		      M-x ... RET
  Run an inferior Python process.
write-file		      <menu-bar> <file> <write-file>
  Write current buffer into file FILENAME.
Info-menu		      M-x ... RET
  Go to the node pointed to by the menu item named (or abbreviated)
  MENU-ITEM.
corfu-mode		      M-x ... RET
  Completion Overlay Region FUnction.
cwarn-mode		      M-x ... RET
  Minor mode that highlights suspicious C and C++ constructions.
eldoc-mode		      <menu-bar> <emacs-lisp> <Auto-Display Documentation Strings>
  Toggle echo area display of Lisp objects at point (ElDoc mode).
  Value: nil
ido-dired		      M-x ... RET
  Call `dired' the Ido way.
org-return		      M-x ... RET
  Goto next table row or insert a newline.
shortdoc		      M-x ... RET
  Pop to a buffer with short documentation summary for functions in
  GROUP.
subed-mode		      M-x ... RET
  Major mode for editing subtitles.
  Value: ((lambda nil (set (make-local-variable 'max-image-size) nil)))
text-mode		      M-x ... RET
  Major mode for editing text written for humans to read.
unrmail			      M-x ... RET
  Convert old-style Rmail Babyl file FILE to mbox format file TO-FILE.
wucuo-mode		      M-x ... RET
  Toggle spell checking (Wucuo mode).
  Value: nil
ansi-term		      M-x ... RET
  Start a terminal-emulator in a new buffer.
bibtex-url		      M-x ... RET
  Browse a URL for the BibTeX entry at point.
checkdoc		      <menu-bar> <emacs-lisp> <Check Documentation Strings>
  Interactively check the entire buffer for style errors.
citre-mode		      M-x ... RET
  Enable `completion-at-point', xref and imenu integration.
  Value: nil
dired-jump		      M-x ... RET
  Jump to Dired buffer corresponding to current buffer.
dired-mark		      M-x ... RET
  Mark the file at point in the Dired buffer.
dsssl-mode		      M-x ... RET
  Major mode for editing DSSSL code.
forms-mode		      M-x ... RET
  Major mode to visit files in a field-structured manner using a form.
help-mode		      M-x ... RET
  Major mode for viewing help text and navigating references in it.
mark-page		      M-x ... RET
  Put mark at end of page, point at beginning.
markdown		      M-x ... RET
  Run `markdown-command' on buffer, sending output to
  OUTPUT-BUFFER-NAME.
mule-diag		      <menu-bar> <options> <mule> <mule-diag>, <menu-bar> <help-menu> <describe> <mule-diag>
  Display diagnosis of the multilingual environment (Mule).
occur-mode		      M-x ... RET
  Major mode for output from \[occur].
pcomplete		      M-x ... RET
  Support extensible programmable completion.
ps-despool		      M-x ... RET
  Send the spooled PostScript to the printer.
raise-sexp		      M-x ... RET
  Raise N sexps one level higher up the tree.
ruler-mode		      M-x ... RET
  Toggle display of ruler in header line (Ruler mode).
  Value: nil
tab-new-to		      M-x ... RET
  Add a new tab at the absolute position TAB-NUMBER.
tab-switch		      M-x ... RET
  Switch to the tab by NAME.
type-break		      M-x ... RET
  Take a typing break.
Info-split		      M-x ... RET
  Split an info file into an indirect file plus bounded-size subfiles.
eww-reload		      M-x ... RET
  Reload the current page.
find-dired		      M-x ... RET
  Run `find' and go into Dired mode on a buffer of the output.
grep-mode		      M-x ... RET
  Sets `grep-last-buffer' and `compilation-window-height'.
indent-to		      M-x ... RET
  Indent from point with tabs and spaces until COLUMN is reached.
make-face		      M-x ... RET
  Define a new face with name FACE, a symbol.
mark-defun		      C-M-h
  Put mark at end of this defun, point at beginning.
open-line		      <insertline>, C-o
  Insert a newline and leave point before it.
recentf			      M-x ... RET
  Prompt for FILE in `recentf-list' and visit it.
run-scheme		      M-x ... RET
  Run an inferior Scheme process, input and output via buffer
  `*scheme*'.
tab-select		      M-x ... RET
  Switch to the tab by its absolute position TAB-NUMBER in the tab
  bar.
yas/expand		      M-x ... RET
  Expand a snippet before point.
allout-mode		      M-x ... RET
  Toggle Allout outline mode.
artist-mode		      M-x ... RET
  Toggle Artist mode.
bibtex-mode		      M-x ... RET
  Major mode for editing BibTeX files.
comint-run		      M-x ... RET
  Run PROGRAM in a Comint buffer and switch to that buffer.
cvs-status		      M-x ... RET
  Run a `cvs status' in the current working DIRECTORY.
cvs-update		      M-x ... RET
  Run a `cvs update' in the current working DIRECTORY.
eww-browse		      M-x ... RET
  Function to be run to parse command line URLs.
follow-mode		      M-x ... RET
  Toggle Follow mode.
image-mode		      M-x ... RET
  Major mode for image files.
magit-init		      M-x ... RET
  Initialize a Git repository, then show its status.
mpv-seek		      M-x ... RET
  Seek to the given (absolute) time in SECONDS.
org-capture		      M-x ... RET
  Capture something.
org-reveal		      M-x ... RET
  Show current entry, hierarchy above it, and the following headline.
right-word		      M-<right>, C-<right>
  Move point N words to the right (to the left if N is negative).
run-octave		      M-x ... RET
  Run an inferior Octave process, I/O via `inferior-octave-buffer'.
simula-mode		      M-x ... RET
  Major mode for editing SIMULA code.
xeft-mode		      M-x ... RET
  Search for notes and display summaries.
amread-mode		      M-x ... RET
  I'm reading mode.
arp			      M-x ... RET
  Run `arp-program' and display diagnostic output.
buffer-menu		      M-x ... RET
  Switch to the Buffer Menu.
comint-mode		      M-x ... RET
  Major mode for interacting with an inferior interpreter.
delphi-mode		      M-x ... RET
  Major mode for editing OPascal code.
dired-undo		      M-x ... RET
  Undo in a Dired buffer.
dired-why		      M-x ... RET
  Pop up a buffer with error log output from Dired.
edebug-mode		      M-x ... RET
  Mode for Emacs Lisp buffers while in Edebug.
  Value: nil
flush-lines		      M-x ... RET
  Delete lines containing matches for REGEXP.
fringe-mode		      M-x ... RET
  Set the default appearance of fringes on all frames.
  Value: 10
insert-char		      M-x ... RET
  Insert COUNT copies of CHARACTER.
ispell-word		      <menu-bar> <tools> <spell> <ispell-word>, <menu-bar> <edit> <spell> <ispell-word>, M-$
  Check spelling of word under or before the cursor.
kill-region		      <menu-bar> <edit> <cut>, S-<delete>, s-x, C-w
  Kill ("cut") text between point and mark.
master-mode		      M-x ... RET
  Toggle Master mode.
mpc			      M-x ... RET
  Main entry point for MPC.
pascal-mode		      M-x ... RET
  Major mode for editing Pascal code.\<pascal-mode-map>
reftex-mode		      M-x ... RET
  Minor mode with distinct support for \label, \ref and \cite in
  LaTeX.
repeat-mode		      M-x ... RET
  Toggle Repeat mode.
  Value: t
right-char		      <right>
  Move point N characters to the right (to the left if N is negative).
run-at-time		      M-x ... RET
  Perform an action at time TIME.
save-buffer		      <menu-bar> <file> <save-buffer>, s-s
  Save current buffer in visited file if modified.
scheme-mode		      M-x ... RET
  Major mode for editing Scheme code.
server-mode		      M-x ... RET
  Toggle Server mode.
  Value: nil
slitex-mode		      M-x ... RET
  Major mode for editing files of input for SliTeX.
sql-mariadb		      M-x ... RET
  Run mysql by MariaDB as an inferior process.
vc-annotate		      <menu-bar> <tools> <vc> <vc-annotate>
  Display the edit history of the current FILE using colors.
winner-mode		      M-x ... RET
  Toggle Winner mode on or off.
  Value: nil
Custom-mode		      M-x ... RET
  Major mode for editing customization buffers.
abbrev-mode		      M-x ... RET
  Toggle Abbrev mode in the current buffer.
  Value: t
ad-activate		      M-x ... RET
  Activate all the advice information of an advised FUNCTION.
beacon-mode		      M-x ... RET
  Toggle Beacon mode on or off.
  Value: t
c-set-style		      M-x ... RET
  Set the current buffer to use the style STYLENAME.
customize		      <menu-bar> <options> <customize> <customize>, s-,
  Select a customization buffer which you can use to set user options.
eval-buffer		      <menu-bar> <emacs-lisp> <Evaluate Buffer>
  Execute the accessible portion of current buffer as Lisp code.
eval-region		      <menu-bar> <emacs-lisp> <Evaluate Region>
  Execute the region as Lisp code.
fill-region		      <menu-bar> <edit> <fill>
  Fill each of the paragraphs in the region.
hanoi			      <menu-bar> <tools> <games> <hanoi>
  Towers of Hanoi diversion.  Use NRINGS rings.
iimage-mode		      M-x ... RET
  Toggle Iimage mode on or off.
image-save		      M-x ... RET
  Save the image under point.
kill-buffer		      M-x ... RET
  Kill the buffer specified by BUFFER-OR-NAME.
kkc-region		      M-x ... RET
  Convert Kana string in the current region to Kanji-Kana mixed
  string.
org-info		      M-x ... RET
  Read documentation for Org in the info system.
org-publish		      M-x ... RET
  Publish PROJECT.
pkg-dm-mode		      M-x ... RET
  Minor mode `pkg-dm-mode'.
  Value: nil
popwin-mode		      M-x ... RET
  Minor mode for `popwin-mode'.
  Value: t
refill-mode		      M-x ... RET
  Toggle automatic refilling (Refill mode).
tab-rename		      M-x ... RET
  Give the tab specified by its absolute position TAB-NUMBER a new
  NAME.
vc-revert		      <menu-bar> <tools> <vc> <vc-revert>
  Revert working copies of the selected fileset to their repository
  contents.
yank-media		      M-x ... RET
  Yank media (images, HTML and the like) from the clipboard.
comint-bol		      M-x ... RET
  Go to the beginning of line, then skip past the prompt, if any.
count-words		      M-x ... RET
  Count words between START and END.
css-ts-mode		      M-x ... RET
  Major mode to edit Cascading Style Sheets (CSS).
js-jsx-mode		      M-x ... RET
  Major mode for editing JavaScript+JSX.
normal-mode		      M-x ... RET
  Choose the major mode for this buffer automatically.
org-metaup		      M-x ... RET
  Move subtree up or move table row up.
org-version		      M-x ... RET
  Show the Org version.
  Value: "9.6"
push-button		      M-x ... RET
  Perform the action specified by a button at location POS.
quit-window		      M-x ... RET
  Quit WINDOW and bury its buffer.
rename-file		      M-x ... RET
  Rename FILE as NEWNAME.  Both args must be strings.
sort-lines		      M-x ... RET
  Sort lines in region alphabetically; REVERSE non-nil means
  descending order.
vc-git-grep		      M-x ... RET
  Run git grep, searching for REGEXP in FILES in directory DIR.
vc-register		      <menu-bar> <tools> <vc> <vc-register>
  Register into a version control system.
view-buffer		      M-x ... RET
  View BUFFER in View mode, returning to previous buffer when done.
Info-index		      M-x ... RET
  Look up a string TOPIC in the index for this manual and go to that
  entry.
button-mode		      M-x ... RET
  A minor mode for navigating to buttons with the TAB key.
  Value: nil
citre-jump		      M-x ... RET
  Jump to the definition of the symbol at point.
debug-watch		      M-x ... RET
  Trigger a debugger invocation when VARIABLE is changed.
double-mode		      M-x ... RET
  Toggle special insertion on double keypresses (Double mode).
ediff-merge		      M-x ... RET
  Merge two files without ancestor.
emoji-list		      M-x ... RET
  List emojis and insert the one that's selected.
grugru-edit		      M-x ... RET
  Edit grugru which can be rotated at point.
image-cut		      M-x ... RET
  Cut a rectangle from the image under point, filling it with COLOR.
insert-pair		      M-x ... RET
  Enclose following ARG sexps in a pair of OPEN and CLOSE characters.
mh-smail		      M-x ... RET
  Compose a message with the MH mail system.
modify-face		      M-x ... RET
  Modify attributes of faces interactively.
nroff-mode		      M-x ... RET
  Major mode for editing text intended for nroff to format.
octave-mode		      M-x ... RET
  Major mode for editing Octave code.
org-columns		      M-x ... RET
  Turn on column view on an Org mode file.
org-sort		      M-x ... RET
  Call `org-sort-entries', `org-table-sort-lines' or `org-sort-list'.
prog-mode		      M-x ... RET
  Major mode for editing programming language source code.
  Value: (hs-minor-mode)
prolog-mode		      M-x ... RET
  Major mode for editing Prolog code.
remove-hook		      M-x ... RET
  Remove from the value of HOOK the function FUNCTION.
reveal-mode		      M-x ... RET
  Toggle uncloaking of invisible text near point (Reveal mode).
serial-term		      M-x ... RET
  Start a terminal-emulator for a serial port in a new buffer.
smerge-mode		      M-x ... RET
  Minor mode to simplify editing output from the diff3 program.
smime-mode		      M-x ... RET
  Major mode for browsing, viewing and fetching certificates.
windmove-up		      M-x ... RET
  Select the window above the current one.
yas-expand		      <menu-bar> <yasnippet> <Expand trigger>
  Expand a snippet before point. If no snippet
bury-buffer		      M-x ... RET
  Put BUFFER-OR-NAME at the end of the list of all buffers.
c-add-style		      M-x ... RET
  Add a style to `c-style-alist', or update an existing one.
cvs-examine		      M-x ... RET
  Run a `cvs -n update' in the specified DIRECTORY.
end-of-line		      M-x ... RET
  Move point to end of current line (in the logical order).
image-bol		      M-x ... RET
  Scroll horizontally to the left edge of the image in the current
  window.
image-eol		      M-x ... RET
  Scroll horizontally to the right edge of the image in the current
  window.
iso-german		      M-x ... RET
  Translate net conventions for German to ISO 8859-1.
js-json-mode		      M-x ... RET
  (not documented)
org-reload		      M-x ... RET
  Reload all Org Lisp files.
org-switchb		      M-x ... RET
  Switch between Org buffers.
orgtbl-mode		      M-x ... RET
  The Org mode table editor as a minor mode for use in other modes.
  Value: nil
scroll-left		      C-<next>
  Scroll selected window display ARG columns left.
sieve-manage		      M-x ... RET
  (not documented)
sieve-upload		      M-x ... RET
  (not documented)
snmpv2-mode		      M-x ... RET
  Major mode for editing SNMPv2 MIBs.
sort-pages		      M-x ... RET
  Sort pages in region alphabetically; argument means descending
  order.
split-line		      C-M-o
  Split current line, moving portion beyond point vertically down.
Info-toc		      M-x ... RET
  Go to a node with table of contents of the current Info file.
bibtex-yank		      M-x ... RET
  Reinsert the last BibTeX item.
citre-peek		      M-x ... RET
  Peek the definition of symbol at pointthe symbol in BUF at POINT.
edit-picture		      M-x ... RET
  Switch to Picture mode, in which a quarter-plane screen model is
  used.
first-error		      M-x ... RET
  Restart at the first error.
flymake-mode		      M-x ... RET
  Toggle Flymake mode on or off.
fortran-mode		      M-x ... RET
  Major mode for editing Fortran code in fixed format.
hanoi-unix		      M-x ... RET
  Towers of Hanoi, 32-bit UNIX doomsday version.
hi-lock-mode		      M-x ... RET
  Toggle selective highlighting of patterns (Hi Lock mode).
ibuffer-mode		      M-x ... RET
  A major mode for viewing a list of buffers.
idlwave-mode		      M-x ... RET
  Major mode for editing IDL source files (version 6.1_em22).
multi-occur		      M-x ... RET
  Show all lines in buffers BUFS containing a match for REGEXP.
picture-mode		      M-x ... RET
  Switch to Picture mode, in which a quarter-plane screen model is
  used.
pr-help			      M-x ... RET
  Help for the printing package.
scroll-down		      M-x ... RET
  Scroll text of selected window down ARG lines.
select-frame		      M-x ... RET
  Select FRAME.
table-insert		      M-x ... RET
  Insert an editable text table.
texinfo-mode		      M-x ... RET
  Major mode for editing Texinfo files.
tmm-menubar		      M-`
  Text-mode emulation of looking and choosing from a menubar.
topsy-mode		      M-x ... RET
  Minor mode to show a simple sticky header.
  Value: nil
verilog-mode		      M-x ... RET
  Major mode for editing Verilog code.
zap-to-char		      M-z
  Kill up to and including ARGth occurrence of CHAR.
Info-next		      M-x ... RET
  Go to the "next" node, staying on the same hierarchical level.
View-quit		      M-x ... RET
  Quit View mode, trying to restore window and buffer to previous
  state.
ad-recover		      M-x ... RET
  Try to recover FUNCTION's original definition, and unadvise it.
ad-update		      M-x ... RET
  Update the advised definition of FUNCTION if its advice is active.
calculator		      <menu-bar> <tools> <simple-calculator>
  Run the Emacs calculator.
company-mode		      M-x ... RET
  "complete anything"; is an in-buffer completion framework.
compose-mail		      <menu-bar> <tools> <compose-mail>
  Start composing a mail message to send.
delete-char		      <kp-delete>, C-d
  Delete the following N characters (previous if N is negative).
edmacro-mode		      M-x ... RET
  \<edmacro-mode-map>Keyboard Macro Editing mode.  Press
  \[edmacro-finish-edit] to save and exit.
elide-head		      M-x ... RET
  Hide header material in buffer according to
  `elide-head-headers-to-hide'.
elpl			      M-x ... RET
  Run an inferior instance of `elpl-cli' inside Emacs.
help			      M-x ... RET
  Help command.
manual-entry		      <menu-bar> <help-menu> <more-manuals> <man>, s-M
  Get a Un*x manual page and put it in a buffer.
mml-preview		      M-x ... RET
  Display current buffer with Gnus, in a new buffer.
mpuz			      <menu-bar> <tools> <games> <mult>
  Multiplication puzzle with GNU Emacs.
opascal-mode		      M-x ... RET
  Major mode for editing OPascal code.
other-frame		      s-`
  Select the ARGth different visible frame on current display, and
  raise it.
recentf-mode		      M-x ... RET
  Toggle keeping track of opened files (Recentf mode).
  Value: t
sql-postgres		      M-x ... RET
  Run psql by Postgres as an inferior process.
strokes-mode		      M-x ... RET
  Toggle Strokes mode, a global minor mode.
  Value: nil
subword-mode		      M-x ... RET
  Toggle subword movement and editing (Subword mode).
  Value: t
talk			      M-x ... RET
  Connect to the Emacs talk group from the current X display or tty
  frame.
tildify-mode		      M-x ... RET
  Adds electric behavior to space character.
tooltip-mode		      <menu-bar> <options> <showhide> <showhide-tooltip-mode>
  Toggle Tooltip mode.
  Value: t
undo-redo		      <menu-bar> <edit> <undo-redo>, C-M-_, C-?, s-Z
  Undo the last ARG undos, i.e., redo the last ARG changes.
write-region		      M-x ... RET
  Write current region into specified file.
xeft			      M-x ... RET
  Start Xeft.
zoom			      M-x ... RET
  Zoom the current window and balance the others according to
  `zoom-size'.
Info-prev		      M-x ... RET
  Go to the "previous" node, staying on the same hierarchical level.
align-regexp		      M-x ... RET
  Align the current region using an ad-hoc rule read from the
  minibuffer.
bookmark-set		      <menu-bar> <edit> <bookmark> <set>
  Set a bookmark named NAME at the current location.
clone-buffer		      M-x ... RET
  Create and return a twin copy of the current buffer.
delete-file		      M-x ... RET
  Delete file named FILENAME.  If it is a symlink, remove the symlink.
delete-frame		      <menu-bar> <file> <delete-this-frame>, s-w
  Delete FRAME, eliminating it from use.
desktop-save		      M-x ... RET
  Save the state of Emacs in a desktop file in directory DIRNAME.
diff-backup		      M-x ... RET
  Diff this file with its backup file or vice versa.
diff-hl-mode		      M-x ... RET
  Toggle VC diff highlighting.
disassemble		      <menu-bar> <emacs-lisp> <Disassemble Byte Compiled Object...>
  Print disassembled code for OBJECT in (optional) BUFFER.
epkg-update		      M-x ... RET
  Update the Epkg database.
eww-up-url		      M-x ... RET
  Go to the page marked `up'.
hl-line-mode		      M-x ... RET
  Toggle highlighting of the current line (Hl-Line mode).
  Value: nil
holiday-list		      M-x ... RET
  Display holidays for years Y1 to Y2 (inclusive).
image-dired		      M-x ... RET
  Make a thumbnail buffer for all images in DIR and display it.
invert-face		      M-x ... RET
  Swap the foreground and background colors of FACE.
magit-status		      M-x ... RET
  Show the status of the current Git repository in a buffer.
mhtml-mode		      M-x ... RET
  Major mode based on `html-mode', but works with embedded JS and CSS.
org-clock-in		      M-x ... RET
  Start the clock on the current item.
other-window		      M-x ... RET
  Select another window in cyclic ordering of windows.
outline-mode		      M-x ... RET
  Set major mode for editing outlines with selective display.
rainbow-mode		      M-x ... RET
  Colorize strings that represent colors.
set-variable		      M-x ... RET
  Set VARIABLE to VALUE.  VALUE is a Lisp object.
sis-log-mode		      M-x ... RET
  Log the execution of this package.
  Value: nil
so-long-mode		      M-x ... RET
  This major mode is the default `so-long-action' option.
sort-fields		      M-x ... RET
  Sort lines in region lexicographically by the ARGth field of each
  line.
sql-informix		      M-x ... RET
  Run dbaccess by Informix as an inferior process.
tags-search		      <menu-bar> <edit> <search> <tags-srch>
  Search through all files listed in tags table for match for REGEXP.
calc-keypad		      M-x ... RET
  Invoke the Calculator in "visual keypad" mode.
clean-mode		      M-x ... RET
  A mode that removes all overlays and text properties.
clone-frame		      M-x ... RET
  Make a new frame with the same parameters and windows as FRAME.
describe-key		      <menu-bar> <help-menu> <describe> <describe-key-1>
  Display documentation of the function invoked by KEY-LIST.
do-auto-save		      M-x ... RET
  Auto-save all buffers that need it.
edit-abbrevs		      s-E
  Alter abbrev definitions by editing the list of abbrevs.
elp-results		      <menu-bar> <emacs-lisp> <Profiling> <Show Profiling Results>
  Display current profiling results.
end-of-defun		      ESC C-<end>, C-M-e, C-M-<end>
  Move forward to next end of defun.
erts-mode		      M-x ... RET
  Major mode for editing erts (Emacs testing) files.
forward-line		      M-x ... RET
  Move N lines forward (backward if N is negative).
forward-word		      ESC <right>, M-f
  Move point forward ARG words (backward if ARG is negative).
generic-mode		      M-x ... RET
  Enter generic mode MODE.
glasses-mode		      M-x ... RET
  Minor mode for making identifiers likeThis readable.
insert-file		      <menu-bar> <file> <insert-file>
  Insert contents of file FILENAME into buffer after point.
isearch-exit		      M-x ... RET
  Exit search normally.
keyfreq-mode		      M-x ... RET
  Keyfreq mode records number of times each command was
  Value: t
org-src-mode		      M-x ... RET
  Minor mode for language major mode buffers generated by Org.
  Value: nil
print-buffer		      <menu-bar> <file> <print> <print-buffer>
  Paginate and print buffer contents.
print-region		      <menu-bar> <file> <print> <print-region>
  Paginate and print the region contents.
raise-frame		      M-x ... RET
  Bring FRAME to the front, so it occludes any frames it overlaps.
server-start		      M-x ... RET
  Allow this Emacs process to be a server for client processes.
tab-detach		      M-x ... RET
  Move tab number FROM-NUMBER to a new frame.
tab-move-to		      M-x ... RET
  Move tab from FROM-NUMBER position to new position at TO-NUMBER.
top-level		      M-x ... RET
  Exit all recursive editing levels.
  Value: (normal-top-level)
zoom-toggle		      s-+, s-<mouse-1>
  Zoom in or out on the line at point.
comment-dwim		      M-;
  Call the comment command you want (Do What I Mean).
diff-buffers		      M-x ... RET
  Find and display the differences between OLD and NEW buffers.
ebnf-despool		      M-x ... RET
  Send the spooled PostScript to the printer.
edebug-defun		      <menu-bar> <emacs-lisp> <Instrument Function for Debugging>
  Evaluate the top level form point is in, stepping through with
  Edebug.
eww-top-url		      M-x ... RET
  Go to the page marked `top'.
forward-sexp		      ESC C-<right>, C-M-f, C-M-<right>, <menu-bar> <emacs-lisp> <Navigation> <Forward Sexp>
  Move forward across one balanced expression (sexp).
hide-body		      M-x ... RET
  Hide all body lines in buffer, leaving all headings visible.
org-num-mode		      M-x ... RET
  Dynamic numbering of headlines in an Org buffer.
org-shiftup		      M-x ... RET
  Act on current element according to context.
quick-calc		      M-x ... RET
  Do a quick calculation in the minibuffer without invoking full
  Calculator.
sort-columns		      M-x ... RET
  Sort lines in region alphabetically by a certain range of columns.
sql-connect		      M-x ... RET
  Connect to an interactive session using CONNECTION settings.
tab-bar-mode		      M-x ... RET
  Toggle the tab bar in all graphical frames (Tab Bar mode).
  Value: nil
tab-recent		      M-x ... RET
  Switch to ARGth most recently visited tab.
tab-switcher		      M-x ... RET
  Display a list of named window configurations.
timeclock-in		      M-x ... RET
  Clock in, recording the current time moment in the timelog.
visible-mode		      M-x ... RET
  Toggle making all invisible text temporarily visible (Visible mode).
  Value: nil
desktop-read		      M-x ... RET
  Read and process the desktop file in directory DIRNAME.
ediff-files		      <menu-bar> <tools> <compare> <ediff-files>
  Run Ediff on a pair of files, FILE-A and FILE-B.
edirs-merge		      M-x ... RET
  Run Ediff on a pair of directories, DIR1 and DIR2, merging files
  that have
forward-list		      C-M-n
  Move forward across one balanced group of parentheses.
gnus-undo		      M-x ... RET
  Undo some previous changes in Gnus buffers.
html-erb-mode		      M-x ... RET
  (not documented)
info-finder		      M-x ... RET
  Display descriptions of the keywords in the Finder virtual manual.
iso-iso2tex		      M-x ... RET
  Translate ISO 8859-1 characters to TeX sequences.
iso-spanish		      M-x ... RET
  Translate net conventions for Spanish to ISO 8859-1.
iso-tex2iso		      M-x ... RET
  Translate TeX sequences to ISO 8859-1 characters.
list-buffers		      <menu-bar> <buffer> <list-all-buffers>
  Display a list of existing buffers.
load-library		      M-x ... RET
  Load the Emacs Lisp library named LIBRARY.
lpr-region		      M-x ... RET
  Print region contents without pagination or page headers.
magit-clean		      M-x ... RET
  Remove untracked files from the working tree.
message-info		      M-x ... RET
  Display the Message manual.
message-mail		      M-x ... RET
  Start editing a mail message to be sent.
message-send		      M-x ... RET
  Send the message in the current buffer.
nxml-web-mode		      M-x ... RET
  (not documented)
org-attach		      M-x ... RET
  The dispatcher for attachment commands.
org-priority		      M-x ... RET
  Change the priority of an item.
scroll-right		      C-<prior>
  Scroll selected window display ARG columns right.
typit-test		      M-x ... RET
  Run a typing test using the NUM most common words from the
  dictionary.
upcase-word		      M-u
  Convert to upper case from point to end of word, moving over.
vc-msg-show		      M-x ... RET
  Show commit message of current line.
check-parens		      M-x ... RET
  Check for unbalanced parentheses in the current buffer.
comment-box		      M-x ... RET
  Comment out the BEG .. END region, putting it inside a box.
dired-unmark		      M-x ... RET
  Unmark the file at point in the Dired buffer.
enable-theme		      M-x ... RET
  Reenable all variable and face settings defined by THEME.
find-library		      M-x ... RET
  Find the Emacs Lisp source of LIBRARY.
ido-find-file		      M-x ... RET
  Edit file with name obtained via minibuffer.
lower-frame		      M-x ... RET
  Send FRAME to the back, so it is occluded by any frames that overlap
  it.
lpr-buffer		      M-x ... RET
  Print buffer contents without pagination or page headers.
makefile-mode		      M-x ... RET
  Major mode for editing standard Makefiles.
message-tab		      M-x ... RET
  Complete names according to `message-completion-alist'.
next-buffer		      <menu-bar> <buffer> <next-buffer>, <XF86Forward>
  In selected window switch to ARGth next buffer.
org-deadline		      M-x ... RET
  Insert a "DEADLINE:" string with a timestamp to make a deadline.
org-sort-list		      M-x ... RET
  Sort list items.
pyim-activate		      M-x ... RET
  pyim 启动函数.
snippet-mode		      M-x ... RET
  A mode for editing yasnippets
table-capture		      M-x ... RET
  Convert plain text into a table by capturing the text in the region.
annotate-mode		      M-x ... RET
  Toggle Annotate mode.
avy-resume		      M-x ... RET
  Stub to hold last avy command.
center-line		      M-x ... RET
  Center the line point is on, within the width specified by
  `fill-column'.
comment-line		      M-x ... RET
  Comment or uncomment current line and leave point after it.
cvs-quickdir		      M-x ... RET
  Open a *cvs* buffer on DIR without running cvs.
delete-window		      M-x ... RET
  Delete specified WINDOW.
epa-key-mode		      M-x ... RET
  Major mode for a key description.
flyspell-mode		      <menu-bar> <tools> <spell> <flyspell-mode>, <menu-bar> <edit> <spell> <flyspell-mode>
  Toggle on-the-fly spell checking (Flyspell mode).
  Value: nil
footnote-mode		      M-x ... RET
  Toggle Footnote mode.
forward-char		      C-f
  Move point N characters forward (backward if N is negative).
goto-address		      M-x ... RET
  Sets up goto-address functionality in the current buffer.
helpful-mode		      M-x ... RET
  Major mode for *Helpful* buffers.
inferior-tcl		      M-x ... RET
  Run inferior Tcl process.
mpv-version		      M-x ... RET
  Return the mpv version string.
org-metaleft		      M-x ... RET
  Promote heading, list item at point or move table column left.
org-schedule		      M-x ... RET
  Insert a "SCHEDULED:" string with a timestamp to schedule an item.
previous-line		      <up>, C-p
  Move cursor vertically up ARG lines.
query-replace		      <menu-bar> <edit> <replace> <query-replace>, M-%
  Replace some occurrences of FROM-STRING with TO-STRING.
repeat-exit		      M-x ... RET
  Exit the repeating sequence.
revert-buffer		      <menu-bar> <file> <revert-buffer>, s-u
  Replace current buffer text with the text of the visited file on
  disk.
route			      M-x ... RET
  Run `route-program' and display diagnostic output.
savehist-mode		      M-x ... RET
  Toggle saving of minibuffer history (Savehist mode).
  Value: nil
shell-command		      <menu-bar> <tools> <shell-commands> <shell>, M-!, s-L
  Execute string COMMAND in inferior shell; display output, if any.
sieve-mode		      M-x ... RET
  Major mode for editing Sieve code.
smime			      M-x ... RET
  Go to the SMIME buffer.
spook			      M-x ... RET
  Add that special touch of class to your outgoing mail.
toggle-theme		      M-x ... RET
  Switch from the current theme to one of its variants.
url-view-url		      M-x ... RET
  View the current document's URL.
vc-print-log		      <menu-bar> <tools> <vc> <vc-print-log>
  List the change log of the current fileset in a window.
vc-root-diff		      <menu-bar> <tools> <vc> <vc-root-diff>
  Display diffs between VC-controlled whole tree revisions.
vundo			      M-x ... RET
  Display visual undo for the current buffer.
bookmark-jump		      <menu-bar> <edit> <bookmark> <jump>
  Jump to bookmark BOOKMARK (a point in some file).
cvs-checkout		      M-x ... RET
  Run a `cvs checkout MODULES' in DIR.
cycle-spacing		      M-x ... RET
  Manipulate whitespace around point in a smart way.
debugger-mode		      M-x ... RET
  Mode for debugging Emacs Lisp using a backtrace.
describe-char		      M-x ... RET
  Describe position POS (interactively, point) and the char after POS.
desktop-clear		      M-x ... RET
  Empty the Desktop.
dired-do-copy		      M-x ... RET
  Copy all marked (or next ARG) files, or copy the current file.
end-kbd-macro		      M-x ... RET
  Finish defining a keyboard macro.
enriched-mode		      M-x ... RET
  Minor mode for editing text/enriched files.
flyspell-word		      M-x ... RET
  Spell check a word.
hs-minor-mode		      M-x ... RET
  Minor mode to selectively hide/show code and comment blocks.
image-rotate		      M-x ... RET
  Rotate the image under point by ANGLE degrees clockwise.
indent-region		      C-M-\, <menu-bar> <emacs-lisp> <Indent Region>
  Indent each nonblank line in the region.
kill-word		      M-d, C-<delete>
  Kill characters forward until encountering the end of a word.
list-abbrevs		      M-x ... RET
  Display a list of the defined abbrevs.
list-holidays		      M-x ... RET
  Display holidays for years Y1 to Y2 (inclusive).
message-mode		      M-x ... RET
  Major mode for editing mail and news to be sent.
mouse-kill		      M-x ... RET
  Kill the region between point and the mouse click.
quoted-insert		      C-q
  Read next input character and insert it.
rxt-explain		      M-x ... RET
  Pop up a buffer with pretty-printed `rx' syntax for the regex at
  point.
semantic-mode		      <menu-bar> <tools> <semantic>
  Toggle parser features (Semantic mode).
  Value: nil
sql-interbase		      M-x ... RET
  Run isql by Interbase as an inferior process.
upcase-dwim		      M-x ... RET
  Upcase words in the region, if active; if not, upcase word at point.
world-clock		      M-x ... RET
  Display a world clock buffer with times in various time zones.
align-entire		      M-x ... RET
  Align the selected region as if it were one alignment section.
blink-search		      s-p
  Start blink-search.
bookmark-load		      <menu-bar> <edit> <bookmark> <load>
  Load bookmarks from FILE (which must be in bookmark format).
bookmark-save		      <menu-bar> <edit> <bookmark> <save>
  Save currently defined bookmarks in FILE.
count-matches		      M-x ... RET
  Print and return number of matches for REGEXP following point.
delete-pair		      M-x ... RET
  Delete a pair of characters enclosing ARG sexps that follow point.
dirtrack-mode		      M-x ... RET
  Toggle directory tracking in shell buffers (Dirtrack mode).
keyfreq-show		      M-x ... RET
  Show command usage statistics in `keyfreq-buffer'.
local-set-key		      M-x ... RET
  Give KEY a local binding as COMMAND.
mail-recover		      M-x ... RET
  Recover interrupted mail composition from auto-save files.
menu-bar-open		      <f10>
  Start key navigation of the menu bar in FRAME.
midnight-mode		      M-x ... RET
  Non-nil means run `midnight-hook' at midnight.
  Value: nil
modula-2-mode		      M-x ... RET
  This is a mode intended to support program development in Modula-2.
not-modified		      M-~
  Mark current buffer as unmodified, not needing to be saved.
org-metadown		      M-x ... RET
  Move subtree down or move table row down.
org-show-all		      M-x ... RET
  Show all contents in the visible part of the buffer.
org-table-sum		      M-x ... RET
  Sum numbers in region of current table column.
origami-mode		      M-x ... RET
  Minor mode to selectively hide/show text in the current buffer.
  Value: nil
pop-to-buffer		      M-x ... RET
  Display buffer specified by BUFFER-OR-NAME and select its window.
windmove-mode		      M-x ... RET
  Global minor mode for default windmove commands.
  Value: t
Custom-save		      M-x ... RET
  Set all edited settings, then save all settings that have been set.
backward-word		      ESC <left>, M-b
  Move backward until encountering the beginning of a word.
beacon-blink		      M-x ... RET
  Blink the beacon at the position of the cursor.
browse-url-w3		      M-x ... RET
  Ask the w3 WWW browser to load URL.
decipher-mode		      M-x ... RET
  Major mode for decrypting monoalphabetic substitution ciphers.
emacs-uptime		      M-x ... RET
  Return a string giving the uptime of this instance of Emacs.
epa-mail-mode		      M-x ... RET
  A minor-mode for composing encrypted/clearsigned mails.
epa-mail-sign		      M-x ... RET
  Sign the current buffer.
idlwave-shell		      M-x ... RET
  Run an inferior IDL, with I/O through buffer
  `(idlwave-shell-buffer)'.
indent-sexp		      M-x ... RET
  Indent each line of the list starting just after point.
isearch-occur		      M-x ... RET
  Run `occur' using the last search string as the regexp.
list-packages		      M-x ... RET
  Display a list of packages.
menu-bar-mode		      M-x ... RET
  Toggle display of a menu bar on each frame (Menu Bar mode).
  Value: t
run-prolog		      M-x ... RET
  Run an inferior Prolog process, input and output via buffer
  *prolog*.
set-face-font		      M-x ... RET
  Change font-related attributes of FACE to those of FONT.
shr-copy-url		      M-x ... RET
  Copy the URL under point to the kill ring.
special-mode		      M-x ... RET
  Parent major mode from which special major modes should inherit.
tildify-space		      M-x ... RET
  Convert space before point into a hard space if the context is
  right.
tool-bar-mode		      M-x ... RET
  Toggle the tool bar in all graphical frames (Tool Bar mode).
  Value: nil
windmove-down		      M-x ... RET
  Select the window below the current one.
apropos-value		      <menu-bar> <help-menu> <search-documentation> <find-option-by-value>
  Show all symbols whose value's printed representation matches
  PATTERN.
authinfo-mode		      M-x ... RET
  Mode for editing .authinfo/.netrc files.
c-or-c++-mode		      M-x ... RET
  Analyze buffer and enable either C or C++ mode.
c-set-offset		      M-x ... RET
  Change the value of a syntactic element symbol in `c-offsets-alist'.
complete-tag		      M-x ... RET
  Perform tags completion on the text around point.
describe-face		      <menu-bar> <help-menu> <describe> <describe-face>
  Display the properties of face FACE on FRAME.
describe-mode		      <menu-bar> <help-menu> <describe> <describe-mode>, <help> m, <f1> m
  Display documentation of current major mode and minor modes.
display-time		      M-x ... RET
  Enable display of time, load level, and mail flag in mode lines.
doc-view-mode		      M-x ... RET
  Major mode in DocView buffers.
ediff-backup		      M-x ... RET
  Run Ediff on FILE and its backup file.
ediff-buffers		      <menu-bar> <tools> <compare> <ediff-buffers>
  Run Ediff on a pair of buffers, BUFFER-A and BUFFER-B.
elint-defun		      <menu-bar> <emacs-lisp> <Linting> <Lint Defun>
  Lint the function at point.
end-of-buffer		      <menu-bar> <edit> <goto> <end-of-buf>, M->, <end>, C-<end>, <kp-end>
  Move point to the end of the buffer.
epatch-buffer		      M-x ... RET
  Run Ediff by patching the buffer specified at prompt.
gnus-agentize		      M-x ... RET
  Allow Gnus to be an offline newsreader.
ls-lisp--dired		      M-x ... RET
  (not documented)
markdown-do		      M-x ... RET
  Do something sensible based on context at point.
mlscroll-mode		      M-x ... RET
  Minor mode for displaying an interactive scrollbar in the mode line.
  Value: t
mwheel-scroll		      <header-line> <wheel-up>, <header-line> <wheel-down>, <header-line> <wheel-left>, <header-line> <wheel-right>, <header-line> <mouse-4>, <header-line> <mouse-5>, <header-line> <mouse-6>, <header-line> <mouse-7>, <mode-line> <wheel-up>, <mode-line> <wheel-down>, <mode-line> <wheel-left>, <mode-line> <wheel-right>, <mode-line> <mouse-4>, <mode-line> <mouse-5>, <mode-line> <mouse-6>, <mode-line> <mouse-7>, <vertical-scroll-bar> <wheel-up>, <vertical-scroll-bar> <wheel-down>, <vertical-scroll-bar> <wheel-left>, <vertical-scroll-bar> <wheel-right>, <vertical-scroll-bar> <mouse-4>, <vertical-scroll-bar> <mouse-5>, <vertical-scroll-bar> <mouse-6>, <vertical-scroll-bar> <mouse-7>, <horizontal-scroll-bar> <wheel-up>, <horizontal-scroll-bar> <wheel-down>, <horizontal-scroll-bar> <wheel-left>, <horizontal-scroll-bar> <wheel-right>, <horizontal-scroll-bar> <mouse-4>, <horizontal-scroll-bar> <mouse-5>, <horizontal-scroll-bar> <mouse-6>, <horizontal-scroll-bar> <mouse-7>, <left-margin> <wheel-up>, <left-margin> <wheel-down>, <left-margin> <wheel-left>, <left-margin> <wheel-right>, <left-margin> <mouse-4>, <left-margin> <mouse-5>, <left-margin> <mouse-6>, <left-margin> <mouse-7>, <right-margin> <wheel-up>, <right-margin> <wheel-down>, <right-margin> <wheel-left>, <right-margin> <wheel-right>, <right-margin> <mouse-4>, <right-margin> <mouse-5>, <right-margin> <mouse-6>, <right-margin> <mouse-7>, <left-fringe> <wheel-up>, <left-fringe> <wheel-down>, <left-fringe> <wheel-left>, <left-fringe> <wheel-right>, <left-fringe> <mouse-4>, <left-fringe> <mouse-5>, <left-fringe> <mouse-6>, <left-fringe> <mouse-7>, <right-fringe> <wheel-up>, <right-fringe> <wheel-down>, <right-fringe> <wheel-left>, <right-fringe> <wheel-right>, <right-fringe> <mouse-4>, <right-fringe> <mouse-5>, <right-fringe> <mouse-6>, <right-fringe> <mouse-7>, <wheel-up>, <wheel-down>, <wheel-left>, <wheel-right>, <mouse-4>, <mouse-5>, <mouse-6>, <mouse-7>, S-<wheel-up>, S-<wheel-down>, S-<wheel-left>, S-<wheel-right>, S-<mouse-4>, S-<mouse-5>, S-<mouse-6>, S-<mouse-7>, M-<wheel-up>, M-<wheel-down>, M-<wheel-left>, M-<wheel-right>, M-<mouse-4>, M-<mouse-5>, M-<mouse-6>, M-<mouse-7>
  Scroll up or down according to the EVENT.
my/open-with		      M-x ... RET
  使用外部程序打开浏览的文件或者当前光标下的链接.
org-bibtex		      M-x ... RET
  Export each headline in the current file to a bibtex entry.
org-clock-out		      M-x ... RET
  Stop the currently running clock.
org-shiftleft		      M-x ... RET
  Act on current element according to context.
org-shifttab		      M-x ... RET
  Global visibility cycling or move to previous table field.
pr-interface		      M-x ... RET
  Activate the printing interface buffer.
rename-buffer		      M-x ... RET
  Change current buffer's name to NEWNAME (a string).
sh-base-mode		      M-x ... RET
  Generic major mode for editing shell scripts.
show-entry		      M-x ... RET
  Show the body directly following this heading.
smerge-ediff		      M-x ... RET
  Invoke ediff to resolve the conflicts.
suspend-emacs		      M-x ... RET
  Stop Emacs and return to superior process.  You can resume later.
tab-line-mode		      M-x ... RET
  Toggle display of tab line in the windows displaying the current
  buffer.
vc-log-search		      M-x ... RET
  Search the log of changes for PATTERN.
vc-mode-line		      M-x ... RET
  Set `vc-mode' to display type of version control for FILE.
view-lossage		      <menu-bar> <help-menu> <describe> <list-recent-keystrokes>, <help> l, <f1> l
  Display last few input keystrokes and the commands run.
windmove-left		      M-x ... RET
  Select the window to the left of the current one.
about-emacs		      <menu-bar> <help-menu> <about-emacs>, <help> C-a, <f1> C-a
  Display the *About GNU Emacs* buffer.
align-current		      M-x ... RET
  Call `align' on the current alignment section.
apropos-mode		      M-x ... RET
  Major mode for following hyperlinks in output of apropos commands.
avy-goto-line		      M-g f
  Jump to a line start in current buffer.
backward-list		      C-M-p
  Move backward across one balanced group of parentheses.
backward-sexp		      ESC C-<left>, C-M-b, C-M-<left>, <menu-bar> <emacs-lisp> <Navigation> <Backward Sexp>
  Move backward across one balanced expression (sexp).
color-rg-mode		      M-x ... RET
  Major mode derived from `text-mode' by `define-derived-mode'.
ediff-files3		      <menu-bar> <tools> <compare> <ediff-files3>
  Run Ediff on three files, FILE-A, FILE-B, and FILE-C.
emacs-version		      M-x ... RET
  Display the version of Emacs that is running in this session.
  Value: "30.0.50"
emoji-search		      M-x ... RET
  Choose and insert an emoji glyph by typing its Unicode name.
find-function		      M-x ... RET
  Find the definition of the FUNCTION near point.
help-follow		      M-x ... RET
  Follow cross-reference at point.
hippie-expand		      M-/
  Try to expand text before point, using multiple methods.
httpd-start		      M-x ... RET
  Start the web server process. If the server is already
iso-gtex2iso		      M-x ... RET
  Translate German TeX sequences to ISO 8859-1 characters.
iso-iso2gtex		      M-x ... RET
  Translate ISO 8859-1 characters to German TeX sequences.
js-mode			      M-x ... RET
  Major mode for editing JavaScript.
list-fontsets		      M-x ... RET
  Display a list of all fontsets.
lossage-size		      M-x ... RET
  Return or set the maximum number of keystrokes to save.
m4-mode			      M-x ... RET
  A major mode to edit m4 macro files.
nslookup-host		      M-x ... RET
  Look up the DNS information for HOST (name or IP address).
org-metaright		      M-x ... RET
  Demote heading, list item at point or move table column right.
origami-undo		      M-x ... RET
  Undo the last folding operation applied to BUFFER.
outline-cycle		      M-x ... RET
  Cycle visibility state of the current heading line's body.
shrink-window		      M-x ... RET
  Make the selected window DELTA lines smaller.
subword-kill		      M-x ... RET
  Do the same as `kill-word' but on subwords.
subword-mark		      M-x ... RET
  Do the same as `mark-word' but on subwords.
tab-previous		      M-x ... RET
  Switch to ARGth previous tab.
vc-dir-root		      <menu-bar> <tools> <vc> <vc-dir-root>
  Run `vc-dir' in the repository root directory without prompt.
Info-search		      M-x ... RET
  Search for REGEXP, starting from point, and select node it's found
  in.
backtrace-mode		      M-x ... RET
  Generic major mode for examining an Elisp stack backtrace.
backward-char		      C-b
  Move point N characters backward (forward if N is negative).
c-subword-mode		      M-x ... RET
  Toggle subword movement and editing (Subword mode).
calendar-mode		      M-x ... RET
  A major mode for the calendar window.
compose-region		      M-x ... RET
  Compose characters in the current region.
conf-ppd-mode		      M-x ... RET
  Conf Mode starter for Adobe/CUPS PPD files.
dabbrev-expand		      C-<tab>
  Expand previous word "dynamically".
doctor			      <menu-bar> <help-menu> <emacs-psychotherapist>
  Switch to *doctor* buffer and start giving psychotherapy.
dunnet			      <menu-bar> <tools> <games> <adventure>
  Switch to *dungeon* buffer and start game.
epa-info-mode		      M-x ... RET
  Major mode for `epa-info-buffer'.
eww-readable		      M-x ... RET
  View the main "readable" parts of the current web page.
find-variable		      M-x ... RET
  Find the definition of the VARIABLE at or before point.
font-lock-mode		      M-x ... RET
  Toggle syntax highlighting in this buffer (Font Lock mode).
  Value: t
gnus-child		      M-x ... RET
  Read news as a child.
  Value: nil
gnus-slave		      M-x ... RET
  Read news as a child.
inferior-lisp		      M-x ... RET
  Run an inferior Lisp process, input and output via buffer
  `*inferior-lisp*'.
js-base-mode		      M-x ... RET
  Generic major mode for editing JavaScript.
mairix-search		      M-x ... RET
  Call Mairix with SEARCH.
org-emphasize		      M-x ... RET
  Insert or change an emphasis, i.e. a font like bold or italic.
overwrite-mode		      <insert>, <insertchar>
  Toggle Overwrite mode.
  Value: nil
pcre-to-elisp		      M-x ... RET
  Translate PCRE, a regexp in Perl-compatible syntax, to Emacs Lisp.
plain-TeX-mode		      M-x ... RET
  Major mode for editing files of input for plain TeX.
plain-tex-mode		      M-x ... RET
  Major mode for editing files of input for plain TeX.
read-only-mode		      M-x ... RET
  Change whether the current buffer is read-only.
replace-regexp		      M-x ... RET
  Replace things after point matching REGEXP with TO-STRING.
replace-string		      M-x ... RET
  Replace occurrences of FROM-STRING with TO-STRING.
rst-minor-mode		      M-x ... RET
  Toggle ReST minor mode.
search-forward		      <find>
  Search forward from point for STRING.
set-frame-font		      M-x ... RET
  Set the default font to FONT.
timeclock-out		      M-x ... RET
  Clock out, recording the current time moment in the timelog.
trace-function		      <menu-bar> <emacs-lisp> <Tracing> <Trace Function...>
  Trace calls to function FUNCTION.
unload-feature		      M-x ... RET
  Unload the library that provided FEATURE.
wallpaper-set		      M-x ... RET
  Set the desktop background to FILE in a graphical environment.
yas-minor-mode		      M-x ... RET
  Toggle YASnippet mode.
  Value: t
ad-activate-on		      M-x ... RET
  Activate all the advice information of an advised FUNCTION.
auto-fill-mode		      M-x ... RET
  Toggle automatic line breaking (Auto Fill mode).
auto-save-mode		      M-x ... RET
  Toggle auto-saving in the current buffer (Auto Save mode).
bash-ts-mode		      M-x ... RET
  Major mode for editing Bash shell scripts.
edit-kbd-macro		      M-x ... RET
  Edit a keyboard macro.
eieio-browse		      M-x ... RET
  Create an object browser window to show all objects.
erase-buffer		      M-x ... RET
  Delete the entire contents of the current buffer.
eval-last-sexp		      <menu-bar> <emacs-lisp> <Evaluate Last S-expression>
  Evaluate sexp before point; print value in the echo area.
eww-next-url		      M-x ... RET
  Go to the page marked `next'.
fill-paragraph		      M-q
  Fill paragraph at or after point.
free-keys-mode		      M-x ... RET
  Free keys mode.
full-calc		      M-x ... RET
  Invoke the Calculator and give it a full-sized window.
global-set-key		      M-x ... RET
  Give KEY a global binding as COMMAND.
gnus-no-server		      M-x ... RET
  Read network news.
gpm-mouse-mode		      M-x ... RET
  Toggle mouse support in GNU/Linux consoles (GPM Mouse mode).
  Value: t
icomplete-mode		      M-x ... RET
  Toggle incremental minibuffer completion (Icomplete mode).
  Value: t
iconify-frame		      s-m
  Make the frame FRAME into an icon.
indent-rigidly		      M-x ... RET
  Indent all lines starting in the region.
iso-iso2sgml		      M-x ... RET
  Translate ISO 8859-1 characters in the region to SGML entities.
iso-sgml2iso		      M-x ... RET
  Translate SGML entities in the region to ISO 8859-1 characters.
ispell-message		      <menu-bar> <tools> <spell> <ispell-message>, <menu-bar> <edit> <spell> <ispell-message>
  Check the spelling of a mail message or news post.
ispell-region		      <menu-bar> <tools> <spell> <ispell-region>, <menu-bar> <edit> <spell> <ispell-region>
  Interactively check a region for spelling errors.
keyboard-quit		      C-g
  Signal a `quit' condition.
kill-ring-save		      M-w, C-<insert>, C-<insertchar>, s-c
  Save the region as if killed, but don't kill it.
list-timers		      M-x ... RET
  List all timers in a buffer.
magit-version		      M-x ... RET
  Return the version of Magit currently in use.
magit-wip-mode		      M-x ... RET
  Save uncommitted changes to work-in-progress refs.
  Value: nil
markdown-mode		      M-x ... RET
  Major mode for editing Markdown files.
mh-folder-mode		      M-x ... RET
  Major MH-E mode for "editing" an MH folder scan
  listing.\<mh-folder-mode-map>
move-to-column		      M-g TAB
  Move point to column COLUMN in the current line.
org-next-block		      M-x ... RET
  Jump to the next block.
org-store-link		      M-x ... RET
  Store a link to the current location.
package-delete		      M-x ... RET
  Delete package PKG-DESC.
profiler-start		      <menu-bar> <emacs-lisp> <Profiling> <Start Native Profiler...>
  Start/restart profilers.
regexp-builder		      M-x ... RET
  Construct a regexp interactively.
translate-mode		      M-x ... RET
  Minor mode for translation buffer.
vc-next-action		      <menu-bar> <tools> <vc> <vc-next-action>
  Do the next logical version control operation on the current
  fileset.
ad-deactivate		      M-x ... RET
  Deactivate the advice of an actively advised FUNCTION.
avy-linum-mode		      M-x ... RET
  Minor mode that uses avy hints for `linum-mode'.
  Value: nil
comment-region		      <menu-bar> <emacs-lisp> <Comment Out Region>
  Comment or uncomment each line in the region.
copy-directory		      M-x ... RET
  Copy DIRECTORY to NEWNAME.  Both args must be strings.
dbus-monitor		      M-x ... RET
  Invoke `dbus-register-monitor' interactively, and switch to the
  buffer.
easy-menu-add		      M-x ... RET
  Do nothing and return nil.
ebnf-eps-file		      M-x ... RET
  Generate an EPS file from EBNF file FILE.
edebug-stop		      M-x ... RET
  Stop execution and do not continue.
expand-abbrev		      M-x ... RET
  Expand the abbrev before point, if there is an abbrev there.
ffap-at-mouse		      M-x ... RET
  Find file or URL guessed from text around mouse click.
font-show-log		      M-x ... RET
  Show log of font listing and opening.
gnus-undo-mode		      M-x ... RET
  Minor mode for providing `undo' in Gnus buffers.
  Value: nil
locate-library		      M-x ... RET
  Show the precise file name of Emacs library LIBRARY.
mail-fcc		      M-x ... RET
  Add a new Fcc field, with file name completion.
org-kill-line		      M-x ... RET
  Kill line, to tags or end of line.
org-next-link		      M-x ... RET
  Move forward to the next link.
org-time-stamp		      M-x ... RET
  Prompt for a date/time and insert a time stamp.
origami-redo		      M-x ... RET
  Redo the last folding operation applied to BUFFER.
outline-demote		      M-x ... RET
  Demote headings lower down the tree.
read-kbd-macro		      M-x ... RET
  Read the region as a keyboard macro definition.
recursive-edit		      M-x ... RET
  Invoke the editor command loop recursively.
remember-notes		      M-x ... RET
  Return the notes buffer, creating it if needed, and maybe switch to
  it.
superword-mode		      M-x ... RET
  Toggle superword movement and editing (Superword mode).
  Value: nil
vc-create-tag		      <menu-bar> <tools> <vc> <vc-create-tag>
  Descending recursively from DIR, make a tag called NAME.
Info-goto-node		      M-x ... RET
  Go to Info node named NODENAME.  Give just NODENAME or
  (FILENAME)NODENAME.
appt-activate		      M-x ... RET
  Toggle checking of appointments.
copyright		      M-x ... RET
  Insert a copyright by $ORGANIZATION notice at cursor.
debug-on-entry		      M-x ... RET
  Request FUNCTION to invoke debugger each time it is called.
delete-region		      M-x ... RET
  Delete the text between START and END.
dired-do-chmod		      M-x ... RET
  Change the mode of the marked (or next ARG) files.
hide-other		      M-x ... RET
  Hide everything except current body and parent and top-level
  headings.
info-apropos		      <menu-bar> <help-menu> <more-manuals> <lookup-subject-in-all-manuals>
  Search indices of all known Info files on your system for STRING.
js-ts-mode		      M-x ... RET
  Major mode for editing JavaScript.
lock-file-mode		      M-x ... RET
  Toggle file locking in the current buffer (Lock File mode).
  Value: nil
lunar-phases		      M-x ... RET
  Display the quarters of the moon for last month, this month, and
  next month.
mail-signature		      M-x ... RET
  Sign letter with signature.
  Value: t
mark-paragraph		      M-h
  Put point at beginning of this paragraph, mark at end.
mpv-play-url		      M-x ... RET
  Start an mpv process playing the video stream at URL.
org-content		      M-x ... RET
  Show all headlines in the buffer, like a table of contents.
org-shiftdown		      M-x ... RET
  Act on current element according to context.
org-todo-list		      M-x ... RET
  Show all (not done) TODO entries from all agenda files in a single
  list.
restart-emacs		      M-x ... RET
  Kill the current Emacs process and start a new one.
show-children		      M-x ... RET
  Show all direct subheadings of this heading.
signal-process		      M-x ... RET
  Send PROCESS the signal with code SIGCODE.
subed-align		      M-x ... RET
  Align AUDIO-FILE with TEXT-FILE to get timestamps.
tildify-buffer		      M-x ... RET
  Add hard spaces in the current buffer.
upcase-region		      M-x ... RET
  Convert the region to upper case.  In programs, wants two arguments.
xeft-refresh		      M-x ... RET
  Search for notes and display their summaries.
Info-scroll-up		      M-x ... RET
  Scroll one screenful forward in Info, considering all nodes as one
  sequence.
bibtex-entry		      M-x ... RET
  Insert a template for a BibTeX entry of type ENTRY-TYPE.
c-macro-expand		      M-x ... RET
  Expand C macros in the region, using the C preprocessor.
comment-kill		      M-x ... RET
  Kill the first comment on this line, if any.
copy-to-buffer		      M-x ... RET
  Copy to specified BUFFER the text of the region.
csharp-mode		      M-x ... RET
  Major mode for editing Csharp code.
diff-wiggle		      M-x ... RET
  Use `wiggle' to apply the whole current file diff by hook or by
  crook.
ediff-revision		      <menu-bar> <tools> <compare> <ediff-revision>
  Run Ediff by comparing versions of a file.
gfm-view-mode		      M-x ... RET
  Major mode for viewing GitHub Flavored Markdown content.
iso-iso2duden		      M-x ... RET
  Translate ISO 8859-1 characters to Duden sequences.
kill-comment		      M-x ... RET
  Kill the first comment on this line, if any.
kill-process		      M-x ... RET
  Kill process PROCESS.  May be process or name of one.
kill-rectangle		      M-x ... RET
  Delete the region-rectangle and save it as the last killed one.
list-processes		      M-x ... RET
  Display a list of all processes that are Emacs sub-processes.
make-directory		      M-x ... RET
  Create the directory DIR and optionally any nonexistent parent dirs.
markdown-cycle		      M-x ... RET
  Visibility cycling for Markdown mode.
memory-report		      M-x ... RET
  Generate a report of how Emacs is using memory.
mml-insert-part		      M-x ... RET
  (not documented)
org-add-note		      M-x ... RET
  Add a note to the current entry.
org-next-item		      M-x ... RET
  Move to the beginning of the next item.
org-open-line		      M-x ... RET
  Insert a new row in tables, call `open-line' elsewhere.
org-shiftright		      M-x ... RET
  Act on the current element according to context.
project-shell		      M-x ... RET
  Start an inferior shell in the current project's root directory.
reverse-region		      M-x ... RET
  Reverse the order of lines in a region.
send-invisible		      M-x ... RET
  Read a string without echoing.
shr-browse-url		      M-x ... RET
  Browse the URL at point using `browse-url'.
suspend-frame		      C-z
  Do whatever is right to suspend the current frame.
tags-next-file		      M-x ... RET
  Select next file among files in current tags table.
todo-mode		      M-x ... RET
  Major mode for displaying, navigating and editing todo lists.
windmove-right		      M-x ... RET
  Select the window to the right of the current one.
2C-two-columns		      M-x ... RET
  Split current window vertically for two-column editing.
ad-unadvise		      M-x ... RET
  Deactivate FUNCTION and then remove all its advice information.
append-to-file		      M-x ... RET
  Append the contents of the region to the end of file FILENAME.
company-files		      M-x ... RET
  `company-mode' completion backend existing file names.
customize-face		      <menu-bar> <options> <customize> <customize-face>
  Customize FACE, which should be a face name or nil.
diff-undo		      M-x ... RET
  Perform `undo', ignoring the buffer's read-only status.
downcase-word		      M-l
  Convert to lower case from point to end of word, moving over.
duplicate-dwim		      M-x ... RET
  Duplicate the current line or region N times.
ediff-buffers3		      <menu-bar> <tools> <compare> <ediff-buffers3>
  Run Ediff on three buffers, BUFFER-A, BUFFER-B, and BUFFER-C.
enlarge-window		      M-x ... RET
  Make the selected window DELTA lines taller.
epa-mark-key		      M-x ... RET
  Mark a key on the current line.
forward-button		      M-x ... RET
  Move to the Nth next button, or Nth previous button if N is
  negative.
load-file		      M-x ... RET
  Load the Lisp file named FILE.
open-rectangle		      M-x ... RET
  Blank out the region-rectangle, shifting text right.
org-id-goto		      M-x ... RET
  Switch to the buffer containing the entry with id ID.
pr-ps-fast-fire		      M-x ... RET
  Fast fire function for PostScript printing.
tildify-region		      M-x ... RET
  Add hard spaces in the region between BEG and END.
type-break-mode		      M-x ... RET
  Enable or disable typing-break mode.
  Value: nil
zap-up-to-char		      M-x ... RET
  Kill up to, but not including ARGth occurrence of CHAR.
auto-insert		      M-x ... RET
  Insert default contents into new files if variable `auto-insert' is
  non-nil.
c-guess-region		      M-x ... RET
  Guess the style on the region and install it.
conf-toml-mode		      M-x ... RET
  Conf Mode starter for TOML files.
diff-minor-mode		      M-x ... RET
  Toggle Diff minor mode.
  Value: nil
edit-tab-stops		      M-x ... RET
  Edit the tab stops used by `tab-to-tab-stop'.
elint-file		      <menu-bar> <emacs-lisp> <Linting> <Lint File...>
  Lint the file FILE.
emerge-files		      M-x ... RET
  Run Emerge on two files FILE-A and FILE-B.
eval-expression		      M-ESC :, M-:
  Evaluate EXP and print value in the echo area.
eww-download		      M-x ... RET
  Download URL to `eww-download-directory'.
git-gutter-mode		      M-x ... RET
  Git-Gutter mode
  Value: nil
gnus-group-mode		      M-x ... RET
  Major mode for reading news.
grugru-select		      M-x ... RET
  Replace string between BEGIN and END to STR.
hide-ifdef-mode		      M-x ... RET
  Toggle features to hide/show #ifdef blocks (Hide-Ifdef mode).
ibuffer-update		      M-x ... RET
  Regenerate the list of all buffers.
isearch-abort		      M-x ... RET
  Abort incremental search mode if searching is successful, signaling
  quit.
kill-sentence		      M-k
  Kill from point to end of sentence.
lsp-bridge-mode		      M-x ... RET
  LSP Bridge mode.
  Value: nil
mail-cc			      M-x ... RET
  Move point to end of Cc field, creating it if necessary.
mail-to			      M-x ... RET
  Move point to end of To field, creating it if necessary.
message-mailto		      M-x ... RET
  Command to parse command line mailto: links.
netstat			      M-x ... RET
  Run `netstat-program' and display diagnostic output.
nov-mode		      M-x ... RET
  Major mode for reading EPUB documents
org-indent-line		      M-x ... RET
  Indent line depending on context.
org-insert-link		      M-x ... RET
  Insert a link.  At the prompt, enter the link.
org-roam-graph		      M-x ... RET
  Build and possibly display a graph for NODE.
org-search-view		      M-x ... RET
  Show all entries that contain a phrase or words or regular
  expressions.
org-set-effort		      M-x ... RET
  Set the effort property of the current entry.
rot13-region		      M-x ... RET
  ROT13 encrypt the region between START and END in current buffer.
run-with-timer		      M-x ... RET
  Perform an action after a delay of SECS seconds.
search-backward		      M-x ... RET
  Search backward from point for STRING.
set-file-modes		      M-x ... RET
  Set mode bits of file named FILENAME to MODE (an integer).
shr-probe-url		      M-x ... RET
  Pass URL's redirect destination to CONT, if it has one.
sis-get			      M-x ... RET
  Get input source.
srt-mode		      M-x ... RET
  Major-mode for writing SRecode macros.
table-release		      M-x ... RET
  Convert a table into plain text by removing the frame from a table.
undelete-frame		      <menu-bar> <file> <undelete-last-deleted-frame>
  Undelete a frame deleted with `delete-frame'.
x-set-selection		      M-x ... RET
  Make an X selection of type TYPE and value DATA.
ace-pinyin-mode		      M-x ... RET
  Toggle `ace-pinyin-mode'.
  Value: t
auto-lower-mode		      M-x ... RET
  Toggle whether or not the selected frame should auto-lower.
auto-raise-mode		      M-x ... RET
  Toggle whether or not selected frames should auto-raise.
bs-cycle-next		      M-x ... RET
  Select next buffer defined by buffer cycling.
citre-ace-peek		      M-x ... RET
  Peek the definition of a symbol on screen using ace jump.
company-css		      M-x ... RET
  `company-mode' completion backend for `css-mode'.
compare-windows		      <menu-bar> <tools> <compare> <window>
  Compare text in current window with text in another window.
compile-defun		      M-x ... RET
  Compile and evaluate the current top-level form.
define-abbrevs		      M-x ... RET
  Define abbrevs according to current visible buffer contents.
disable-theme		      M-x ... RET
  Disable all variable and face settings defined by THEME.
downcase-dwim		      M-x ... RET
  Downcase words in the region, if active; if not, downcase word at
  point.
edit-bookmarks		      M-x ... RET
  Display a list of existing bookmarks.
emacs-lock-mode		      M-x ... RET
  Toggle Emacs Lock mode in the current buffer.
emoji-describe		      M-x ... RET
  Display the name of the grapheme cluster composed from GLYPH.
forward-page		      M-x ... RET
  Move forward to page boundary.  With arg, repeat, or go back if
  negative.
garbage-collect		      M-x ... RET
  Reclaim storage for Lisp objects no longer needed.
goto-next-locus		      M-x ... RET
  Visit next `next-error' message and corresponding source code.
ibuffer-jump		      M-x ... RET
  Call Ibuffer and set point at the line listing the current buffer.
info-xref-check		      M-x ... RET
  Check external references in FILENAME, an info document.
kill-whole-line		      C-S-<backspace>
  Kill current line.
  Value: nil
list-bookmarks		      M-x ... RET
  Display a list of existing bookmarks.
message-bounce		      M-x ... RET
  Re-mail the current message.
mml-attach-file		      M-x ... RET
  Attach a file to the outgoing MIME message.
morse-region		      M-x ... RET
  Convert plain text in region to Morse code.
org-sparse-tree		      M-x ... RET
  Create a sparse tree, prompt for the details.
package-install		      M-x ... RET
  Install the package PKG.
set-frame-name		      M-x ... RET
  Set the name of the selected frame to NAME.
shortdoc-next		      M-x ... RET
  Move point to the next function.
view-register		      M-x ... RET
  Display what is contained in register named REGISTER.
widget-describe		      M-x ... RET
  Describe the widget at point.
xref-etags-mode		      M-x ... RET
  Minor mode to make xref use etags again.
  Value: nil
ad-update-all		      M-x ... RET
  Update all currently advised functions.
advertised-undo		      M-x ... RET
  Undo some previous changes.
c-guess-buffer		      M-x ... RET
  Guess the style on the whole current buffer, and install it.
checkdoc-defun		      M-x ... RET
  Examine the doc string of the function or variable under point.
conf-space-mode		      M-x ... RET
  Conf Mode starter for space separated conf files.
corfu-echo-mode		      M-x ... RET
  Show candidate documentation in echo area.
  Value: nil
elide-head-mode		      M-x ... RET
  Toggle eliding (hiding) header material in the current buffer.
find-tag-regexp		      M-x ... RET
  Find tag (in current tags table) whose name matches REGEXP.
gc-buffers-mode		      M-x ... RET
  Toggle killing garbage buffers automatically.
  Value: nil
global-ede-mode		      <menu-bar> <tools> <ede>
  Toggle global EDE (Emacs Development Environment) mode.
  Value: nil
gnus-version		      M-x ... RET
  Version number of this version of Gnus.
  Value: "Gnus v5.13"
help-make-xrefs		      M-x ... RET
  Parse and hyperlink documentation cross-references in the given
  BUFFER.
hexl-find-file		      M-x ... RET
  Edit file FILENAME as a binary file in hex dump format.
image-scroll-up		      M-x ... RET
  Scroll image in current window upward by N lines.
kbd-macro-query		      M-x ... RET
  Query user during kbd macro execution.
list-directory		      M-x ... RET
  Display a list of files in or matching DIRNAME, a la `ls'.
magit-checkout		      M-x ... RET
  Checkout REVISION, updating the index and the working tree.
magit-find-file		      M-x ... RET
  View FILE from REV.
maximize-window		      M-x ... RET
  Maximize WINDOW.
mercury-mode		      M-x ... RET
  Major mode for editing Mercury programs.
minimize-window		      M-x ... RET
  Minimize WINDOW.
mouse-drag-drag		      M-x ... RET
  "Drag" the page according to a mouse drag.
mouse-set-font		      M-x ... RET
  Set the default font for the selected frame.
on-screen-mode		      M-x ... RET
  Buffer local minor mode guiding your eyes while scrolling.
  Value: nil
org-indent-mode		      M-x ... RET
  When active, indent text according to outline structure.
outline-promote		      M-x ... RET
  Promote headings higher up the tree.
project-eshell		      M-x ... RET
  Start Eshell in the current project's root directory.
rxt-global-mode		      M-x ... RET
  Toggle Rxt mode in all buffers.
  Value: nil
save-place-mode		      M-x ... RET
  Non-nil means automatically save place in each file.
  Value: nil
scroll-bar-mode		      M-x ... RET
  Toggle vertical scroll bars on all frames (Scroll Bar mode).
  Value: right
set-frame-width		      M-x ... RET
  Set text width of frame FRAME to WIDTH columns.
show-paren-mode		      <menu-bar> <options> <highlight-paren-mode>
  Toggle visualization of matching parens (Show Paren mode).
  Value: t
shr-zoom-image		      M-x ... RET
  Toggle the image size.
sis-inline-mode		      M-x ... RET
  English overlay mode for mixed language editing.
  Value: t
so-long-revert		      M-x ... RET
  Revert the active `so-long-action' and run `so-long-revert-hook'.
table-justify		      M-x ... RET
  Justify contents of a cell, a row of cells or a column of cells.
text-scale-set		      M-x ... RET
  Set the scale factor of the default face in the current buffer to
  LEVEL.
whitespace-mode		      M-x ... RET
  Toggle whitespace visualization (Whitespace mode).
  Value: nil
wy-go-to-char		      M-x ... RET
  Move forward to Nth occurence of CHAR.
yas-reload-all		      <menu-bar> <yasnippet> <Reload everything>
  Reload all snippets and rebuild the YASnippet menu.
yas/reload-all		      M-x ... RET
  Reload all snippets and rebuild the YASnippet menu.
ad-recover-all		      M-x ... RET
  Recover all currently advised functions.  Use in emergencies.
apropos-command		      <menu-bar> <help-menu> <search-documentation> <find-commands-by-name>, <help> a, <f1> a
  Show commands (interactively callable functions) that match PATTERN.
bookmark-delete		      <menu-bar> <edit> <bookmark> <delete>
  Delete BOOKMARK-NAME from the bookmark list.
bookmark-rename		      <menu-bar> <edit> <bookmark> <rename>
  Change the name of OLD-NAME bookmark to NEW-NAME name.
browse-url-mail		      M-x ... RET
  Open a new mail message buffer within Emacs for the RFC 2368 URL.
cfengine2-mode		      M-x ... RET
  Major mode for editing CFEngine2 input.
cfengine3-mode		      M-x ... RET
  Major mode for editing CFEngine3 input.
command-apropos		      M-x ... RET
  Show commands (interactively callable functions) that match PATTERN.
delete-process		      M-x ... RET
  Delete PROCESS: kill it and forget about it immediately.
dired-omit-mode		      M-x ... RET
  Toggle omission of uninteresting files in Dired (Dired-Omit mode).
duplicate-line		      M-x ... RET
  Duplicate the current line N times.
forward-symbol		      M-x ... RET
  Move point to the next position that is the end of a symbol.
hide-subtree		      M-x ... RET
  Hide everything after this heading at deeper levels.
ido-write-file		      M-x ... RET
  Write current buffer to a file.
indent-relative		      M-x ... RET
  Space out to under next indent point in previous nonblank line.
insert-buffer		      M-x ... RET
  Insert after point the contents of BUFFER.
kill-paragraph		      M-x ... RET
  Kill forward to end of paragraph.
make-face-bold		      M-x ... RET
  Make the font of FACE be bold, if possible.
marginalia-mode		      M-x ... RET
  Annotate completion candidates with richer information.
  Value: t
narrow-to-page		      M-x ... RET
  Make text outside current page invisible.
org-beamer-mode		      M-x ... RET
  Support for editing Beamer oriented Org mode files.
org-publish-all		      M-x ... RET
  Publish all projects.
org-timer-start		      M-x ... RET
  Set the starting time for the relative timer to now.
quit-windows-on		      M-x ... RET
  Quit all windows showing BUFFER-OR-NAME.
scroll-all-mode		      M-x ... RET
  Toggle shared scrolling in same-frame windows (Scroll-All mode).
  Value: nil
show-subtree		      M-x ... RET
  Show everything after this heading at deeper levels.
sunrise-sunset		      M-x ... RET
  Local time of sunrise and sunset for today.  Accurate to a few
  seconds.
tab-bar-new-tab		      M-x ... RET
  Create a new tab ARG positions to the right.
vc-dir-git-mode		      M-x ... RET
  A minor mode for git-specific commands in `vc-dir-mode' buffers.
  Value: nil
vc-retrieve-tag		      <menu-bar> <tools> <vc> <vc-retrieve-tag>
  For each file in or below DIR, retrieve their tagged version NAME.
yas-global-mode		      M-x ... RET
  Toggle Yas minor mode in all buffers.
  Value: t
yas/global-mode		      M-x ... RET
  Toggle Yas minor mode in all buffers.
  Value: t
add-mode-abbrev		      M-x ... RET
  Define a mode-specific abbrev whose expansion is the last word
  before point.
bibtex-validate		      M-x ... RET
  Validate if buffer or region is syntactically correct.
bibtex-yank-pop		      M-x ... RET
  Replace just-yanked killed BibTeX item with a different item.
browse-url-kde		      M-x ... RET
  Ask the KDE WWW browser to load URL.
buffer-face-set		      M-x ... RET
  Enable `buffer-face-mode', using face specs SPECS.
checkdoc-dired		      M-x ... RET
  In Dired, run `checkdoc' on marked files.
checkdoc-start		      M-x ... RET
  Start scanning the current buffer for documentation string style
  errors.
clear-rectangle		      M-x ... RET
  Blank out the region-rectangle.
command-history		      M-x ... RET
  Examine commands from variable `command-history' in a buffer.
  Value: ((apropos-command ".*" nil) (execute-extended-command nil "apropos-command")…
describe-widget		      M-x ... RET
  Display a buffer with information about a widget.
diff-file-next		      M-x ... RET
  Go to the next COUNT'th file.
diff-hunk-next		      M-x ... RET
  Go to the next COUNT'th hunk.
ebnf-eps-buffer		      M-x ... RET
  Generate a PostScript syntactic chart image of the buffer in an EPS
  file.
ebnf-eps-region		      M-x ... RET
  Generate a PostScript syntactic chart image of the region in an EPS
  file.
emacs-lisp-mode		      M-x ... RET
  Major mode for editing Lisp code to run in Emacs.
epa-sign-region		      <menu-bar> <tools> <encryption-decryption> <sign-region>
  Sign the current region between START and END by SIGNERS keys
  selected.
fortune-compile		      M-x ... RET
  Compile fortune file.
ido-kill-buffer		      M-x ... RET
  Kill a buffer.
lisp-data-mode		      M-x ... RET
  Major mode for buffers holding data written in Lisp syntax.
magit-next-line		      M-x ... RET
  Like `next-line' but with Magit-specific shift-selection.
occur-edit-mode		      M-x ... RET
  Major mode for editing *Occur* buffers.
org-agenda-list		      M-x ... RET
  Produce a daily/weekly view from all files in variable
  `org-agenda-files'.
org-do-demote		      M-x ... RET
  Demote the current heading lower down the tree.
org-shiftmetaup		      M-x ... RET
  Drag the line at point up.
previous-error		      M-g M-p, M-g p
  Visit previous `next-error' message and corresponding source code.
ps-print-buffer		      <menu-bar> <file> <print> <ps-print-buffer>
  Generate and print a PostScript image of the buffer.
python-mode		      M-x ... RET
  Major mode for editing Python files.
recentf-open		      M-x ... RET
  Prompt for FILE in `recentf-list' and visit it.
set-goal-column		      M-x ... RET
  Set the current horizontal position as a goal column.
sql-vertica		      M-x ... RET
  Run vsql as an inferior process.
tab-duplicate		      M-x ... RET
  Clone the current tab to ARG positions to the right.
url-cookie-mode		      M-x ... RET
  Mode for listing cookies.
View-quit-all		      M-x ... RET
  Quit View mode, trying to restore windows and buffers to previous
  state.
backward-button		      M-x ... RET
  Move to the Nth previous button, or Nth next button if N is
  negative.
bibtex-reformat		      M-x ... RET
  Reformat all BibTeX entries in buffer or region.
capitalize-word		      M-c
  Capitalize from point to the end of word, moving over.
conf-colon-mode		      M-x ... RET
  Conf Mode starter for Colon files.
customize-mode		      M-x ... RET
  Customize options related to a major or minor mode.
describe-symbol		      <help> o, <f1> o
  Display the full documentation of SYMBOL.
diff-file-prev		      M-x ... RET
  Go to the previous COUNT'th file.
diff-hunk-prev		      M-x ... RET
  Go to the previous COUNT'th hunk.
dired-do-print		      M-x ... RET
  Print the marked (or next ARG) files.
dired-do-rename		      M-x ... RET
  Rename current file or all marked (or next ARG) files.
epa-search-keys		      M-x ... RET
  Ask a keyserver for all keys matching QUERY.
eww-follow-link		      M-x ... RET
  Browse the URL under point.
handle-focus-in		      M-x ... RET
  Handle a focus-in event.
inferior-octave		      M-x ... RET
  Run an inferior Octave process, I/O via `inferior-octave-buffer'.
local-unset-key		      M-x ... RET
  Remove local binding of KEY.
mspools-show		      M-x ... RET
  Show the list of non-empty spool files in the *spools* buffer.
org-cite-insert		      M-x ... RET
  Insert a citation at point.
org-ctrl-c-tab		      M-x ... RET
  Toggle columns width in a table, or show children.
org-end-of-line		      M-x ... RET
  Go to the end of the line, but before ellipsis, if any.
org-feed-update		      M-x ... RET
  Get inbox items from FEED.
org-fold-reveal		      M-x ... RET
  Show current entry, hierarchy above it, and the following headline.
org-insert-item		      M-x ... RET
  Insert a new item at the current level.
rename-uniquely		      M-x ... RET
  Rename current buffer to a similar name not already taken.
rfc-mode-read		      M-x ... RET
  Read the RFC document NUMBER.
ruby-mode		      M-x ... RET
  Major mode for editing Ruby code.
start-kbd-macro		      M-x ... RET
  Record subsequent keyboard input, defining a keyboard macro.
traceroute		      M-x ... RET
  Run `traceroute-program' for TARGET.
transpose-sexps		      C-M-t
  Like \[transpose-chars] (`transpose-chars'), but applies to sexps.
view-mode-enable	      M-x ... RET
  Toggle View mode, a minor mode for viewing text but not editing it.
yank-in-context		      M-x ... RET
  Insert the last stretch of killed text while preserving syntax.
Custom-set		      M-x ... RET
  Set the current value of all edited settings in the buffer.
avy-next		      M-x ... RET
  Go to the next candidate of the last `avy-read'.
avy-prev		      M-x ... RET
  Go to the previous candidate of the last `avy-read'.
balance-windows		      M-x ... RET
  Balance the sizes of windows of WINDOW-OR-FRAME.
c-guess-install		      M-x ... RET
  Install the latest guessed style into the current buffer.
calendar-exit		      M-x ... RET
  Get out of the calendar window and hide it and related buffers.
citre-peek--mode	      M-x ... RET
  Mode for `citre-peek'.
  Value: nil
close-rectangle		      M-x ... RET
  Delete all whitespace following a specified column in each line.
company-bbdb		      M-x ... RET
  `company-mode' completion backend for BBDB.
company-box-mode	      M-x ... RET
  Company-box minor mode.
complete-symbol		      M-x ... RET
  Perform completion on the text around point.
decipher		      M-x ... RET
  Format a buffer of ciphertext for cryptanalysis and enter Decipher
  mode.
diff-apply-hunk		      M-x ... RET
  Apply the current hunk to the source file and go to the next.
dig-exit		      M-x ... RET
  Quit dig output buffer.
dired-hide-all		      M-x ... RET
  Hide all subdirectories, leaving only their header lines.
edebug-go-mode		      M-x ... RET
  Go, evaluating until break.
edebug-on-entry		      M-x ... RET
  Cause Edebug to stop when FUNCTION is called.
elisp-refs-mode		      M-x ... RET
  Major mode for refs results buffers.
git-rebase-mode		      M-x ... RET
  Major mode for editing of a Git rebase file.
ifconfig		      M-x ... RET
  Run `ifconfig-program' and display diagnostic output.
isearch-mouse-2		      M-x ... RET
  Handle mouse-2 in Isearch mode.
iwconfig		      M-x ... RET
  Run `iwconfig-program' and display diagnostic output.
mail-bcc		      M-x ... RET
  Move point to end of Bcc field, creating it if necessary.
mouse-set-point		      C-M-<mouse-1>, <mouse-1>
  Move point to the position clicked on with the mouse.
mpv-kill		      M-x ... RET
  Kill the mpv process.
narrow-to-defun		      M-x ... RET
  Make text outside current defun invisible.
nslookup		      M-x ... RET
  Run `nslookup-program'.
org-copy		      M-x ... RET
  Like `org-refile', but preserve the refiled subtree.
org-roam-ui-mode	      M-x ... RET
  Enable org-roam-ui.
  Value: nil
org-sort-entries	      M-x ... RET
  Sort entries on a certain level of an outline tree.
org-table-import	      M-x ... RET
  Import FILE as a table.
org-toggle-item		      M-x ... RET
  Convert headings or normal lines to items, items to normal lines.
pr-txt-fast-fire	      M-x ... RET
  Fast fire function for text printing.
scss-mode		      M-x ... RET
  Major mode to edit "Sassy CSS" files.
set-face-extend		      M-x ... RET
  Specify whether face FACE should be extended.
show-all		      M-x ... RET
  Show all of the text in the buffer.
subword-upcase		      M-x ... RET
  Do the same as `upcase-word' but on subwords.
switch-to-buffer	      <menu-bar> <buffer> <select-named-buffer>
  Display buffer BUFFER-OR-NAME in the selected window.
tab-undo		      M-x ... RET
  Restore the most recently closed tab.
woman-find-file		      M-x ... RET
  Find, decode and browse a specific UN*X man-page source file
  FILE-NAME.
xterm-mouse-mode	      M-x ... RET
  Toggle XTerm mouse mode.
  Value: t
auto-insert-mode	      M-x ... RET
  Toggle Auto-insert mode, a global minor mode.
  Value: nil
auto-revert-mode	      M-x ... RET
  Toggle reverting buffer when the file changes (Auto-Revert Mode).
  Value: nil
backward-page		      M-x ... RET
  Move backward to page boundary.  With arg, repeat, or go fwd if
  negative.
c-ts-mode		      M-x ... RET
  Major mode for editing C, powered by tree-sitter.
canlock-verify		      M-x ... RET
  Verify Cancel-Lock or Cancel-Key in BUFFER.
change-log-mode		      M-x ... RET
  Major mode for editing change logs; like Indented Text mode.
dired-async-mode	      M-x ... RET
  Do dired actions asynchronously.
  Value: nil
dired-kill-tree		      M-x ... RET
  Kill all proper subdirs of DIRNAME, excluding DIRNAME itself.
edir-revisions		      M-x ... RET
  Run Ediff on a directory, DIR1, comparing its files with their
  revisions.
gdb-enable-debug	      M-x ... RET
  Toggle logging of transaction between Emacs and Gdb.
  Value: nil
gnus-other-frame	      M-x ... RET
  Pop up a frame to read news.
gud-tooltip-mode	      M-x ... RET
  Toggle the display of GUD tooltips.
  Value: nil
highlight-regexp	      M-s h r
  Set face of each match of REGEXP to FACE.
image-minor-mode	      M-x ... RET
  Toggle Image minor mode in this buffer.
  Value: nil
insert-register		      M-x ... RET
  Insert contents of register REGISTER.  (REGISTER is a character.)
kill-visual-line	      <deleteline>, C-k
  Kill the rest of the visual line.
magit-patch-save	      M-x ... RET
  Write current diff into patch FILE.
magit-push-tags		      M-x ... RET
  Push all tags to another repository.
make-empty-file		      M-x ... RET
  Create an empty file FILENAME.
message-news		      M-x ... RET
  Start editing a news article to be sent.
mouse-drag-throw	      M-x ... RET
  "Throw" the page according to a mouse drag.
ns-drag-n-drop		      <drag-n-drop>
  Edit the files listed in the drag-n-drop EVENT.
occur-next		      M-x ... RET
  Move to the Nth (default 1) next match in an Occur mode buffer.
org-cdlatex-mode	      M-x ... RET
  Toggle the minor `org-cdlatex-mode'.
  Value: nil
org-edit-special	      M-x ... RET
  Call a special editor for the element at point.
org-id-copy		      M-x ... RET
  Copy the ID of the entry at point to the kill ring.
org-odt-convert		      M-x ... RET
  Convert IN-FILE to format OUT-FMT using a command line converter.
play-sound-file		      M-x ... RET
  Play sound stored in FILE.
project-search		      M-x ... RET
  Search for REGEXP in all the files of the project.
rmail-input		      M-x ... RET
  Run Rmail on file FILENAME.
scroll-lock-mode	      <Scroll_Lock>
  Buffer-local minor mode for pager-like scrolling.
set-left-margin		      M-x ... RET
  Set the left margin of the region to WIDTH.
set-mark-command	      M-SPC
  Set the mark where point is, and activate it; or jump to the mark.
smiley-buffer		      M-x ... RET
  Run `smiley-region' at the BUFFER, specified in the argument or
spam-initialize		      M-x ... RET
  Install the spam.el hooks and do other initialization.
tags-table-mode		      M-x ... RET
  Major mode for tags table file buffers.
titdic-convert		      M-x ... RET
  Convert a TIT dictionary of FILENAME into a Quail package.
unix-sync		      M-x ... RET
  Tell Unix to finish all pending disk updates.
vc-rename-file		      <menu-bar> <tools> <vc> <vc-rename-file>
  Rename file OLD to NEW in both work area and repository.
Info-tagify		      M-x ... RET
  Create or update Info file tag table in current buffer or in a
  region.
apropos-function	      M-x ... RET
  Show functions that match PATTERN.
backward-up-list	      ESC C-<up>, C-M-u, C-M-<up>
  Move backward out of one level of parentheses.
bookmark-insert		      <menu-bar> <edit> <bookmark> <insert>
  Insert the text of the file pointed to by bookmark BOOKMARK-NAME.
buffer-face-mode	      M-x ... RET
  Minor mode for a buffer-specific default face.
company-nxml		      M-x ... RET
  `company-mode' completion backend for `nxml-mode'.
company-tng-mode	      M-x ... RET
  This minor mode enables `company-tng-frontend'.
  Value: nil
copy-to-register	      M-x ... RET
  Copy region into register REGISTER.
diff-goto-source	      M-x ... RET
  Jump to the corresponding source line.
dired-do-touch		      M-x ... RET
  Change the timestamp of the marked (or next ARG) files.
dns-lookup-host		      M-x ... RET
  Look up the DNS information for HOST (name or IP address).
edebug-eval-mode	      M-x ... RET
  Mode for evaluation list buffer while in Edebug.
eshell-command		      M-x ... RET
  Execute the Eshell command string COMMAND.
eudc-query-form		      <menu-bar> <tools> <directory-search> <query>
  Display a form to query the directory server.
find-name-dired		      M-x ... RET
  Search DIR recursively for files matching the globbing PATTERN,
grugru-forward		      M-x ... RET
  `grugru' forwardly PREFIX times.
helpful-update		      M-x ... RET
  Update the current *Helpful* buffer to the latest
highlight-phrase	      M-s h p
  Set face of each match of phrase REGEXP to FACE.
ispell-buffer		      <menu-bar> <tools> <spell> <ispell-buffer>, <menu-bar> <edit> <spell> <ispell-buffer>
  Check the current buffer for spelling errors interactively.
jump-to-register	      M-x ... RET
  Move point to location stored in a register.
keypression-mode	      M-x ... RET
  Keystroke visualizer for GUI version Emacs.
  Value: nil
line-number-mode	      <menu-bar> <options> <showhide> <line-number-mode>
  Toggle line number display in the mode line (Line Number mode).
  Value: t
magit-diff-range	      M-x ... RET
  Show differences between two commits.
magit-log-other		      M-x ... RET
  Show log for one or more revs read from the minibuffer.
mouse-wheel-mode	      M-x ... RET
  Toggle mouse wheel support (Mouse Wheel mode).
  Value: t
narrow-to-region	      M-x ... RET
  Restrict editing in this buffer to the current region.
newsticker-start	      M-x ... RET
  Start the newsticker.
occur-prev		      M-x ... RET
  Move to the Nth (default 1) previous match in an Occur mode buffer.
org-babel-tangle	      M-x ... RET
  Write code blocks to source-specific files.
org-delete-char		      M-x ... RET
  Like `delete-char', but insert whitespace at field end in tables.
org-table-export	      M-x ... RET
  Export table to a file, with configurable format.
org-tags-view		      M-x ... RET
  Show all headlines for all `org-agenda-files' matching a TAGS
  criterion.
pr-toggle-tumble	      M-x ... RET
  Toggle tumble.
recover-session		      <menu-bar> <file> <recover-session>
  Recover auto save files from a previous Emacs session.
set-frame-height	      M-x ... RET
  Set text height of frame FRAME to HEIGHT lines.
set-fringe-style	      M-x ... RET
  Set the default appearance of fringes on the selected frame.
table-insert-row	      M-x ... RET
  Insert N table row(s).
thing-copy-url		      M-x ... RET
  Copy url at current point.
url-handler-mode	      M-x ... RET
  Handle URLs as if they were file names throughout Emacs.
  Value: nil
vc-pull-and-push	      M-x ... RET
  First pull, and then push the current branch.
visual-line-mode	      M-x ... RET
  Toggle visual line based editing (Visual Line mode) in the current
  buffer.
  Value: t
View-goto-line		      M-x ... RET
  Move to first (or prefix LINE) line in View mode.
ad-clear-cache		      M-x ... RET
  Clears a previously cached advised definition of FUNCTION.
button-describe		      M-x ... RET
  Display a buffer with information about the button at point.
dired-do-chgrp		      M-x ... RET
  Change the group of the marked (or next ARG) files.
dired-do-chown		      M-x ... RET
  Change the owner of the marked (or next ARG) files.
dired-kill-line		      M-x ... RET
  Kill the current line (not the files).
find-grep-dired		      M-x ... RET
  Find files in DIR that contain matches for REGEXP and start Dired on
  output.
flyspell-region		      M-x ... RET
  Flyspell text between BEG and END.
gif-screencast		      M-x ... RET
  Start recording the GIF.
ibuffer-do-view		      M-x ... RET
  View marked buffers, or the buffer on the current line.
indent-tabs-mode	      M-x ... RET
  Toggle whether indentation can insert TAB characters.
  Value: t
info-lookup-file	      M-x ... RET
  Look up and display documentation of FILE in the relevant Info
  manual.
keycast-log-mode	      M-x ... RET
  Log invoked commands and their key bindings in a buffer.
  Value: nil
kmacro-end-macro	      M-x ... RET
  Finish defining a keyboard macro.
magit-ediff-dwim	      M-x ... RET
  Compare, stage, or resolve using Ediff.
menu-set-font		      <menu-bar> <options> <menu-set-font>, s-t
  Interactively select a font and make it the default on all frames.
org-copy-subtree	      M-x ... RET
  Copy the current subtree into the clipboard.
org-footnote-new	      M-x ... RET
  Insert a new footnote.
org-meta-return		      M-x ... RET
  Insert a new heading or wrap a region in a table.
org-set-property	      M-x ... RET
  In the current entry, set PROPERTY to VALUE.
pr-despool-print	      M-x ... RET
  Send the spooled PostScript to the printer.
ps-spool-buffer		      M-x ... RET
  Generate and spool a PostScript image of the buffer.
report-emacs-bug	      <menu-bar> <help-menu> <send-emacs-bug-report>
  Report a bug in GNU Emacs.
scroll-up-line		      M-x ... RET
  Scroll text of selected window upward ARG lines; or one line if no
  ARG.
set-fill-column		      M-x ... RET
  Set `fill-column' to specified argument.
sis-context-mode	      M-x ... RET
  Switch input source smartly according to context.
  Value: t
table-recognize		      M-x ... RET
  Recognize all tables within the current buffer and activate them.
transpose-words		      M-t
  Interchange words around point, leaving point at end of them.
vc-delete-file		      M-x ... RET
  Delete file and mark it as such in the version control system.
vc-version-diff		      M-x ... RET
  Report diffs between revisions REV1 and REV2 in the repository
  history.
visit-tags-table	      <menu-bar> <edit> <goto> <set-tags-name>
  Tell tags commands to use tags table file FILE.
xref--mouse-2		      M-x ... RET
  Move point to the button and show the xref definition.
Info-validate		      M-x ... RET
  Check current buffer for validity as an Info file.
append-next-kill	      C-M-w
  Cause following command, if it kills, to add to previous kill.
apropos-library		      M-x ... RET
  List the variables and functions defined by library FILE.
browse-url-man		      M-x ... RET
  Open a man page.
compilation-mode	      M-x ... RET
  Major mode for compilation log buffers.
dired-at-point		      M-x ... RET
  Start Dired, defaulting to file at point.  See `ffap'.
dired-do-search		      M-x ... RET
  Search through all marked files for a match for REGEXP.
downcase-region		      M-x ... RET
  Convert the region to lower case.  In programs, wants two arguments.
edebug-eval-defun	      M-x ... RET
  (not documented)
edebug-step-in		      M-x ... RET
  Step into the definition of the function, macro or method about to
  be called.
elint-initialize	      M-x ... RET
  Initialize elint.
enable-command		      M-x ... RET
  Allow COMMAND to be executed without special confirmation from now
  on.
epa-mail-encrypt	      M-x ... RET
  Encrypt the outgoing mail message in the current buffer.
fortune-message		      M-x ... RET
  Display a fortune cookie to the mini-buffer.
gnus-recenter		      M-x ... RET
  Center point in window and redisplay frame.
handle-focus-out	      M-x ... RET
  Handle a focus-out event.
image-next-file		      M-x ... RET
  Visit the next image in the same directory as the current image
  file.
insert-kbd-macro	      M-x ... RET
  Insert in buffer the definition of kbd macro MACRONAME, as Lisp
  code.
isearch-backward	      <menu-bar> <edit> <i-search> <isearch-backward>, s-F, C-r
  Do incremental search backward.
keymap-local-set	      M-x ... RET
  Give KEY a local binding as COMMAND.
kill-this-buffer	      <menu-bar> <file> <kill-buffer>
  Kill the current buffer.
magit-log-merged	      M-x ... RET
  Show log for the merge of COMMIT into BRANCH.
magit-tag-delete	      M-x ... RET
  Delete one or more tags.
man-follow		      M-x ... RET
  Get a Un*x manual page of the item under point and put it in a
  buffer.
markdown-demote		      M-x ... RET
  Demote or move element at point to the right.
markdown-export		      M-x ... RET
  Run Markdown on the current buffer, save to file, and return the
  filename.
markdown-move-up	      M-x ... RET
  Move thing at point up.
message-forward		      M-x ... RET
  Forward the current message via mail.
mouse-set-mark		      M-x ... RET
  Set mark at the position clicked on with the mouse.
move-end-of-line	      s-<right>, C-e
  Move point to end of current line as displayed.
org-clock-report	      M-x ... RET
  Update or create a table containing a report about clocked time.
org-cut-subtree		      M-x ... RET
  Cut the current subtree into the clipboard.
org-do-promote		      M-x ... RET
  Promote the current heading higher up the tree.
org-mark-subtree	      M-x ... RET
  Mark the current subtree.
org-roam-capture	      M-x ... RET
  Launches an `org-capture' process for a new or existing node.
org-show-entry		      M-x ... RET
  Show the body directly following its heading.
orgtbl-tab		      M-x ... RET
  Justification and field motion for `orgtbl-mode'.
origami-reset		      M-x ... RET
  Remove all folds from BUFFER and reset all origami state associated
  with
pop-tag-mark		      M-x ... RET
  Go back to the previous position in xref history.
pr-txt-directory	      M-x ... RET
  Print directory using text printer.
set-face-stipple	      M-x ... RET
  Change the stipple pixmap of face FACE to STIPPLE.
set-input-method	      <menu-bar> <options> <mule> <set-input-method>
  Select and activate input method INPUT-METHOD for the current
  buffer.
transpose-chars		      C-t
  Interchange characters around point, moving forward one character.
tree-sitter-mode	      M-x ... RET
  Minor mode that keeps an up-to-date syntax tree using incremental
  parsing.
  Value: nil
vc-git-stash-menu	      M-x ... RET
  (not documented)
vc-prepare-patch	      M-x ... RET
  Compose an Email sending patches for REVISIONS to ADDRESSEE.
wucuo-start		      M-x ... RET
  Turn on wucuo to spell check code.  ARG is ignored.
xref-go-back		      <menu-bar> <edit> <goto> <xref-pop>, M-,
  Go back to the previous position in xref history.
xref-goto-xref		      M-x ... RET
  Jump to the xref on the current line and select its window.
yank-media-types	      M-x ... RET
  Yank any element present in the primary selection or the clipboard.
add-name-to-file	      M-x ... RET
  Give FILE additional name NEWNAME.  Both args must be strings.
append-to-buffer	      M-x ... RET
  Append to specified BUFFER the text of the region.
apropos-variable	      M-x ... RET
  Show variables that match PATTERN.
bibtex-find-text	      M-x ... RET
  Move point to end of text of current BibTeX field or entry head.
comint-send-input	      M-x ... RET
  Send input to process.
copyright-update	      M-x ... RET
  Update copyright notice to indicate the current year.
delete-directory	      M-x ... RET
  Delete the directory named DIRECTORY.  Does not follow symlinks.
delete-rectangle	      M-x ... RET
  Delete (don't save) text in the region-rectangle.
digit-argument		      C-M-0, C-M-1, C-M-2, C-M-3, C-M-4, C-M-5, C-M-6, C-M-7, C-M-8, C-M-9, ESC 0..9, C-0, C-1, C-2, C-3, C-4, C-5, C-6, C-7, C-8, C-9
  Part of the numeric argument for the next command.
dired-do-delete		      M-x ... RET
  Delete all marked (or next ARG) files.
dired-do-symlink	      M-x ... RET
  Make symbolic links to current file or all marked (or next ARG)
  files.
ediff-version		      M-x ... RET
  Return string describing the version of Ediff.
follow-scroll-up	      M-x ... RET
  Scroll text in a Follow mode window chain up.
gnus-batch-kill		      M-x ... RET
  Run batched scoring.
grugru-backward		      M-x ... RET
  `grugru' backwardly PREFIX times.
ido-switch-buffer	      M-x ... RET
  Switch to another buffer.
magit-merge-into	      M-x ... RET
  Merge the current branch into BRANCH and remove the former.
magit-stage-file	      M-x ... RET
  Stage all changes to FILE.
mml-secure-sign		      M-x ... RET
  Add MML tags to sign this MML part.
org-cut-special		      M-x ... RET
  Cut region in table or cut current subtree.
org-open-at-point	      M-x ... RET
  Open thing at point.
other-tab-prefix	      M-x ... RET
  Display the buffer of the next command in a new tab.
recode-file-name	      M-x ... RET
  Change the encoding of FILE's name from CODING to NEW-CODING.
shell-script-mode	      M-x ... RET
  Major mode for editing shell scripts.
sort-paragraphs		      M-x ... RET
  Sort paragraphs in region alphabetically; argument means descending
  order.
subword-forward		      M-x ... RET
  Do the same as `forward-word' but on subwords.
table-delete-row	      M-x ... RET
  Delete N row(s) of cells.
thing-copy-word		      M-x ... RET
  Copy words at point.
2048-game		      M-x ... RET
  Start playing 2048.
Custom-newline		      M-x ... RET
  Invoke button at POS, or refuse to allow editing of Custom buffer.
Info-help		      M-x ... RET
  Enter the Info tutorial.
Info-last		      M-x ... RET
  Go back in the history to the last node visited.
View-exit		      M-x ... RET
  Exit View mode but stay in current buffer.
antlr-mode		      M-x ... RET
  Major mode for editing ANTLR grammar files.
center-region		      M-x ... RET
  Center each nonblank line starting in the region.
common-lisp-mode	      M-x ... RET
  Major mode for editing programs in Common Lisp and other similar
  Lisps.
company-etags		      M-x ... RET
  `company-mode' completion backend for etags.
company-tempo		      M-x ... RET
  `company-mode' completion backend for tempo.
context-menu-mode	      <menu-bar> <options> <showhide> <showhide-context-menu>
  Toggle Context Menu mode.
  Value: nil
delete-windows-on	      M-x ... RET
  Delete all windows showing BUFFER-OR-NAME.
describe-keymap		      M-x ... RET
  Describe key bindings in KEYMAP.
desktop-save-mode	      M-x ... RET
  Toggle desktop saving (Desktop Save mode).
  Value: nil
diary-mode		      M-x ... RET
  Major mode for editing the diary file.
ebnf-print-file		      M-x ... RET
  Generate and print a PostScript syntactic chart image of the file
  FILE.
ebnf-spool-file		      M-x ... RET
  Generate and spool a PostScript syntactic chart image of the file
  FILE.
elpl-edit		      M-x ... RET
  Edit in seperate buffer.
epa-unmark-key		      M-x ... RET
  Unmark a key on the current line.
er/expand-region	      M-x ... RET
  Increase selected region by semantic units.
eregistry		      M-x ... RET
  Display Ediff's registry.
eudc-get-email		      <menu-bar> <tools> <directory-search> <email>
  Get the email field of NAME from the directory server.
eudc-get-phone		      <menu-bar> <tools> <directory-search> <phone>
  Get the phone field of NAME from the directory server.
eudc-load-eudc		      <menu-bar> <tools> <directory-search> <load>
  Load the Emacs Unified Directory Client.
eudc-set-server		      <menu-bar> <tools> <directory-search> <new>
  Set the directory server to SERVER using PROTOCOL.
global-unset-key	      M-x ... RET
  Remove global binding of KEY.
gnus-copy-file		      M-x ... RET
  Copy FILE to TO.
gui-set-selection	      M-x ... RET
  Make an X selection of type TYPE and value DATA.
helm-tldr		      M-x ... RET
  Helm interface for `tldr'.
help-quit		      M-x ... RET
  Just exit from the Help command's command loop.
helpful-key		      <help> k, <f1> k
  Show help for interactive command bound to KEY-SEQUENCE.
ido-insert-file		      M-x ... RET
  Insert contents of file in current buffer.
image-bob		      M-x ... RET
  Scroll to the top-left corner of the image in the current window.
image-eob		      M-x ... RET
  Scroll to the bottom-right corner of the image in the current
  window.
kill-grep		      M-x ... RET
  Kill the process made by the \[compile] or \[grep] commands.
magit-stash-pop		      M-x ... RET
  Apply a stash to the working tree and remove it from stash list.
magit-tag-release	      M-x ... RET
  Create a release tag for `HEAD'.
mail-text		      M-x ... RET
  Move point to beginning of text field.
mixal-mode		      M-x ... RET
  Major mode for the mixal asm language.
mpv-pause		      M-x ... RET
  Pause or unpause playback.
mpv-seek-forward	      M-x ... RET
  Seek forward ARG seconds.
next-completion		      M-x ... RET
  Move to the next item in the completion list.
org-timestamp-up	      M-x ... RET
  Increase the date item at the cursor by one.
pp-buffer		      M-x ... RET
  Prettify the current buffer with printed representation of a Lisp
  object.
previous-buffer		      <menu-bar> <buffer> <previous-buffer>, <XF86Back>
  In selected window switch to ARGth previous buffer.
rng-validate-mode	      M-x ... RET
  Minor mode performing continual validation against a RELAX NG
  schema.
set-cursor-color	      M-x ... RET
  Set the text cursor color of the selected frame to COLOR-NAME.
string-rectangle	      M-x ... RET
  Replace rectangle contents with STRING on each line.
tab-bar-mouse-1		      M-x ... RET
  Close the tab whose "x" close button you click.
tab-bar-move-tab	      M-x ... RET
  Move the current tab ARG positions to the right.
text-scale-adjust	      s--, s-=, s-0
  Adjust the font size in the current buffer by INC steps.
thing-copy-list		      M-x ... RET
  Cut list around point.
thing-copy-page		      M-x ... RET
  Cut page around point.
timeclock-change	      M-x ... RET
  Change to working on a different project.
tramp-bug		      M-x ... RET
  Submit a bug report to the Tramp developers.
undefined		      M-x ... RET
  Beep to tell the user this binding is undefined.
vc-version-ediff	      M-x ... RET
  Show differences between REV1 and REV2 of FILES using ediff.
w/see-you		      M-x ... RET
  Highlight ZERO WIDTH chars in all buffers.
what-line		      M-x ... RET
  Print the current buffer line number and narrowed line number of
  point.
what-page		      M-x ... RET
  Print page and line number of point.
widget-forward		      M-x ... RET
  Move point to the next field or button.
xeft-next		      M-x ... RET
  Move to next file excerpt.
abridge-diff-mode	      M-x ... RET
  Toggle abridged diff mode.
  Value: nil
article-date-ut		      M-x ... RET
  Convert DATE date to TYPE in the current article.
bibtex-initialize	      M-x ... RET
  (Re)Initialize BibTeX buffers.
blink-cursor-mode	      <menu-bar> <options> <blink-cursor-mode>
  Toggle cursor blinking (Blink Cursor mode).
  Value: t
bookmark-locate		      <menu-bar> <edit> <bookmark> <locate>
  Insert the name of the file associated with BOOKMARK-NAME.
company-elisp		      M-x ... RET
  `company-mode' completion backend for Emacs Lisp.
company-gtags		      M-x ... RET
  `company-mode' completion backend for GNU Global.
company-yasnippet	      M-x ... RET
  `company-mode' backend for `yasnippet'.
conf-unix-mode		      M-x ... RET
  Conf Mode starter for Unix style Conf files.
dash-fontify-mode	      M-x ... RET
  Toggle fontification of Dash special variables.
  Value: nil
diff-test-hunk		      M-x ... RET
  See whether it's possible to apply the current hunk.
doc-view-search		      M-x ... RET
  Jump to the next match or initiate a new search if NEW-QUERY is
  given.
ebnf-reset-style	      M-x ... RET
  Reset current style.
eww-search-words	      M-s M-w
  Search the web for the text in the region.
gnus-article-mode	      M-x ... RET
  Major mode for displaying an article.
ispell-minor-mode	      M-x ... RET
  Toggle last-word spell checking (Ispell minor mode).
  Value: nil
just-one-space		      M-x ... RET
  Delete all spaces and tabs around point, leaving one space (or N
  spaces).
magit-abort-dwim	      M-x ... RET
  Abort current operation.
magit-am-abort		      M-x ... RET
  Abort the current patch applying sequence.
magit-dired-jump	      M-x ... RET
  Visit file at point using Dired.
magit-stash-both	      M-x ... RET
  Create a stash of the index and working tree.
mail-dont-send		      M-x ... RET
  Don't send the message you have been editing.
mail-yank-region	      M-x ... RET
  Insert the selected region from the message being replied to.
org-ctrl-c-ctrl-c	      M-x ... RET
  Set tags in headline, or update according to changed information at
  point.
org-paste-subtree	      M-x ... RET
  Paste the clipboard as a subtree, with modification of headline
  level.
ps-spool-region		      M-x ... RET
  Generate a PostScript image of the region and spool locally.
pyim-isearch-mode	      M-x ... RET
  这个 mode 为 isearch 添加拼音搜索功能.
  Value: nil
re-search-forward	      M-x ... RET
  Search forward from point for regular expression REGEXP.
recentf-cleanup		      M-x ... RET
  Cleanup the recent list.
recentf-excl-mode	      M-x ... RET
  Minor mode `recentf-excl-mode'.
  Value: nil
reposition-window	      C-M-l
  Make the current definition and/or comment visible.
rmail-add-label		      M-x ... RET
  Add LABEL to labels associated with current RMAIL message.
rxt-elisp-to-rx		      M-x ... RET
  Translate REGEXP, a regexp in Emacs Lisp syntax, to `rx' syntax.
rxt-pcre-to-rx		      M-x ... RET
  Translate PCRE, a regexp in Perl-compatible syntax, to `rx' syntax.
save-some-buffers	      M-x ... RET
  Save some modified file-visiting buffers.  Asks user about each one.
scratch-buffer		      M-x ... RET
  Switch to the *scratch* buffer.
set-justification	      M-x ... RET
  Set the region's justification style to STYLE.
set-right-margin	      M-x ... RET
  Set the right margin of the region to WIDTH.
shr-browse-image	      M-x ... RET
  Browse the image under point.
tab-switcher-mode	      M-x ... RET
  Major mode for selecting a window configuration.
tab-to-tab-stop		      M-i
  Insert spaces or tabs to next defined tab-stop column.
translate-region	      M-x ... RET
  From START to END, translate characters according to TABLE.
use-hard-newlines	      M-x ... RET
  Toggle between hard and soft newlines in the current buffer.
  Value: nil
vc-log-incoming		      <menu-bar> <tools> <vc> <vc-log-in>
  Show log of changes that will be received with pull from
  REMOTE-LOCATION.
vc-log-outgoing		      <menu-bar> <tools> <vc> <vc-log-out>
  Show log of changes that will be sent with a push operation to
  REMOTE-LOCATION.
widget-minor-mode	      M-x ... RET
  Minor mode for traversing widgets.
ad-activate-all		      M-x ... RET
  Activate all currently advised functions.
avy-kill-region		      M-x ... RET
  Select two lines and kill the region between them.
bibtex-find-entry	      M-x ... RET
  Move point to the beginning of BibTeX entry named KEY.
capitalize-dwim		      M-x ... RET
  Capitalize words in the region, if active; if not, capitalize word
  at point.
change-log-merge	      M-x ... RET
  Merge the contents of change log file OTHER-LOG with this buffer.
choose-completion	      M-x ... RET
  Choose the completion at point.
debugger-frame		      M-x ... RET
  Request entry to debugger when this frame exits.
display-call-tree	      M-x ... RET
  Display a call graph of a specified file.
display-time-mode	      <menu-bar> <options> <showhide> <showhide-date-time>
  Toggle display of time, load level, and mail flag in mode lines.
  Value: nil
easy-menu-remove	      M-x ... RET
  Do nothing and return nil.
ebnf-pop-style		      M-x ... RET
  Pop a style from the stack of pushed styles and set it as the
  current style.
ebnf-syntax-file	      M-x ... RET
  Do a syntactic analysis of the named FILE.
find-tag-noselect	      M-x ... RET
  Find tag (in current tags table) whose name contains TAGNAME.
forward-paragraph	      M-}, C-<down>
  Move forward to end of paragraph.
global-corfu-mode	      M-x ... RET
  Toggle Corfu mode in all buffers.
  Value: nil
global-cwarn-mode	      M-x ... RET
  Toggle Cwarn mode in all buffers.
  Value: nil
global-eldoc-mode	      M-x ... RET
  Toggle Eldoc mode in all buffers.
  Value: t
graphviz-dot-mode	      M-x ... RET
  Major mode for the dot language. \<graphviz-dot-mode-map>
image-scroll-down	      M-x ... RET
  Scroll image in current window downward by N lines.
kmacro-set-format	      M-x ... RET
  Set the format of `kmacro-counter' to FORMAT.
less-css-mode		      M-x ... RET
  Major mode for editing Less files (http://lesscss.org/).
magit-log-head		      M-x ... RET
  Show log for `HEAD'.
mail-abbrevs-mode	      M-x ... RET
  Toggle abbrev expansion of mail aliases (Mail Abbrevs mode).
  Value: nil
message-reply		      M-x ... RET
  Start editing a reply to the article in the current buffer.
nato-region		      M-x ... RET
  Convert plain text in region to NATO spelling alphabet.
org-clock-display	      M-x ... RET
  Show subtree times in the entire buffer.
org-edit-src-code	      M-x ... RET
  Edit the source or example block at point.
org-insert-drawer	      M-x ... RET
  Insert a drawer at point.
org-latex-preview	      M-x ... RET
  Toggle preview of the LaTeX fragment at point.
org-mobile-push		      M-x ... RET
  Push the current state of Org affairs to the target directory.
pixel-scroll-mode	      M-x ... RET
  A minor mode to scroll text pixel-by-pixel.
  Value: nil
pr-customize		      M-x ... RET
  Customization of the `printing' group.
python-base-mode	      M-x ... RET
  Generic major mode for editing Python files.
register-to-point	      M-x ... RET
  Move point to location stored in a register.
tab-bar-close-tab	      M-x ... RET
  Close the tab specified by its absolute position TAB-NUMBER.
thing-copy-sexp		      M-x ... RET
  Copy sexp at current point.
toggle-word-wrap	      M-x ... RET
  Toggle whether to use word-wrapping for continuation lines.
transpose-lines		      M-x ... RET
  Exchange current line and previous line, leaving point after both.
transpose-regions	      M-x ... RET
  Transpose region STARTR1 to ENDR1 with STARTR2 to ENDR2.
yas-new-snippet		      <menu-bar> <yasnippet> <New snippet...>
  Pops a new buffer for writing a snippet.
yas/new-snippet		      M-x ... RET
  Pops a new buffer for writing a snippet.
ad-update-regexp	      M-x ... RET
  Update functions with an advice name containing a REGEXP match.
avy-copy-line		      M-x ... RET
  Copy a selected line above the current line.
avy-move-line		      M-x ... RET
  Move a selected line above the current line.
beginning-of-line	      M-x ... RET
  Move point to beginning of current line (in the logical order).
bibtex-fill-entry	      M-x ... RET
  Fill current BibTeX entry.
bibtex-make-field	      M-x ... RET
  Make a field named FIELD in current BibTeX entry.
bibtex-pop-next		      M-x ... RET
  Replace text of current field with the text of similar field in next
  entry.
clean-buffer-list	      M-x ... RET
  Kill old buffers that have not been displayed recently.
comment-indent		      M-x ... RET
  Indent this line's comment to `comment-column', or insert an empty
  comment.
describe-syntax		      <help> s, <f1> s
  Describe the syntax specifications in the syntax table of BUFFER.
desktop-remove		      M-x ... RET
  Delete desktop file in `desktop-dirname'.
disable-command		      M-x ... RET
  Require special confirmation to execute COMMAND from now on.
edebug-step-out		      M-x ... RET
  Proceed from the current point to the end of the containing sexp.
ediff-merge-files	      <menu-bar> <tools> <ediff-merge> <ediff-merge-files>
  Merge two files without ancestor.
ediff-patch-file	      <menu-bar> <tools> <epatch> <ediff-patch-file>
  Query for a file name, and then run Ediff by patching that file.
emacs-init-time		      M-x ... RET
  Return a string giving the duration of the Emacs initialization.
emerge-buffers		      M-x ... RET
  Run Emerge on two buffers BUFFER-A and BUFFER-B.
epa-verify-region	      <menu-bar> <tools> <encryption-decryption> <verify-region>
  Verify the current region between START and END.
font-lock-update	      M-x ... RET
  Update the syntax highlighting in this buffer.
format-write-file	      M-x ... RET
  Write current buffer into FILENAME, using a format based on FORMAT.
forward-sentence	      M-e
  Move forward to next end of sentence.  With argument, repeat.
gnus-score-mode		      M-x ... RET
  Mode for editing Gnus score files.
helpful-symbol		      M-x ... RET
  Show help for SYMBOL, a variable, function or macro.
image-goto-frame	      M-x ... RET
  Show frame N of a multi-frame image.
org-clock-goto		      M-x ... RET
  Go to the currently clocked-in entry, or to the most recently
  clocked one.
org-clock-in-last	      M-x ... RET
  Clock in the last closed clocked item.
org-comment-dwim	      M-x ... RET
  Call the comment command you mean.
org-cycle-level		      M-x ... RET
  Cycle the level of an empty headline through possible states.
org-edit-table.el	      M-x ... RET
  Edit "table.el" table at point.
org-remove-file		      M-x ... RET
  Remove current file from the list of files in variable
  `org-agenda-files'.
org-shiftmetadown	      M-x ... RET
  Drag the line at point down.
org-table-convert	      M-x ... RET
  Convert from Org table to table.el and back.
scroll-up-command	      <next>, <kp-next>, C-v
  Scroll text of selected window upward ARG lines; or near full screen
  if no ARG.
set-fill-prefix		      M-x ... RET
  Set the fill prefix to the current line up to point.
set-mouse-color		      M-x ... RET
  Set the color of the mouse pointer of the selected frame to
  COLOR-NAME.
subword-left		      M-x ... RET
  Do the same as `left-word' but on subwords.
vc-refresh-state	      M-x ... RET
  Refresh the VC state of the current buffer's file.
view-emacs-news		      <menu-bar> <help-menu> <emacs-news>, <help> C-n, <help> n, <f1> C-n, <f1> n
  Display info on recent changes to Emacs.
add-global-abbrev	      M-x ... RET
  Define a global (all modes) abbrev whose expansion is last word
  before point.
avy-copy-region		      M-x ... RET
  Select two lines and copy the text between them to point.
blink-search-mode	      M-x ... RET
  Major mode derived from `text-mode' by `define-derived-mode'.
conf-windows-mode	      M-x ... RET
  Conf Mode starter for Windows style Conf files.
dbus-handle-event	      M-x ... RET
  Handle events from the D-Bus.
decompose-region	      M-x ... RET
  Decompose text in the current region.
dired-next-line		      M-x ... RET
  Move down lines then position at filename.
eldoc-doc-buffer	      M-x ... RET
  Get or display ElDoc documentation buffer.
elint-directory		      <menu-bar> <emacs-lisp> <Linting> <Lint Directory...>
  Lint all the .el files in DIRECTORY.
elisp-refs-macro	      M-x ... RET
  Display all the references to macro SYMBOL, in all loaded
epa-decrypt-file	      <menu-bar> <tools> <encryption-decryption> <decrypt-file>
  Decrypt DECRYPT-FILE into PLAIN-FILE.
epa-sign-file		      <menu-bar> <tools> <encryption-decryption> <sign-file>
  Sign FILE by SIGNERS keys selected.
eshell-mode		      M-x ... RET
  Emacs shell interactive mode.
goto-address-mode	      M-x ... RET
  Minor mode to buttonize URLs and e-mail addresses in the current
  buffer.
hexlify-buffer		      M-x ... RET
  Convert a binary buffer to hexl format.
ibuffer-auto-mode	      M-x ... RET
  Toggle use of Ibuffer's auto-update facility (Ibuffer Auto mode).
image-next-frame	      M-x ... RET
  Switch to the next frame of a multi-frame image.
indent-to-column	      M-x ... RET
  Indent from point with tabs and spaces until COLUMN is reached.
isearch-yank-pop	      M-x ... RET
  Replace just-yanked search string with previously killed string.
keymap-global-set	      M-x ... RET
  Give KEY a global binding as COMMAND.
kmacro-call-macro	      M-x ... RET
  Call the keyboard MACRO that you defined with \[kmacro-start-macro].
lao-compose-region	      M-x ... RET
  (not documented)
magit-log-all		      M-x ... RET
  Show log for all references and `HEAD'.
magit-stash-index	      M-x ... RET
  Create a stash of the index only.
make-empty-face		      M-x ... RET
  Define a new, empty face with name FACE.
make-face-italic	      M-x ... RET
  Make the font of FACE be italic, if possible.
mlscroll-mouse		      M-x ... RET
  Handle click and drag mouse events on the mode line scroll bar.
open-dribble-file	      M-x ... RET
  Start writing input events to a dribble file called FILE.
org-roam-version	      M-x ... RET
  Return `org-roam' version.
org-table-expand	      M-x ... RET
  Expand all columns in the table at point.
orgtbl-ascii-plot	      M-x ... RET
  Draw an ASCII bar plot in a column.
ps-line-lengths		      M-x ... RET
  Display the correspondence between a line length and a font size.
ps-print-region		      <menu-bar> <file> <print> <ps-print-region>
  Generate and print a PostScript image of the region.
python-add-import	      M-x ... RET
  Add an import statement to the current buffer.
read-abbrev-file	      M-x ... RET
  Read abbrev definitions from file written with `write-abbrev-file'.
relint-file		      M-x ... RET
  Scan FILE, an elisp file, for regexp-related errors.
rxt-elisp-to-pcre	      M-x ... RET
  Translate REGEXP, a regexp in Emacs Lisp syntax, to Perl-compatible
  syntax.
tab-close-other		      M-x ... RET
  Close all tabs on the selected frame, except the tab TAB-NUMBER.
what-domain		      M-x ... RET
  Convert mail domain DOMAIN to the country it corresponds to.
yas-next-field		      M-x ... RET
  Navigate to the ARGth next field.
yas/next-field		      M-x ... RET
  Navigate to the ARGth next field.
Buffer-menu-sort	      M-x ... RET
  Sort Tabulated List entries by the column at point.
Info-index-next		      M-x ... RET
  Go to the next matching index item from the last
  \<Info-mode-map>\[Info-index] command.
Info-scroll-down	      M-x ... RET
  Scroll one screenful back in Info, considering all nodes as one
  sequence.
browse-url-elinks	      M-x ... RET
  Ask the Elinks WWW browser to load URL.
capitalize-region	      M-x ... RET
  Convert the region to capitalized form.
checkdoc-ispell		      M-x ... RET
  Check the style and spelling of everything interactively.
customize-apropos	      <menu-bar> <options> <customize> <customize-apropos>
  Customize loaded options, faces and groups matching PATTERN.
customize-group		      <menu-bar> <options> <customize> <customize-group>
  Customize GROUP, which must be a customization group.
customize-themes	      <menu-bar> <options> <customize> <customize-themes>
  Display a selectable list of Custom themes.
dired-upcase		      M-x ... RET
  Rename all marked (or next ARG) files to upper case.
dissociated-press	      M-x ... RET
  Dissociate the text of the current buffer.
doctex-mode		      M-x ... RET
  Major mode to edit DocTeX files.
ebnf-print-buffer	      M-x ... RET
  Generate and print a PostScript syntactic chart image of the buffer.
ebnf-push-style		      M-x ... RET
  Push the current style onto a stack and set STYLE as the current
  style.
ebrowse-tree-mode	      M-x ... RET
  Major mode for Ebrowse class tree buffers.
epa-key-list-mode	      M-x ... RET
  Major mode for `epa-list-keys'.
gnus-batch-score	      M-x ... RET
  Run batched scoring.
gnus-fetch-group	      M-x ... RET
  Start Gnus if necessary and enter GROUP.
gnus-jog-cache		      M-x ... RET
  Go through all groups and put the articles into the cache.
image-mode-as-hex	      M-x ... RET
  Set `hexl-mode' as major mode in combination with image minor mode.
isearch-del-char	      M-x ... RET
  Delete character from end of search string and search again.
magit-bisect-mark	      M-x ... RET
  While bisecting, mark the current commit with a bisect term.
magit-bisect-run	      M-x ... RET
  Bisect automatically by running commands after each step.
mail-add-payment	      M-x ... RET
  Add X-Payment: and X-Hashcash: headers with a hashcash payment
markdown-promote	      M-x ... RET
  Promote or move element at point to the left.
mlscroll-wheel		      M-x ... RET
  Handle mouse scroll events on the scroll bar.
mouse-drag-region	      <down-mouse-1>
  Set the region to the text that the mouse is dragged over.
move-to-tab-stop	      M-x ... RET
  Move point to next defined tab-stop column.
org-mark-element	      M-x ... RET
  Put point at beginning of this element, mark at end.
org-mobile-pull		      M-x ... RET
  Pull the contents of `org-mobile-capture-file' and integrate them.
org-table-shrink	      M-x ... RET
  Shrink all columns with a width cookie in the table at point.
prepend-to-buffer	      M-x ... RET
  Prepend to specified BUFFER the text of the region.
prog-indent-sexp	      M-x ... RET
  Indent the expression after point.
scroll-bar-drag		      M-x ... RET
  Scroll the window by dragging the scroll bar slider.
so-long-enable		      M-x ... RET
  Enable the `so-long' library's functionality.
subword-backward	      M-x ... RET
  Do the same as `backward-word' but on subwords.
tsx-ts-mode		      M-x ... RET
  Major mode for editing TypeScript.
underline-region	      M-x ... RET
  Underline all nonblank characters in the region.
unexpand-abbrev		      M-x ... RET
  Undo the expansion of the last abbrev that expanded.
untrace-function	      <menu-bar> <emacs-lisp> <Tracing> <Untrace Function...>
  Untraces FUNCTION and possibly activates all remaining advice.
vc-print-root-log	      <menu-bar> <tools> <vc> <vc-print-root-log>
  List the revision history for the current VC controlled tree in a
  window.
vc-switch-branch	      M-x ... RET
  Switch to the branch NAME in the directory DIR.
write-abbrev-file	      M-x ... RET
  Write all user-level abbrev definitions to a file of Lisp code.
Info-forward-node	      M-x ... RET
  Go forward one node, considering all nodes as forming one sequence.
View-leave		      M-x ... RET
  Quit View mode and maybe switch buffers, but don't kill this buffer.
bibtex-kill-field	      M-x ... RET
  Kill the entire enclosing BibTeX field.
byte-compile-file	      M-x ... RET
  Compile a file of Lisp code named FILENAME into a file of byte code.
calc-embedded		      M-x ... RET
  Start Calc Embedded mode on the formula surrounding point.
company-abbrev		      M-x ... RET
  `company-mode' completion backend for abbrev.
compose-last-chars	      <compose-last-chars>
  Compose last characters.
conf-desktop-mode	      M-x ... RET
  Conf Mode started for freedesktop.org Desktop files.
define-mail-alias	      M-x ... RET
  Define NAME as a mail alias that translates to DEFINITION.
dictionary		      M-x ... RET
  Create a new dictionary buffer and install `dictionary-mode'.
dired-backup-diff	      M-x ... RET
  Diff this file with its backup file or vice versa.
dired-do-compress	      M-x ... RET
  Compress or uncompress marked (or next ARG) files.
dired-do-hardlink	      M-x ... RET
  Add names (hard links) current file or all marked (or next ARG)
  files.
dired-view-file		      M-x ... RET
  In Dired, examine a file in view mode, returning to Dired when done.
ebnf-apply-style	      M-x ... RET
  Set STYLE as the current style.
elpl-clean		      M-x ... RET
  Clean the ELPL buffer.
epa-delete-keys		      M-x ... RET
  Delete selected KEYS.
eww-submit		      M-x ... RET
  Submit the current form.
ff-find-other-file	      M-x ... RET
  Find the header or source file corresponding to this file.
gc-buffers		      M-x ... RET
  Kill garbage buffers.
git-gutter		      M-x ... RET
  Show diff information in gutter
gnus-article-edit	      M-x ... RET
  Edit the current article.
help-quick		      M-x ... RET
  Display a quick-help buffer.
hide-entry		      M-x ... RET
  Hide the body directly following this heading.
httpd-stop		      M-x ... RET
  Stop the web server if it is currently running, otherwise do
  nothing.
ilog-toggle-view	      M-x ... RET
  Toggle between different view states.
image-scroll-left	      M-x ... RET
  Scroll image in current window leftward by N character widths.
info-lookup-reset	      M-x ... RET
  Throw away all cached data.
lpr-customize		      M-x ... RET
  Customization of the `lpr' group.
magit-git-command	      M-x ... RET
  Execute COMMAND asynchronously; display output.
magit-info		      M-x ... RET
  Visit the Magit manual.
magit-merge-plain	      M-x ... RET
  Merge commit REV into the current branch; using default message.
magit-push-tag		      M-x ... RET
  Push a tag to another repository.
magit-reset-index	      M-x ... RET
  Reset the index to COMMIT.
mark-whole-buffer	      <menu-bar> <edit> <mark-whole-buffer>, s-a
  Put point at beginning and mark at end of buffer.
mh-version		      M-x ... RET
  Display version information about MH-E and the MH mail handling
  system.
mpv-seek-backward	      M-x ... RET
  Seek backward ARG seconds.
org-copy-special	      M-x ... RET
  Copy region in table or copy current subtree.
org-ctrl-c-minus	      M-x ... RET
  Insert separator line in table or modify bullet status of line.
org-ctrl-c-star		      M-x ... RET
  Compute table, or change heading status of lines.
org-cycle-global	      M-x ... RET
  Cycle the global visibility.  For details see `org-cycle'.
org-global-cycle	      M-x ... RET
  Cycle the global visibility.  For details see `org-cycle'.
org-indent-region	      M-x ... RET
  Indent each non-blank line in the region.
org-roam-refile		      M-x ... RET
  Refile node at point to an Org-roam node.
orgtbl-ret		      M-x ... RET
  Justification and field motion for `orgtbl-mode'.
outline-minor-mode	      M-x ... RET
  Toggle Outline minor mode.
  Value: nil
package-vc-install	      M-x ... RET
  Fetch a PACKAGE and set it up for using with Emacs.
point-to-register	      M-x ... RET
  Store current location of point in register REGISTER.
pr-ps-name		      M-x ... RET
  Interactively select a PostScript printer.
project-find-file	      M-x ... RET
  Visit a file (with completion) in the current project.
push-mark-command	      M-x ... RET
  Set mark at where point is.
replace-rectangle	      M-x ... RET
  Replace rectangle contents with STRING on each line.
rxt-pcre-to-elisp	      M-x ... RET
  Translate PCRE, a regexp in Perl-compatible syntax, to Emacs Lisp.
sis-switch		      M-x ... RET
  Switch input source between english and other.
smiley-region		      M-x ... RET
  Replace in the region `smiley-regexp-alist' matches with
  corresponding images.
smime-exit		      M-x ... RET
  Quit the S/MIME buffer.
subword-downcase	      M-x ... RET
  Do the same as `downcase-word' but on subwords.
table-forward-cell	      M-x ... RET
  Move point forward to the beginning of the next cell.
table-justify-row	      M-x ... RET
  Justify cells of a row.
testcover-start		      M-x ... RET
  Use Edebug to instrument for coverage all macros and functions in
  FILENAME.
uncomment-region	      M-x ... RET
  Uncomment each line in the BEG .. END region.
upcase-char		      M-x ... RET
  Uppercasify ARG chars starting from point.  Point doesn't move.
use-package-lint	      M-x ... RET
  Check for errors in `use-package' declarations.
vc-switch-backend	      M-x ... RET
  Make BACKEND the current version control system for FILE.
viper-mode		      M-x ... RET
  Turn on Viper emulation of Vi in Emacs.  See Info node `(viper)Top'.
whitespace-cleanup	      M-x ... RET
  Cleanup some blank problems in all buffer or at region.
wucuo-stop		      M-x ... RET
  Turn off wucuo and stop spell checking code.
abort-minibuffers	      M-x ... RET
  Abort the current minibuffer.
ace-pinyin-dwim		      M-x ... RET
  With PREFIX, only search Chinese.
basic-save-buffer	      M-x ... RET
  Save the current buffer in its visited file, if it has been
  modified.
beginning-of-defun	      ESC C-<home>, C-M-<home>, <menu-bar> <emacs-lisp> <Navigation> <Beginning Of Defun>
  Move backward to the beginning of a defun.
browse-url-firefox	      M-x ... RET
  Ask the Firefox WWW browser to load URL.
browse-url-mozilla	      M-x ... RET
  Ask the Mozilla WWW browser to load URL.
c++-ts-mode		      M-x ... RET
  Major mode for editing C++, powered by tree-sitter.
company-ispell		      M-x ... RET
  `company-mode' completion backend using Ispell.
corfu-indexed-mode	      M-x ... RET
  Prefix candidates with indices.
  Value: nil
cursor-sensor-mode	      M-x ... RET
  Handle the `cursor-sensor-functions' text property.
customize-changed	      <menu-bar> <options> <customize> <customize-changed>
  Customize all settings whose meanings have changed in Emacs itself.
describe-bindings	      <menu-bar> <help-menu> <describe> <list-keybindings>, <help> b, <f1> b
  Display a buffer showing a list of all defined keys, and their
  definitions.
dired-mouse-drag	      M-x ... RET
  Begin a drag-and-drop operation for the file at EVENT.
display-local-help	      <help> ., <f1> .
  Display local help in the echo area.
electric-pair-mode	      M-x ... RET
  Toggle automatic parens pairing (Electric Pair mode).
  Value: t
epa-decrypt-region	      <menu-bar> <tools> <encryption-decryption> <decrypt-region>
  Decrypt the current region between START and END.
eww-previous-url	      M-x ... RET
  Go to the page marked `previous'.
explain-pause-mode	      M-x ... RET
  Toggle whether to attempt to discover and explain pauses in emacs.
  Value: nil
faceup-write-file	      M-x ... RET
  Save the faceup representation of the current buffer to the file
  FILE-NAME.
find-sibling-file	      M-x ... RET
  Visit a "sibling" file of FILE.
help-with-tutorial	      <menu-bar> <help-menu> <emacs-tutorial>, <help> t, <f1> t
  Select the Emacs learn-by-doing tutorial.
indent-pp-sexp		      C-M-q
  Indent each line of the list starting just after point, or
  prettyprint it.
magit-cherry		      M-x ... RET
  Show commits in a branch that are not merged in the upstream branch.
magit-tag-create	      M-x ... RET
  Create a new tag with the given NAME at REV.
mouse-set-region	      <drag-mouse-1>
  Set the region to the text dragged over, and copy to kill ring.
next-logical-line	      M-x ... RET
  Move cursor vertically down ARG lines.
ns-insert-file		      M-x ... RET
  Insert contents of file `ns-input-file' like insert-file but with
  less
org-agenda-to-appt	      M-x ... RET
  Activate appointments found in `org-agenda-files'.
org-bibtex-read		      M-x ... RET
  Read a bibtex entry and save to `org-bibtex-entries'.
org-fill-paragraph	      M-x ... RET
  Fill element at point, when applicable.
org-insert-heading	      M-x ... RET
  Insert a new heading or an item with the same depth at point.
pangu-spacing-mode	      M-x ... RET
  Toggle pangu-spacing-mode
  Value: t
pr-despool-preview	      M-x ... RET
  Preview spooled PostScript.
recentf-load-list	      M-x ... RET
  Load a previously saved recent list.
recode-region		      M-x ... RET
  Re-decode the region (previously decoded by CODING) by NEW-CODING.
rxt-explain-pcre	      M-x ... RET
  Insert the pretty-printed `rx' syntax for REGEXP in a new buffer.
set-border-color	      M-x ... RET
  Set the color of the border of the selected frame to COLOR-NAME.
table-span-cell		      M-x ... RET
  Span current cell into adjacent cell in DIRECTION.
tramp-rename-files	      M-x ... RET
  Replace in all buffers the visiting file name from SOURCE to TARGET.
vc-create-branch	      M-x ... RET
  Descending recursively from DIR, make a branch called NAME.
vc-git-stash		      M-x ... RET
  Create a stash given the name NAME.
backward-paragraph	      M-{, C-<up>
  Move backward to start of paragraph.
bibtex-set-dialect	      M-x ... RET
  Select BibTeX DIALECT for editing BibTeX files.
browse-url-chrome	      M-x ... RET
  Ask the Google Chrome WWW browser to load URL.
buffer-face-toggle	      M-x ... RET
  Toggle `buffer-face-mode', using face specs SPECS.
comint-accumulate	      M-x ... RET
  Accumulate a line to send as input along with more lines.
comint-kill-region	      M-x ... RET
  Like `kill-region', but ignore read-only properties, if safe.
company-complete	      M-x ... RET
  Insert the common part of all candidates or the current selection.
cperl-perldoc		      M-x ... RET
  Run `perldoc' on WORD.
dabbrev-completion	      C-M-/
  Completion on current word.
describe-variable	      <menu-bar> <help-menu> <describe> <describe-variable>
  Display the full documentation of VARIABLE (a symbol).
dired-do-redisplay	      M-x ... RET
  Redisplay all marked (or next ARG) files.
eudc-expand-inline	      <menu-bar> <tools> <directory-search> <expand-inline>
  Query the directory server, and expand the query string before
  point.
eww-open-file		      M-x ... RET
  Render FILE using EWW.
fido-vertical-mode	      M-x ... RET
  Toggle vertical candidate display in `fido-mode'.
  Value: t
global-reveal-mode	      M-x ... RET
  Toggle Reveal mode in all buffers (Global Reveal mode).
  Value: nil
helpful-function	      <help> F, <f1> F
  Show help for function named SYMBOL.
ibuffer-do-occur	      M-x ... RET
  View lines which match REGEXP in all marked buffers.
ibuffer-redisplay	      M-x ... RET
  Redisplay the current list of buffers.
kmacro-start-macro	      M-x ... RET
  Record subsequent keyboard input, defining a keyboard macro.
locate-with-filter	      M-x ... RET
  Run the executable program `locate' with a filter.
magit-bisect-start	      M-x ... RET
  Start a bisect session.
magit-log-current	      M-x ... RET
  Show log for the current branch.
magit-stash-apply	      M-x ... RET
  Apply a stash to the working tree.
magit-stash-drop	      M-x ... RET
  Remove a stash from the stash list.
markdown-enter-key	      M-x ... RET
  Handle RET depending on the context.
number-to-register	      M-x ... RET
  Store a number in a register.
org-fold-show-all	      M-x ... RET
  Show all contents in the visible part of the buffer.
org-shiftmetaleft	      M-x ... RET
  Promote subtree or delete table column.
org-toggle-heading	      M-x ... RET
  Convert headings to normal text, or items or text to headings.
package-initialize	      M-x ... RET
  Load Emacs Lisp packages, and activate them.
pr-ps-buffer-print	      M-x ... RET
  Print buffer using PostScript printer.
process-menu-mode	      M-x ... RET
  Major mode for listing the processes called by Emacs.
rmail-kill-label	      M-x ... RET
  Remove LABEL from labels associated with current RMAIL message.
set-face-underline	      M-x ... RET
  Specify whether face FACE is underlined.
so-long-minor-mode	      M-x ... RET
  This is the minor mode equivalent of `so-long-mode'.
  Value: nil
table-justify-cell	      M-x ... RET
  Justify cell contents.
table-split-cell	      M-x ... RET
  Split current cell in ORIENTATION.
talk-connect		      M-x ... RET
  Connect to display DISPLAY for the Emacs talk group.
thing-copy-defun	      M-x ... RET
  Cut function around point.
widget-backward		      M-x ... RET
  Move point to the previous field or button.
abbrev-prefix-mark	      M-'
  Mark point as the beginning of an abbreviation.
bibtex-clean-entry	      M-x ... RET
  Finish editing the current BibTeX entry and clean it up.
browse-url-emacs	      M-x ... RET
  Ask Emacs to load URL into a buffer and show it in another window.
column-number-mode	      <menu-bar> <options> <showhide> <column-number-mode>
  Toggle column number display in the mode line (Column Number mode).
  Value: t
comint-stop-subjob	      M-x ... RET
  Stop the current subjob.
corfu-history-mode	      M-x ... RET
  Update Corfu history and sort completions by history.
  Value: nil
css-lookup-symbol	      M-x ... RET
  Display the CSS documentation for SYMBOL, as found on MDN.
customize-icon		      M-x ... RET
  Customize ICON.
describe-fontset	      M-x ... RET
  Display information about FONTSET.
diary-mail-entries	      M-x ... RET
  Send a mail message showing diary entries for next NDAYS days.
diary-mark-entries	      M-x ... RET
  Mark days in the calendar window that have diary entries.
diff-hl-amend-mode	      M-x ... RET
  Show changes against the second-last revision in `diff-hl-mode'.
diff-hl-dired-mode	      M-x ... RET
  Toggle VC diff highlighting on the side of a Dired window.
emerge-revisions	      M-x ... RET
  Emerge two RCS revisions of a file.
gnus-article-hide	      M-x ... RET
  Hide all the gruft in the current article.
gnus-sieve-update	      M-x ... RET
  Update the Sieve script in gnus-sieve-file, by replacing the region
ido-display-file	      M-x ... RET
  Display a file in another window but don't select it.
increment-register	      M-x ... RET
  Augment contents of REGISTER.
info-lookup-symbol	      <menu-bar> <help-menu> <search-documentation> <lookup-symbol-in-manual>, <help> S, <f1> S
  Look up and display documentation of SYMBOL in the relevant Info
  manual.
insert-parentheses	      M-(
  Enclose following ARG sexps in parentheses.
isearch-complete	      M-x ... RET
  Complete the search string from the strings on the search ring.
magit-commit-fixup	      M-x ... RET
  Create a fixup commit.
make-face-unbold	      M-x ... RET
  Make the font of FACE be non-bold, if possible.
make-symbolic-link	      M-x ... RET
  Make a symbolic link to TARGET, named LINKNAME.
markdown-insert-hr	      M-x ... RET
  Insert or replace a horizontal rule.
move-file-to-trash	      M-x ... RET
  Move the file (or directory) named FILENAME to the trash.
mpv-speed-set		      M-x ... RET
  Set playback speed to FACTOR.
occur-next-error	      M-x ... RET
  Move to the ARGPth (default 1) next match in an Occur mode buffer.
org-bibtex-create	      M-x ... RET
  Create a new entry at the given level.
org-priority-show	      M-x ... RET
  Show the priority of the current item.
org-show-children	      M-x ... RET
  Show all direct subheadings of this heading.
org-show-priority	      M-x ... RET
  Show the priority of the current item.
outlinify-sticky	      M-x ... RET
  Activate outline mode and establish file var so it is started
  subsequently.
package-reinstall	      M-x ... RET
  Reinstall package PKG.
pop-global-mark		      M-x ... RET
  Pop off global mark ring and jump to the top location.
scroll-down-line	      M-x ... RET
  Scroll text of selected window down ARG lines; or one line if no
  ARG.
split-window-below	      <menu-bar> <file> <new-window-below>
  Split WINDOW-TO-SPLIT into two windows, one above the other.
split-window-right	      <menu-bar> <file> <new-window-on-right>
  Split WINDOW-TO-SPLIT into two side-by-side windows.
subword-right		      M-x ... RET
  Do the same as `right-word' but on subwords.
tab-bar-new-tab-to	      M-x ... RET
  Add a new tab at the absolute position TAB-NUMBER.
  Value: right
tab-window-detach	      M-x ... RET
  Move the selected window to a new tab.
tags-apropos		      M-x ... RET
  Display list of all tags in tags table REGEXP matches.
thing-copy-email	      M-x ... RET
  Copy email at current point.
universal-argument	      C-u
  Begin a numeric argument for the following command.
vc-find-file-hook	      M-x ... RET
  Refresh the VC state of the current buffer's file.
window-swap-states	      M-x ... RET
  Swap the states of live windows WINDOW-1 and WINDOW-2.
xref-find-apropos	      <menu-bar> <edit> <goto> <xref-apropos>, C-M-.
  Find all meaningful symbols that match PATTERN.
ad-remove-advice	      M-x ... RET
  Remove FUNCTION's advice with NAME from its advices in CLASS.
bibtex-String		      M-x ... RET
  Insert a new BibTeX @String entry with key KEY.
bibtex-kill-entry	      M-x ... RET
  Kill the entire enclosing BibTeX entry.
browse-url-of-file	      M-x ... RET
  Use a web browser to display FILE.
bug-reference-mode	      M-x ... RET
  Toggle hyperlinking bug references in the buffer (Bug Reference
  mode).
count-words-region	      M-=
  Count the number of words in the region.
describe-function	      <menu-bar> <help-menu> <describe> <describe-function>
  Display the full documentation of FUNCTION (a symbol).
dired-do-load		      M-x ... RET
  Load the marked (or next ARG) Emacs Lisp files.
edebug-trace-mode	      M-x ... RET
  Begin trace mode.
ediff-directories	      <menu-bar> <tools> <compare> <ediff-directories>
  Run Ediff on directories DIR1 and DIR2, comparing files.
ediff-patch-buffer	      <menu-bar> <tools> <epatch> <ediff-patch-buffer>
  Run Ediff by patching the buffer specified at prompt.
epa-encrypt-region	      <menu-bar> <tools> <encryption-decryption> <encrypt-region>
  Encrypt the current region between START and END for RECIPIENTS.
epkg-find-file		      M-x ... RET
  Visit a file in a submodule of `epkg-repository'.
gnus-group-exit		      M-x ... RET
  Quit reading news after updating .newsrc.eld and .newsrc.
gnus-group-quit		      M-x ... RET
  Quit reading news without updating .newsrc.eld or .newsrc.
helpful-macro		      M-x ... RET
  Show help for macro named SYMBOL.
htmlfontify-buffer	      M-x ... RET
  Create a new buffer, named for the current buffer + a .html
  extension,
image-next-line		      M-x ... RET
  Scroll image in current window upward by N lines.
kmacro-bind-to-key	      M-x ... RET
  When not defining or executing a macro, offer to bind last macro to
  a key.
kmacro-view-macro	      M-x ... RET
  Display the last keyboard macro.
magit-branch-reset	      M-x ... RET
  Reset a branch to the tip of another branch or any other commit.
magit-clone-bare	      M-x ... RET
  Create a bare clone of REPOSITORY in DIRECTORY.
magit-log-related	      M-x ... RET
  Show log for the current branch, its upstream and its push target.
magit-push-other	      M-x ... RET
  Push an arbitrary branch or commit somewhere.
mail-split-line		      M-x ... RET
  Split current line, moving portion beyond point vertically down.
message-followup	      M-x ... RET
  Follow up to the message in the current buffer.
mml-attach-buffer	      M-x ... RET
  Attach a buffer to the outgoing MIME message.
newline-and-indent	      M-x ... RET
  Insert a newline, then indent according to major mode.
open-termscript		      M-x ... RET
  Start writing all terminal output to FILE as well as the terminal.
org-bibtex-check	      M-x ... RET
  Check the current headline for required fields.
org-indent-item		      M-x ... RET
  Indent a local list item, but not its children.
org-move-item-up	      M-x ... RET
  Move the item at point up, i.e. swap with previous item.
org-roam-node-find	      M-x ... RET
  Find and open an Org-roam node by its title or alias.
org-roam-ref-find	      M-x ... RET
  Find and open an Org-roam node that's dedicated to a specific ref.
pyim-default-scheme	      M-x ... RET
  (not documented)
re-search-backward	      M-x ... RET
  Search backward from point for regular expression REGEXP.
read-aloud-buf		      M-x ... RET
  Read the current buffer, highlighting words along the
recentf-open-files	      M-x ... RET
  Show a dialog to open a recent file.
tab-bar-select-tab	      M-x ... RET
  Switch to the tab by its absolute position TAB-NUMBER in the tab
  bar.
table-widen-cell	      M-x ... RET
  Widen the current cell by N columns and expand the cell
  horizontally.
tcl-help-on-word	      M-x ... RET
  Get help on Tcl command.  Default is word at point.
toggle-scroll-bar	      M-x ... RET
  Toggle whether or not the selected frame has vertical scroll bars.
append-to-register	      M-x ... RET
  Append region to text in register REGISTER.
backtrace-revert	      M-x ... RET
  The `revert-buffer-function' for `backtrace-mode'.
bibtex-next-entry	      M-x ... RET
  Move point ARG entries forward.
calendar-set-mark	      M-x ... RET
  Mark the date under the cursor, or jump to marked date.
checkdoc-comments	      M-x ... RET
  Find missing comment sections in the current Emacs Lisp file.
comment-set-column	      M-x ... RET
  Set the comment column based on point.
defining-kbd-macro	      M-x ... RET
  Record subsequent keyboard input, defining a keyboard macro.
  Value: nil
delete-all-space	      M-x ... RET
  Delete all spaces, tabs, and newlines around point.
delete-indentation	      M-^
  Join this line to previous and fix up whitespace at join.
doc-view-set-slice	      M-x ... RET
  Set the slice of the images that should be displayed.
ebnf-delete-style	      M-x ... RET
  Delete style NAME.
ensure-empty-lines	      M-x ... RET
  Ensure that there are LINES number of empty lines before point.
epa-list-keys		      <menu-bar> <tools> <encryption-decryption> <list-keys>
  List all keys matched with NAME from the public keyring.
find-file-at-point	      M-x ... RET
  Find FILENAME, guessing a default from text around point.
follow-scroll-down	      M-x ... RET
  Scroll text in a Follow mode window chain down.
help-xref-interned	      M-x ... RET
  Display the full documentation of SYMBOL.
image-mode-as-text	      M-x ... RET
  Set a non-image mode as major mode in combination with image minor
  mode.
json-pretty-print	      M-x ... RET
  Pretty-print selected region.
magit-bisect-bad	      M-x ... RET
  While bisecting, mark the current commit as bad.
magit-bisect-skip	      M-x ... RET
  While bisecting, skip the current commit.
magit-cherry-copy	      M-x ... RET
  Copy COMMITS from another branch onto the current branch.
magit-merge-absorb	      M-x ... RET
  Merge BRANCH into the current branch and remove the former.
magit-status-quick	      M-x ... RET
  Show the status of the current Git repository, maybe without
  refreshing.
markdown-shifttab	      M-x ... RET
  Handle S-TAB keybinding based on context.
mouse-yank-primary	      <mouse-2>
  Insert the primary selection,
org-end-of-item		      M-x ... RET
  Go to the end of the current item.
org-goto-calendar	      M-x ... RET
  Go to the Emacs calendar at the current date.
org-mark-ring-goto	      M-x ... RET
  Jump to the previous position in the mark ring.
org-previous-item	      M-x ... RET
  Move to the beginning of the previous item.
org-table-create	      M-x ... RET
  Query for a size and insert a table skeleton.
package-vc-rebuild	      M-x ... RET
  Rebuild the installation for package given by PKG-DESC.
pyim-dicts-manager	      M-x ... RET
  pyim 词库管理器。
recover-file		      M-x ... RET
  Visit file FILE, but get contents from its last auto-save file.
same-window-prefix	      M-x ... RET
  Display the buffer of the next command in the same window.
set-comment-column	      M-x ... RET
  Set the comment column based on point.
shell-resync-dirs	      M-x ... RET
  Resync the buffer's idea of the current directory stack.
so-long-disable		      M-x ... RET
  Disable the `so-long' library's functionality.
tags-query-replace	      <menu-bar> <edit> <replace> <tags-repl>
  Do `query-replace-regexp' of FROM with TO on all files listed in
  tags table.
thing-copy-line		      M-x ... RET
  Copy current line into Kill-Ring without mark the line.
tmm-menubar-mouse	      M-x ... RET
  Text-mode emulation of looking and choosing from a menubar.
toggle-viper-mode	      M-x ... RET
  Toggle Viper on/off.
widget-browse		      M-x ... RET
  Create a widget browser for WIDGET.
Buffer-menu-mark	      M-x ... RET
  Mark the Buffer menu entry at point for later display.
Buffer-menu-save	      M-x ... RET
  Mark the buffer on this Buffer Menu line for saving.
Custom-help		      M-x ... RET
  Read the node on Easy Customization in the Emacs manual.
Helper-help		      M-x ... RET
  Provide help for current mode.
View-back-to-mark	      M-x ... RET
  Return to last mark set in View mode, else beginning of file.
allout-widgets-mode	      M-x ... RET
  Toggle Allout Widgets mode.
amread-stop		      M-x ... RET
  Stop amread.
avy-goto-symbol-1	      M-x ... RET
  Jump to the currently visible CHAR at a symbol start.
avy-isearch		      M-x ... RET
  Jump to one of the current isearch candidates.
backward-kill-sexp	      ESC C-<delete>, ESC C-<backspace>, C-M-<delete>, C-M-<backspace>
  Kill the sexp (balanced expression) preceding point.
backward-sentence	      M-a
  Move backward to start of sentence.
bibtex-Book		      M-x ... RET
  Insert a template for a @Book entry; see also `bibtex-entry'.
bibtex-Misc		      M-x ... RET
  Insert a template for a @Misc entry; see also `bibtex-entry'.
bibtex-fill-field	      M-x ... RET
  Like \[fill-paragraph], but fill current BibTeX field.
bibtex-next-field	      M-x ... RET
  Move point to end of text of next BibTeX field or entry head.
bibtex-sort-buffer	      M-x ... RET
  Sort BibTeX buffer alphabetically by key.
cheat-sheet		      M-x ... RET
  Display a quick-help buffer.
describe-icon		      M-x ... RET
  Pop to a buffer to describe ICON.
dired-prev-subdir	      M-x ... RET
  Go to previous subdirectory, regardless of level.
dired-toggle-marks	      M-x ... RET
  Toggle marks: marked files become unmarked, and vice versa.
ebnf-spool-buffer	      M-x ... RET
  Generate and spool a PostScript syntactic chart image of the buffer.
edebug-help		      M-x ... RET
  Describe `edebug-mode'.
edebug-menu		      M-x ... RET
  Edebug menus.
  Value: (keymap "Edebug" (Stop menu-item "Stop" edebug-stop) (Step menu-item "Step" …
elpl-return		      M-x ... RET
  Newline or evaluate the sexp before the prompt.
end-of-visual-line	      s-<right>, C-e
  Move point to end of current visual line.
ff-get-other-file	      M-x ... RET
  Find the header or source file corresponding to this file.
format-find-file	      M-x ... RET
  Find the file FILENAME using data format FORMAT.
gif-screencast-mode	      M-x ... RET
  gif-screencast bindings
  Value: nil
handle-move-frame	      M-x ... RET
  Handle a move-frame event.
hide-leaves		      M-x ... RET
  Hide the body after this heading and at deeper levels.
image-scroll-right	      M-x ... RET
  Scroll image in current window rightward by N character widths.
insert-abbrevs		      M-x ... RET
  Insert the description of all defined abbrevs after point.
kill-matching-lines	      M-x ... RET
  Kill lines containing matches for REGEXP.
list-faces-display	      M-x ... RET
  List all faces, using the same sample text in each.
list-matching-lines	      M-x ... RET
  Show all lines in the current buffer containing a match for REGEXP.
lost-selection-mode	      M-x ... RET
  Toggle `lost-selection-mode'.
  Value: nil
magit-show-commit	      M-x ... RET
  Visit the revision at point in another buffer.
magit-unstage-file	      M-x ... RET
  Unstage all changes to FILE.
mail-yank-original	      M-x ... RET
  Insert the message being replied to, if any (in Rmail).
mailcap-view-file	      M-x ... RET
  View FILE according to rules given by the mailcap system.
markdown-move-down	      M-x ... RET
  Move thing at point down.
markdown-preview	      M-x ... RET
  Run `markdown-command' on the current buffer and view output in
  browser.
mode-minder		      M-x ... RET
  Show heirarchy of all major and minor modes.
modify-syntax-entry	      M-x ... RET
  Set syntax for character CHAR according to string NEWENTRY.
org-bibtex-write	      M-x ... RET
  Insert a heading built from the first element of
  `org-bibtex-entries'.
org-dblock-update	      M-x ... RET
  User command for updating dynamic blocks.
org-id-get-create	      M-x ... RET
  Create an ID for the current entry and return it.
org-plot/gnuplot	      M-x ... RET
  Plot table using gnuplot.  Gnuplot options can be specified with
  PARAMS.
org-previous-block	      M-x ... RET
  Jump to the previous block.
org-timestamp-down	      M-x ... RET
  Decrease the date item at the cursor by one.
org-update-dblock	      M-x ... RET
  Update the dynamic block at point.
other-frame-prefix	      M-x ... RET
  Display the buffer of the next command in a new frame.
package-recompile	      M-x ... RET
  Byte-compile package PKG again.
pending-delete-mode	      M-x ... RET
  Toggle Delete Selection mode.
plstore-mode		      M-x ... RET
  Major mode for editing PLSTORE files.
pr-txt-mode		      M-x ... RET
  Print major mode using text printer.
pr-txt-name		      M-x ... RET
  Interactively select a text printer.
recentf-save-list	      M-x ... RET
  Save the recent list.
rot13-other-window	      M-x ... RET
  Display current buffer in ROT13 in another window.
subword-transpose	      M-x ... RET
  Do the same as `transpose-words' but on subwords.
table-shorten-cell	      M-x ... RET
  Shorten the current cell by N lines by shrinking the cell
  vertically.
tabulated-list-mode	      M-x ... RET
  Generic major mode for browsing a list of items.
thing-copy-number	      M-x ... RET
  Copy number at point.
untrace-all		      <menu-bar> <emacs-lisp> <Tracing> <Untrace All>
  Untraces all currently traced functions.
url-cookie-list		      M-x ... RET
  Display a buffer listing the current URL cookies, if there are any.
article-hide-pem	      M-x ... RET
  Toggle hiding of any PEM headers and signatures in the current
  article.
async-shell-command	      <menu-bar> <tools> <shell-commands> <async-shell-command>, M-&
  Execute string COMMAND asynchronously in background.
bookmark-relocate	      M-x ... RET
  Relocate BOOKMARK-NAME to another file, reading file name with
  minibuffer.
browse-url-conkeror	      M-x ... RET
  Ask the Conkeror WWW browser to load URL.
buffer-menu-open	      C-<f10>
  Start key navigation of the buffer menu.
cal-tex-cursor-day	      M-x ... RET
  Make a buffer with LaTeX commands for the day cursor is on.
company-dabbrev		      M-x ... RET
  dabbrev-like `company-mode' completion backend.
copy-matching-lines	      M-x ... RET
  Copy lines containing matches for REGEXP to the kill ring.
define-mail-abbrev	      M-x ... RET
  Define NAME as a mail alias abbrev that translates to DEFINITION.
describe-command	      <menu-bar> <help-menu> <describe> <describe-command>, <help> x, <f1> x
  Display the full documentation of COMMAND (a symbol).
desktop-change-dir	      M-x ... RET
  Change to desktop saved in DIRNAME.
dired-do-kill-lines	      M-x ... RET
  Remove all marked lines, or the next ARG lines.
dired-tree-up		      M-x ... RET
  Go up ARG levels in the dired tree.
ebnf-merge-style	      M-x ... RET
  Merge values of style NAME with style VALUES.
ediff-merge-buffers	      <menu-bar> <tools> <ediff-merge> <ediff-merge-buffers>
  Merge buffers without ancestor.
electric-quote-mode	      M-x ... RET
  Toggle on-the-fly requoting (Electric Quote mode).
  Value: nil
epa-verify-file		      <menu-bar> <tools> <encryption-decryption> <verify-file>
  Verify FILE.
er/contract-region	      M-x ... RET
  Contract the selected region to its previous size.
find-file-literally	      M-x ... RET
  Visit file FILENAME with no conversion of any kind.
  Value: nil
format-insert-file	      M-x ... RET
  Insert the contents of file FILENAME using data format FORMAT.
forward-to-word		      M-x ... RET
  Move forward until encountering the beginning of a word.
global-so-long-mode	      M-x ... RET
  Toggle automated performance mitigations for files with long lines.
  Value: t
helpful-callable	      <help> f, <f1> f
  Show help for function, macro or special form named SYMBOL.
helpful-command		      <help> C, <f1> C
  Show help for interactive function named SYMBOL.
hi-lock-face-buffer	      M-x ... RET
  Set face of each match of REGEXP to FACE.
ido-display-buffer	      M-x ... RET
  Display a buffer in another window but don't select it.
isearchb-activate	      M-x ... RET
  Active isearchb mode for subsequent alphanumeric keystrokes.
java-ts-mode		      M-x ... RET
  Major mode for editing Java, powered by tree-sitter.
jit-lock-debug-mode	      M-x ... RET
  Minor mode to help debug code run from jit-lock.
  Value: nil
json-ts-mode		      M-x ... RET
  Major mode for editing JSON, powered by tree-sitter.
keymap-local-unset	      M-x ... RET
  Remove local binding of KEY (if any).
magit-diff-staged	      M-x ... RET
  Show changes between the index and the `HEAD' commit.
magit-request-pull	      M-x ... RET
  Request upstream to pull from your public repository.
markdown-view-mode	      M-x ... RET
  Major mode for viewing Markdown content.
org-archive-subtree	      M-x ... RET
  Move the current subtree to the archive.
org-export-dispatch	      M-x ... RET
  Export dispatcher for Org mode.
org-publish-project	      M-x ... RET
  Publish PROJECT.
org-return-indent	      M-x ... RET
  Goto next table row or insert a newline and indent.
org-table-copy-down	      M-x ... RET
  Copy the value of the current field one row below.
pr-ps-mode-print	      M-x ... RET
  Print major mode using PostScript printer.
rectangle-mark-mode	      M-x ... RET
  Toggle the region as rectangular.
self-insert-command
  Insert the character you type.
submit-emacs-patch	      M-x ... RET
  Send an Emacs patch to the Emacs maintainers.
tab-bar-rename-tab	      M-x ... RET
  Give the tab specified by its absolute position TAB-NUMBER a new
  NAME.
tab-close-group		      M-x ... RET
  Close all tabs that belong to GROUP-NAME on the selected frame.
thing-copy-symbol	      M-x ... RET
  Copy symbol around point.
undelete-frame-mode	      <menu-bar> <file> <undelete-frame-mode>
  Enable the `undelete-frame' command.
  Value: nil
use-package-report	      M-x ... RET
  Show current statistics gathered about `use-package' declarations.
widget-complete		      M-x ... RET
  Complete content of editable field from point.
yas-load-directory	      <menu-bar> <yasnippet> <Load snippets...>
  Load snippets in directory hierarchy TOP-LEVEL-DIR.
bibtex-entry-update	      M-x ... RET
  Update an existing BibTeX entry.
bibtex-search-entry	      M-x ... RET
  Move point to the beginning of BibTeX entry named KEY.
browse-url-epiphany	      M-x ... RET
  Ask the GNOME Web (Epiphany) WWW browser to load URL.
bs-cycle-previous	      M-x ... RET
  Select previous buffer defined by buffer cycling.
byte-recompile-file	      M-x ... RET
  Recompile FILENAME file if it needs recompilation.
calc-dispatch		      M-x ... RET
  Invoke the GNU Emacs Calculator.  See \[calc-dispatch-help] for
  details.
cancel-debug-watch	      M-x ... RET
  Undo effect of \[debug-on-variable-change] on VARIABLE.
checkdoc-minor-mode	      M-x ... RET
  Toggle automatic docstring checking (Checkdoc minor mode).
color-rg-rerun		      M-x ... RET
  Run `color-rg-recompile' with `compilation-arguments' taken
comint-next-prompt	      M-x ... RET
  Move to end of Nth next prompt in the buffer.
company-oddmuse		      M-x ... RET
  `company-mode' completion backend for `oddmuse-mode'.
conf-javaprop-mode	      M-x ... RET
  Conf Mode starter for Java properties files.
copy-region-as-kill	      M-x ... RET
  Save the region as if killed, but don't kill it.
delete-forward-char	      <deletechar>
  Delete the following N characters (previous if N is negative).
describe-font		      M-x ... RET
  Display information about a font whose name is FONTNAME.
diff-fixup-modifs	      M-x ... RET
  Fixup the hunk headers (in case the buffer was modified).
dired-goto-subdir	      M-x ... RET
  Go to end of header line of inserted directory DIR in this Dired
  buffer.
dired-up-directory	      M-x ... RET
  Run Dired on parent directory of current directory.
ebnf-eps-directory	      M-x ... RET
  Generate EPS files from EBNF files in DIRECTORY.
epa-mail-verify		      M-x ... RET
  Verify OpenPGP cleartext signed messages in the current buffer.
finder-commentary	      M-x ... RET
  Display FILE's commentary section.
fit-frame-to-buffer	      M-x ... RET
  Adjust size of FRAME to display the contents of its buffer exactly.
  Value: nil
global-company-mode	      M-x ... RET
  Toggle Company mode in all buffers.
  Value: nil
global-diff-hl-mode	      M-x ... RET
  Toggle Diff-Hl mode in all buffers.
  Value: nil
global-hi-lock-mode	      M-x ... RET
  Toggle Hi-Lock mode in all buffers.
  Value: nil
global-hl-line-mode	      M-x ... RET
  Toggle line highlighting in all buffers (Global Hl-Line mode).
  Value: t
global-origami-mode	      M-x ... RET
  Toggle Origami mode in all buffers.
  Value: nil
global-subword-mode	      M-x ... RET
  Toggle Subword mode in all buffers.
  Value: t
ibuffer-kill-line	      M-x ... RET
  Kill the filter group at point.
indented-text-mode	      M-x ... RET
  Major mode for editing text written for humans to read.
kill-some-buffers	      s-^
  Kill some buffers.  Asks the user whether to kill each one of them.
make-local-variable	      M-x ... RET
  Make VARIABLE have a separate value in the current buffer.
org-edit-headline	      M-x ... RET
  Edit the current headline.
org-resolve-clocks	      M-x ... RET
  Resolve all currently open Org clocks.
org-shiftmetaright	      M-x ... RET
  Demote subtree or insert table column.
org-show-todo-tree	      M-x ... RET
  Make a compact tree which shows all headlines marked with TODO.
org-table-iterate	      M-x ... RET
  Recalculate the table until it does not change anymore.
org-timer-set-timer	      M-x ... RET
  Prompt for a duration in minutes or hh:mm:ss and set a timer.
org-toggle-checkbox	      M-x ... RET
  Toggle the checkbox in the current line.
rxt-convert-syntax	      M-x ... RET
  Convert regex at point to other kind of syntax, depending on major
  mode.
rxt-explain-elisp	      M-x ... RET
  Insert the pretty-printed `rx' syntax for REGEXP in a new buffer.
shortdoc-previous	      M-x ... RET
  Move point to the previous function.
smime-sign-buffer	      M-x ... RET
  S/MIME sign BUFFER with key in KEYFILE.
thing-copy-comment	      M-x ... RET
  Copy the comment around line.
tree-sitter-hl-mode	      M-x ... RET
  Toggle syntax highlighting based on Tree-sitter's syntax tree.
  Value: nil
unhighlight-regexp	      M-s h u
  Remove highlighting of each match to REGEXP set by hi-lock.
whitespace-report	      M-x ... RET
  Report some whitespace problems in buffer.
window-divider-mode	      M-x ... RET
  Display dividers between windows (Window Divider mode).
  Value: nil
word-search-forward	      M-x ... RET
  Search forward from point for STRING, ignoring differences in
  punctuation.
yank-from-kill-ring	      M-x ... RET
  Select a stretch of previously killed text and insert ("paste") it.
Buffer-menu-delete	      M-x ... RET
  Mark the buffer on this Buffer Menu buffer line for deletion.
apropos-user-option	      <menu-bar> <help-menu> <search-documentation> <find-options-by-name>
  Show user options that match PATTERN.
browse-url-generic	      M-x ... RET
  Ask the WWW browser defined by `browse-url-generic-program' to load
  URL.
comint-write-output	      M-x ... RET
  Write output from interpreter since last input to FILENAME.
denato-region		      M-x ... RET
  Convert NATO spelling alphabet text in region to plain text.
diary-view-entries	      M-x ... RET
  Prepare and display a buffer with diary entries.
dictionary-search	      M-x ... RET
  Search the WORD in DICTIONARY if given or in all if nil.
diff-hl-margin-mode	      M-x ... RET
  Toggle displaying `diff-hl-mode' highlights on the margin.
  Value: nil
doc-view-minor-mode	      M-x ... RET
  Toggle displaying buffer via Doc View (Doc View minor mode).
  Value: nil
ebnf-spool-region	      M-x ... RET
  Generate a PostScript syntactic chart image of the region and spool
  locally.
epkg-list-packages	      M-x ... RET
  Display a list of packages.
gnus-group-suspend	      M-x ... RET
  Suspend the current Gnus session.
goto-line-relative	      M-x ... RET
  Go to LINE, counting from line at (point-min).
ido-insert-buffer	      M-x ... RET
  Insert contents of a buffer in current buffer after point.
isearch-edit-string	      M-x ... RET
  Edit the search string in the minibuffer.
kill-current-buffer	      s-&, s-k
  Kill the current buffer.
list-character-sets	      <menu-bar> <options> <mule> <list-character-sets>
  Display a list of all character sets.
list-colors-display	      M-x ... RET
  Display names of defined colors, and show what they look like.
magit-branch-rename	      M-x ... RET
  Rename the branch named OLD to NEW.
magit-diff-dwim		      M-x ... RET
  Show changes for the thing at point.
magit-ediff-compare	      M-x ... RET
  Compare REVA:FILEA with REVB:FILEB using Ediff.
mail-other-frame	      M-x ... RET
  Like `mail' command, but display mail buffer in another frame.
markdown-complete	      M-x ... RET
  Complete markup of object near point or in region when active.
markdown-mark-page	      M-x ... RET
  Put mark at end of this top level section, point at beginning.
move-to-window-line	      M-x ... RET
  Position point relative to window.
mpv-volume-set		      M-x ... RET
  Set playback volume to FACTOR.
org-footnote-action	      M-x ... RET
  Do the right thing for footnotes.
org-paste-special	      M-x ... RET
  Paste rectangular region into table, or past subtree relative to
  level.
org-roam-db-sync	      M-x ... RET
  Synchronize the cache state with the current Org files on-disk.
outlineify-sticky	      M-x ... RET
  Activate outline mode and establish file var so it is started
  subsequently.
package-upload-file	      M-x ... RET
  Upload the Emacs Lisp package FILE to the package archive.
pp-eval-last-sexp	      M-x ... RET
  Run `pp-eval-expression' on sexp before point.
python-ts-mode		      M-x ... RET
  Major mode for editing Python files, using tree-sitter library.
run-with-idle-timer	      M-x ... RET
  Perform an action the next time Emacs is idle for SECS seconds.
scroll-down-command	      M-v, <prior>, <kp-prior>
  Scroll text of selected window down ARG lines; or near full screen
  if no ARG.
select-tags-table	      M-x ... RET
  Select a tags table file from a menu of those you have already used.
shell-dirtrack-mode	      M-x ... RET
  Toggle directory tracking in this shell buffer (Shell Dirtrack
  mode).
  Value: t
symbol-overlay-mode	      M-x ... RET
  Minor mode for auto-highlighting symbol at point.
table-insert-column	      M-x ... RET
  Insert N table column(s).
thing-cut-comment	      M-x ... RET
  Cut the comment around line.
toggle-input-method	      <menu-bar> <options> <mule> <toggle-input-method>, C-\
  Enable or disable multilingual text input method for the current
  buffer.
typit-basic-test	      M-x ... RET
  Basic typing test (top 200 words).
vc-log-mergebase	      M-x ... RET
  Show a log of changes between the merge base of REV1 and REV2
  revisions.
which-function-mode	      M-x ... RET
  Toggle mode line display of current function (Which Function mode).
  Value: nil
Custom-no-edit		      M-x ... RET
  Invoke button at POS, or refuse to allow editing of Custom buffer.
View-goto-percent	      M-x ... RET
  Move to end (or prefix PERCENT) of buffer in View mode.
ad-activate-regexp	      M-x ... RET
  Activate functions with an advice name containing a REGEXP match.
avy-kill-whole-line	      M-x ... RET
  Select line and kill the whole selected line.
beginning-of-buffer	      <menu-bar> <edit> <goto> <beg-of-buf>, M-<, <home>, C-<home>, <begin>, <kp-home>
  Move point to the beginning of the buffer.
checkdoc-continue	      M-x ... RET
  Find the next doc string in the current buffer which has a style
  error.
diff-hl-show-hunk	      M-x ... RET
  Show the VC diff hunk at point.
dired-insert-subdir	      M-x ... RET
  Insert this subdirectory into the same Dired buffer.
dired-mark-symlinks	      M-x ... RET
  Mark all symbolic links.
ebnf-find-style		      M-x ... RET
  Return style definition if NAME is already defined; otherwise,
  return nil.
edit-indirect-save	      M-x ... RET
  Save the modifications done in an edit-indirect buffer.
encode-hz-region	      M-x ... RET
  Encode the text in the current region to HZ.
epa-mail-decrypt	      M-x ... RET
  Decrypt OpenPGP armors in the current buffer.
find-alternate-file	      M-x ... RET
  Find file FILENAME, select its buffer, kill previous buffer.
find-file-existing	      M-x ... RET
  Edit the existing file FILENAME.
fixup-whitespace	      M-x ... RET
  Fixup white space between objects around point.
forward-whitespace	      M-x ... RET
  Move point to the end of the next sequence of whitespace chars.
fundamental-mode	      M-x ... RET
  Major mode not specialized for anything in particular.
isearch-delete-char	      M-x ... RET
  Undo last input item during a search.
lsp-bridge-ref-mode	      M-x ... RET
  Major mode derived from `text-mode' by `define-derived-mode'.
magit-cherry-apply	      M-x ... RET
  Apply the changes in COMMITS but do not commit them.
magit-previous-line	      M-x ... RET
  Like `previous-line' but with Magit-specific shift-selection.
markdown-insert-uri	      M-x ... RET
  Insert markup for an inline URI.
message-supersede	      M-x ... RET
  Start composing a message to supersede the current message.
mml-insert-multipart	      M-x ... RET
  (not documented)
org--math-always-on	      M-x ... RET
  Always return t in Org buffers.
org-id-store-link	      M-x ... RET
  Store a link to the current entry, using its ID.
org-open-at-mouse	      M-x ... RET
  Open file link or URL at mouse.
org-outdent-item	      M-x ... RET
  Outdent a local list item, but not its children.
org-refile-reverse	      M-x ... RET
  Refile while temporarily toggling `org-reverse-note-order'.
origami-next-fold	      M-x ... RET
  Move point to the end of the fold after POINT.
package-browse-url	      M-x ... RET
  Open the website of the package under point in a browser.
package-menu-filter	      M-x ... RET
  Filter the "*Packages*" buffer by KEYWORD.
popwin:popup-buffer	      M-x ... RET
  Show BUFFER in a popup window and return the popup window. If
  Value: nil
recenter-top-bottom	      C-l
  Scroll the window so that current line is in the middle of the
  window.
vc-finish-logentry	      M-x ... RET
  Complete the operation implied by the current log entry.
view-emacs-todo		      <help> C-t, <f1> C-t
  Display the Emacs TODO list.
windmove-delete-up	      M-x ... RET
  Delete the window above the current one.
Buffer-menu-select	      M-x ... RET
  Select this line's buffer; also, display buffers marked with `>'.
ad-enable-regexp	      M-x ... RET
  Enables all advices with names that contain a match for REGEXP.
customize-set-value	      M-x ... RET
  Set VARIABLE to VALUE, and return VALUE.  VALUE is a Lisp object.
decode-hz-region	      M-x ... RET
  Decode HZ/ZW encoded text in the current region.
delete-other-frames	      M-x ... RET
  Delete all frames on FRAME's terminal, except FRAME.
dired-downcase		      M-x ... RET
  Rename all marked (or next ARG) files to lower case.
ediff-directories3	      <menu-bar> <tools> <compare> <ediff-directories3>
  Run Ediff on directories DIR1, DIR2, and DIR3, comparing files.
elisp-refs-symbol	      M-x ... RET
  Display all the references to SYMBOL in all loaded elisp files.
find-file-other-tab	      M-x ... RET
  Edit file FILENAME, in another tab.
gnus-random-face	      M-x ... RET
  Return randomly chosen Face from `gnus-face-directory'.
grugru-define-local	      M-x ... RET
  Add new grugru STRINGS-OR-GENERATOR with GETTER on buffer-local.
handle-switch-frame	      <switch-frame>
  Handle a switch-frame event EVENT.
ibuffer-or-filter	      M-x ... RET
  Replace the top two filters in this buffer with their logical OR.
icomplete-fido-kill	      M-x ... RET
  Kill line or current completion, like `ido-mode'.
info-xref-check-all	      M-x ... RET
  Check external references in all info documents in the info path.
isearch-yank-word	      M-x ... RET
  Pull next word from buffer into search string.
magit-bisect-good	      M-x ... RET
  While bisecting, mark the current commit as good.
magit-branch-shelve	      M-x ... RET
  Shelve a BRANCH.
magit-commit-reword	      M-x ... RET
  Reword the last commit, ignoring staged changes.
message-resend		      M-x ... RET
  Resend the current article to ADDRESS.
mm-uu-configure		      M-x ... RET
  Configure detection of non-MIME attachments.
mouse-buffer-menu	      C-<down-mouse-1>
  Pop up a menu of buffers for selection with the mouse.
move-to-left-margin	      M-x ... RET
  Move to the left margin of the current line.
multi-isearch-files	      M-x ... RET
  Start multi-buffer Isearch on a list of FILES.
ns-toggle-toolbar	      M-x ... RET
  Switch the tool bar on and off in frame FRAME.
occur-rename-buffer	      M-x ... RET
  Rename the current *Occur* buffer to *Occur: original-buffer-name*.
org-at-date-range-p	      M-x ... RET
  Non-nil if point is inside a date range.
org-check-deadlines	      M-x ... RET
  Check if there are any deadlines due or past due.
org-reftex-citation	      M-x ... RET
  Use `reftex-citation' to insert a citation into the buffer.
package-autoremove	      M-x ... RET
  Remove packages that are no longer needed.
package-vc-checkout	      M-x ... RET
  Clone the sources for PKG-DESC into DIRECTORY and visit that
  directory.
prepend-to-register	      M-x ... RET
  Prepend region to text in register REGISTER.
project-find-regexp	      M-x ... RET
  Find all matches for REGEXP in the current project's roots.
query-replace-regexp	      <menu-bar> <edit> <replace> <query-replace-regexp>, C-M-%
  Replace some things after point matching REGEXP with TO-STRING.
revert-buffer-quick	      M-x ... RET
  Like `revert-buffer', but asks for less confirmation.
sort-numeric-fields	      M-x ... RET
  Sort lines in region numerically by the ARGth field of each line.
studlify-region		      M-x ... RET
  Studlify-case the region.
studlify-word		      M-x ... RET
  Studlify-case the current word, or COUNT words if given an argument.
table-delete-column	      M-x ... RET
  Delete N column(s) of cells.
thing-edit-internal	      M-x ... RET
  A fast edit complexes object.
tramp-change-syntax	      M-x ... RET
  Change Tramp syntax.
transpose-sentences	      M-x ... RET
  Interchange the current sentence with the next one.
yas-minor-mode-on	      M-x ... RET
  Turn on YASnippet minor mode.
yas/minor-mode-on	      M-x ... RET
  Turn on YASnippet minor mode.
Info-history		      M-x ... RET
  Go to a node with a menu of visited nodes.
  Value: nil
Info-summary		      M-x ... RET
  Display a brief summary of all Info commands.
amread-start		      M-x ... RET
  Start / resume amread.
article-date-user	      M-x ... RET
  Convert the current article date to the user-defined format.
avy-pop-mark		      M-x ... RET
  Jump back to the last location of `avy-push-mark'.
backward-to-word	      M-x ... RET
  Move backward until encountering the end of a word.
bs-customize		      M-x ... RET
  Customization of group bs for Buffer Selection Menu.
cal-tex-cursor-week	      M-x ... RET
  Make a one page LaTeX calendar for one week, showing hours of the
  day.
conf-xdefaults-mode	      M-x ... RET
  Conf Mode starter for Xdefaults files.
delete-other-windows	      <menu-bar> <file> <one-window>
  Make WINDOW fill its frame.
dired-do-eww		      M-x ... RET
  In Dired, visit file in EWW.
dired-do-man		      M-x ... RET
  In Dired, run `man' on this file.
dired-hide-subdir	      M-x ... RET
  Hide or unhide the current subdirectory and move to next directory.
edebug-where		      M-x ... RET
  Show the debug windows and where we stopped in the program.
edit-indirect-region	      M-x ... RET
  Edit the region BEG..END in a separate buffer.
electric-buffer-list	      M-x ... RET
  Pop up the Buffer Menu in an "electric" window.
emoji-insert		      M-x ... RET
  Choose and insert an emoji glyph.
emoji-recent		      M-x ... RET
  Choose and insert one of the recently-used emoji glyphs.
enable-flow-control	      M-x ... RET
  Toggle flow control handling.
epa-show-key		      M-x ... RET
  Show a key on the current line.
ethio-replace-space	      M-x ... RET
  Replace ASCII spaces with Ethiopic word separators in the region.
eww-back-url		      M-x ... RET
  Go to the previously displayed page.
ff-find-related-file	      M-x ... RET
  Find the header or source file corresponding to this file.
fit-window-to-buffer	      M-x ... RET
  Adjust size of WINDOW to display its buffer's contents exactly.
full-calc-keypad	      M-x ... RET
  Invoke the Calculator in full-screen "visual keypad" mode.
gnus-plugged		      M-x ... RET
  Start Gnus plugged.
  Value: t
gnus-summary-exit	      M-x ... RET
  Exit reading current newsgroup, and then return to group selection
  mode.
help-go-back		      M-x ... RET
  Go back to previous topic in this help buffer.
ibuffer-yank		      M-x ... RET
  Yank the last killed filter group before group at point.
info-display-manual	      <help> R, <f1> R
  Display an Info buffer displaying MANUAL.
isearch-yank-char	      M-x ... RET
  Pull next character from buffer into search string.
keyfreq-html		      M-x ... RET
  Save an HTML file as FILENAME with all the statistics of each mode.
keyfreq-json		      M-x ... RET
  Save a file (as FILENAME) with a JSON structure of the data.
list-coding-systems	      M-x ... RET
  Display a list of all coding systems.
list-threads		      M-x ... RET
  Display a list of threads.
log-view-mode		      M-x ... RET
  Major mode for browsing CVS log output.
magit-branch-delete	      M-x ... RET
  Delete one or multiple branches.
magit-branch-spinoff	      M-x ... RET
  Create new branch from the unpushed commits.
magit-commit-squash	      M-x ... RET
  Create a squash commit, without editing the squash message.
magit-ediff-stage	      M-x ... RET
  Stage and unstage changes to FILE using Ediff.
magit-push-refspecs	      M-x ... RET
  Push one or multiple REFSPECS to a REMOTE.
mail-subject		      M-x ... RET
  Move point to end of Subject field, creating it if necessary.
message-insert-to	      M-x ... RET
  Insert a To header that points to the author of the article being
  replied to.
metafont-mode		      M-x ... RET
  Major mode for editing Metafont sources.
metapost-mode		      M-x ... RET
  Major mode for editing MetaPost sources.
mh-fully-kill-draft	      M-x ... RET
  Quit editing and delete draft message.
mml-secure-encrypt	      M-x ... RET
  Add MML tags to encrypt this MML part.
mml-validate		      M-x ... RET
  Validate the current MML document.
mouse-avoidance-mode	      M-x ... RET
  Set Mouse Avoidance mode to MODE.
  Value: nil
ns-find-file		      M-x ... RET
  Do a `find-file' with the `ns-input-file' as argument.
org-mark-ring-push	      M-x ... RET
  Put the current position into the mark ring and rotate it.
org-org-menu		      M-x ... RET
  Org menu.
  Value: (keymap "Org" (Show/Hide menu-item "Show/Hide" (keymap "Show/Hide" (Cycle\ V…
org-overview		      M-x ... RET
  Switch to overview mode, showing only top-level headlines.
org-table-sort-lines	      M-x ... RET
  Sort table lines according to the column at point.
org-tbl-menu		      M-x ... RET
  Org Table menu.
  Value: (keymap "Table" (Align menu-item "Align" org-ctrl-c-ctrl-c :enable (org-at-t…
org-timer-item		      M-x ... RET
  Insert a description-type item with the current timer value.
org-transpose-words	      M-x ... RET
  Transpose words for Org.
orgtbl-error		      M-x ... RET
  Error when there is no default binding for a table key.
other-window-prefix	      M-x ... RET
  Display the buffer of the next command in a new window.
package-update-all	      M-x ... RET
  Refresh package list and upgrade all packages.
pr-despool-ps-print	      M-x ... RET
  Send the spooled PostScript to the printer or use ghostscript to
  print it.
refresh-file		      M-x ... RET
  Automatic reload current file.
speedbar-get-focus	      M-x ... RET
  Change frame focus to or from the speedbar frame.
strokes-help		      M-x ... RET
  Get instruction on using the Strokes package.
subword-capitalize	      M-x ... RET
  Do the same as `capitalize-word' but on subwords.
tab-bar-menu-bar	      M-x ... RET
  Pop up the same menu as displayed by the menu bar.
table-narrow-cell	      M-x ... RET
  Narrow the current cell by N columns and shrink the cell
  horizontally.
tramp-version		      M-x ... RET
  Print version number of tramp.el in echo area or current buffer.
  Value: "2.6.0-pre"
vc-msg-close		      M-x ... RET
  Close the popup.
vc-region-history	      M-x ... RET
  Show the history of the region between FROM and TO.
Info-virtual-index	      M-x ... RET
  Show a node with all lines in the index containing a string TOPIC.
ad-activate-internal	      M-x ... RET
  Activate all the advice information of an advised FUNCTION.
add-change-log-entry	      M-x ... RET
  Find ChangeLog buffer, add an entry for today and an item for this
  file.
ange-ftp-reread-dir	      M-x ... RET
  Reread remote directory DIR to update the directory cache.
autoconf-mode		      M-x ... RET
  Major mode for editing Autoconf configure.ac files.
avy-goto-line-above	      M-x ... RET
  Goto visible line above the cursor.
avy-goto-line-below	      M-x ... RET
  Goto visible line below the cursor.
bookmark-bmenu-list	      <menu-bar> <edit> <bookmark> <edit>
  Display a list of existing bookmarks.
browse-url-chromium	      M-x ... RET
  Ask the Chromium WWW browser to load URL.
c-guess-no-install	      M-x ... RET
  Guess the style in the region up to `c-guess-region-max'; don't
  install it.
call-last-kbd-macro	      M-x ... RET
  Call the last keyboard macro that you defined with
  \[start-kbd-macro].
corfu-popupinfo-mode	      M-x ... RET
  Corfu info popup minor mode.
  Value: nil
debug-help-follow	      M-x ... RET
  Follow cross-reference at POS, defaulting to point.
decode-coding-region	      M-x ... RET
  Decode the current region using the specified coding system.
diff-hl-flydiff-mode	      M-x ... RET
  Perform highlighting on-the-fly.
  Value: nil
diff-restrict-view	      M-x ... RET
  Restrict the view to the current hunk.
dired-kill-subdir	      M-x ... RET
  Remove all lines of current subdirectory.
dired-other-tab		      M-x ... RET
  "Edit" directory DIRNAME.  Like `dired' but make a new tab.
display-time-world	      M-x ... RET
  Display a world clock buffer with times in various time zones.
ebnf-print-region	      M-x ... RET
  Generate and print a PostScript syntactic chart image of the region.
electric-indent-mode	      M-x ... RET
  Toggle on-the-fly reindentation of text lines (Electric Indent
  mode).
  Value: t
encode-coding-region	      M-x ... RET
  Encode the current region using th specified coding system.
epa-export-keys		      <menu-bar> <tools> <encryption-decryption> <export-keys>
  Export selected KEYS to FILE.
etc-authors-mode	      M-x ... RET
  Major mode for viewing "etc/AUTHORS" from the Emacs distribution.
eudc-expand-try-all	      M-x ... RET
  Wrap `eudc-expand-inline' with a prefix argument.
ilog-log-buffer-mode	      M-x ... RET
  Minor mode for the ilog log buffer.
  Value: nil
isearch-yank-line	      M-x ... RET
  Pull rest of line from buffer into search string.
justify-current-line	      M-x ... RET
  Do some kind of justification on this line.
keymap-global-unset	      M-x ... RET
  Remove global binding of KEY (if any).
kmacro-set-counter	      M-x ... RET
  Set the value of `kmacro-counter' to ARG, or prompt for value if no
  argument.
magit-commit-extend	      M-x ... RET
  Amend the last commit, without editing the message.
magit-stash-show	      M-x ... RET
  Show all diffs of a stash in a buffer.
make-frame-invisible	      M-x ... RET
  Make the frame FRAME invisible.
markdown-insert-link	      M-x ... RET
  Insert new or update an existing link, with interactive prompt.
markdown-next-link	      M-x ... RET
  Jump to next inline, reference, or wiki link.
negative-argument	      C-M--, M--, C--
  Begin a negative numeric argument for the next command.
org-babel-detangle	      M-x ... RET
  Propagate changes in source file back original to Org file.
org-babel-load-file	      M-x ... RET
  Load Emacs Lisp source code blocks in the Org FILE.
org-bibtex-search	      M-x ... RET
  Search for bibliographical entries in agenda files.
orgtbl-send-table	      M-x ... RET
  Send a transformed version of table at point to the receiver
  position.
override-global-mode	      M-x ... RET
  A minor mode for allowing keybindings to override other modes.
  Value: t
posix-search-forward	      M-x ... RET
  Search forward from point for REGEXP according to Posix rules.
pr-ps-buffer-preview	      M-x ... RET
  Preview buffer using ghostview.
tab-bar-history-mode	      M-x ... RET
  Toggle tab history mode for the tab bar.
  Value: nil
tags-loop-continue	      M-x ... RET
  Continue last \[tags-search] or \[tags-query-replace] command.
unmorse-region		      M-x ... RET
  Convert Morse coded text in region to plain text.
variable-pitch-mode	      M-x ... RET
  Variable-pitch default-face mode.
vc-diff-mergebase	      M-x ... RET
  Report diffs between the merge base of REV1 and REV2 revisions.
vc-insert-headers	      <menu-bar> <tools> <vc> <vc-insert-header>
  Insert headers into a file for use with a version control system.
backward-delete-char	      M-x ... RET
  Delete the previous N characters (following if N is negative).
base64-decode-region	      M-x ... RET
  Base64-decode the region between BEG and END.
bibtex-find-crossref	      M-x ... RET
  Move point to the beginning of BibTeX entry CROSSREF-KEY.
centered-cursor-mode	      M-x ... RET
  Makes the cursor stay vertically in a defined
citre-peek-through	      M-x ... RET
  Peek through a symbol in current peek window.
context-menu-open	      S-<f10>
  Start key navigation of the context menu.
copyright-fix-years	      M-x ... RET
  Convert 2 digit years to 4 digit years.
delete-backward-char	      M-x ... RET
  Delete the previous N characters (following if N is negative).
diff-refresh-hunk	      M-x ... RET
  Re-diff the current hunk.
dired-change-marks	      M-x ... RET
  Change all OLD marks to NEW marks.
dired-do-relsymlink	      M-x ... RET
  Relative symlink all marked (or next ARG) files into a directory.
display-battery-mode	      <menu-bar> <options> <showhide> <showhide-battery>
  Toggle battery status display in mode line (Display Battery mode).
  Value: t
electric-layout-mode	      M-x ... RET
  Automatically insert newlines around some chars.
  Value: nil
epa-encrypt-file	      <menu-bar> <tools> <encryption-decryption> <encrypt-file>
  Encrypt FILE for RECIPIENTS.
epa-global-mail-mode	      M-x ... RET
  Minor mode to hook EasyPG into Mail mode.
  Value: nil
epa-import-keys		      <menu-bar> <tools> <encryption-decryption> <import-keys>
  Import keys from FILE.
find-tag-other-frame	      M-x ... RET
  Find tag (in current tags table) whose name contains TAGNAME.
fortune-add-fortune	      M-x ... RET
  Add STRING to a fortune file FILE.
global-tab-line-mode	      <menu-bar> <options> <showhide> <showhide-tab-line-mode>
  Toggle Tab-Line mode in all buffers.
  Value: nil
icomplete-fido-exit	      M-x ... RET
  Attempt to exit minibuffer immediately with current input.
indent-code-rigidly	      M-x ... RET
  Indent all lines of code, starting in the region, sideways by ARG
  columns.
info-xref-docstrings	      M-x ... RET
  Check docstring info node references in source files.
ispell-kill-ispell	      M-x ... RET
  Kill current Ispell process (so that you may start a fresh one).
magit-commit-amend	      M-x ... RET
  Amend the last commit.
magit-fetch-all		      M-x ... RET
  Fetch from all remotes.
magit-remote-add	      M-x ... RET
  Add a remote named REMOTE and fetch it.
magit-reset-hard	      M-x ... RET
  Reset the `HEAD', index, and working tree to COMMIT.
magit-shell-command	      M-x ... RET
  Execute COMMAND asynchronously; display output.
magit-snapshot-both	      M-x ... RET
  Create a snapshot of the index and working tree.
markdown-mark-block	      M-x ... RET
  Put mark at end of this block, point at beginning.
minibuffer-complete	      M-x ... RET
  Complete the minibuffer contents as far as possible.
mouse-save-then-kill	      <mouse-3>, S-<mouse-1>
  Set the region according to CLICK; the second time, kill it.
name-last-kbd-macro	      M-x ... RET
  Assign a name to the last keyboard macro defined.
package-menu-execute	      M-x ... RET
  Perform marked Package Menu actions.
scan-buf-next-region	      M-x ... RET
  Go to the start of the next region with non-nil help-echo.
set-face-background	      M-x ... RET
  Change the background color of face FACE to COLOR (a string).
set-face-foreground	      M-x ... RET
  Change the foreground color of face FACE to COLOR (a string).
smime-verify-buffer	      M-x ... RET
  Verify integrity of S/MIME message in BUFFER.
speedbar-frame-mode	      <menu-bar> <options> <showhide> <showhide-speedbar>
  Enable or disable speedbar.  Positive ARG means turn on, negative
  turn off.
tab-bar-detach-tab	      M-x ... RET
  Move tab number FROM-NUMBER to a new frame.
tab-bar-move-tab-to	      M-x ... RET
  Move tab from FROM-NUMBER position to new position at TO-NUMBER.
table-heighten-cell	      M-x ... RET
  Heighten the current cell by N lines by expanding the cell
  vertically.
text-scale-increase	      M-x ... RET
  Increase the font size of the default face in current buffer by INC
  steps.
toggle-frame-tab-bar	      M-x ... RET
  Toggle tab bar of the selected frame.
word-search-backward	      M-x ... RET
  Search backward from point for STRING, ignoring differences in
  punctuation.
xeft--preview-file	      M-x ... RET
  View FILE in another window.
Info-backward-node	      M-x ... RET
  Go backward one node, considering all nodes as forming one sequence.
auto-image-file-mode	      M-x ... RET
  Toggle visiting of image files as images (Auto Image File mode).
  Value: t
color-rg-open-file	      M-x ... RET
  Open file in other window.
company-keywords	      M-x ... RET
  `company-mode' backend for programming language keywords.
company-semantic	      M-x ... RET
  `company-mode' completion backend using CEDET Semantic.
describe-key-briefly	      <help> c, <f1> c
  Print the name of the functions KEY-LIST invokes.
doc-view-shrink		      M-x ... RET
  Shrink the document by FACTOR.
ediff-current-file	      M-x ... RET
  Start ediff between current buffer and its file on disk.
eval-print-last-sexp	      M-x ... RET
  Evaluate sexp before point; print value into current buffer.
expand-mail-aliases	      M-x ... RET
  Expand all mail aliases in suitable header fields found between BEG
  and END.
gmm-customize-mode	      M-x ... RET
  Customize customization group for MODE.
gnus-summary-catchup	      M-x ... RET
  Mark all unread articles in this newsgroup as read.
image-mode-fit-frame	      M-x ... RET
  Fit FRAME to the current image.
interaction-log-mode	      M-x ... RET
  Global minor mode logging keys, commands, file loads and messages.
  Value: nil
ispell-pdict-save	      M-x ... RET
  Check to see if the personal dictionary has been modified.
keycast-tab-bar-mode	      M-x ... RET
  Show current command and its key binding in the tab bar.
  Value: nil
lisp-complete-symbol	      M-x ... RET
  Perform completion on Lisp symbol preceding point.
magit-clone-mirror	      M-x ... RET
  Create a mirror of REPOSITORY in DIRECTORY.
magit-commit-create	      M-x ... RET
  Create a new commit on `HEAD'.
magit-dired-log		      M-x ... RET
  Show log for all marked files, or the current file.
magit-reshelve-since	      M-x ... RET
  Change the author and committer dates of the commits since REV.
magit-tag-prune		      M-x ... RET
  Offer to delete tags missing locally from REMOTE, and vice versa.
mail-other-window	      M-x ... RET
  Like `mail' command, but display mail buffer in another window.
make-frame-command	      <menu-bar> <file> <make-frame>
  Make a new frame, on the same terminal as the selected frame.
message-dont-send	      M-x ... RET
  Don't send the message you have been editing.
message-goto-eoh	      M-x ... RET
  Move point to the end of the headers.
mouse-drag-mode-line	      <mode-line> <down-mouse-1>, <bottom-divider> <down-mouse-1>
  This function has :override advice: ‘ignore’.
mouse-yank-at-click	      M-x ... RET
  Insert the last stretch of killed text at the position clicked on.
org-cycle-content	      M-x ... RET
  Show all headlines in the buffer, like a table of contents.
org-previous-link	      M-x ... RET
  Move backward to the previous link.
org-set-tags-command	      M-x ... RET
  Set the tags for the current visible entry.
origami-close-node	      M-x ... RET
  Close the fold node at POINT in BUFFER.
outline-hide-body	      M-x ... RET
  Hide all body lines in buffer, leaving all headings visible.
package-update		      M-x ... RET
  Update package NAME if a newer version exists.
pcomplete-expand	      M-x ... RET
  Expand the textual value of the current argument.
pr-printify-buffer	      M-x ... RET
  Replace nonprinting characters in buffer with printable
  representations.
pr-printify-region	      M-x ... RET
  Replace nonprinting characters in region with printable
  representations.
prefer-coding-system	      M-x ... RET
  Add CODING-SYSTEM at the front of the priority list for automatic
  detection.
profiler-report-mode	      M-x ... RET
  Profiler Report Mode.
project-kill-buffers	      M-x ... RET
  Kill the buffers belonging to the current project.
quickrun-region		      M-x ... RET
  Run commands with specified region.
rmail-output-as-seen	      M-x ... RET
  Append this message to mbox file named FILE-NAME.
vc-git-stash-pop	      M-x ... RET
  Pop stash NAME.
what-cursor-position	      M-x ... RET
  Print info on cursor position (on screen and within buffer).
y-or-n-p-insert-n	      M-x ... RET
  Insert the answer "n" and exit the minibuffer of `y-or-n-p'.
y-or-n-p-insert-y	      M-x ... RET
  Insert the answer "y" and exit the minibuffer of `y-or-n-p'.
auto-encryption-mode	      M-x ... RET
  Toggle automatic file encryption/decryption (Auto Encryption mode).
  Value: t
base64-encode-region	      M-x ... RET
  Base64-encode the region between BEG and END.
browse-url-at-point	      M-x ... RET
  Open URL at point using a configurable method.
browse-url-gnome-moz	      M-x ... RET
  Ask Mozilla to load URL via the GNOME program `gnome-moz-remote'.
buffer-enable-undo	      M-x ... RET
  Start keeping undo information for buffer BUFFER.
csharp-ts-mode		      M-x ... RET
  Major mode for editing C# code.
describe-theme		      M-x ... RET
  Display a description of the Custom theme THEME (a symbol).
dired-do-find-regexp	      M-x ... RET
  Find all matches for REGEXP in all marked files.
dired-next-subdir	      M-x ... RET
  Go to next subdirectory, regardless of level.
ebnf-insert-style	      M-x ... RET
  Insert a new style NAME with inheritance INHERITS and values VALUES.
elisp-byte-code-mode	      M-x ... RET
  Major mode for *.elc files.
elisp-refs-function	      M-x ... RET
  Display all the references to function SYMBOL, in all loaded
elisp-refs-special	      M-x ... RET
  Display all the references to special form SYMBOL, in all loaded
image-previous-file	      M-x ... RET
  Visit the preceding image in the same directory as the current file.
kmacro-add-counter	      M-x ... RET
  Add the value of numeric prefix arg (prompt if missing) to
  `kmacro-counter'.
kmacro-to-register	      M-x ... RET
  Store the last keyboard macro in register R.
lisp-indent-line	      <menu-bar> <emacs-lisp> <Indent Line>
  Indent current line as Lisp code.
magit-cherry-donate	      M-x ... RET
  Move COMMITS from the current branch onto another existing BRANCH.
magit-clone-shallow	      M-x ... RET
  Create a shallow clone of REPOSITORY in DIRECTORY.
magit-reset-soft	      M-x ... RET
  Reset the `HEAD' to COMMIT, but not the index and working tree.
make-face-unitalic	      M-x ... RET
  Make the font of FACE be non-italic, if possible.
make-indirect-buffer	      M-x ... RET
  Create and return an indirect buffer for buffer BASE-BUFFER, named
  NAME.
markdown-indent-line	      M-x ... RET
  Indent the current line using some heuristics.
markdown-insert-kbd	      M-x ... RET
  Insert markup to wrap region or word in <kbd> tags.
message-bold-region	      M-x ... RET
  Bold all nonblank characters in the region.
mouse-delete-window	      <mode-line> <mouse-3>
  Delete the window you click on.
org-move-item-down	      M-x ... RET
  Move the item at point down, i.e. swap with following item.
org-table-edit-field	      M-x ... RET
  Edit table field in a different window.
outline-cycle-buffer	      M-x ... RET
  Cycle visibility state of the body lines of the whole buffer.
pp-eval-expression	      M-x ... RET
  Evaluate EXPRESSION and pretty-print its value.
pr-ps-mode-preview	      M-x ... RET
  Preview major mode using ghostview.
project-list-buffers	      M-x ... RET
  Display a list of project buffers.
rng-nxml-mode-init	      M-x ... RET
  Initialize `nxml-mode' to take advantage of `rng-validate-mode'.
shell-strip-ctrl-m	      M-x ... RET
  Strip trailing `^M' characters from the current output group.
sieve-upload-and-bury	      M-x ... RET
  (not documented)
sieve-upload-and-kill	      M-x ... RET
  (not documented)
size-indication-mode	      <menu-bar> <options> <showhide> <size-indication-mode>
  Toggle buffer size display in the mode line (Size Indication mode).
  Value: nil
tab-switcher-select	      M-x ... RET
  Select this line's window configuration.
Buffer-menu-execute	      M-x ... RET
  Save and/or delete marked buffers in the Buffer Menu.
ad-disable-regexp	      M-x ... RET
  Disable all advices with names that contain a match for REGEXP.
apropos-local-value	      M-x ... RET
  Show buffer-local variables whose values match PATTERN.
bibtex-end-of-entry	      M-x ... RET
  Move to end of BibTeX entry (past the closing brace).
bookmark-delete-all	      <menu-bar> <edit> <bookmark> <delete-all>
  Permanently delete all bookmarks.
browse-url-xdg-open	      M-x ... RET
  Pass the specified URL to the "xdg-open" command.
calendar-basic-setup	      M-x ... RET
  Create a three-month calendar.
checkdoc-eval-defun	      M-x ... RET
  Evaluate the current form with `eval-defun' and check its
  documentation.
checkdoc-interactive	      M-x ... RET
  Interactively check the current buffer for doc string errors.
decrease-left-margin	      M-x ... RET
  Make the left margin of the region smaller.
describe-categories	      M-x ... RET
  Describe the category specifications in the current category table.
ebrowse-tags-search	      M-x ... RET
  Search for REGEXP in all files in a tree.
find-file-read-only	      M-x ... RET
  Edit file FILENAME but don't allow changes.
gnus-sieve-generate	      M-x ... RET
  Generate the Sieve script in gnus-sieve-file, by replacing the
  region
ibuffer-and-filter	      M-x ... RET
  Replace the top two filters in this buffer with their logical AND.
isearch-forward-word	      <menu-bar> <edit> <i-search> <isearch-forward-word>, M-s w
  Do incremental search forward for a sequence of words.
ispell-minor-check	      M-x ... RET
  Check previous word, then continue with the normal binding of this
  key.
keymap-global-lookup	      M-x ... RET
  Return the binding for command KEYS in current global keymap only.
magit-push-matching	      M-x ... RET
  Push all matching branches to another repository.
mml-attach-external	      M-x ... RET
  Attach an external file into the buffer.
mode-line-widen		      M-x ... RET
  Widen a buffer from the mode-line.
mouse-kill-secondary	      M-x ... RET
  Kill the text in the secondary selection.
org-babel-sha1-hash	      M-x ... RET
  Generate a sha1 hash based on the value of INFO.
org-delete-directory	      M-x ... RET
  Delete the directory named DIRECTORY.  Does not follow symlinks.
org-roam-node-random	      M-x ... RET
  Find and open a random Org-roam node.
org-tags-sparse-tree	      M-x ... RET
  Create a sparse tree according to tags string MATCH.
outline-up-heading	      M-x ... RET
  Move to the visible heading line of which the present line is a
  subheading.
pr-ps-region-print	      M-x ... RET
  Print region using PostScript printer.
pr-show-pr-setup	      M-x ... RET
  Show current printing settings.
pr-show-ps-setup	      M-x ... RET
  Show current ps-print settings.
rmail-sort-by-labels	      M-x ... RET
  Sort messages of current Rmail buffer by labels.
rxt-pcre-to-strings	      M-x ... RET
  Return a list of all strings matched by PCRE, a Perl-compatible
  regexp.
table-recognize-cell	      M-x ... RET
  Recognize a table cell that contains current point.
tabulated-list-sort	      M-x ... RET
  Sort Tabulated List entries by the column at point.
thai-compose-region	      M-x ... RET
  Compose Thai characters in the region.
thing-comment-copy	      M-x ... RET
  Copy the first comment on this line, if any.
transpose-paragraphs	      M-x ... RET
  Interchange the current paragraph with the next one.
ununderline-region	      M-x ... RET
  Remove all underlining (overstruck underscores) in the region.
vc-git-log-edit-mode	      M-x ... RET
  Major mode for editing Git log messages.
vc-git-log-view-mode	      M-x ... RET
  Major mode derived from `log-view-mode' by `define-derived-mode'.
vc-svn-log-view-mode	      M-x ... RET
  Major mode derived from `log-view-mode' by `define-derived-mode'.
vc-update-change-log	      <menu-bar> <tools> <vc> <vc-update-change-log>
  Find change log file and add entries from recent version control
  logs.
xref-find-references	      M-?
  Find references to the identifier at point.
yas-insert-snippet	      <menu-bar> <yasnippet> <Insert at point...>
  Choose a snippet to expand, pop-up a list of choices according
yas/insert-snippet	      M-x ... RET
  Choose a snippet to expand, pop-up a list of choices according
Info-top-node		      M-x ... RET
  Go to the Top node of this file.
align-highlight-rule	      M-x ... RET
  Highlight the whitespace which a given rule would have modified.
article-babel		      M-x ... RET
  Translate article using an online translation service.
auto-composition-mode	      M-x ... RET
  Toggle Auto Composition mode.
  Value: t
auto-compression-mode	      M-x ... RET
  Toggle Auto Compression mode.
  Value: t
avy-goto-char		      C-;
  AceJump with pinyin by QUERY-CHAR.
bibtex-InBook		      M-x ... RET
  Insert a template for a @InBook entry; see also `bibtex-entry'.
bibtex-MVBook		      M-x ... RET
  Insert a template for a @MVBook entry; see also `bibtex-entry'.
bibtex-Manual		      M-x ... RET
  Insert a template for a @Manual entry; see also `bibtex-entry'.
bibtex-Online		      M-x ... RET
  Insert a template for a @Online entry; see also `bibtex-entry'.
bibtex-Patent		      M-x ... RET
  Insert a template for a @Patent entry; see also `bibtex-entry'.
bibtex-Report		      M-x ... RET
  Insert a template for a @Report entry; see also `bibtex-entry'.
bibtex-Thesis		      M-x ... RET
  Insert a template for a @Thesis entry; see also `bibtex-entry'.
bibtex-convert-alien	      M-x ... RET
  Make an alien BibTeX buffer fully usable by BibTeX mode.
bibtex-empty-field	      M-x ... RET
  Delete the text part of the current field, replace with empty text.
binary-overwrite-mode	      M-x ... RET
  Toggle Binary Overwrite mode.
browse-url-of-buffer	      M-x ... RET
  Use a web browser to display BUFFER.
cal-tex-cursor-month	      M-x ... RET
  Make a LaTeX calendar buffer for the month the cursor is on.
cal-tex-cursor-week2	      M-x ... RET
  Make a two page LaTeX calendar for one week, showing hours of the
  day.
completion-list-mode	      M-x ... RET
  Major mode for buffers showing lists of possible completions.
debugger-jump		      M-x ... RET
  Continue to exit from this frame, with all `debug-on-entry'
  suspended.
debugger-quit		      M-x ... RET
  Quit debugging and return to the top level.
delete-active-region	      <menu-bar> <edit> <clear>
  Delete the active region.
  Value: t
delete-blank-lines	      M-x ... RET
  On blank line, delete all surrounding blank lines, leaving just one.
delete-matching-lines	      M-x ... RET
  Delete lines containing matches for REGEXP.
delete-selection-mode	      M-x ... RET
  Toggle Delete Selection mode.
  Value: t
dired-do-info		      M-x ... RET
  In Dired, run `info' on this file.
dired-summary		      M-x ... RET
  Summarize basic Dired commands and show recent Dired errors.
doc-view-menu		      M-x ... RET
  Menu for Doc View mode.
  Value: (keymap "DocView" (Next\ page menu-item "Next page" doc-view-next-page :help…
ediff-documentation	      <menu-bar> <tools> <compare> <ediff-misc> <ediff-doc>
  Display Ediff's manual.
eieio-version		      M-x ... RET
  Display the current version of EIEIO.
  Value: "1.4"
ffap-bindings		      M-x ... RET
  Evaluate the forms in variable `ffap-bindings'.
find-file-other-frame	      M-x ... RET
  Edit file FILENAME, in another frame.
forms-find-file		      M-x ... RET
  Visit a file in Forms mode.
fortune-to-signature	      M-x ... RET
  Create signature from output of the fortune program.
gitconfig-mode		      M-x ... RET
  A major mode for editing .gitconfig files.
gitignore-mode		      M-x ... RET
  A major mode for editing .gitignore files.
gnus-mime-copy-part	      M-x ... RET
  Put the MIME part under point into a new buffer.
gnus-sticky-article	      M-x ... RET
  Make the current article sticky.
grep-menu-map		      M-x ... RET
  Menu for grep buffers.
  Value: (keymap "Grep" (Next\ Match menu-item "Next Match" next-error :help "Visit t…
hanoi-unix-64		      M-x ... RET
  Like `hanoi-unix', but with a 64-bit clock.
helm-quickrun		      M-x ... RET
  Run quickrun with `helm'.
help-for-help		      <help> C-h, <help> <help>, <help> <f1>, <help> ?, <f1> C-h, <f1> <help>, <f1> <f1>, <f1> ?
  Help command.
ibuffer-toggle-marks	      M-x ... RET
  Toggle which buffers are marked.
icomplete-ret		      M-x ... RET
  Exit minibuffer for icomplete.
indent-buffer		      M-x ... RET
  Automatic format current buffer.
isearch-quote-char	      M-x ... RET
  Quote special characters for incremental search.
iswitchb-mode		      M-x ... RET
  Toggle Iswitchb mode.
keyfreq-reset		      M-x ... RET
  Reset all statistics including those in the file.
ld-script-mode		      M-x ... RET
  A major mode to edit GNU ld script files.
lisp-fill-paragraph	      M-x ... RET
  Like \[fill-paragraph], but handle Emacs Lisp comments and
  docstrings.
magit-am-skip		      M-x ... RET
  Skip the stopped at patch during a patch applying sequence.
magit-reset-keep	      M-x ... RET
  Reset the `HEAD' and index to COMMIT, while keeping uncommitted
  changes.
mail-reply-to		      M-x ... RET
  Move point to end of Reply-To field, creating it if necessary.
markdown-insert-bold	      M-x ... RET
  Insert markup to make a region or word bold.
markdown-open		      M-x ... RET
  Open file for the current buffer with `markdown-open-command'.
menu-bar-open-mouse	      <menu-bar> <mouse-1>
  Open the menu bar for the menu item clicked on by the mouse.
ns-next-frame		      M-x ... RET
  Switch to next visible frame.
ns-prev-frame		      s-~
  Switch to previous visible frame.
org-customize		      M-x ... RET
  Call the customize function with org as argument.
org-insert-all-links	      M-x ... RET
  Insert all links in `org-stored-links'.
org-org-export-as-org	      M-x ... RET
  Export current buffer to an Org buffer.
origami-open-node	      M-x ... RET
  Open the fold node at POINT in BUFFER. The fold node opened
pkg-dm-doctor		      M-x ... RET
  Check for matching package list.
pr-ps-utility		      M-x ... RET
  Interactively select a PostScript utility.
pr-txt-buffer		      M-x ... RET
  Print buffer using text printer.
pr-txt-region		      M-x ... RET
  Print region using text printer.
prettify-symbols-mode	      M-x ... RET
  Toggle Prettify Symbols mode.
  Value: nil
previous-completion	      M-x ... RET
  Move to the previous item in the completion list.
profiler-stop		      <menu-bar> <emacs-lisp> <Profiling> <Stop Native Profiler>
  Stop started profilers.  Profiler logs will be kept.
project-dired		      M-x ... RET
  Start Dired in the current project's root.
python-remove-import	      M-x ... RET
  Remove an import statement from the current buffer.
redraw-display		      M-x ... RET
  Clear and redisplay all visible frames.
rename-visited-file	      M-x ... RET
  Rename the file visited by the current buffer to NEW-LOCATION.
rmail-summary		      M-x ... RET
  Display a summary of all messages, one line per message.
show-branches		      M-x ... RET
  Show all subheadings of this heading, but not their bodies.
shr-next-link		      M-x ... RET
  Skip to the next link.
sis-set-other		      M-x ... RET
  Command to set input source to `other'.
smime-decrypt-buffer	      M-x ... RET
  Decrypt S/MIME message in BUFFER using KEYFILE.
switch-to-prev-buffer	      M-x ... RET
  In WINDOW switch to previous buffer.
theme-choose-variant	      M-x ... RET
  Switch from the current theme to one of its variants.
thing-cut-url		      M-x ... RET
  Cut url at current point.
unbury-buffer		      M-x ... RET
  Switch to the last buffer in the buffer list.
vs-dark-theme		      M-x ... RET
  Load Visual Studio dark theme.
widget-end-of-line	      M-x ... RET
  Go to end of field or end of line, whichever is first.
xeft-previous		      M-x ... RET
  Move to previous file excerpt.
Info-nth-menu-item	      M-x ... RET
  Go to the node of the Nth menu item.
ange-ftp-re-read-dir	      M-x ... RET
  Reread remote directory DIR to update the directory cache.
auto-revert-tail-mode	      M-x ... RET
  Toggle reverting tail of buffer when the file grows.
  Value: nil
bibtex-search-entries	      M-x ... RET
  Search BibTeX entries for FIELD matching REGEXP.
browse-url-at-mouse	      M-x ... RET
  Use a web browser to load a URL clicked with the mouse.
browse-url-text-emacs	      M-x ... RET
  Ask a text browser to load URL.
c-ts-base-mode		      M-x ... RET
  Major mode for editing C, powered by tree-sitter.
cal-tex-cursor-year	      M-x ... RET
  Make a buffer with LaTeX commands for the year cursor is on.
center-paragraph	      M-x ... RET
  Center each nonblank line in the paragraph at or after point.
compile-goto-error	      M-x ... RET
  Visit the source for the error message at point.
diary-insert-entry	      M-x ... RET
  Insert a diary entry for the date indicated by point.
edebug-bounce-point	      M-x ... RET
  Bounce the point in the outside current buffer.
elp-instrument-list	      M-x ... RET
  Instrument, for profiling, all functions in `elp-function-list'.
epa-insert-keys		      <menu-bar> <tools> <encryption-decryption> <insert-keys>
  Insert selected KEYS after the point.
format-decode-buffer	      M-x ... RET
  Translate the buffer from some FORMAT.
format-decode-region	      M-x ... RET
  Decode the region from some format.
format-encode-buffer	      M-x ... RET
  Translate the buffer into FORMAT.
fortune-from-region	      M-x ... RET
  Append the current region to a local fortune-like data file.
gnus-group-read-group	      M-x ... RET
  Read news in this newsgroup.
help-follow-symbol	      M-x ... RET
  In help buffer, show docs for symbol at POS, defaulting to point.
image-decrease-size	      M-x ... RET
  Decrease the image size by a factor of N.
image-increase-size	      M-x ... RET
  Increase the image size by a factor of N.
image-toggle-display	      M-x ... RET
  Toggle between image and text display.
increase-left-margin	      M-x ... RET
  Increase or decrease the `left-margin' of the region.
keyboard-escape-quit	      M-ESC ESC
  Exit the current "mode" (in a generalized sense of the word).
keyfreq-autosave-mode	      M-x ... RET
  Keyfreq Autosave mode automatically saves
  Value: t
list-command-history	      M-x ... RET
  List history of commands that used the minibuffer.
magit-branch-checkout	      M-x ... RET
  Checkout an existing or new local branch.
mail-check-payment	      M-x ... RET
  Look for a valid X-Payment: or X-Hashcash: header.
make-frame-visible	      M-x ... RET
  Make the frame FRAME visible (assuming it is an X window).
mark-end-of-sentence	      M-x ... RET
  Put mark at end of sentence.
markdown-check-refs	      M-x ... RET
  Show all undefined Markdown references in current `markdown-mode'
  buffer.
message-cancel-news	      M-x ... RET
  Cancel an article you posted.
message-goto-body	      M-x ... RET
  Move point to the beginning of the message body.
messages-buffer-mode	      M-x ... RET
  Major mode used in the "*Messages*" buffer.
org-demote-subtree	      M-x ... RET
  Demote the entire subtree.
org-odt-export-to-odt	      M-x ... RET
  Export current buffer to a ODT file.
org-org-export-to-org	      M-x ... RET
  Export current buffer to an Org file.
org-roam-node-insert	      M-x ... RET
  Find an Org-roam node and insert (where the point is) an "id:" link
  to it.
org-table-wrap-region	      M-x ... RET
  Wrap several fields in a column like a paragraph.
posix-search-backward	      M-x ... RET
  Search backward from point for match for REGEXP according to Posix
  rules.
pr-ps-directory-print	      M-x ... RET
  Print directory using PostScript printer.
quickrun-autorun-mode	      M-x ... RET
  `quickrun' after saving buffer.
rmail-sort-by-date	      M-x ... RET
  Sort messages of current Rmail buffer by "Date" header.
rxt-elisp-to-strings	      M-x ... RET
  Return a list of all strings matched by REGEXP, an Emacs Lisp
  regexp.
search-forward-regexp	      M-x ... RET
  Search forward from point for regular expression REGEXP.
sis-auto-refresh-mode	      M-x ... RET
  Automaticly refresh input source.
  Value: t
subed-srt-mode		      M-x ... RET
  Major mode for editing SubRip subtitle files.
subed-vtt-mode		      M-x ... RET
  Major mode for editing WebVTT subtitle files.
table-generate-source	      M-x ... RET
  Generate source of the current table in the specified language.
table-insert-sequence	      M-x ... RET
  Travel cells forward while inserting a specified sequence string in
  each cell.
table-justify-column	      M-x ... RET
  Justify cells of a column.
2C-associate-buffer	      M-x ... RET
  Associate another BUFFER with this one in two-column minor mode.
ad-enable-advice	      M-x ... RET
  Enables the advice of FUNCTION with CLASS and NAME.
bibtex-count-entries	      M-x ... RET
  Count number of entries in current buffer or region.
buffer-disable-undo	      M-x ... RET
  Make BUFFER stop keeping undo information.
calendar-scroll-left	      M-x ... RET
  Scroll the displayed calendar left by ARG months.
comint-magic-space	      M-x ... RET
  Expand input history references before point and insert ARG spaces.
define-mode-abbrev	      M-x ... RET
  Define ABBREV as a mode-specific abbreviation that expands into
  EXPANSION.
diff-auto-refine-mode	      M-x ... RET
  Toggle automatic diff hunk finer highlighting (Diff Auto Refine
  mode).
  Value: nil
executable-set-magic	      M-x ... RET
  Set this buffer's interpreter to INTERPRETER with optional ARGUMENT.
file-name-shadow-mode	      M-x ... RET
  Toggle file-name shadowing in minibuffers (File-Name Shadow mode).
  Value: t
find-face-definition	      M-x ... RET
  Find the definition of FACE.  FACE defaults to the name near point.
find-tag-other-window	      M-x ... RET
  Find tag (in current tags table) whose name contains TAGNAME.
format-encode-region	      M-x ... RET
  Translate the region into some FORMAT.
global-font-lock-mode	      M-x ... RET
  Toggle Font-Lock mode in all buffers.
  Value: t
global-hl-column-mode	      M-x ... RET
  Toggle Hl-Column mode in all buffers.
  Value: nil
global-superword-mode	      M-x ... RET
  Toggle Superword mode in all buffers.
  Value: nil
gnus-face-from-file	      M-x ... RET
  Return a Face header based on an image FILE.
gnus-group-kill-group	      M-x ... RET
  Kill the next N groups.
gnus-group-make-group	      M-x ... RET
  Add a new newsgroup.
goto-history-element	      M-x ... RET
  Puts element of the minibuffer history in the minibuffer.
grugru-highlight-mode	      M-x ... RET
  Highlight if any rotatable text is at point.
  Value: nil
link-hint-copy-link	      M-x ... RET
  Copy a visible link of a supported type to the kill ring with avy.
lisp-interaction-mode	      M-x ... RET
  Major mode for typing and evaluating Lisp forms.
magit-branch-spinout	      M-x ... RET
  Create new branch from the unpushed commits.
magit-commit-augment	      M-x ... RET
  Create a squash commit, editing the squash message.
magit-merge-editmsg	      M-x ... RET
  Merge commit REV into the current branch; and edit message.
magit-stage-modified	      M-x ... RET
  Stage all changes to files modified in the worktree.
markdown-insert-image	      M-x ... RET
  Insert new or update an existing image, with interactive prompt.
org-edit-export-block	      M-x ... RET
  Edit export block at point.
org-persist-gc:elisp	      M-x ... RET
  Do nothing and return nil.
org-persist-gc:index	      M-x ... RET
  Do nothing and return nil.
org-shiftcontrolup	      M-x ... RET
  Change timestamps synchronously up in CLOCK log lines.
osx-dictionary-mode	      M-x ... RET
  Major mode to look up word through dictionary.
pr-ps-file-print	      M-x ... RET
  Print PostScript file FILENAME.
remember-clipboard	      M-x ... RET
  Remember the contents of the current clipboard.
save-place-local-mode	      M-x ... RET
  Toggle whether to save your place in this file between sessions.
server-force-delete	      M-x ... RET
  Unconditionally delete connection file for server NAME.
show-paren-local-mode	      M-x ... RET
  Toggle `show-paren-mode' only in this buffer.
spam-report-agentize	      M-x ... RET
  Add spam-report support to the Agent.
subed-ass-mode		      M-x ... RET
  Major mode for editing Advanced SubStation Alpha subtitle files.
switch-to-next-buffer	      M-x ... RET
  In WINDOW switch to next buffer.
table-backward-cell	      M-x ... RET
  Move backward to the beginning of the previous cell.
thing-copy-sentence	      M-x ... RET
  Cut sentence around point.
todo-archive-mode	      M-x ... RET
  Major mode for archived todo categories.
view-file-other-frame	      M-x ... RET
  View FILE in View mode in another frame.
windmove-display-up	      M-x ... RET
  Display the next buffer in window above the current one.
apropos-documentation	      <menu-bar> <help-menu> <search-documentation> <search-documentation-strings>, <help> d, <f1> d
  Show symbols whose documentation contains matches for PATTERN.
browse-url-text-xterm	      M-x ... RET
  Ask a text browser to load URL.
clone-indirect-buffer	      M-x ... RET
  Create an indirect buffer that is a twin copy of the current buffer.
customize-browse	      <menu-bar> <options> <customize> <customize-browse>
  Create a tree browser for the customize hierarchy.
dash-enable-font-lock	      M-x ... RET
  Toggle Dash-Fontify mode in all buffers.
describe-minor-mode	      M-x ... RET
  Display documentation of a minor mode given as MINOR-MODE.
dired-clean-directory	      M-x ... RET
  Flag numerical backups for deletion.
dired-previous-line	      M-x ... RET
  Move up lines then position at filename.
dired-show-file-type	      M-x ... RET
  Print the type of FILE, according to the `file' command.
dired-vc-next-action	      M-x ... RET
  Do the next version control operation on marked files/directories.
edir-merge-revisions	      M-x ... RET
  Run Ediff on a directory, DIR1, merging its files with their
  revisions.
edit-indirect-commit	      M-x ... RET
  Commit the modifications done in an edit-indirect buffer.
elisp-refs-variable	      M-x ... RET
  Display all the references to variable SYMBOL, in all loaded
forward-same-syntax	      M-x ... RET
  Move point past all characters with the same syntax class.
frameset-to-register	      M-x ... RET
  Store the current frameset in register REGISTER.
gnus-narrow-to-page	      M-x ... RET
  Narrow the article buffer to a page.
indent-for-comment	      M-x ... RET
  Indent this line's comment to `comment-column', or insert an empty
  comment.
isearch-query-replace	      M-x ... RET
  Start `query-replace' with string to replace from last search
  string.
ispell-complete-word	      <menu-bar> <tools> <spell> <ispell-complete-word>, <menu-bar> <edit> <spell> <ispell-complete-word>
  Try to complete the word before or at point.
magit-clone-regular	      M-x ... RET
  Create a clone of REPOSITORY in DIRECTORY.
magit-rebase-subset	      M-x ... RET
  Rebase a subset of the current branch's history onto a new base.
magit-stash-worktree	      M-x ... RET
  Create a stash of unstaged changes in the working tree.
markdown-pre-region	      M-x ... RET
  Format the region as preformatted text.
mouse-drag-top-edge	      <top-edge> <down-mouse-1>
  Drag top edge of a frame with the mouse.
org-beginning-of-line	      M-x ... RET
  Go to the beginning of the current visible line.
org-columns-compute	      M-x ... RET
  Summarize the values of PROPERTY hierarchically.
org-forward-element	      M-x ... RET
  Move forward by one element.
org-forward-paragraph	      M-x ... RET
  Move forward by a paragraph, or equivalent, unit.
package-list-packages	      <menu-bar> <options> <package>
  Display a list of packages.
pr-ps-buffer-ps-print	      M-x ... RET
  Print buffer using PostScript printer or through ghostscript.
pr-show-lpr-setup	      M-x ... RET
  Show current lpr settings.
set-visited-file-name	      M-x ... RET
  Change name of file visited in current buffer to FILENAME.
strokes-do-stroke	      s-<down-mouse-1>
  Read a simple stroke from the user and then execute its command.
tab-bar-switch-to-tab	      M-x ... RET
  Switch to the tab by NAME.
table-query-dimension	      M-x ... RET
  Return the dimension of the current cell and the current table.
Info-next-reference	      M-x ... RET
  Move cursor to the next cross-reference or menu item in the node.
bibtex-pop-previous	      M-x ... RET
  Replace text of current field with the similar field in previous
  entry.
comint-insert-input	      M-x ... RET
  In a Comint buffer, set the current input to the previous input at
  point.
comint-send-invisible	      M-x ... RET
  Read a string without echoing.
comint-strip-ctrl-m	      M-x ... RET
  Strip trailing `^M' characters from the current output group.
cpp-highlight-buffer	      M-x ... RET
  Highlight C code according to preprocessor conditionals.
decrease-right-margin	      M-x ... RET
  Make the right margin of the region smaller.
dired-goto-file		      M-x ... RET
  Go to line describing file FILE in this Dired buffer.
doc-view-enlarge	      M-x ... RET
  Enlarge the document by FACTOR.
executable-interpret	      M-x ... RET
  Run script with user-specified args, and collect output in a buffer.
help-find-keybinding	      M-x ... RET
  Display all keymaps containing a binding for the key sequence KEYS.
ibuffer-visit-buffer	      M-x ... RET
  Visit the buffer on this line.
increase-right-margin	      M-x ... RET
  Increase the right-margin of the region.
magit-cherry-spinoff	      M-x ... RET
  Move COMMITS from the current branch onto a new BRANCH.
magit-cherry-spinout	      M-x ... RET
  Move COMMITS from the current branch onto a new BRANCH.
magit-commit-reshelve	      M-x ... RET
  Change the committer date and possibly the author date of `HEAD'.
magit-rebase-branch	      M-x ... RET
  Rebase the current branch onto a branch read in the minibuffer.
magit-reset-quickly	      M-x ... RET
  Reset the `HEAD' and index to COMMIT, and possibly the working tree.
markdown-insert-code	      M-x ... RET
  Insert markup to make a region or word an inline code fragment.
markdown-standalone	      M-x ... RET
  Special function to provide standalone HTML output.
markdown-up-heading	      M-x ... RET
  Move to the visible heading line of which the present line is a
  subheading.
message-elide-region	      M-x ... RET
  Elide the text in the region.
message-split-line	      M-x ... RET
  Split current line, moving portion beyond point vertically down.
message-yank-original	      M-x ... RET
  Insert the message being replied to, if any.
mml-quote-region	      M-x ... RET
  Quote the MML tags in the region.
org-babel-tangle-file	      M-x ... RET
  Extract the bodies of source code blocks in FILE.
org-decompose-region	      M-x ... RET
  Decompose text in the current region.
org-fold-show-entry	      M-x ... RET
  Show the body directly following its heading.
org-hide-block-toggle	      M-x ... RET
  Toggle the visibility of the current block.
org-indent-to-column	      M-x ... RET
  Indent from point with tabs and spaces until COLUMN is reached.
org-table-fedit-scroll	      M-x ... RET
  (not documented)
org-unindent-buffer	      M-x ... RET
  Un-indent the visible part of the buffer.
outline-show-entry	      M-x ... RET
  Show the body directly following this heading.
rotate-yank-pointer	      M-x ... RET
  Rotate the yanking point in the kill ring.
smime-certificate-info	      M-x ... RET
  (not documented)
so-long-mode-revert	      M-x ... RET
  Call the `major-mode' which was selected before `so-long-mode'
  replaced it.
thing-copy-filename	      M-x ... RET
  Copy filename at current point.
vc-resolve-conflicts	      M-x ... RET
  Invoke ediff to resolve the conflicts.
windmove-delete-down	      M-x ... RET
  Delete the window below the current one.
xref-find-definitions	      <menu-bar> <edit> <goto> <xref-find-def>, M-.
  Find the definition of the identifier at point.
Custom-goto-parent	      M-x ... RET
  Go to the parent group listed at the top of this buffer.
Info-prev-reference	      M-x ... RET
  Move cursor to the previous cross-reference or menu item in the
  node.
bibtex-mark-entry	      M-x ... RET
  Put mark at end, point at beginning of current BibTeX entry.
build-mail-aliases	      M-x ... RET
  Read mail aliases from personal aliases file and set `mail-aliases'.
check-declare-file	      M-x ... RET
  Check veracity of all `declare-function' statements in FILE.
company-dabbrev-code	      M-x ... RET
  dabbrev-like `company-mode' backend for code.
completion-at-point	      C-M-i
  Perform completion on the text around point.
cvs-status-mode		      M-x ... RET
  Mode used for cvs status output.
dired-do-shell-command	      M-x ... RET
  Run a shell command COMMAND on the marked files.
doc-view-scale-adjust	      M-x ... RET
  Adjust the scale of the DocView page images by FACTOR.
ebnf-print-directory	      M-x ... RET
  Generate and print a PostScript syntactic chart image of DIRECTORY.
ebnf-spool-directory	      M-x ... RET
  Generate and spool a PostScript syntactic chart image of DIRECTORY.
ebrowse-save-tree-as	      M-x ... RET
  Write the current tree data structure to a file.
edebug-continue-mode	      M-x ... RET
  Begin continue mode.
edebug-eval-last-sexp	      M-x ... RET
  Evaluate sexp before point in the outside environment.
ediff-merge-revisions	      <menu-bar> <tools> <ediff-merge> <ediff-merge-revisions>
  Run Ediff by merging two revisions of a file.
gdb-script-mode		      M-x ... RET
  Major mode for editing GDB scripts.
global-on-screen-mode	      M-x ... RET
  Global minor mode guiding your eyes while scrolling.
gnus-group-list-limit	      M-x ... RET
  List groups limited within the current selection.
help-find-function	      M-x ... RET
  Display all keymaps containing a binding to the function FN.
helpful-variable	      <help> v, <f1> v
  Show help for variable named SYMBOL.
image-mode-mark-file	      M-x ... RET
  Mark the current file in the appropriate Dired buffer(s).
image-previous-frame	      M-x ... RET
  Switch to the previous frame of a multi-frame image.
info-other-window	      <help> 4 i, <f1> 4 i
  Like `info' but show the Info buffer in another window.
isearch-end-of-buffer	      M-x ... RET
  Go to the last occurrence of the current search string.
iso-cvt-read-only	      M-x ... RET
  Warn that format is read-only.
javascript-mode		      M-x ... RET
  Major mode for editing JavaScript.
magit-branch-unshelve	      M-x ... RET
  Unshelve a BRANCH
magit-cherry-harvest	      M-x ... RET
  Move COMMITS from another BRANCH onto the current branch.
magit-merge-abort	      M-x ... RET
  Abort the current merge operation.
magit-reset-mixed	      M-x ... RET
  Reset the `HEAD' and index to COMMIT, but not the working tree.
magit-subtree-add	      M-x ... RET
  Add REF from REPOSITORY as a new subtree at PREFIX.
markdown-toggle-math	      M-x ... RET
  Toggle support for inline and display LaTeX math expressions.
mouse-drag-tab-line	      <tab-line> <down-mouse-1>
  Drag frame with tab line in its topmost window.
on-screen-global-mode	      M-x ... RET
  Global minor mode guiding your eyes while scrolling.
  Value: t
org-copy-visible	      M-x ... RET
  Copy the visible parts of the region.
org-match-sparse-tree	      M-x ... RET
  Create a sparse tree according to tags string MATCH.
org-odt-export-as-odf	      M-x ... RET
  Export LATEX-FRAG as OpenDocument formula file ODF-FILE.
org-submit-bug-report	      M-x ... RET
  Submit a bug report on Org via mail.
org-table-recalculate	      M-x ... RET
  Recalculate the current table line by applying all stored formulas.
package-install-file	      M-x ... RET
  Install a package from FILE.
pop-to-mark-command	      M-x ... RET
  Jump to mark, and pop a new position for mark off the ring.
repunctuate-sentences	      M-x ... RET
  Put two spaces at the end of sentences from point to the end of
  buffer.
set-rmail-inbox-list	      M-x ... RET
  Set the inbox list of the current RMAIL file to FILE-NAME.
shadow-define-cluster	      M-x ... RET
  Edit (or create) the definition of a cluster NAME.
smime-encrypt-buffer	      M-x ... RET
  S/MIME encrypt BUFFER for recipients specified in CERTFILES.
table-recognize-table	      M-x ... RET
  Recognize a table at point.
tear-off-window		      M-x ... RET
  Delete the selected window, and create a new frame displaying its
  buffer.
windmove-delete-left	      M-x ... RET
  Delete the window to the left of the current one.
Custom-reset-saved	      M-x ... RET
  Reset all edited or set settings in the buffer to their saved value.
Info-directory		      <menu-bar> <help-menu> <more-manuals> <other-manuals>
  Go to the Info directory node.
Info-mode-menu		      M-x ... RET
  Menu for Info files.
  Value: (keymap "Info" (Up menu-item "Up" Info-up :enable (Info-check-pointer "up") …
Info-undefined		      M-x ... RET
  Make command be undefined in Info.
acm-doc-toggle		      M-x ... RET
  Toggle documentation preview for selected candidate.
apropos-follow		      M-x ... RET
  Invokes any button at point, otherwise invokes the nearest label
  button.
bibtex-Article		      M-x ... RET
  Insert a template for a @Article entry; see also `bibtex-entry'.
bibtex-Booklet		      M-x ... RET
  Insert a template for a @Booklet entry; see also `bibtex-entry'.
bibtex-Dataset		      M-x ... RET
  Insert a template for a @Dataset entry; see also `bibtex-entry'.
bibtex-autofill-entry	      M-x ... RET
  Try to fill fields of current BibTeX entry based on neighboring
  entries.
bookmark-write		      <menu-bar> <edit> <bookmark> <write>
  Write bookmarks to a file (reading the file name with the
  minibuffer).
cal-html-cursor-year	      M-x ... RET
  Write HTML calendar files (index and monthly pages) for four-digit
  YEAR.
capitalized-words-mode	      M-x ... RET
  Toggle subword movement and editing (Subword mode).
clipboard-yank		      M-x ... RET
  Insert the clipboard contents, or the last stretch of killed text.
comint-delete-output	      M-x ... RET
  Delete all output from interpreter since last input.
cpp-parse-edit		      M-x ... RET
  Edit display information for cpp conditionals.
desktop-revert		      M-x ... RET
  Revert to the last loaded desktop.
diff-context->unified	      M-x ... RET
  Convert context diffs to unified diffs.
diff-file-kill		      M-x ... RET
  Kill current file's hunks.
diff-hunk-kill		      M-x ... RET
  Kill the hunk at point.
diff-kill-junk		      M-x ... RET
  Kill spurious empty diffs.
diff-mode-menu		      M-x ... RET
  Menu for `diff-mode'.
  Value: (keymap "Diff" (Jump\ to\ Source menu-item "Jump to Source" diff-goto-source…
doc-file-to-man		      M-x ... RET
  Produce an nroff buffer containing the doc-strings from the DOC
  file.
doc-view-reset-slice	      M-x ... RET
  Reset the current slice.
ebnf-customize		      M-x ... RET
  Customization for ebnf group.
emacs-news-mode		      M-x ... RET
  Major mode for editing the Emacs NEWS file.
emoji-zoom-increase	      M-x ... RET
  Increase the size of the character under point.
find-file-other-window	      M-x ... RET
  Edit file FILENAME, in another window.
gnus-article-babel	      M-x ... RET
  Run `article-babel' in the article buffer.
gnus-disable-undo	      M-x ... RET
  Disable undoing in the current buffer.
gnus-group-next-group	      M-x ... RET
  Go to next N'th newsgroup.
gnus-group-split-setup	      M-x ... RET
  Set up the split for `nnmail-split-fancy'.
gnus-unplugged		      M-x ... RET
  Start Gnus unplugged.
graphviz-dot-preview	      M-x ... RET
  Compile the graph between BEGIN and END and preview it in an other
  buffer.
help-customize		      M-x ... RET
  Customize variable or face whose doc string is shown in the current
  buffer.
help-goto-info		      M-x ... RET
  View the *info* node of the current help item.
help-mode-menu		      M-x ... RET
  Menu for Help mode.
  Value: (keymap "Help-Mode" (Show\ Help\ for\ Symbol menu-item "Show Help for Symbol…
ibuffer-list-buffers	      M-x ... RET
  Display a list of buffers, in another window.
image-dired-minor-mode	      M-x ... RET
  Setup easy-to-use keybindings for Image-Dired in Dired mode.
info-emacs-bug		      <menu-bar> <help-menu> <emacs-manual-bug>
  Display the "Reporting Bugs" section of the Emacs manual in Info
  mode.
isearch-cancel		      M-x ... RET
  Terminate the search and go back to the starting point.
isearch-yank-pop-only	      M-x ... RET
  Replace just-yanked search string with previously killed string.
lisp-mode-menu		      M-x ... RET
  Menu for ordinary Lisp mode.
  Value: (keymap "Lisp" (Indent\ sexp menu-item "Indent sexp" indent-sexp :help "Inde…
list-load-path-shadows	      M-x ... RET
  Display a list of Emacs Lisp files that shadow other files.
list-registers		      M-x ... RET
  Display a list of nonempty registers saying briefly what they
  contain.
magit-fetch-other	      M-x ... RET
  Fetch from another repository.
magit-merge-nocommit	      M-x ... RET
  Merge commit REV into the current branch; pretending it failed.
magit-run-gitk		      M-x ... RET
  Run `gitk' in the current repository.
mail-recover-1		      M-x ... RET
  Pop up a list of auto-saved draft messages so you can recover one of
  them.
make-frame-on-monitor	      <menu-bar> <file> <make-frame-on-monitor>
  Make a frame on monitor MONITOR.
markdown-insert-pre	      M-x ... RET
  Start a preformatted section (or apply to the region).
mouse-drag-secondary	      M-<down-mouse-1>
  Set the secondary selection to the text that the mouse is dragged
  over.
next-error-no-select	      M-x ... RET
  Move point to the next error in the `next-error' buffer and
  highlight match.
occur-menu-map		      M-x ... RET
  Menu for `occur-mode'.
  Value: (keymap "Occur" (Move\ to\ Previous\ Match menu-item "Move to Previous Match…
org-ctrl-c-ret		      M-x ... RET
  Call `org-table-hline-and-move' or `org-insert-heading'.
org-hide-entry		      M-x ... RET
  Hide the body directly following this heading.
org-inc-effort		      M-x ... RET
  Increment the value of the effort property in the current entry.
org-roam-ui-node-zoom	      M-x ... RET
  Move the view of the graph to current node.
org-table-next-row	      M-x ... RET
  Go to the next row (same column) in the current table.
org-timer-stop		      M-x ... RET
  Stop the relative or countdown timer.
org-up-element		      M-x ... RET
  Move to upper element.
password-reset		      M-x ... RET
  Clear the password cache.
pcomplete-help		      M-x ... RET
  Display any help information relative to the current argument.
  Value: nil
pcomplete-list		      M-x ... RET
  Show the list of possible completions for the current argument.
pkg-dm-cleanup		      M-x ... RET
  Clean up for package dependencies.
pr-printify-directory	      M-x ... RET
  Replace nonprinting characters in directory with printable
  representations.
pr-toggle-line		      M-x ... RET
  Toggle line number.
pr-toggle-lock		      M-x ... RET
  Toggle menu lock.
pr-toggle-mode		      M-x ... RET
  Toggle auto mode.
previous-logical-line	      M-x ... RET
  Move cursor vertically up ARG lines.
profiler-reset		      M-x ... RET
  Reset profiler logs.
project-vc-dir		      M-x ... RET
  Run VC-Dir in the current project's root.
quickrun-shell		      M-x ... RET
  Run commands in shell for interactive programs.
rmail-sort-by-author	      M-x ... RET
  Sort messages of current Rmail buffer by author.
set-locale-environment	      M-x ... RET
  Set up multilingual environment for using LOCALE-NAME.
smime-noverify-buffer	      M-x ... RET
  Verify integrity of S/MIME message in BUFFER.
strokes-list-strokes	      M-x ... RET
  Pop up a buffer containing an alphabetical listing of strokes in
  STROKES-MAP.
text-mode-menu		      M-x ... RET
  Menu for `text-mode'.
  Value: (keymap "Text" (Center\ Line menu-item "Center Line" center-line :help "Cent…
thing-cut-line		      M-x ... RET
  Cut current line into Kill-Ring without mark the line.
thing-cut-list		      M-x ... RET
  Cut list around point.
thing-cut-page		      M-x ... RET
  Cut page around point.
thing-cut-sexp		      M-x ... RET
  Cut sexp at current point.
thing-cut-word		      M-x ... RET
  Cut words at point.
thread-handle-event	      M-x ... RET
  Handle thread events, propagated by `thread-signal'.
toggle-truncate-lines	      M-x ... RET
  Toggle truncating of long lines for the current buffer.
view-emacs-FAQ		      <menu-bar> <help-menu> <emacs-faq>, <help> C-f, <f1> C-f
  Display the Emacs Frequently Asked Questions (FAQ) file.
vs-light-theme		      M-x ... RET
  Load Visual Studio light theme.
widget-browse-at	      M-x ... RET
  Browse the widget under point.
xref-next-line		      M-x ... RET
  Move to the next xref and display its source in the appropriate
  window.
xref-prev-line		      M-x ... RET
  Move to the previous xref and display its source in the appropriate
  window.
yank-rectangle		      M-x ... RET
  Yank the last killed rectangle with upper left corner at point.
yas-prev-field		      M-x ... RET
  Navigate to prev field.  If there's none, exit the snippet.
yas-tryout-snippet	      M-x ... RET
  Test current buffer's snippet template in other buffer.
yas/prev-field		      M-x ... RET
  Navigate to prev field.  If there's none, exit the snippet.
yas/tryout-snippet	      M-x ... RET
  Test current buffer's snippet template in other buffer.
auto-revert-set-timer	      M-x ... RET
  Restart or cancel the timer used by Auto-Revert Mode.
auto-save-visited-mode	      M-x ... RET
  Toggle automatic saving of file-visiting buffers to their files.
  Value: t
avy-goto-char-2-above	      M-x ... RET
  Jump to the currently visible CHAR1 followed by CHAR2.
avy-goto-char-2-below	      M-x ... RET
  Jump to the currently visible CHAR1 followed by CHAR2.
avy-goto-char-timer	      M-x ... RET
  Read one or many consecutive chars and jump to the first one.
avy-goto-word-1-above	      M-x ... RET
  Jump to the currently visible CHAR at a word start.
avy-goto-word-1-below	      M-x ... RET
  Jump to the currently visible CHAR at a word start.
cancel-debug-on-entry	      M-x ... RET
  Undo effect of \[debug-on-entry] on FUNCTION.
citre-update-tags-file	      M-x ... RET
  Update TAGSFILE.
comint-show-output	      M-x ... RET
  Display start of this batch of interpreter output at top of window.
compilation-minor-mode	      M-x ... RET
  Toggle Compilation minor mode.
  Value: nil
customize-option	      <menu-bar> <options> <customize> <customize-option>
  Customize SYMBOL, which must be a user option.
delimit-columns-region	      M-x ... RET
  Prettify all columns in a text region.
dired-do-compress-to	      M-x ... RET
  Compress selected files and directories to an archive.
dired-mouse-find-file	      M-x ... RET
  In Dired, visit the file or directory name you click on.
dired-other-frame	      M-x ... RET
  "Edit" directory DIRNAME.  Like `dired' but make a new frame.
emacs-authors-mode	      M-x ... RET
  Major mode for viewing "etc/AUTHORS" from the Emacs distribution.
gnus-dead-summary-mode	      M-x ... RET
  Minor mode for Gnus summary buffers.
  Value: nil
gnus-random-x-face	      M-x ... RET
  Return X-Face header data chosen randomly from
  `gnus-x-face-directory'.
goto-address-at-point	      M-x ... RET
  Compose a new message to the e-mail address or open URL at point.
highlight-changes-mode	      M-x ... RET
  Toggle highlighting changes in this buffer (Highlight Changes mode).
icalendar-import-file	      M-x ... RET
  Import an iCalendar file and append to a diary file.
isearch-toggle-word	      M-x ... RET
  Toggle word searching on or off.
kmacro-insert-counter	      M-x ... RET
  Insert current value of `kmacro-counter', then increment it by ARG.
magit-fetch-branch	      M-x ... RET
  Fetch a BRANCH from a REMOTE.
mail-insert-file	      M-x ... RET
  Insert a file at the end of the buffer, with separator lines around
  it.
mail-send-and-exit	      M-x ... RET
  Send message like `mail-send', then, if no errors, exit from mail
  buffer.
markdown-insert-header	      M-x ... RET
  Insert or replace header markup.
markdown-insert-table	      M-x ... RET
  Insert an empty pipe table.
mouse-yank-secondary	      M-<mouse-2>
  Insert the secondary selection at the position clicked on.
multi-isearch-buffers	      M-x ... RET
  Start multi-buffer Isearch on a list of BUFFERS.
next-history-element	      M-x ... RET
  Puts next element of the minibuffer history in the minibuffer.
next-line-completion	      M-x ... RET
  Move to the item on the next line in the completion list.
org-cycle-list-bullet	      M-x ... RET
  Cycle through the different itemize/enumerate bullets.
org-forward-sentence	      M-x ... RET
  Go to end of sentence, or end of table field.
org-table-eval-formula	      M-x ... RET
  Replace the table field value at the cursor by the result of a
  calculation.
org-toggle-fixed-width	      M-x ... RET
  Toggle fixed-width markup.
origami-toggle-node	      M-x ... RET
  Toggle the fold node at POINT in BUFFER open or closed. The
pr-toggle-file-tumble	      M-x ... RET
  Toggle tumble for PostScript file.
ps-nb-pages-buffer	      M-x ... RET
  Display number of pages to print this buffer, for various font
  heights.
ps-nb-pages-region	      M-x ... RET
  Display number of pages to print the region, for various font
  heights.
set-background-color	      M-x ... RET
  Set the background color of the selected frame to COLOR-NAME.
set-foreground-color	      M-x ... RET
  Set the foreground color of the selected frame to COLOR-NAME.
shr-render-region	      M-x ... RET
  Display the HTML rendering of the region between BEGIN and END.
tab-bar-mouse-down-1	      M-x ... RET
  Select the tab at mouse click, or add a new tab on the tab bar.
table-fixed-width-mode	      M-x ... RET
  Cell width is fixed when this is non-nil.
texinfo-format-buffer	      M-x ... RET
  Process the current buffer as texinfo code, into an Info file.
toggle-debug-on-quit	      <menu-bar> <options> <debug-on-quit>
  Toggle whether to enter Lisp debugger when C-g is pressed.
toggle-frame-maximized	      ESC <f10>, M-<f10>, S-s-<return>
  Toggle maximization state of FRAME.
Buffer-menu-unmark	      M-x ... RET
  Cancel all requested operations on buffer on this line and move
  down.
ace-pinyin-jump-word	      M-x ... RET
  Jump to Chinese word.
ad-disable-advice	      M-x ... RET
  Disable the advice of FUNCTION with CLASS and NAME.
backward-kill-word	      M-DEL, C-<backspace>
  Kill characters backward until encountering the beginning of a word.
bibtex-search-crossref	      M-x ... RET
  Move point to the beginning of BibTeX entry CROSSREF-KEY.
checkdoc-rogue-spaces	      M-x ... RET
  Find extra spaces at the end of lines in the current file.
delete-duplicate-lines	      M-x ... RET
  Delete all but one copy of any identical lines in the region.
describe-package	      <menu-bar> <help-menu> <describe> <describe-package>, <help> P, <f1> P
  Display the full documentation of PACKAGE (a symbol).
diff-mouse-goto-source	      M-x ... RET
  Jump to the corresponding source line.
diff-tell-file-name	      M-x ... RET
  Tell Emacs where the find the source file of the current hunk.
dired-next-marked-file	      M-x ... RET
  Move to the ARGth next marked file.
doc-view-next-page	      M-x ... RET
  Browse ARG pages forward.
elint-current-buffer	      <menu-bar> <emacs-lisp> <Linting> <Lint Buffer>
  Lint the current buffer.
gnus-group-list-level	      M-x ... RET
  List groups on LEVEL.
gnus-mailing-list-mode	      M-x ... RET
  Minor mode for providing mailing-list commands.
hi-lock-unface-buffer	      M-x ... RET
  Remove highlighting of each match to REGEXP set by hi-lock.
ibuffer-change-marks	      M-x ... RET
  Change all OLD marks to NEW marks.
ibuffer-other-window	      M-x ... RET
  Like `ibuffer', but displayed in another window by default.
icalendar-export-file	      M-x ... RET
  Export diary file to iCalendar format.
ido-enter-magit-status	      M-x ... RET
  Drop into `magit-status' from file switching.
indent-for-tab-command	      TAB
  Indent the current line or region, or insert a tab, as appropriate.
isearch-char-by-name	      M-x ... RET
  Read a character by its Unicode name and add it to the search
  string.
kill-local-variable	      M-x ... RET
  Make VARIABLE no longer have a separate value in the current buffer.
magit-auto-revert-mode	      M-x ... RET
  Toggle Auto-Revert mode in all buffers.
  Value: t
magit-remote-set-head	      M-x ... RET
  Set the local representation of REMOTE's default branch.
move-beginning-of-line	      s-<left>, C-a
  Move point to visible beginning of current logical line.
org-backward-element	      M-x ... RET
  Move backward by one element.
org-bibtex-check-all	      M-x ... RET
  Check all headlines in the current file.
org-calendar-select	      M-x ... RET
  Return to `org-read-date' with the date currently selected.
org-info-find-node	      M-x ... RET
  Find Info documentation NODENAME or Org documentation according
  context.
org-table-goto-column	      M-x ... RET
  Move the cursor to the Nth column in the current table line.
package-report-bug	      M-x ... RET
  Prepare a message to send to the maintainers of a package.
pr-ps-file-up-ps-print	      M-x ... RET
  Process a PostScript file IFILENAME and send it to printer.
recenter-other-window	      C-M-S-l
  Call `recenter-top-bottom' in the other window.
select-frame-by-name	      M-x ... RET
  Select the frame whose name is NAME and raise it.
tree-sitter-debug-mode	      M-x ... RET
  Toggle syntax tree debugging for the current buffer.
  Value: nil
view-file-other-window	      M-x ... RET
  View FILE in View mode in another window.
Info-follow-reference	      M-x ... RET
  Follow cross reference named FOOTNOTENAME to the node it refers to.
Info-goto-node-web	      M-x ... RET
  Use `browse-url' to go to the gnu.org web server's version of NODE.
ace-pinyin-global-mode	      M-x ... RET
  Toggle Ace-Pinyin mode in all buffers.
  Value: t
ad-deactivate-regexp	      M-x ... RET
  Deactivate functions with an advice name containing REGEXP match.
calc-grab-region	      M-x ... RET
  Parse the region as a vector of numbers and push it on the
  Calculator stack.
calendar-scroll-right	      M-x ... RET
  Scroll the displayed calendar window right by ARG months.
check-ispell-version	      M-x ... RET
  Ensure that `ispell-program-name' is valid and has the correct
  version.
company-quickhelp-mode	      M-x ... RET
  Toggle Company-Quickhelp-Local mode in all buffers.
  Value: nil
cursor-intangible-mode	      M-x ... RET
  Keep cursor outside of any `cursor-intangible' text property.
customize-create-theme	      M-x ... RET
  Create or edit a custom theme.
customize-set-variable	      M-x ... RET
  Set the default for VARIABLE to VALUE, and return VALUE.
dired-do-rename-regexp	      M-x ... RET
  Rename selected files whose names match REGEXP to NEWNAME.
dired-next-dirline	      M-x ... RET
  Goto ARGth next directory file line.
ebnf-syntax-directory	      M-x ... RET
  Do a syntactic analysis of the files in DIRECTORY.
ethio-modify-vowel	      M-x ... RET
  Modify the vowel of the FIDEL that is under the cursor.
global-git-gutter-mode	      M-x ... RET
  Toggle Git-Gutter mode in all buffers.
  Value: t
global-whitespace-mode	      M-x ... RET
  Toggle whitespace visualization globally (Global Whitespace mode).
  Value: t
glyphless-display-mode	      M-x ... RET
  Minor mode for displaying glyphless characters in the current
  buffer.
gnus-article-highlight	      M-x ... RET
  Highlight current article.
gnus-group-apropos	      M-x ... RET
  List all newsgroups that have names that match a regexp.
gnus-summary-next-page	      M-x ... RET
  Show next page of the selected article.
gnus-summary-quit	      M-x ... RET
  Quit reading current newsgroup without updating read article info.
gnus-warp-to-article	      M-x ... RET
  Look up the current article in the group where it originated.
goto-address-prog-mode	      M-x ... RET
  Like `goto-address-mode', but only for comments and strings.
help-goto-next-page	      M-x ... RET
  Go to the next page (if any) in the current buffer.
ibuffer-find-file	      M-x ... RET
  Like `find-file', but default to the directory of the buffer at
  point.
image-previous-line	      M-x ... RET
  Scroll image in current window downward by N lines.
isearch-forward-regexp	      <menu-bar> <edit> <i-search> <isearch-forward-regexp>, M-s-f, C-M-s
  Do incremental search forward for regular expression.
isearch-repeat-forward	      s-g
  Repeat incremental search forwards.
ispell-check-version	      M-x ... RET
  Ensure that `ispell-program-name' is valid and has the correct
  version.
keycast-mode-line-mode	      M-x ... RET
  Show current command and its key binding in the mode line.
  Value: nil
kill-matching-buffers	      M-x ... RET
  Kill buffers whose name matches the specified REGEXP.
magit-diff-paths	      M-x ... RET
  Show changes between any two files on disk.
magit-pull-branch	      M-x ... RET
  Pull from a branch read in the minibuffer.
magit-rebase-continue	      M-x ... RET
  Restart the current rebasing operation.
magit-submodule-remove	      M-x ... RET
  Unregister MODULES and remove their working directories.
markdown-table-align	      M-x ... RET
  Align table at point.
message-to-list-only	      M-x ... RET
  Send a message to the list only.
mouse-drag-header-line	      <header-line> <down-mouse-1>
  This function has :override advice: ‘ignore’.
org-backward-paragraph	      M-x ... RET
  Move backward by a paragraph, or equivalent, unit.
org-edit-comment-block	      M-x ... RET
  Edit comment block at point.
org-table-copy-region	      M-x ... RET
  Copy rectangular region in table to clipboard.
orgtbl-ctrl-c-ctrl-c	      M-x ... RET
  If the cursor is inside a table, realign the table.
outline-hide-other	      M-x ... RET
  Hide everything except current body and parent and top-level
  headings.
outline-show-children	      M-x ... RET
  Show all direct subheadings of this heading.
pr-ps-region-preview	      M-x ... RET
  Preview region using ghostview.
rcirc-track-minor-mode	      M-x ... RET
  Global minor mode for tracking activity in rcirc buffers.
  Value: nil
rmail-sort-by-lines	      M-x ... RET
  Sort messages of current Rmail buffer by the number of lines.
set-selective-display	      M-x ... RET
  Set `selective-display' to ARG; clear it if no arg.
shr-save-contents	      M-x ... RET
  Save the contents from URL in a file.
sis--inline-activate	      M-x ... RET
  Activate the inline region overlay from START.
sis-global-inline-mode	      M-x ... RET
  Toggle Sis-Inline mode in all buffers.
  Value: t
texinfo-format-region	      M-x ... RET
  Convert the current region of the Texinfo file to Info format.
widget-button-press	      M-x ... RET
  Invoke button at POS.
article-emphasize	      M-x ... RET
  Emphasize text according to `gnus-emphasis-alist'.
beginning-of-defun-raw	      M-x ... RET
  Move point to the character that starts a defun.
beginning-of-line-text	      M-x ... RET
  Move to the beginning of the text on this line.
cal-html-cursor-month	      M-x ... RET
  Write an HTML calendar file for numeric MONTH of four-digit YEAR.
cl-describe-type	      M-x ... RET
  Display the documentation for type TYPE (a symbol).
debugger-return-value	      M-x ... RET
  Continue, specifying value to return.
dired-mark-executables	      M-x ... RET
  Mark all executable files.
ediff-regions-linewise	      <menu-bar> <tools> <compare> <ediff-regions-linewise>
  Run Ediff on a pair of regions in specified buffers.
ediff-regions-wordwise	      <menu-bar> <tools> <compare> <ediff-regions-wordwise>
  Run Ediff on a pair of regions in specified buffers.
edit-indirect-abort	      M-x ... RET
  Abort indirect editing in the current buffer and kill the buffer.
edt-set-scroll-margins	      M-x ... RET
  Set scroll margins.
explain-pause-top-mode	      M-x ... RET
  Major mode for listing the statistics generated by explain-pause for
  recently
gitattributes-mode	      M-x ... RET
  A major mode for editing .gitattributes files.
gnus-article-edit-mode	      M-x ... RET
  Major mode for editing articles.
  Value: nil
gnus-bookmark-jump	      M-x ... RET
  Jump to a Gnus bookmark (BMK-NAME).
gnus-mime-pipe-part	      M-x ... RET
  Pipe the MIME part under point to a process.
gnus-mouse-pick-article	      M-x ... RET
  (not documented)
ibuffer-jump-to-buffer	      M-x ... RET
  Move point to the buffer whose name is NAME.
ibuffer-mark-forward	      M-x ... RET
  Mark the buffers in the region, or ARG buffers.
isearch-forward-symbol	      <menu-bar> <edit> <i-search> <isearch-forward-symbol>, M-s _
  Do incremental search forward for a symbol.
magit-log-buffer-file	      M-x ... RET
  Show log for the blob or file visited in the current buffer.
magit-subtree-pull	      M-x ... RET
  Pull REF from REPOSITORY into the PREFIX subtree.
make-frame-on-display	      <menu-bar> <file> <make-frame-on-display>
  Make a frame on display DISPLAY.
mouse-drag-left-edge	      <left-edge> <down-mouse-1>
  Drag left edge of a frame with the mouse.
org-hide-drawer-toggle	      M-x ... RET
  Toggle the visibility of the current drawer.
org-newline-and-indent	      M-x ... RET
  Insert a newline, then indent according to major mode.
package-recompile-all	      M-x ... RET
  Byte-compile all installed packages.
pr-ps-mode-ps-print	      M-x ... RET
  Print major mode using PostScript or through ghostscript.
repeat-complex-command	      <redo>, <again>
  Edit and re-evaluate last complex command, or ARGth from last.
search-backward-regexp	      M-x ... RET
  Search backward from point for regular expression REGEXP.
universal-argument-more	      M-x ... RET
  (not documented)
Custom-reset-standard	      M-x ... RET
  Erase all customizations (either current or saved) in current
  buffer.
bibtex-previous-entry	      M-x ... RET
  Move point ARG entries backward.
comint-kill-whole-line	      M-x ... RET
  Kill current line, ignoring read-only and field properties.
compilation-next-error	      M-x ... RET
  Move point to the next error in the compilation buffer.
diff-unified->context	      M-x ... RET
  Convert unified diffs to context diffs.
dired-do-copy-regexp	      M-x ... RET
  Copy selected files whose names match REGEXP to NEWNAME.
dired-prev-marked-file	      M-x ... RET
  Move to the ARGth previous marked file.
display-about-screen	      M-x ... RET
  Display the *About GNU Emacs* buffer.
doc-file-to-info	      M-x ... RET
  Produce a texinfo buffer with sorted doc-strings from the DOC file.
edebug-forward-sexp	      M-x ... RET
  Proceed from the current point to the end of the ARGth sexp ahead.
gnus-button-patch	      M-x ... RET
  Visit an Emacs Lisp library LIBRARY on line LINE.
gnus-group-list-cached	      M-x ... RET
  List all groups with cached articles.
gnus-group-list-ticked	      M-x ... RET
  List all groups with ticked articles.
gnus-group-yank-group	      M-x ... RET
  Yank the last newsgroups killed with \[gnus-group-kill-group],
  inserting it
gnus-summary-recenter	      M-x ... RET
  Center point in the summary window.
help-at-pt-set-timer	      M-x ... RET
  Enable `help-at-pt-display-when-idle'.
imenu-add-to-menubar	      M-x ... RET
  Add an `imenu' entry to the menu bar for the current buffer.
isearch-emoji-by-name	      M-x ... RET
  Read an Emoji name and add it to the search string COUNT times.
iso-cvt-write-only	      M-x ... RET
  Warn that format is write-only.
magit-edit-line-commit	      M-x ... RET
  Edit the commit that added the current line.
magit-log-branches	      M-x ... RET
  Show log for all local branches and `HEAD'.
mail-abbrev-next-line	      M-x ... RET
  Expand a mail abbrev before point, then move vertically down ARG
  lines.
make-face-bold-italic	      M-x ... RET
  Make the font of FACE be bold and italic, if possible.
markdown-mark-subtree	      M-x ... RET
  Mark the current subtree.
package-vc-update	      M-x ... RET
  Attempt to update the package PKG-DESC.
relint-directory	      M-x ... RET
  Scan all *.el files in DIR for regexp-related errors.
set-face-inverse-video	      M-x ... RET
  Specify whether face FACE is in inverse video.
tab-switcher-delete	      M-x ... RET
  Mark window configuration on this line to be deleted by
  \<tab-switcher-mode-map>\[tab-switcher-execute] command.
table-recognize-region	      M-x ... RET
  Recognize all tables within region.
typit-advanced-test	      M-x ... RET
  Advanced typing test (top 1000 words).
vc-git-stash-apply	      M-x ... RET
  Apply stash NAME.
windmove-delete-right	      M-x ... RET
  Delete the window to the right of the current one.
Info-final-node		      M-x ... RET
  Go to the final node in this file.
acm-select-last		      M-x ... RET
  Select last candidate.
acm-select-next		      M-x ... RET
  Select next candidate.
acm-select-prev		      M-x ... RET
  Select previous candidate.
ad-unadvise-all		      M-x ... RET
  Unadvise all currently advised functions.
amread-speed-up		      M-x ... RET
  Speed up `amread-mode'.
article-date-original	      M-x ... RET
  Convert the current article date to what it was originally.
avy-goto-char-2		      C-'
  Ace-pinyin replacement of `avy-goto-char-2'.
avy-goto-word-0		      M-g e
  Ace-pinyin replacement of `avy-goto-word-0'.
avy-goto-word-1		      M-g w
  Ace-pinyin replacement of `avy-goto-word-1'.
avy-move-region		      M-x ... RET
  Select two lines and move the text between them above the current
  line.
bibtex-Preamble		      M-x ... RET
  Insert a new BibTeX @Preamble entry.
bibtex-Software		      M-x ... RET
  Insert a template for a @Software entry; see also `bibtex-entry'.
bibtex-SuppBook		      M-x ... RET
  Insert a template for a @SuppBook entry; see also `bibtex-entry'.
calendar-redraw		      M-x ... RET
  Redraw the calendar display, if `calendar-buffer' is live.
calendar-unmark		      M-x ... RET
  Delete all diary/holiday marks/highlighting from the calendar.
citre-jump-back		      M-x ... RET
  Go back to the position before last `citre-jump'.
citre-peek-jump		      M-x ... RET
  Jump to the definition that is currently peeked.
clipboard-kill-region	      M-x ... RET
  Kill the region, and save it in the GUI's clipboard.
comint-send-eof		      M-x ... RET
  Send an EOF to the current buffer's process.
customize-rogue		      M-x ... RET
  Customize all user variables modified outside customize.
customize-saved		      <menu-bar> <options> <customize> <customize-saved>
  Customize all saved options and faces.
define-global-abbrev	      M-x ... RET
  Define ABBREV as a global abbreviation that expands into EXPANSION.
diff-split-hunk		      M-x ... RET
  Split the current (unified diff) hunk at point into two hunks.
dired-find-file		      M-x ... RET
  In Dired, visit the file or directory named on this line.
dired-tree-down		      M-x ... RET
  Go down in the dired tree.
dired-unmark-backward	      M-x ... RET
  In Dired, move up lines and remove marks or deletion flags there.
edebug-all-defs		      M-x ... RET
  Toggle edebugging of all definitions.
  Value: nil
ediff-windows-linewise	      <menu-bar> <tools> <compare> <ediff-windows-linewise>
  Compare WIND-A and WIND-B, which are selected by clicking, linewise.
ediff-windows-wordwise	      <menu-bar> <tools> <compare> <ediff-windows-wordwise>
  Compare WIND-A and WIND-B, which are selected by clicking, wordwise.
epa-exit-buffer		      M-x ... RET
  Exit the current buffer using `epa-exit-buffer-function'.
ert-describe-test	      M-x ... RET
  Display the documentation for TEST-OR-TEST-NAME (a symbol or
  ert-test).
eww-buffer-kill		      M-x ... RET
  Kill buffer from eww list.
eww-end-of-text		      M-x ... RET
  Move to the end of the text in the input field.
eww-forward-url		      M-x ... RET
  Go to the next displayed page.
eww-select-file		      M-x ... RET
  Change the value of the upload file menu under point.
eww-view-source		      M-x ... RET
  View the HTML source code of the current page.
exit-minibuffer		      M-x ... RET
  Terminate this minibuffer argument.
flyspell-buffer		      M-x ... RET
  Flyspell whole buffer.
global-auto-revert-mode	      M-x ... RET
  Toggle Global Auto-Revert Mode.
  Value: t
gnus-group-prev-group	      M-x ... RET
  Go to previous N'th newsgroup.
gnus-summary-muttprint	      M-x ... RET
  Print the current article using Muttprint.
gnus-summary-up-thread	      M-x ... RET
  Go up thread N steps.
help-go-forward		      M-x ... RET
  Go to the next topic in this help buffer.
ibuffer-bs-show		      M-x ... RET
  Emulate `bs-show' from the bs.el package.
ibuffer-do-save		      M-x ... RET
  Save marked buffers as with `save-buffer'.
image-mode-menu		      M-x ... RET
  Menu for Image mode.
  Value: (keymap "Image" (Show\ as\ Text menu-item "Show as Text" image-toggle-displa…
indent-relative-maybe	      M-x ... RET
  Indent the current line like the previous nonblank line.
insert-file-literally	      M-x ... RET
  Insert contents of file FILENAME into buffer after point with no
  conversion.
ispell-continue		      <menu-bar> <tools> <spell> <ispell-continue>, <menu-bar> <edit> <spell> <ispell-continue>
  Continue a halted spelling session beginning with the current word.
last-completion		      M-x ... RET
  Move to the last item in the completion list.
list-dynamic-libraries	      M-x ... RET
  Display a list of all dynamic libraries known to Emacs.
magit-fetch-all-prune	      M-x ... RET
  Fetch from all remotes, and prune.
magit-reflog-current	      M-x ... RET
  Display the reflog of the current branch.
magit-reset-worktree	      M-x ... RET
  Reset the worktree to COMMIT.
mailcap-parse-mailcaps	      M-x ... RET
  Parse out all the mailcaps specified in a path string PATH.
markdown-footnote-kill	      M-x ... RET
  Kill the footnote at point.
markdown-insert-italic	      M-x ... RET
  Insert markup to make a region or word italic.
message-goto-cc		      M-x ... RET
  Move point to the Cc header.
message-goto-to		      M-x ... RET
  Move point to the To header.
message-recover		      M-x ... RET
  Reread contents of current buffer from its last auto-save file.
message-rename-buffer	      M-x ... RET
  Rename the *message* buffer to "*message* RECIPIENT".
mpv-revert-seek		      M-x ... RET
  Undo the previous seek command.
ns-print-buffer		      M-x ... RET
  Interactive front-end to `print-buffer': asks for user confirmation
  first.
org-babel-tangle-clean	      M-x ... RET
  Remove comments inserted by `org-babel-tangle'.
org-bibtex-yank		      M-x ... RET
  If kill ring holds a bibtex entry yank it as an Org headline.
org-browse-news		      M-x ... RET
  Browse the news for the latest major release.
org-html-export-as-html	      M-x ... RET
  Export current buffer to an HTML buffer.
org-list-repair		      M-x ... RET
  Fix indentation, bullets and checkboxes in the list at point.
org-priority-up		      M-x ... RET
  Increase the priority of the current item.
org-refile-copy		      M-x ... RET
  Like `org-refile', but preserve the refiled subtree.
org-speed-move-safe	      M-x ... RET
  Execute CMD, but make sure that the cursor always ends up in a
  headline.
org-table-align		      M-x ... RET
  Align the table at point by aligning all vertical bars.
outline-mark-subtree	      M-x ... RET
  Mark the current subtree in an outlined document.
popwin:messages		      M-x ... RET
  Display *Messages* buffer in a popup window.
pr-toggle-faces		      M-x ... RET
  Toggle printing with faces.
pr-toggle-spool		      M-x ... RET
  Toggle spooling.
pr-toggle-zebra		      M-x ... RET
  Toggle zebra stripes.
profiler-report		      <menu-bar> <emacs-lisp> <Profiling> <Show Profiler Report>
  Report profiling results.
project-compile		      M-x ... RET
  Run `compile' in the project root.
project-display-buffer	      M-x ... RET
  Display BUFFER-OR-NAME in some window, without selecting it.
quickrun-select		      M-x ... RET
  Run commands after selecting the backend.
read-aloud-stop		      M-x ... RET
  Ask a TTS engine to stop.
read-aloud-this		      M-x ... RET
  Pronounce either the selection or a word under the pointer.
rectangle-number-lines	      M-x ... RET
  Insert numbers in front of the region-rectangle.
rfc-mode-browse		      M-x ... RET
  Browse through all RFC documents referenced in the index.
rfc2047-decode-region	      M-x ... RET
  Decode MIME-encoded words in region between START and END.
shell-command-on-region	      <menu-bar> <tools> <shell-commands> <shell-on-region>, M-|, s-|, s-<kp-bar>
  Execute string COMMAND in inferior shell with region as input.
sis-set-english		      M-x ... RET
  Command to set input source to `english'.
studlify-buffer		      M-x ... RET
  Studlify-case the current buffer.
subword-backward-kill	      M-x ... RET
  Do the same as `backward-kill-word' but on subwords.
text-scale-pinch	      <pinch>
  Adjust the height of the default face by the scale in the pinch
  event EVENT.
thing-cut-defun		      M-x ... RET
  Cut function around point.
thing-cut-email		      M-x ... RET
  Cut email at current point.
toggle-auto-composition	      M-x ... RET
  Toggle Auto Composition mode.
upcase-initials-region	      M-x ... RET
  Upcase the initial of each word in the region.
url-cookie-undo		      M-x ... RET
  Undo deletion of a cookie.
vc-edit-next-command	      M-x ... RET
  Request editing the next VC shell command before execution.
vc-git-stash-show	      M-x ... RET
  Show the contents of stash NAME.
vc-msg-copy-all		      M-x ... RET
  Copy the content of popup into `kill-ring'.
view-hello-file		      <menu-bar> <options> <mule> <view-hello-file>, <help> h, <f1> h
  Display the HELLO file, which lists many languages and characters.
xref--xref-buffer-mode	      M-x ... RET
  Mode for displaying cross-references.
xref-go-forward		      <menu-bar> <edit> <goto> <xref-forward>, C-M-,
  Got to the point where a previous \[xref-go-back] was invoked.
xref-next-group		      M-x ... RET
  Move to the first item of the next xref group and display its
  source.
xref-prev-group		      M-x ... RET
  Move to the first item of the previous xref group and display its
  source.
yas-clear-field		      M-x ... RET
  Clears unmodified FIELD if at field start.
avy-goto-word-0-above	      M-x ... RET
  Jump to a word start between window start and point.
avy-goto-word-0-below	      M-x ... RET
  Jump to a word start between point and window end.
balance-windows-area	      M-x ... RET
  Make all visible windows the same area (approximately).
browse-url-w3-gnudoit	      M-x ... RET
  Ask another Emacs running gnuserv to load the URL using the W3
  browser.
calendar-goto-date	      M-x ... RET
  Move cursor to DATE.
cua-rectangle-mark-mode	      M-x ... RET
  Toggle the region as rectangular.
debugger-frame-clear	      M-x ... RET
  Do not enter debugger when this frame exits.
elp-instrument-package	      <menu-bar> <emacs-lisp> <Profiling> <Instrument Package...>
  Instrument for profiling, all functions which start with PREFIX.
expand-region-abbrevs	      M-x ... RET
  For each abbrev occurrence in the region, offer to expand it.
flyspell-delay-command	      M-x ... RET
  Set COMMAND to be delayed, for Flyspell.
gnus-group-list-groups	      M-x ... RET
  List newsgroups with level LEVEL or lower that have unread articles.
gnus-group-restart	      M-x ... RET
  Force Gnus to read the .newsrc file.
gnus-group-sort-groups	      M-x ... RET
  Sort the group buffer according to FUNC.
gnus-mime-delete-part	      M-x ... RET
  Delete the MIME part under point.
kill-backward-up-list	      M-x ... RET
  Kill the form containing the current sexp, leaving the sexp itself.
kmacro-edit-macro	      M-x ... RET
  As edit last keyboard macro, but without kmacro-repeat property.
magit-clone-sparse	      M-x ... RET
  Clone REPOSITORY into DIRECTORY and create a sparse checkout.
magit-fetch-refspec	      M-x ... RET
  Fetch a REFSPEC from a REMOTE.
magit-merge-squash	      M-x ... RET
  Squash commit REV into the current branch; don't create a commit.
magit-worktree-move	      M-x ... RET
  Move WORKTREE to PATH.
markdown-forward-block	      M-x ... RET
  Move forward to the next end of a Markdown block.
markdown-unused-refs	      M-x ... RET
  Show all unused Markdown references in current `markdown-mode'
  buffer.
message-goto-body-1	      M-x ... RET
  Move point to the beginning of the message body.
org-babel-exp-src-block	      M-x ... RET
  Process source block for export.
org-capture-string	      M-x ... RET
  Capture STRING with the template selected by KEYS.
org-delete-indentation	      M-x ... RET
  Join current line to previous and fix whitespace at join.
org-html-export-to-html	      M-x ... RET
  Export current buffer to a HTML file.
org-promote-subtree	      M-x ... RET
  Promote the entire subtree.
org-time-stamp-inactive	      M-x ... RET
  Insert an inactive time stamp.
origami-indicators-mode	      M-x ... RET
  Minor mode for indicators mode.
  Value: nil
outline-hide-sublevels	      M-x ... RET
  Hide everything but the top LEVELS levels of headers, in whole
  buffer.
pr-ps-directory-preview	      M-x ... RET
  Preview directory using ghostview.
replace-buffer-contents	      M-x ... RET
  Replace accessible portion of current buffer with that of SOURCE.
scroll-other-window	      ESC <next>, C-M-v, M-<next>
  Scroll next window upward LINES lines; or near full screen if no
  ARG.
set-justification-left	      M-x ... RET
  Make paragraphs in the region left-justified.
shortdoc-next-section	      M-x ... RET
  Move point to the next section.
sis-global-respect-mode	      M-x ... RET
  Respect buffer/mode by proper input source.
  Value: t
thing-copy-paragraph	      M-x ... RET
  Copy current paragraph around the point.
toggle-frame-fullscreen	      <f11>, C-s-f
  Toggle fullscreen state of FRAME.
type-break-statistics	      M-x ... RET
  Print statistics about typing breaks in a temporary buffer.
view-buffer-other-frame	      M-x ... RET
  View BUFFER in View mode in another frame.
word-search-forward-lax	      M-x ... RET
  Search forward from point for STRING, ignoring differences in
  punctuation.
apropos-local-variable	      M-x ... RET
  Show buffer-local variables that match PATTERN.
article-date-local	      M-x ... RET
  Convert the current article date to the local timezone.
calendar-forward-day	      M-x ... RET
  Move the cursor forward ARG days.
citre-create-tags-file	      M-x ... RET
  Create a new tags file.
comint-next-input	      M-x ... RET
  Cycle forwards through input history.
dired-hide-details-mode	      M-x ... RET
  Toggle visibility of detailed information in current Dired buffer.
  Value: nil
dired-other-window	      M-x ... RET
  "Edit" directory DIRNAME.  Like `dired' but select in another
  window.
dynamic-completion-mode	      M-x ... RET
  Toggle dynamic word-completion on or off.
  Value: nil
edebug-eval-expression	      M-x ... RET
  Evaluate an expression in the outside environment.
explain-pause-top-sort	      M-x ... RET
  Sort top table in the BUFFER using COLUMN, which is the 0-based
feedmail-queue-reminder	      M-x ... RET
  Perform some kind of reminder activity about queued and draft
  messages.
find-lisp-find-dired	      M-x ... RET
  Find the files within DIR whose names match REGEXP.
gnus-group-get-new-news	      M-x ... RET
  Get newly arrived articles.
gnus-group-select-group	      M-x ... RET
  Select this newsgroup.
gnus-summary-prev-page	      M-x ... RET
  Show previous page of selected article.
gnus-x-face-from-file	      M-x ... RET
  Insert an X-Face header based on an image FILE.
header-line-indent-mode	      M-x ... RET
  Mode to indent the header line in `display-line-numbers-mode'
  buffers.
  Value: nil
ibuffer-do-toggle-lock	      M-x ... RET
  Toggle locked status in marked buffers.
icalendar-import-buffer	      M-x ... RET
  Extract iCalendar events from current buffer.
icomplete-vertical-mode	      M-x ... RET
  Toggle vertical candidate display in `icomplete-mode' or
  `fido-mode'.
  Value: nil
info-complete-file	      M-x ... RET
  Perform completion on file preceding point.
kmacro-name-last-macro	      M-x ... RET
  Assign a name to the last keyboard macro defined.
magit-snapshot-index	      M-x ... RET
  Create a snapshot of the index only.
markdown-previous-link	      M-x ... RET
  Jump to previous wiki link.
message-wide-reply	      M-x ... RET
  Make a "wide" reply to the message in the current buffer.
org-latex-export-to-pdf	      M-x ... RET
  Export current buffer to LaTeX then process through to PDF.
org-shiftcontroldown	      M-x ... RET
  Change timestamps synchronously down in CLOCK log lines.
org-timestamp-up-day	      M-x ... RET
  Increase the date in the time stamp by one day.
shr-render-buffer	      M-x ... RET
  Display the HTML rendering of the current buffer.
smerge-start-session	      M-x ... RET
  Turn on `smerge-mode' and move point to first conflict marker.
temp-buffer-resize-mode	      M-x ... RET
  Toggle auto-resizing temporary buffer windows (Temp Buffer Resize
  Mode).
  Value: nil
toggle-debug-on-error	      <menu-bar> <options> <debug-on-error>
  Toggle whether to enter Lisp debugger when an error is signaled.
whitespace-newline-mode	      M-x ... RET
  Toggle newline visualization (Whitespace Newline mode).
  Value: nil
windmove-display-down	      M-x ... RET
  Display the next buffer in window below the current one.
calendar-other-month	      M-x ... RET
  Display a three-month calendar centered around MONTH and YEAR.
checkdoc-ispell-start	      M-x ... RET
  Check the style and spelling of the current buffer.
comint-previous-prompt	      M-x ... RET
  Move to end of Nth previous prompt in the buffer.
dired-mark-directories	      M-x ... RET
  Mark all directory file lines except `.' and `..'.
dired-unmark-all-files	      M-x ... RET
  Remove a specific mark (or any mark) from every file.
find-function-on-key	      M-x ... RET
  Find the function that KEY invokes.  KEY is a string.
global-tree-sitter-mode	      M-x ... RET
  Toggle Tree-Sitter mode in all buffers.
  Value: t
global-visual-line-mode	      M-x ... RET
  Toggle Visual-Line mode in all buffers.
  Value: t
gnus-group-mark-group	      M-x ... RET
  Mark the current group.
gnus-info-find-node	      M-x ... RET
  Find Info documentation of Gnus.
gnus-mime-print-part	      M-x ... RET
  Print the MIME part under point.
gnus-summary-scroll-up	      M-x ... RET
  Scroll up (or down) one line current article.
inverse-add-mode-abbrev	      M-x ... RET
  Define the word before point as a mode-specific abbreviation.
isearch-repeat-backward	      s-d
  Repeat incremental search backwards.
lazy-highlight-cleanup	      M-x ... RET
  Stop lazy highlighting and remove extra highlighting from current
  buffer.
  Value: t
magit-diff-unmerged	      M-x ... RET
  Show changes that are being merged.
magit-stash-branch	      M-x ... RET
  Create and checkout a new BRANCH from STASH.
magit-subtree-push	      M-x ... RET
  Extract the history of the subtree PREFIX and push it to REF on
  REPOSITORY.
mairix-widget-search	      M-x ... RET
  Create mairix query interactively using graphical widgets.
markdown-fill-paragraph	      M-x ... RET
  Fill paragraph at or after point.
message-send-and-exit	      M-x ... RET
  Send message like `message-send', then, if no errors, exit from mail
  buffer.
mouse-set-secondary	      M-<drag-mouse-1>
  Set the secondary selection to the text that the mouse is dragged
  over.
org-backward-sentence	      M-x ... RET
  Go to beginning of sentence, or beginning of table field.
org-fold-show-children	      M-x ... RET
  Show all direct subheadings of this heading.
org-roam-ui-follow-mode	      M-x ... RET
  Set whether ORUI should follow your every move in Emacs.
  Value: nil
org-table-insert-row	      M-x ... RET
  Insert a new row above the current line into the table.
outline-hide-subtree	      M-x ... RET
  Hide everything after this heading at deeper levels.
outline-show-subtree	      M-x ... RET
  Show everything after this heading at deeper levels.
pr-ps-file-preview	      M-x ... RET
  Preview PostScript file FILENAME.
sis-global-context-mode	      M-x ... RET
  Toggle Sis-Context mode in all buffers.
  Value: t
split-window-vertically	      M-x ... RET
  Split WINDOW-TO-SPLIT into two windows, one above the other.
sql-product-interactive	      M-x ... RET
  Run PRODUCT interpreter as an inferior process.
strokes-decode-buffer	      M-x ... RET
  Decode stroke strings in BUFFER and display their corresponding
  glyphs.
thing-copy-whitespace	      M-x ... RET
  Cut whitespace around point.
tramp-recompile-elpa	      M-x ... RET
  Recompile the installed Tramp ELPA package.
bug-reference-prog-mode	      M-x ... RET
  Like `bug-reference-mode', but only buttonize in comments and
  strings.
cancel-function-timers	      M-x ... RET
  Cancel all timers which would run FUNCTION.
comment-indent-new-line	      M-x ... RET
  Break line at point and indent, continuing comment if within one.
customize-save-variable	      M-x ... RET
  Set the default for VARIABLE to VALUE, and save it for future
  sessions.
diary-show-all-entries	      M-x ... RET
  Show all of the diary entries in the diary file.
diff-reverse-direction	      M-x ... RET
  Reverse the direction of the diffs.
dired-create-directory	      M-x ... RET
  Create a directory called DIRECTORY.
doc-view-goto-page	      M-x ... RET
  View the page given by PAGE.
ebnf-syntax-region	      M-x ... RET
  Do a syntactic analysis of a region.
edebug-set-breakpoint	      M-x ... RET
  Set the breakpoint of nearest sexp.
edebug-view-outside	      M-x ... RET
  Change to the outside window configuration.
emerge-merge-directories      M-x ... RET
  (not documented)
eww-change-select	      M-x ... RET
  Change the value of the select drop-down menu under point.
gnus-mime-save-part	      M-x ... RET
  Save the MIME part under point.
ibuffer-save-filters	      M-x ... RET
  Save FILTERS in this buffer with name NAME in
  `ibuffer-saved-filters'.
icalendar-export-region	      M-x ... RET
  Export region in diary file to iCalendar format.
ido-dired-other-frame	      M-x ... RET
  "Edit" a directory.  Like `ido-dired' but make a new frame.
indent-new-comment-line	      M-x ... RET
  Break line at point and indent, continuing comment if within one.
magit-blob-visit-file	      M-x ... RET
  View the file from the worktree corresponding to the current blob.
magit-diff-buffer-file	      M-x ... RET
  Show diff for the blob or file visited in the current buffer.
magit-ediff-show-stash	      M-x ... RET
  Show changes introduced by STASH using Ediff.
magit-remote-unshallow	      M-x ... RET
  Convert a shallow remote into a full one.
magit-stash-keep-index	      M-x ... RET
  Create a stash of the index and working tree, keeping index intact.
magit-subtree-merge	      M-x ... RET
  Merge COMMIT into the PREFIX subtree.
message-unbold-region	      M-x ... RET
  Remove all boldness (overstruck characters) in the region.
mouse-drag-right-edge	      <right-edge> <down-mouse-1>
  Drag right edge of a frame with the mouse.
mouse-kill-ring-save	      M-x ... RET
  Copy the region between point and the mouse click in the kill ring.
org-create-math-formula	      M-x ... RET
  Convert LATEX-FRAG to MathML and store it in MATHML-FILE.
org-indent-item-tree	      M-x ... RET
  Indent a local list item including its children.
org-show-hidden-entry	      M-x ... RET
  Show the body directly following its heading.
org-table-end-of-field	      M-x ... RET
  Move to the end of the current table field.
org-toggle-archive-tag	      M-x ... RET
  Toggle the archive tag for the current headline.
org-toggle-tags-groups	      M-x ... RET
  Toggle support for group tags.
project-switch-project	      M-x ... RET
  "Switch" to another project by running an Emacs command.
rmail-sort-by-subject	      M-x ... RET
  Sort messages of current Rmail buffer by "Subject" header.
rmail-summary-by-topic	      M-x ... RET
  Display a summary of all messages with the given SUBJECT.
save-buffers-kill-emacs	      s-q
  Offer to save each buffer, then kill this Emacs process.
set-justification-full	      M-x ... RET
  Make paragraphs in the region fully justified.
tab-bar-duplicate-tab	      M-x ... RET
  Clone the current tab to ARG positions to the right.
tab-switcher-unmark	      M-x ... RET
  Cancel requested operations on window configuration on this line and
  move down.
table-unrecognize-region      M-x ... RET
  (not documented)
text-scale-decrease	      M-x ... RET
  Decrease the font size of the default face in the current buffer by
  DEC steps.
tree-sitter-query-mode	      M-x ... RET
  Major mode for building tree-sitter queries and testing them live.
windmove-display-left	      M-x ... RET
  Display the next buffer in window to the left of the current one.
Info-search-backward	      M-x ... RET
  Search for REGEXP in the reverse direction.
browse-url-button-open	      M-x ... RET
  Follow the link under point using `browse-url'.
byte-force-recompile	      M-x ... RET
  Recompile every `.el' file in DIRECTORY that already has a `.elc'
  file.
calendar-list-holidays	      M-x ... RET
  Create a buffer containing the holidays for the current calendar
  window.
checkdoc-ispell-defun	      M-x ... RET
  Check the style and spelling of the current defun with Ispell.
default-indent-new-line	      M-j, C-M-j
  Break line at point and indent.
diff-next-complex-hunk	      M-x ... RET
  Jump to the next "complex" hunk.
dired-mark-files-regexp	      M-x ... RET
  Mark all files matching REGEXP for use in later commands.
dired-toggle-read-only	      M-x ... RET
  Edit Dired buffer with Wdired, or make it read-only.
feedmail-run-the-queue	      M-x ... RET
  Visit each message in the feedmail queue directory and send it out.
gnus-agent-regenerate	      M-x ... RET
  Regenerate all agent covered files.
gnus-article-next-page	      M-x ... RET
  Show the next page of the current article.
helpful-kill-buffers	      M-x ... RET
  Kill all `helpful-mode' buffers.
ibuffer-filter-disable	      M-x ... RET
  Disable all filters currently in effect in this buffer.
ilog-show-in-new-frame	      M-x ... RET
  Display log in a pop up frame.
image-mode-unmark-file	      M-x ... RET
  Unmark the current file in the appropriate Dired buffer(s).
magit-file-checkout	      M-x ... RET
  Checkout FILE from REV.
magit-revert-no-commit	      M-x ... RET
  Revert COMMIT by applying it in reverse to the worktree.
magit-stash-clear	      M-x ... RET
  Remove all stashes saved in REF's reflog by deleting REF.
markdown-other-window	      M-x ... RET
  Run `markdown-command' on current buffer and display in other
  window.
message-fill-paragraph	      M-x ... RET
  Message specific function to fill a paragraph.
mouse-start-secondary	      M-<mouse-1>
  Set one end of the secondary selection to the position clicked on.
network-connection	      M-x ... RET
  Open a network connection to HOST on PORT.
occur-mode-mouse-goto	      M-x ... RET
  Go to the occurrence specified by EVENT, a mouse click.
org-cycle-agenda-files	      M-x ... RET
  Cycle through the files in `org-agenda-files'.
org-element-cache-reset	      M-x ... RET
  Reset cache in current buffer.
org-insert-subheading	      M-x ... RET
  Insert a new subheading and demote it.
org-table-cut-region	      M-x ... RET
  Copy region in table to the clipboard and blank all relevant fields.
origami-previous-fold	      M-x ... RET
  Move point to the beginning of the fold before POINT.
quickrun-with-arg	      M-x ... RET
  Run commands quickly for current buffer with arguments.
select-tags-table-mode	      M-x ... RET
  Major mode for choosing a current tags table among those already
  loaded.
tabulated-list-revert	      M-x ... RET
  The `revert-buffer-function' for `tabulated-list-mode'.
thing-replace-internal	      M-x ... RET
  A fast replace complexes object.
y-or-n-p-insert-other	      M-x ... RET
  Handle inserting of other answers in the minibuffer of `y-or-n-p'.
Buffer-menu-bury	      M-x ... RET
  Bury the buffer listed on this line.
Buffer-menu-view	      M-x ... RET
  View this line's buffer in View mode.
Custom-mode-menu	      M-x ... RET
  Menu used in customization buffers.
  Value: (keymap "Custom" (Customize menu-item "Customize" nil :filter #[257 "\302\30…
Info-on-current-buffer	      M-x ... RET
  Use Info mode to browse the current Info buffer.
Info-search-next	      M-x ... RET
  Search for next regexp from a previous `Info-search' command.
acm-select-first	      M-x ... RET
  Select first candidate.
add-file-local-variable	      M-x ... RET
  Add file-local VARIABLE with its VALUE to the Local Variables list.
amread-mode-quit	      M-x ... RET
  Disable `amread-mode'.
applescript-mode	      M-x ... RET
  Major mode for editing AppleScript files.
bibtex-PhdThesis	      M-x ... RET
  Insert a template for a @PhdThesis entry; see also `bibtex-entry'.
bibtex-Reference	      M-x ... RET
  Insert a template for a @Reference entry; see also `bibtex-entry'.
bibtex-edit-menu	      M-x ... RET
  BibTeX-Edit Menu in BibTeX mode.
  Value: (keymap "BibTeX-Edit" (Moving\ inside\ an\ Entry menu-item "Moving inside an…
bibtex-style-mode	      M-x ... RET
  Major mode for editing BibTeX style files.
bln-forward-half	      M-x ... RET
  This function is used in combination with `bln-backward-half' to
  provide binary line navigation (see `bln-mode').
browse-kill-ring	      M-x ... RET
  Display items in the `kill-ring' in another buffer.
browse-url-webpositive	      M-x ... RET
  Ask the WebPositive WWW browser to load URL.
cal-tex-cursor-week-iso	      M-x ... RET
  Make a one page LaTeX calendar for one week, in the ISO-style.
calendar-lunar-phases	      M-x ... RET
  Create a buffer with the lunar phases for the current calendar
  window.
calendar-mark-holidays	      M-x ... RET
  Mark notable days in the calendar window.
citre-peek-abort	      M-x ... RET
  Abort peeking.
citre-peek-save-session	      M-x ... RET
  Save current peek session.
conf-space-keywords	      M-x ... RET
  Enter Conf Space mode using regexp KEYWORDS to match the keywords.
corfu-quick-jump	      M-x ... RET
  Jump to candidate using quick keys.
count-lines-page	      M-x ... RET
  Report number of lines on current page, and how many are before or
  after point.
decode-hz-buffer	      M-x ... RET
  Decode HZ/ZW encoded text in the current buffer.
describe-copying	      <menu-bar> <help-menu> <describe-copying>, <help> C-c, <f1> C-c
  Display info on how you may redistribute copies of GNU Emacs.
dictionary-tooltip-mode	      M-x ... RET
  Display tooltips for the current word.
diff-buffer-with-file	      M-x ... RET
  View the differences between BUFFER and its associated file.
diff-ediff-patch	      M-x ... RET
  Call `ediff-patch-file' on the current buffer.
diff-refine-hunk	      M-x ... RET
  Highlight changes of hunk at point at a finer granularity.
dired-async-do-copy	      M-x ... RET
  Run ‘dired-do-copy’ asynchronously.
dired-do-isearch	      M-x ... RET
  Search for a string through all marked files using Isearch.
edebug-all-forms	      M-x ... RET
  Toggle edebugging of all forms.
  Value: nil
edebug-goto-here	      M-x ... RET
  Proceed to first stop-point at or after current position of point.
edebug-next-mode	      M-x ... RET
  Proceed to next `after' stop point.
edebug-step-mode	      M-x ... RET
  Proceed to next stop point.
edt-emulation-on	      M-x ... RET
  Turn on EDT Emulation.
elpl-electric-backquote	      M-x ... RET
  Insert backquote.
encode-hz-buffer	      M-x ... RET
  Encode the text in the current buffer to HZ.
eww-add-bookmark	      M-x ... RET
  Bookmark the current page.
eww-list-buffers	      M-x ... RET
  Enlist eww buffers.
eww-toggle-fonts	      M-x ... RET
  Toggle whether to use monospaced or font-enabled layouts.
exchange-point-and-mark	      s-j
  Put the mark where point is now, and point where the mark is now.
explain-pause-top	      M-x ... RET
  Show a top-like report of commands recently ran and their runtime.
  Returns
ff-mouse-find-other-file      M-x ... RET
  Find the header or source file corresponding to this file.
fill-region-as-paragraph      M-x ... RET
  Fill the region as if it were a single paragraph.
find-dired-with-command	      M-x ... RET
  Run `find' and go into Dired mode on a buffer of the output.
first-completion	      M-x ... RET
  Move to the first item in the completion list.
follow-scroll-up-window	      M-x ... RET
  Scroll text in a Follow mode window up by that window's size.
font-lock-fontify-block	      M-x ... RET
  Fontify some lines the way `font-lock-fontify-buffer' would.
git-gutter:clear	      M-x ... RET
  Clear diff information in gutter.
gnus-agent-batch	      M-x ... RET
  Start Gnus, send queue and fetch session.
gnus-button-next-page	      M-x ... RET
  Go to the next page.
gnus-button-prev-page	      M-x ... RET
  Go to the prev page.
gnus-group-delete-group	      M-x ... RET
  Delete the current group.  Only meaningful with editable groups.
gnus-group-list-dormant	      M-x ... RET
  List all groups with dormant articles.
gnus-group-list-killed	      M-x ... RET
  List all killed newsgroups in the group buffer.
gnus-group-save-newsrc	      M-x ... RET
  Save the Gnus startup files.
gnus-mime-inline-part	      M-x ... RET
  Insert the MIME part under point into the current buffer.
gnus-start-date-timer	      M-x ... RET
  Start a timer to update the Date headers in the article buffers.
gnus-summary-browse-url	      M-x ... RET
  Scan the current article body for links, and offer to browse them.
grugru-edit-load	      M-x ... RET
  Load edited grugru saved on `grugru-edit-save-file'.
help-view-source	      M-x ... RET
  View the source of the current help item.
helpful-at-point	      M-x ... RET
  Show help for the symbol at point.
ibuffer-unmark-all	      M-x ... RET
  Unmark all buffers with mark MARK.
keyfreq-save-now	      M-x ... RET
  Save keyfreq data now.
kill-all-abbrevs	      M-x ... RET
  Undefine all defined abbrevs.
kill-compilation	      M-x ... RET
  Kill the process made by the \[compile] or \[grep] commands.
kmacro-redisplay	      M-x ... RET
  Force redisplay during keyboard macro execution.
kmacro-swap-ring	      M-x ... RET
  Swap first two elements on keyboard macro ring.
magit-ediff-show-staged	      M-x ... RET
  Show staged changes using Ediff.
magit-run-git-gui-blame	      M-x ... RET
  Run `git gui blame' on the given FILENAME and COMMIT.
magit-stash-list	      M-x ... RET
  List all stashes in a buffer.
magit-subtree-split	      M-x ... RET
  Extract the history of the subtree PREFIX.
mairix-update-database	      M-x ... RET
  Call mairix for updating the database for SERVERS.
marginalia-cycle	      C-M-a
  Cycle between annotators in `marginalia-annotator-registry'.
markdown-up-list	      M-x ... RET
  Move point to beginning of parent list item.
message-goto-bcc	      M-x ... RET
  Move point to the Bcc  header.
message-goto-fcc	      M-x ... RET
  Move point to the Fcc header.
my/alfred-search	      M-x ... RET
  Activate Alfred with selected text.
ns-popup-color-panel	      s-C
  Pop up the color panel.
occur-cease-edit	      M-x ... RET
  Switch from Occur Edit mode to Occur mode.
org-clock-cancel	      M-x ... RET
  Cancel the running clock by removing the start timestamp.
org-down-element	      M-x ... RET
  Move to inner element.
org-edit-src-continue	      M-x ... RET
  Unconditionally return to buffer editing area under point.
org-find-entry-with-id	      M-x ... RET
  Locate the entry that contains the ID property with exact value
  IDENT.
org-indent-block	      M-x ... RET
  Indent the block at point.
org-mode-restart	      M-x ... RET
  Restart `org-mode'.
org-roam-ui-open	      M-x ... RET
  Ensure `org-roam-ui' is running, then open the `org-roam-ui'
  webpage.
org-show-subtree	      M-x ... RET
  Show everything after this heading at deeper levels.
org-table-convert-region      M-x ... RET
  Convert region to a table.
org-table-move-row	      M-x ... RET
  Move the current table line down.  With arg UP, move it up.
orgtbl-mode-menu	      M-x ... RET
  OrgTbl menu.
  Value: (keymap "OrgTbl" (Create\ or\ convert menu-item "Create or convert" org-tabl…
origami-show-node	      M-x ... RET
  Like `origami-open-node' but also opens parent fold nodes
  recursively so as to ensure the position where POINT is is visible.
outline-show-all	      M-x ... RET
  Show all of the text in the buffer.
popwin:find-file	      M-x ... RET
  Edit file FILENAME with popup window by `popwin:popup-buffer'.
pr-toggle-duplex	      M-x ... RET
  Toggle duplex.
pr-toggle-header	      M-x ... RET
  Toggle printing header.
pr-toggle-region	      M-x ... RET
  Toggle whether the region is automagically detected.
project-find-dir	      M-x ... RET
  Start Dired in a directory inside the current project.
reveal-in-folder	      M-x ... RET
  Reveal buffer/path depends on cursor condition.
scheme-mode-menu	      M-x ... RET
  Menu for Scheme mode.
  Value: (keymap "Scheme" (Indent\ Line menu-item "Indent Line" lisp-indent-line) (In…
shr-insert-image	      M-x ... RET
  Insert the image under point into the buffer.
shr-probe-and-copy-url	      M-x ... RET
  Copy the URL under point to the kill ring.
tab-bar-history-back	      M-x ... RET
  Restore a previous window configuration used in the current tab.
  Value: #s(hash-table size 65 test eql rehash-size 1.5 rehash-threshold 0.8125 data …
tab-bar-mouse-move-tab	      M-x ... RET
  Move a tab to a different position on the tab bar.
thing-cut-number	      M-x ... RET
  Cut number at point.
thing-cut-symbol	      M-x ... RET
  Cut symbol around point.
tldr-update-docs	      M-x ... RET
  Get or update the TLDR docs from source.
turn-on-rxt-mode	      M-x ... RET
  Turn on `rxt-mode' in the current buffer.
vc-git-stash-delete	      M-x ... RET
  Delete stash NAME.
whitespace-report-region      M-x ... RET
  Report some whitespace problems in a region.
widget-kill-line	      M-x ... RET
  Kill to end of field or end of line, whichever is first.
xeft-create-note	      M-x ... RET
  Create a new note with the current search phrase as the title.
yas-exit-snippet	      M-x ... RET
  Goto exit-marker of SNIPPET.
yas/exit-snippet	      M-x ... RET
  Goto exit-marker of SNIPPET.
Custom-buffer-done	      M-x ... RET
  Exit current Custom buffer according to `custom-buffer-done-kill'.
article-hide-signature	      M-x ... RET
  Hide the signature in the current article.
avy-goto-symbol-1-above	      M-x ... RET
  Jump to the currently visible CHAR at a symbol start.
avy-goto-symbol-1-below	      M-x ... RET
  Jump to the currently visible CHAR at a symbol start.
avy-goto-whitespace-end	      M-x ... RET
  Jump to the end of a whitespace sequence.
browse-url-of-region	      M-x ... RET
  Use a web browser to display the current region.
calc-grab-sum-down	      M-x ... RET
  Parse a rectangle as a matrix of numbers and sum its columns.
calendar-backward-day	      M-x ... RET
  Move the cursor back ARG days.
cancel-edebug-on-entry	      M-x ... RET
  Cause Edebug to not stop when FUNCTION is called.
customize-variable	      M-x ... RET
  Customize SYMBOL, which must be a user option.
dired-create-empty-file	      M-x ... RET
  Create an empty file called FILE.
edebug-cancel-on-entry	      M-x ... RET
  Cause Edebug to not stop when FUNCTION is called.
explain-pause-top-clear	      M-x ... RET
  Clear the statistics in an `explain-pause-top-mode' buffer, BUFFER.
global-text-scale-adjust      M-x ... RET
  Change (a.k.a. "adjust") the font size of all faces by INCREMENT.
gnus-article-date-ut	      M-x ... RET
  Run `article-date-ut' in the article buffer.
gnus-summary-pipe-output      M-x ... RET
  Pipe the current article to a subprocess.
image-toggle-animation	      M-x ... RET
  Start or stop animating the current image.
isearch-backward-regexp	      <menu-bar> <edit> <i-search> <isearch-backward-regexp>, M-s-F, C-M-r
  Do incremental search backward for regular expression.
isearch-toggle-symbol	      M-x ... RET
  Toggle symbol searching on or off.
iso-transl-set-language	      M-x ... RET
  Set shorter key bindings for some characters relevant for LANG.
lsp-bridge-ref-popup	      M-x ... RET
  Rerun rg with customized arguments. This function will give
magit-branch-create	      M-x ... RET
  Create BRANCH at branch or revision START-POINT.
magit-copy-section-value      M-x ... RET
  Save the value of the current section for later use.
magit-diff-unstaged	      M-x ... RET
  Show changes between the working tree and the index.
magit-pop-revision-stack      M-x ... RET
  Insert a representation of a revision into the current buffer.
magit-push-current	      M-x ... RET
  Push the current branch to a branch read in the minibuffer.
magit-show-refs-head	      M-x ... RET
  List and compare references in a dedicated buffer.
map-query-replace-regexp      M-x ... RET
  Replace some matches for REGEXP with various strings, in rotation.
org-beamer-export-to-pdf      M-x ... RET
  Export current buffer as a Beamer presentation (PDF).
org-evaluate-time-range	      M-x ... RET
  Evaluate a time range by computing the difference between start and
  end.
org-todo-yesterday	      M-x ... RET
  Like `org-todo' but the time of change will be 23:59 of yesterday.
recentf-cancel-dialog	      M-x ... RET
  Cancel the current dialog.
timeclock-reread-log	      M-x ... RET
  Re-read the timeclock, to account for external changes.
view-buffer-other-window      M-x ... RET
  View BUFFER in View mode in another window.
word-search-backward-lax      M-x ... RET
  Search backward from point for STRING, ignoring differences in
  punctuation.
backward-kill-sentence	      M-x ... RET
  Kill back from point to start of sentence.
base64url-encode-region	      M-x ... RET
  Base64url-encode the region between BEG and END.
byte-recompile-directory      <menu-bar> <emacs-lisp> <Byte-recompile Directory...>
  Recompile every `.el' file in DIRECTORY that needs recompilation.
c-put-char-property-fun	      M-x ... RET
  Do nothing and return nil.
calendar-forward-week	      M-x ... RET
  Move the cursor forward ARG weeks.
calendar-forward-year	      M-x ... RET
  Move the cursor forward by ARG years.
debugger-toggle-locals	      M-x ... RET
  Toggle the display of local variables for the backtrace frame at
  point.
dired-do-flagged-delete	      M-x ... RET
  In Dired, delete the files flagged for deletion.
dired-prev-dirline	      M-x ... RET
  Goto ARGth previous directory file line.
edit-last-kbd-macro	      M-x ... RET
  Edit the most recently defined keyboard macro.
electric-pair-local-mode      M-x ... RET
  Toggle `electric-pair-mode' only in this buffer.
emacsql-show-last-sql	      M-x ... RET
  Display the compiled SQL of the s-expression SQL expression before
  point.
epa-mail-import-keys	      M-x ... RET
  Import keys in the OpenPGP armor format in the current buffer.
gnus-summary-pop-limit	      M-x ... RET
  Restore the previous limit.
gnus-treat-from-picon	      M-x ... RET
  Display picons in the From header.
  Value: nil
hi-lock-line-face-buffer      M-x ... RET
  Highlight all lines that match REGEXP using FACE.
ialign-interactive-align      M-x ... RET
  Interactively align region BEG END using regexp read from
  minibuffer.
list-charset-chars	      M-x ... RET
  Display a list of characters in character set CHARSET.
magit-push-notes-ref	      M-x ... RET
  Push a notes ref to another repository.
magit-remote-prune	      M-x ... RET
  Remove stale remote-tracking branches for REMOTE.
magit-revert-and-commit	      M-x ... RET
  Revert COMMIT by creating a new commit.
magit-show-refs-other	      M-x ... RET
  List and compare references in a dedicated buffer.
markdown-backward-block	      M-x ... RET
  Move the point to the start of the current Markdown block.
markdown-indent-region	      M-x ... RET
  Indent the region from BEG to END using some heuristics.
markdown-mark-paragraph	      M-x ... RET
  Put mark at end of this block, point at beginning.
org-insert-link-global	      M-x ... RET
  Insert a link like Org mode does.
org-insert-todo-heading	      M-x ... RET
  Insert a new heading with the same level and TODO state as current
  heading.
org-roam-ui-node-local	      M-x ... RET
  Open the local graph view of the current node.
pr-ps-directory-ps-print      M-x ... RET
  Print directory using PostScript printer or through ghostscript.
search-unencodable-char	      M-x ... RET
  Search forward from point for a character that is not encodable.
shortdoc-display-group	      <menu-bar> <help-menu> <describe> <shortdoc-display-group>
  Pop to a buffer with short documentation summary for functions in
  GROUP.
split-root-window-below	      M-x ... RET
  Split root window of current frame in two.
tab-bar-mouse-close-tab	      M-x ... RET
  Close the tab you click on.
translate-reference-mode      M-x ... RET
  Minor mode for artcle referring buffer.
yas-load-snippet-buffer	      M-x ... RET
  Parse and load current buffer's snippet definition into TABLE.
Info-follow-nearest-node      M-x ... RET
  Follow a node reference near point.
backward-kill-paragraph	      M-x ... RET
  Kill back to start of paragraph.
checkdoc-message-text	      M-x ... RET
  Scan the buffer for occurrences of the error function, and verify
  text.
citre-jump-to-reference	      M-x ... RET
  Jump to the definition of the symbol at point.
comint-truncate-buffer	      M-x ... RET
  Truncate the buffer to `comint-buffer-maximum-size'.
compilation-next-file	      M-x ... RET
  Move point to the next error for a different file than the current
  one.
debug-on-variable-change      M-x ... RET
  Trigger a debugger invocation when VARIABLE is changed.
emacs-index-search	      <menu-bar> <help-menu> <search-documentation> <lookup-subject-in-emacs-manual>
  Look up TOPIC in the indices of the Emacs User Manual.
global-dash-fontify-mode      M-x ... RET
  Toggle Dash-Fontify mode in all buffers.
  Value: nil
global-goto-address-mode      M-x ... RET
  Toggle Goto-Address mode in all buffers.
  Value: nil
gnus-article-prev-page	      M-x ... RET
  Show previous page of current article.
gnus-cloud-upload-data	      M-x ... RET
  Upload data (newsrc and files) to the Gnus Cloud.
gnus-find-new-newsgroups      M-x ... RET
  Search for new newsgroups and add them.
gnus-group-mark-buffer	      M-x ... RET
  Mark all groups in the buffer.
gnus-group-split-update	      M-x ... RET
  Computes `nnmail-split-fancy' from group params and CATCH-ALL.
gnus-update-format	      M-x ... RET
  Update the format specification near point.
image-dired-delete-tag	      M-x ... RET
  Remove tag for selected file(s).
isearch-fold-quotes-mode      M-x ... RET
  Minor mode to aid searching for \=` characters in help modes.
  Value: nil
isearch-printing-char	      M-x ... RET
  Add this ordinary printing CHAR to the search string and search.
keycast-header-line-mode      M-x ... RET
  Show current command and its key binding in the header line.
  Value: nil
kmacro-step-edit-macro	      M-x ... RET
  Step edit and execute last keyboard macro.
magit-ediff-resolve-all	      M-x ... RET
  Resolve all conflicts in the FILE at point using Ediff.
magit-sequencer-abort	      M-x ... RET
  Abort the current cherry-pick or revert sequence.
magit-snapshot-worktree	      M-x ... RET
  Create a snapshot of unstaged changes in the working tree.
mail-add-payment-async	      M-x ... RET
  Add X-Payment: and X-Hashcash: headers with a hashcash payment
mailcap-parse-mimetypes	      M-x ... RET
  Parse out all the mimetypes specified in a Unix-style path string
  PATH.
markdown-forward-page	      M-x ... RET
  Move forward to boundary of the current toplevel section.
ns-insert-working-text	      M-x ... RET
  Insert contents of `ns-working-text' as UTF-8 string and mark with
org-babel-hash-at-point	      M-x ... RET
  Return the value of the hash at POINT.
org-display-outline-path      M-x ... RET
  Display the current outline path in the echo area.
org-end-of-item-list	      M-x ... RET
  Go to the end of the current list or sublist.
org-fold-hide-sublevels	      M-x ... RET
  Hide everything but the top LEVELS levels of headers, in whole
  buffer.
origami-auto-global-mode      M-x ... RET
  Apply initial folding when finding (opening) a file buffer
  Value: nil
pr-ps-file-up-preview	      M-x ... RET
  Preview PostScript file FILENAME.
project-forget-project	      M-x ... RET
  Remove directory PROJECT-ROOT from the project list.
remember-other-frame	      M-x ... RET
  Call `remember' in another frame.
spam-report-deagentize	      M-x ... RET
  Remove spam-report support from the Agent.
tab-switcher-next-line	      M-x ... RET
  Move to ARGth next line in the list of tabs.
thing-copy-parentheses	      M-x ... RET
  Copy content in match parentheses.
timeclock-status-string	      M-x ... RET
  Report the overall timeclock status at the present moment.
tramp-cleanup-connection      M-x ... RET
  Flush all connection related objects.
tramp-rename-these-files      M-x ... RET
  Replace visiting file names to TARGET.
unforward-rmail-message	      M-x ... RET
  Extract a forwarded message from the containing message.
widget-beginning-of-line      M-x ... RET
  Move point to beginning of current line (in the logical order).
article-date-lapsed	      M-x ... RET
  Convert the current article date to time lapsed since it was sent.
article-decode-charset	      M-x ... RET
  Decode charset-encoded text in the article.
copy-from-above-command	      M-x ... RET
  Copy characters from previous nonblank line, starting just above
  point.
dired-flag-backup-files	      M-x ... RET
  Flag all backup files (names ending with `~') for deletion.
doc-view-reconvert-doc	      M-x ... RET
  Reconvert the current document.
edebug-Go-nonstop-mode	      M-x ... RET
  Go, evaluating without debugging.
edebug-Trace-fast-mode	      M-x ... RET
  Trace with no wait at each step.
ediff-merge-directories	      <menu-bar> <tools> <ediff-merge> <ediff-merge-directories>
  Run Ediff on a pair of directories, DIR1 and DIR2, merging files
  that have
edmacro-insert-key	      M-x ... RET
  Insert the written name of a KEY in the buffer.
file-cache-add-file	      M-x ... RET
  Add FILE to the file cache.
flyspell-goto-next-error      M-x ... RET
  Go to the next error.
gnus-article-view-part	      M-x ... RET
  View MIME part N, which is the numerical prefix.
gnus-group-compact-group      M-x ... RET
  Compact the current group.
gnus-sticky-article-mode      M-x ... RET
  Mode for sticky articles.
gnus-summary-next-group	      M-x ... RET
  Exit current newsgroup and then select next unread newsgroup.
gnus-treat-mail-picon	      M-x ... RET
  Display picons in the Cc and To headers.
  Value: nil
indent-according-to-mode      M-x ... RET
  Indent line in proper way for current major mode.
isearch-yank-until-char	      M-x ... RET
  Pull everything until next instance of CHAR from buffer into search
  string.
japanese-hankaku-region	      M-x ... RET
  Convert Japanese `zenkaku' chars in the region to `hankaku' chars.
japanese-zenkaku-region	      M-x ... RET
  Convert hankaku' chars in the region to Japanese `zenkaku' chars.
kmacro-cycle-ring-next	      M-x ... RET
  Move to next keyboard macro in keyboard macro ring.
kmacro-view-macro-repeat      M-x ... RET
  Display the last keyboard macro.
magit-reflog-other	      M-x ... RET
  Display the reflog of a branch or another ref.
magit-remote-remove	      M-x ... RET
  Delete the remote named REMOTE.
magit-remote-rename	      M-x ... RET
  Rename the remote named OLD to NEW.
markdown-remove-header	      M-x ... RET
  Remove header markup if point is at a header.
minibuffer-inactive-mode      M-x ... RET
  Major mode to use in the minibuffer when it is not active.
mouse-drag-bottom-edge	      <bottom-edge> <down-mouse-1>
  Drag bottom edge of a frame with the mouse.
mpv-speed-decrease	      M-x ... RET
  Decrease playback speed by STEPS factors of `mpv-speed-step'.
mpv-speed-increase	      M-x ... RET
  Increase playback speed by STEPS factors of `mpv-speed-step'.
org-delete-property	      M-x ... RET
  In the current entry, delete PROPERTY.
org-outdent-item-tree	      M-x ... RET
  Outdent a local list item including its children.
org-table-next-field	      M-x ... RET
  Go to the next field in the current table, creating new lines as
  needed.
scroll-bar-scroll-up	      M-x ... RET
  Scroll the line next to the scroll bar click to the top of the
  window.
set-justification-right	      M-x ... RET
  Make paragraphs in the region right-justified.
set-language-environment      M-x ... RET
  Set up multilingual environment for using LANGUAGE-NAME.
split-root-window-right	      M-x ... RET
  Split root window of current frame into two side-by-side windows.
string-insert-rectangle	      M-x ... RET
  Insert STRING on each line of region-rectangle, shifting text right.
tab-bar-change-tab-group      M-x ... RET
  Add the tab specified by its absolute position TAB-NUMBER to
  GROUP-NAME.
tab-switcher-prev-line	      M-x ... RET
  Move to ARGth previous line in the list of tabs.
windmove-display-right	      M-x ... RET
  Display the next buffer in window to the right of the current one.
yas-compile-directory	      M-x ... RET
  Create .yas-compiled-snippets.el files under subdirs of
  TOP-LEVEL-DIR.
yas/compile-directory	      M-x ... RET
  Create .yas-compiled-snippets.el files under subdirs of
  TOP-LEVEL-DIR.
beginning-of-visual-line      s-<left>, C-a
  Move point to beginning of current visual line.
debugger-step-through	      M-x ... RET
  Proceed, stepping through subexpressions of this expression.
elisp-index-search	      <menu-bar> <help-menu> <search-documentation> <lookup-subject-in-elisp-manual>
  Look up TOPIC in the indices of the Emacs Lisp Reference Manual.
flyspell-correct-word	      M-x ... RET
  Pop up a menu of possible corrections for a misspelled word.
git-gutter:popup-diff	      M-x ... RET
  Popup current diff hunk.
git-gutter:popup-hunk	      M-x ... RET
  Popup current diff hunk.
gnus-child-unplugged	      M-x ... RET
  Read news as a child unplugged.
gnus-group-clear-data	      M-x ... RET
  Clear all marks and read ranges from the current group.
gnus-group-rename-group	      M-x ... RET
  Rename group from GROUP to NEW-NAME.
gnus-group-subscribe	      M-x ... RET
  Subscribe the current group.
gnus-slave-unplugged	      M-x ... RET
  Read news as a child unplugged.
ibuffer-diff-with-file	      M-x ... RET
  View the differences between marked buffers and their associated
  files.
ibuffer-unmark-forward	      M-x ... RET
  Unmark the buffers in the region, or ARG buffers.
ido-dired-other-window	      M-x ... RET
  "Edit" a directory.  Like `ido-dired' but select in another window.
image-forward-hscroll	      M-x ... RET
  Scroll image in current window to the left by N character widths.
kmacro-end-call-mouse	      S-<mouse-3>
  Move point to the position clicked with the mouse and call last kbd
  macro.
magit-branch-or-checkout      M-x ... RET
  Hybrid between `magit-checkout' and `magit-branch-and-checkout'.
magit-list-repositories	      M-x ... RET
  Display a list of repositories.
merge-mail-abbrevs	      M-x ... RET
  Merge mail aliases from the given file with existing ones.
minibuffer-keyboard-quit      M-x ... RET
  Abort recursive edit.
mouse-drag-vertical-line      <vertical-line> <down-mouse-1>, <right-divider> <down-mouse-1>
  This function has :override advice: ‘ignore’.
org-publish-current-file      M-x ... RET
  Publish the current file.
pr-ps-region-ps-print	      M-x ... RET
  Print region using PostScript printer or through ghostscript.
rmail-summary-by-regexp	      M-x ... RET
  Display a summary of all messages according to regexp REGEXP.
set-face-inverse-video-p      M-x ... RET
  Specify whether face FACE is in inverse video.
typescript-ts-mode	      M-x ... RET
  Major mode for editing TypeScript.
whitespace-toggle-options     M-x ... RET
  Toggle local `whitespace-mode' options.
Info-history-back	      M-x ... RET
  Go back in the history to the last node visited.
acm-insert-common	      M-x ... RET
  Insert common prefix of menu.
ad-deactivate-all	      M-x ... RET
  Deactivate all currently advised functions.
amread-speed-down	      M-x ... RET
  Speed down `amread-mode'.
anything-quickrun	      M-x ... RET
  Run quickrun with `anything'.
article-date-iso8601	      M-x ... RET
  Convert the current article date to ISO8601.
article-decode-HZ	      M-x ... RET
  Translate a HZ-encoded article.
article-display-face	      M-x ... RET
  Display any Face headers in the header.
article-remove-cr	      M-x ... RET
  Remove trailing CRs and then translate remaining CRs into LFs.
article-treat-dumbquotes      M-x ... RET
  Translate "Microsoft smartquotes" and other symbols into proper
  text.
article-wash-html	      M-x ... RET
  Format an HTML article.
bibtex-BookInBook	      M-x ... RET
  Insert a template for a @BookInBook entry; see also `bibtex-entry'.
bibtex-Collection	      M-x ... RET
  Insert a template for a @Collection entry; see also `bibtex-entry'.
bibtex-Conference	      M-x ... RET
  Insert a template for a @Conference entry; see also `bibtex-entry'.
bibtex-Periodical	      M-x ... RET
  Insert a template for a @Periodical entry; see also `bibtex-entry'.
bibtex-TechReport	      M-x ... RET
  Insert a template for a @TechReport entry; see also `bibtex-entry'.
bln-backward-half	      M-x ... RET
  This function is used in combination with `bln-forward-half' to
  provide binary line navigation (see `bln-mode').
bookmark-set-no-overwrite     M-x ... RET
  Set a bookmark named NAME at the current location.
buffer-menu-other-window      M-x ... RET
  Display the Buffer Menu in another window.
canonically-space-region      M-x ... RET
  Remove extra spaces between words in region.
comint-kill-input	      M-x ... RET
  Kill all text from last stuff output by interpreter to point.
cua-selection-mode	      M-x ... RET
  Enable CUA selection mode without the C-z/C-x/C-c/C-v bindings.
customize-unsaved	      M-x ... RET
  Customize all options and faces set in this session but not saved.
debugger-continue	      M-x ... RET
  Continue, evaluating this expression without stopping.
diary-bahai-insert-entry      M-x ... RET
  Insert a diary entry.
dired-flag-file-deletion      M-x ... RET
  In Dired, flag the current line's file for deletion.
doc-view--text-view-mode      M-x ... RET
  View mode used in DocView's text buffers.
ebrowse-save-tree	      M-x ... RET
  Save current tree in same file it was loaded from.
edebug-all-defuns	      M-x ... RET
  Toggle edebugging of all definitions.
elisp--refs-visit-match	      M-x ... RET
  Go to the search result at point.
epa-dired-do-sign	      M-x ... RET
  Sign marked files.
erc-server-select	      M-x ... RET
  Interactively select a server to connect to using
  `erc-server-alist'.
eudc-edit-hotlist	      M-x ... RET
  Edit the hotlist of directory servers in a specialized buffer.
eww-bookmark-kill	      M-x ... RET
  Kill the current bookmark.
eww-bookmark-yank	      M-x ... RET
  Yank a previously killed bookmark to the current line.
eww-buffer-select	      M-x ... RET
  Switch to eww buffer.
eww-copy-page-url	      M-x ... RET
  Copy the URL of the current page into the kill ring.
eww-next-bookmark	      M-x ... RET
  Go to the next bookmark in the list.
eww-toggle-colors	      M-x ... RET
  Toggle whether to use HTML-specified colors or not.
eww-toggle-images	      M-x ... RET
  Toggle whether or not to display images.
fileloop-continue	      <menu-bar> <edit> <replace> <tags-repl-continue>, <menu-bar> <edit> <search> <tags-continue>
  Continue last multi-file operation.
finder-by-keyword	      <menu-bar> <help-menu> <find-emacs-packages>, <help> p, <f1> p
  Find packages matching a given keyword.
git-gutter:toggle	      M-x ... RET
  Toggle to show diff information.
gnus-bookmark-set	      M-x ... RET
  Set a bookmark for this article.
gnus-group-jump-to-group      M-x ... RET
  Jump to newsgroup GROUP.
gnus-group-list-matching      M-x ... RET
  List all groups with unread articles that match REGEXP.
gnus-list-debbugs	      M-x ... RET
  List all open Gnus bug reports.
gnus-mime-view-part	      M-x ... RET
  Interactively choose a viewing method for the MIME part under point.
gnus-treat-smiley	      M-x ... RET
  Toggle display of textual emoticons ("smileys") as small graphical
  icons.
goto-line-preview	      <menu-bar> <edit> <goto> <go-to-line>, M-g M-g, M-g g, s-l
  Preview goto line.
help-follow-mouse	      M-x ... RET
  Follow the cross-reference that you click on.
ibuffer-add-to-tmp-hide	      M-x ... RET
  Add REGEXP to `ibuffer-tmp-hide-regexps'.
ibuffer-add-to-tmp-show	      M-x ... RET
  Add REGEXP to `ibuffer-tmp-show-regexps'.
ibuffer-customize	      M-x ... RET
  Begin customizing Ibuffer interactively.
ibuffer-do-delete	      M-x ... RET
  Kill marked buffers as with `kill-this-buffer'.
ibuffer-forward-line	      M-x ... RET
  Move forward ARG lines, wrapping around the list if necessary.
image-kill-buffer	      M-x ... RET
  Kill the current buffer.
image-reset-speed	      M-x ... RET
  Reset the animation speed of the current image.
info-emacs-manual	      <menu-bar> <help-menu> <emacs-manual>, <help> r, <f1> r
  Display the Emacs manual in Info mode.
isearch-mode-help	      M-x ... RET
  Display documentation of Isearch mode.
isearch-toggle-invisible      M-x ... RET
  Toggle invisible searching on or off.
isearch-yank-kill	      s-e
  Pull string from kill ring into search string.
ispell-change-dictionary      <menu-bar> <tools> <spell> <ispell-change-dictionary>, <menu-bar> <edit> <spell> <ispell-change-dictionary>
  Change to dictionary DICT for Ispell.
magit-am-continue	      M-x ... RET
  Resume the current patch applying sequence.
magit-branch-orphan	      M-x ... RET
  Create and checkout an orphan BRANCH with contents from revision
  START-POINT.
magit-rebase-edit	      M-x ... RET
  Edit the todo list of the current rebase operation.
magit-rebase-skip	      M-x ... RET
  Skip the current commit and restart the current rebase operation.
magit-reflog-head	      M-x ... RET
  Display the `HEAD' reflog.
magit-run-git-gui	      M-x ... RET
  Run `git gui' for the current git repository.
magit-stash-branch-here	      M-x ... RET
  Create and checkout a new BRANCH and apply STASH.
magit-status-here	      M-x ... RET
  Like `magit-status' but with non-nil
  `magit-status-goto-file-position'.
magit-unstage-all	      M-x ... RET
  Remove all changes from the staging area.
message-goto-from	      M-x ... RET
  Move point to the From header.
message-insert-signature      M-x ... RET
  Insert a signature at the end of the buffer.
message-mode-menu	      M-x ... RET
  Message Menu.
  Value: (keymap "Message" (Yank\ Original menu-item "Yank Original" message-yank-ori…
mml-secure-message-sign	      M-x ... RET
  Add MML tags to sign the entire message.
mouse-select-font	      M-x ... RET
  Prompt for a font name, using `x-popup-menu', and return it.
mpv-playlist-next	      M-x ... RET
  Go to the next entry on the playlist.
mpv-playlist-prev	      M-x ... RET
  Go to the previous entry on the playlist.
org-ascii-export-as-ascii     M-x ... RET
  Export current buffer to a text buffer.
org-decrypt-entry	      M-x ... RET
  Decrypt the content of the current headline.
org-display-inline-images     M-x ... RET
  Display inline images.
org-edit-src-exit	      M-x ... RET
  Kill current sub-editing buffer and return to source buffer.
org-edit-src-save	      M-x ... RET
  Save parent buffer with current state source-code buffer.
org-encrypt-entry	      M-x ... RET
  Encrypt the content of the current headline.
org-entities-help	      M-x ... RET
  Create a Help buffer with all available entities.
org-indent-drawer	      M-x ... RET
  Indent the drawer at point.
org-latex-export-as-latex     M-x ... RET
  Export current buffer as a LaTeX buffer.
org-open-at-point-global      M-x ... RET
  Follow a link or a time-stamp like Org mode does.
org-priority-down	      M-x ... RET
  Decrease the priority of the current item.
outline-headers-as-kill	      M-x ... RET
  Save the visible outline headers between BEG and END to the kill
  ring.
pcomplete-reverse	      M-x ... RET
  If cycling completion is in use, cycle backwards.
pkg-dm-autoremove	      M-x ... RET
  Remove packages that are no longer needed.
popwin:display-buffer	      M-x ... RET
  Display BUFFER-OR-NAME, if possible, in a popup window, or as usual.
popwin:one-window	      M-x ... RET
  Delete other window than the popup window. C-g restores the original
  window configuration.
posframe-hide-all	      M-x ... RET
  Hide all posframe frames.
quickrun-eval-print	      M-x ... RET
  Run commands with specified region and replace.
recentf-edit-list	      M-x ... RET
  Show a dialog to delete selected files from the recent list.
  Value: nil
recover-this-file	      M-x ... RET
  Recover the visited file--get contents from its last auto-save file.
relint-current-buffer	      M-x ... RET
  Scan the current buffer for regexp errors.
rmail-sort-by-recipient	      M-x ... RET
  Sort messages of current Rmail buffer by recipient.
rmail-summary-by-thread	      M-x ... RET
  Display a summary of messages in the same discussion thread as
  MSGNUM.
rxt-convert-to-rx	      M-x ... RET
  Convert regex at point to RX syntax. Chooses Emacs or PCRE syntax by
  major mode.
shadow-initialize	      M-x ... RET
  Set up file shadowing.
shr-previous-link	      M-x ... RET
  Skip to the previous link.
shr-show-alt-text	      M-x ... RET
  Show the ALT text of the image under point.
so-long-customize	      M-x ... RET
  Open the customization group `so-long'.
thing-copy-to-line-end	      M-x ... RET
  Copy content from current point to line end.
thing-replace-url	      M-x ... RET
  Replace url at current point with kill ring.
toggle-one-window	      M-x ... RET
  Toggle between window layout and one window.
toggle-rot13-mode	      M-x ... RET
  Toggle the use of ROT13 encoding for the current window.
trace-function-foreground     M-x ... RET
  Trace calls to function FUNCTION.
url-cookie-delete	      M-x ... RET
  Delete the cookie on the current line.
whitespace-cleanup-region     M-x ... RET
  Cleanup some blank problems at region.
xeft-full-reindex	      M-x ... RET
  Do a full reindex of all files.
xeft-refresh-full	      M-x ... RET
  Refresh and display _all_ results.
yas-recompile-all	      M-x ... RET
  Compile every dir in `yas-snippet-dirs'.
yas-visit-snippet-file	      <menu-bar> <yasnippet> <Visit snippet file...>
  Choose a snippet to edit, selection like `yas-insert-snippet'.
yas/recompile-all	      M-x ... RET
  Compile every dir in `yas-snippet-dirs'.
yas/visit-snippet-file	      M-x ... RET
  Choose a snippet to edit, selection like `yas-insert-snippet'.
Info-mouse-scroll-up	      M-x ... RET
  Scroll one screenful forward in Info, using the mouse.
Info-speedbar-browser	      M-x ... RET
  Initialize speedbar to display an Info node browser.
View-scroll-page-forward      M-x ... RET
  Scroll "page size" or prefix LINES lines forward in View mode.
article-strip-banner	      M-x ... RET
  Strip the banners specified by the `banner' group parameter and by
bibtex-reposition-window      M-x ... RET
  Make the current BibTeX entry visible.
c-guess-region-no-install     M-x ... RET
  Guess the style on the region; don't install it.
calendar-backward-week	      M-x ... RET
  Move the cursor back ARG weeks.
calendar-backward-year	      M-x ... RET
  Move the cursor backward ARG years.
calendar-forward-month	      M-x ... RET
  Move the cursor forward ARG months.
comint-fontify-input-mode     M-x ... RET
  Enable input fontification in the current comint buffer.
  Value: nil
delete-non-matching-lines     M-x ... RET
  Delete all lines except those containing matches for REGEXP.
describe-text-properties      M-x ... RET
  Describe widgets, buttons, overlays, and text properties at POS.
diff-hl-set-reference-rev     M-x ... RET
  Set the reference revision globally to REV.
dired-compare-directories     M-x ... RET
  Mark files with different file attributes in two dired buffers.
dired-flag-files-regexp	      M-x ... RET
  In Dired, flag all files containing the specified REGEXP for
  deletion.
dired-maybe-insert-subdir     M-x ... RET
  Insert this subdirectory into the same dired buffer.
display-line-numbers-mode     M-x ... RET
  Toggle display of line numbers in the buffer.
  Value: t
edebug-instrument-callee      M-x ... RET
  Instrument the definition of the function or macro about to be
  called.
elisp-byte-compile-file	      M-x ... RET
  Byte compile the file the current buffer is visiting.
elpl-insert-single-quote      M-x ... RET
  Insert single quote.
gnus-article-hide-pem	      M-x ... RET
  Run `article-hide-pem' in the article buffer.
gnus-group-kill-region	      M-x ... RET
  Kill newsgroups in current region (excluding current point).
gnus-group-list-plus	      M-x ... RET
  List groups plus the current selection.
gnus-group-mark-region	      M-x ... RET
  Mark all groups between point and mark.
gnus-summary-down-thread      M-x ... RET
  Go down thread N steps.
gnus-summary-kill-thread      M-x ... RET
  Mark articles under current thread as read.
gnus-summary-move-article     M-x ... RET
  Move the current article to a different newsgroup.
gnus-summary-next-thread      M-x ... RET
  Go to the same level next N'th thread.
gnus-summary-save-parts	      M-x ... RET
  Save parts matching TYPE to DIR.
help-goto-previous-page	      M-x ... RET
  Go to the previous page (if any) in the current buffer.
isearch-xterm-paste	      M-x ... RET
  Pull terminal paste into search string.
magit-git-command-topdir      M-x ... RET
  Execute COMMAND asynchronously; display output.
markdown-insert-list-item     M-x ... RET
  Insert a new list item.
markdown-promote-subtree      M-x ... RET
  Promote the current subtree of ATX headings.
markdown-table-sort-lines     M-x ... RET
  Sort table lines according to the column at point.
mh-smail-other-window	      M-x ... RET
  Compose a message with the MH mail system in other window.
org-ascii-export-to-ascii     M-x ... RET
  Export current buffer to a text file.
org-beginning-of-item	      M-x ... RET
  Go to the beginning of the current item.
org-latex-export-to-latex     M-x ... RET
  Export current buffer to a LaTeX file.
org-md-export-as-markdown     M-x ... RET
  Export current buffer to a Markdown buffer.
org-move-subtree-up	      M-x ... RET
  Move the current subtree up past ARG headlines of the same level.
package-refresh-contents      M-x ... RET
  Download descriptions of all configured ELPA packages.
package-vc-prepare-patch      M-x ... RET
  Send patch for REVISIONS to maintainer of the package PKG using
  SUBJECT.
recover-session-finish	      M-x ... RET
  Choose one saved session to recover auto-save files from.
set-justification-none	      M-x ... RET
  Disable automatic filling for paragraphs in the region.
table-insert-row-column	      M-x ... RET
  Insert row(s) or column(s).
toggle-case-fold-search	      <menu-bar> <options> <search-options> <case-fold-search>
  Toggle whether to ignore letter-case in search commands.
ad-recover-normality	      M-x ... RET
  Undo all advice related redefinitions and unadvises everything.
article-hide-headers	      M-x ... RET
  Hide unwanted headers and possibly sort them as well.
backtrace-toggle-locals	      M-x ... RET
  Toggle the display of local variables for the backtrace frame at
  point.
calendar-iso-goto-week	      M-x ... RET
  Move cursor to ISO DATE; echo ISO date unless NOECHO is non-nil.
checkdoc-current-buffer	      M-x ... RET
  Check current buffer for document, comment, error style, and rogue
  spaces.
cl-old-struct-compat-mode     M-x ... RET
  Enable backward compatibility with old-style structs.
  Value: nil
delete-horizontal-space	      M-\
  Delete all spaces and tabs around point.
ediff-merge-with-ancestor     M-x ... RET
  Merge two files with ancestor.
ediff-toggle-use-toolbar      M-x ... RET
  Enable or disable Ediff toolbar.
electric-quote-local-mode     M-x ... RET
  Toggle `electric-quote-mode' only in this buffer.
gnus-bookmark-bmenu-list      M-x ... RET
  Display a list of existing Gnus bookmarks.
gnus-summary-edit-article     M-x ... RET
  Edit the current article.
gnus-summary-refer-thread     M-x ... RET
  Fetch all articles in the current thread.
gnus-summary-show-article     M-x ... RET
  Force redisplaying of the current article.
handle-delete-frame	      <delete-frame>
  Handle delete-frame events from the X server.
highlight-symbol-at-point     M-s h .
  Highlight each instance of the symbol at point.
magit-emacs-Q-command	      M-x ... RET
  Show a shell command that runs an uncustomized Emacs with only Magit
  loaded.
magit-generate-changelog      M-x ... RET
  Insert ChangeLog entries into the current buffer.
message-beginning-of-line     M-x ... RET
  Move point to beginning of header value or to beginning of line.
  Value: t
message-goto-signature	      M-x ... RET
  Move point to the beginning of the message signature.
message-yank-buffer	      M-x ... RET
  Insert BUFFER into the current buffer and quote it.
mouse-select-window	      <header-line> <mouse-1>, <tab-line> <mouse-1>, <mode-line> <mouse-1>, <vertical-line> <mouse-1>
  Select the window clicked on; don't move point.
next-error-select-buffer      M-x ... RET
  Select a `next-error' capable BUFFER and set it as the last used.
occur-word-at-mouse	      M-x ... RET
  Display an occur buffer for the word at EVENT.
org-activate-angle-links      M-x ... RET
  Do nothing and return nil.
org-activate-plain-links      M-x ... RET
  Do nothing and return nil.
org-delete-backward-char      M-x ... RET
  Like `delete-backward-char', insert whitespace at field end in
  tables.
org-md-export-to-markdown     M-x ... RET
  Export current buffer to a Markdown file.
org-roam-db-autosync-mode     M-x ... RET
  Global minor mode to keep your Org-roam session automatically
  synchronized.
  Value: nil
org-table-fedit-finish	      M-x ... RET
  Parse the buffer for formula definitions and install them.
org-timestamp-down-day	      M-x ... RET
  Decrease the date in the time stamp by one day.
org-toggle-latex-fragment     M-x ... RET
  Toggle preview of the LaTeX fragment at point.
project-switch-to-buffer      M-x ... RET
  Display buffer BUFFER-OR-NAME in the selected window.
scan-buf-previous-region      M-x ... RET
  Go to the start of the previous region with non-nil help-echo.
vc-print-branch-log	      M-x ... RET
  Show the change log for BRANCH root in a window.
widget-button-click	      M-x ... RET
  Invoke the button that the mouse is pointing at.
wucuo-current-font-face	      M-x ... RET
  Get font face under cursor.
allout-outlinify-sticky	      M-x ... RET
  Activate outline mode and establish file var so it is started
  subsequently.
antlr-show-makefile-rules     M-x ... RET
  Show Makefile rules for all grammar files in the current directory.
calc-embedded-activate	      M-x ... RET
  Scan the current editing buffer for all embedded := and => formulas.
calc-grab-rectangle	      M-x ... RET
  Parse a rectangle as a matrix of numbers and push it on the
  Calculator stack.
completion-in-region-mode     M-x ... RET
  Transient minor mode used during `completion-in-region'.
  Value: nil
diff-hl-margin-local-mode     M-x ... RET
  Toggle displaying `diff-hl-mode' highlights on the margin locally.
dired-mark-subdir-files	      M-x ... RET
  Mark all files except `.' and `..' in current subdirectory.
edebug-set-initial-mode	      M-x ... RET
  Set the initial execution mode of Edebug.
font-lock-debug-fontify	      M-x ... RET
  Reinitialize the font-lock machinery and (re-)fontify the buffer.
git-gutter:next-diff	      M-x ... RET
  Move to next diff hunk
git-gutter:next-hunk	      M-x ... RET
  Move to next diff hunk
global-diff-hl-amend-mode     M-x ... RET
  Toggle Diff-Hl-Amend mode in all buffers.
  Value: nil
global-pangu-spacing-mode     M-x ... RET
  Toggle Pangu-Spacing mode in all buffers.
  Value: t
gnus-group-edit-group	      M-x ... RET
  Edit the group on the current line.
gnus-summary-scroll-down      M-x ... RET
  Scroll down (or up) one line current article.
handle-select-window	      <select-window>
  Handle select-window events.
highlight-compare-buffers     M-x ... RET
  Compare two buffers and highlight the differences.
ido-find-file-read-only	      M-x ... RET
  Edit file read-only with name obtained via minibuffer.
indent-rigidly-left	      M-x ... RET
  Indent all lines between BEG and END leftward by one space.
info-complete-symbol	      M-x ... RET
  Perform completion on symbol preceding point.
magit-shortlog-range	      M-x ... RET
  Show a history summary for commit or range REV-OR-RANGE.
magit-skip-worktree	      M-x ... RET
  Call "git update-index --skip-worktree -- FILE".
magit-wip-after-save-mode     M-x ... RET
  Toggle Magit-Wip-After-Save-Local mode in all buffers.
  Value: nil
magit-worktree-branch	      M-x ... RET
  Create a new BRANCH and check it out in a new worktree at PATH.
markdown-backward-page	      M-x ... RET
  Move backward to boundary of the current toplevel section.
message-send-form-letter      M-x ... RET
  Sent all form letter messages from current buffer.
org-babel-pop-to-session      M-x ... RET
  Switch to the session of the current code block.
org-edit-agenda-file-list     M-x ... RET
  Edit the list of agenda files.
org-feed-goto-inbox	      M-x ... RET
  Go to the inbox that captures the feed named FEED.
org-self-insert-command	      M-x ... RET
  Like `self-insert-command', use overwrite-mode for whitespace in
  tables.
org-store-agenda-views	      M-x ... RET
  Store agenda views.
rebuild-mail-abbrevs	      M-x ... RET
  Rebuild all the mail aliases from the given file.
rmail-output-body-to-file     M-x ... RET
  Write this message body to the file FILE-NAME.
shell-forward-command	      M-x ... RET
  Move forward across ARG shell command(s).  Does not cross lines.
split-window-horizontally     M-x ... RET
  Split WINDOW-TO-SPLIT into two side-by-side windows.
thing-copy-whole-buffer	      M-x ... RET
  Copy content of the current buffer.
url-history-save-history      M-x ... RET
  Write the global history file into `url-history-file'.
widget-field-activate	      M-x ... RET
  Invoke the editable field at point.
word-wrap-whitespace-mode     M-x ... RET
  Allow `word-wrap' to fold on all breaking whitespace characters.
animate-birthday-present      M-x ... RET
  Return a birthday present in the buffer *Birthday-Present*.
backtrace-expand-ellipses     M-x ... RET
  Expand display of all "..."s in the backtrace frame at point.
bibtex-validate-globally      M-x ... RET
  Check for duplicate keys in `bibtex-files'.
comint-continue-subjob	      M-x ... RET
  Send CONT signal to process buffer's process group.
comint-copy-old-input	      M-x ... RET
  Insert after prompt old input at point as new input to be edited.
edebug-display-freq-count     M-x ... RET
  Display the frequency count data for each line of the current
  definition.
elp-instrument-function	      <menu-bar> <emacs-lisp> <Profiling> <Instrument Function...>
  Instrument FUNSYM for profiling.
gnus-gnus-to-newsrc-format    M-x ... RET
  (not documented)
gnus-summary-catchup-all      M-x ... RET
  Mark all articles in this newsgroup as read.
gnus-summary-next-article     M-x ... RET
  Select the next article.
gnus-summary-next-subject     M-x ... RET
  Go to next N'th summary line.
gnus-summary-save-article     M-x ... RET
  Save the current article using the default saver function.
ido-find-alternate-file	      M-x ... RET
  Find another file, select its buffer, kill previous buffer.
magit-am-apply-patches	      M-x ... RET
  Apply the patches FILES.
magit-diff-working-tree	      M-x ... RET
  Show changes between the current working tree and the `HEAD' commit.
magit-ediff-resolve-rest      M-x ... RET
  Resolve outstanding conflicts in the FILE at point using Ediff.
markdown-mark-text-block      M-x ... RET
  Put mark at end of this plain text block, point at beginning.
markdown-table-move-row	      M-x ... RET
  Move table line at point down.
minibuffer-complete-word      M-x ... RET
  Complete the minibuffer contents at most a single word.
mpv-volume-decrease	      M-x ... RET
  Decrease playback volume by STEPS factors of `mpv-volume-step'.
mpv-volume-increase	      M-x ... RET
  Increase playback volume by STEPS factors of `mpv-volume-step'.
org-roam-migrate-wizard	      M-x ... RET
  Migrate all notes from to be compatible with Org-roam v2.
org-table-insert-hline	      M-x ... RET
  Insert a horizontal-line below the current line into the table.
package-menu-hide-package     M-x ... RET
  Hide in Package Menu packages that match a regexp.
popwin:pop-to-buffer	      M-x ... RET
  Same as `pop-to-buffer' except that this function will use
  `popwin:display-buffer-1' instead of `display-buffer'.  BUFFER,
pr-ps-file-ps-print	      M-x ... RET
  Send PostScript file FILENAME to printer or use ghostscript to print
  it.
profiler-find-profile	      M-x ... RET
  Open profile FILENAME.
replace-buffer-in-windows     M-x ... RET
  Replace BUFFER-OR-NAME with some other buffer in all windows showing
  it.
tibetan-compose-buffer	      M-x ... RET
  Composes Tibetan character components in the buffer.
viet-decode-viqr-region	      M-x ... RET
  Convert `VIQR' mnemonics of the current region to Vietnamese
  characters.
viet-encode-viqr-region	      M-x ... RET
  Convert Vietnamese characters of the current region to `VIQR'
  mnemonics.
xref-pop-marker-stack	      M-x ... RET
  Go back to the previous position in xref history.
yas-activate-extra-mode	      M-x ... RET
  Activates the snippets for the given `mode' in the buffer.
article-date-english	      M-x ... RET
  Convert the current article date to something that is proper
  English.
avy-goto-end-of-line	      M-x ... RET
  Call `avy-goto-line' and move to the end of the line.
comint-goto-process-mark      M-x ... RET
  Move point to the process mark.
dired-build-subdir-alist      M-x ... RET
  Build `dired-subdir-alist' by parsing the buffer.
dired-do-symlink-regexp	      M-x ... RET
  Symlink selected files whose names match REGEXP to NEWNAME.
emacs-lisp-macroexpand	      M-x ... RET
  Macroexpand the form after point.
epa-list-secret-keys	      M-x ... RET
  List all keys matched with NAME from the private keyring.
file-cache-add-directory      M-x ... RET
  Add all files in DIRECTORY to the file cache.
gnus-summary-mark-forward     M-x ... RET
  Mark N articles as read forwards.
ibuffer-decompose-filter      M-x ... RET
  Separate this buffer's top compound filter (AND, OR, NOT, or SAVED).
inverse-add-global-abbrev     M-x ... RET
  Define the word before point as a global (mode-independent)
  abbreviation.
magit-bundle-verify	      M-x ... RET
  Check whether FILE is valid and applies to the current repository.
magit-merge-preview	      M-x ... RET
  Preview result of merging REV into the current branch.
magit-show-refs-current	      M-x ... RET
  List and compare references in a dedicated buffer.
message-mark-insert-file      M-x ... RET
  Insert FILE at point, marking it with enclosing tags.
org-agenda-file-to-front      M-x ... RET
  Move/add the current file to the top of the agenda file list.
org-babel-demarcate-block     M-x ... RET
  Wrap or split the code in the region or on the point.
org-babel-next-src-block      M-x ... RET
  Jump to the next source block.
org-cdlatex-math-modify	      M-x ... RET
  Execute `cdlatex-math-modify' in LaTeX fragments.
org-date-from-calendar	      M-x ... RET
  Insert time stamp corresponding to cursor date in *Calendar* buffer.
org-narrow-to-subtree	      M-x ... RET
  Narrow buffer to the current subtree.
org-table-move-cell-up	      M-x ... RET
  Move a single cell up in a table.
package-show-package-list     M-x ... RET
  Display PACKAGES in a *Packages* buffer.
rfc-mode-goto-section	      M-x ... RET
  Move point to SECTION.
tab-bar-move-tab-to-frame     M-x ... RET
  Move tab from FROM-NUMBER position to new position at TO-NUMBER.
tibetan-decompose-region      M-x ... RET
  Decompose Tibetan text in the region FROM and TO.
ucs-normalize-NFC-region      M-x ... RET
  Compose the region between FROM and TO according to the Unicode NFC.
5x5-crack-randomly	      M-x ... RET
  Attempt to crack 5x5 using random solutions.
Info-last-preorder	      M-x ... RET
  Go to the last node, popping up a level if there is none.
Info-next-preorder	      M-x ... RET
  Go to the next subnode or the next node, or go up a level.
View-exit-and-edit	      M-x ... RET
  Exit View mode and make the current buffer editable.
article-treat-smartquotes     M-x ... RET
  Translate "Microsoft smartquotes" and other symbols into proper
  text.
avy-goto-subword-0	      M-x ... RET
  Ace-pinyin replacement of `avy-goto-subword-0'.
avy-goto-subword-1	      M-x ... RET
  Ace-pinyin replacement of `avy-goto-subword-1'.
bibtex-InReference	      M-x ... RET
  Insert a template for a @InReference entry; see also `bibtex-entry'.
bibtex-MVReference	      M-x ... RET
  Insert a template for a @MVReference entry; see also `bibtex-entry'.
bibtex-Proceedings	      M-x ... RET
  Insert a template for a @Proceedings entry; see also `bibtex-entry'.
bibtex-Unpublished	      M-x ... RET
  Insert a template for a @Unpublished entry; see also `bibtex-entry'.
bibtex-remove-OPT-or-ALT      M-x ... RET
  Remove the string starting optional/alternative fields.
bln-forward-half-b	      M-x ... RET
  This function is used in combination with `bln-backward-half-b' to
  provide binary line navigation in the buffer (see `bln-mode').
bln-forward-half-v	      M-x ... RET
  This function is used in combination with `bln-backward-half' to
  provide binary line navigation (see `bln-mode').
bovine-grammar-mode	      M-x ... RET
  Major mode for editing Bovine grammars.
cfengine-auto-mode	      M-x ... RET
  Choose `cfengine2-mode' or `cfengine3-mode' by buffer contents.
citre-peek-restore	      M-x ... RET
  Restore recent peek session.
color-rg-recompile	      M-x ... RET
  Run `recompile' while preserving some buffer local variables.
comint-kill-subjob	      M-x ... RET
  Send kill signal to the current subjob.
comint-quit-subjob	      M-x ... RET
  Send quit signal to the current subjob.
corfu-quick-insert	      M-x ... RET
  Insert candidate using quick keys.
dired-display-file	      M-x ... RET
  In Dired, display this file or directory in another window.
doc-view-kill-proc	      M-x ... RET
  Kill the current converter process(es).
doc-view-last-page	      M-x ... RET
  View the last page.
doc-view-open-text	      M-x ... RET
  Display the current doc's contents as text.
ebnf-syntax-buffer	      M-x ... RET
  Do a syntactic analysis of the current buffer.
ebrowse-member-mode	      M-x ... RET
  Major mode for Ebrowse member buffers.
ebrowse-statistics	      M-x ... RET
  Display statistics for a class tree.
edirs-merge-with-ancestor     M-x ... RET
  Merge files in DIR1 and DIR2 using files in ANCESTOR-DIR as
  ancestors.
eww-history-browse	      M-x ... RET
  Browse the history under point in eww.
eww-list-bookmarks	      M-x ... RET
  Display the bookmarks.
eww-list-histories	      M-x ... RET
  List the eww-histories.
exit-splash-screen	      M-x ... RET
  Stop displaying the splash screen buffer.
faceup-view-buffer	      M-x ... RET
  Display the faceup representation of the current buffer.
flyspell-prog-mode	      M-x ... RET
  Turn on `flyspell-mode' for comments and strings.
follow-scroll-down-window     M-x ... RET
  Scroll text in a Follow mode window down by that window's size.
free-keys-set-prefix	      M-x ... RET
  Change the prefix in current *Free keys* buffer to PREFIX and
gnus-summary-prev-group	      M-x ... RET
  Exit current newsgroup and then select previous unread newsgroup.
gnus-summary-tick-article     M-x ... RET
  Mark current article as unread.
help-quit-or-quick	      <help> q, <f1> q
  Call `help-quit' or  `help-quick' depending on the context.
htmlfontify-load-rgb-file     M-x ... RET
  Load an X11 style rgb.txt FILE.
ibuffer-backward-line	      M-x ... RET
  Move backwards ARG lines, wrapping around the list if necessary.
ibuffer-pop-filter	      M-x ... RET
  Remove the top filter in this buffer.
ibuffer-unmark-backward	      M-x ... RET
  Unmark the buffers in the region, or previous ARG buffers.
icomplete-fido-ret	      M-x ... RET
  Exit minibuffer or enter directory, like `ido-mode'.
image-backward-hscroll	      M-x ... RET
  Scroll image in current window to the right by N character widths.
isearch-highlight-regexp      M-x ... RET
  Exit Isearch mode and call `highlight-regexp'.
json-pretty-print-buffer      M-x ... RET
  Pretty-print current buffer.
list-input-methods	      M-x ... RET
  Display information about all input methods.
magit-am-apply-maildir	      M-x ... RET
  Apply the patches from MAILDIR.
magit-bisect-reset	      M-x ... RET
  After bisecting, cleanup bisection state and return to original
  `HEAD'.
magit-ediff-show-unstaged     M-x ... RET
  Show unstaged changes using Ediff.
magit-rebase-abort	      M-x ... RET
  Abort the current rebase operation, restoring the original branch.
magit-run-gitk-all	      M-x ... RET
  Run `gitk --all' in the current repository.
magit-shortlog-since	      M-x ... RET
  Show a history summary for commits since REV.
magit-wip-after-apply-mode    M-x ... RET
  Commit to work-in-progress refs.
  Value: nil
mail-add-attachment	      M-x ... RET
  Add FILE as a MIME attachment to the end of the mail message being
  composed.
mail-mail-reply-to	      M-x ... RET
  Move point to end of Mail-Reply-To field, creating it if necessary.
mark-end-of-buffer	      M-x ... RET
  Set mark at the end of the buffer.
markdown-complete-region      M-x ... RET
  Complete markup of objects in region from BEG to END.
markdown-mode-info	      M-x ... RET
  Open the `markdown-mode' homepage.
markdown-mode-menu	      M-x ... RET
  Menu for Markdown mode.
  Value: (keymap "Markdown" (nil "--") (Movement menu-item "Movement" (keymap "Moveme…
menu-bar-read-mail	      <menu-bar> <tools> <rmail>
  Read mail using `read-mail-command'.
open-newline-above	      M-x ... RET
  Move to the previous line (like vi) and then opens a line.
open-newline-below	      M-x ... RET
  Move to the next line (like vi) and then opens a line.
org-agenda-columns	      M-x ... RET
  Turn on or update column view in the agenda.
org-cycle-overview	      M-x ... RET
  Switch to overview mode, showing only top-level headlines.
org-edit-src-abort	      M-x ... RET
  Abort editing of the src code and return to the Org buffer.
org-hide-block-all	      M-x ... RET
  Fold all blocks in the current buffer.
org-show-block-all	      M-x ... RET
  Unfold all blocks in the current buffer.
org-table-kill-row	      M-x ... RET
  Delete the current row or horizontal line from the table.
org-table-paste-rectangle     M-x ... RET
  Paste a rectangular region into a table.
org-toggle-comment	      M-x ... RET
  Change the COMMENT state of an entry.
org-toggle-inline-images      M-x ... RET
  Toggle the display of inline images.
org-update-all-dblocks	      M-x ... RET
  Update all dynamic blocks in the buffer.
origami-auto-apply	      M-x ... RET
  Apply folding based on origami-auto-strings-fold-* variables.
outline-hide-entry	      M-x ... RET
  Hide the body directly following this heading.
package-import-keyring	      M-x ... RET
  Import keys from FILE.
pcomplete-continue	      M-x ... RET
  Complete without reference to any cycling completions.
pkg-dm-install-all	      M-x ... RET
  Install all needed packages from this configuration.
pkg-dm-upgrade-all	      M-x ... RET
  Upgrade for archive packages.
posframe-benchmark	      M-x ... RET
  Benchmark tool for posframe.
previous-error-no-select      M-x ... RET
  Move point to the previous error in the `next-error' buffer and
  highlight match.
ps-print-customize	      M-x ... RET
  Customization of ps-print group.
python-fix-imports	      M-x ... RET
  Add missing imports and remove unused ones from the current buffer.
rmail-summary-by-labels	      M-x ... RET
  Display a summary of all messages with one or more LABELS.
siri-shortcuts-run	      M-x ... RET
  Run a macOS Shortcut with a given NAME.
so-long-commentary	      M-x ... RET
  View the `so-long' library's documentation in `outline-mode'.
standard-themes-toggle	      M-x ... RET
  Toggle between the `standard-dark' and `standard-light' themes.
strokes-global-set-stroke     M-x ... RET
  Interactively give STROKE the global binding as COMMAND.
symbol-overlay-put	      M-x ... RET
  Toggle all overlays of symbol at point.
thing-cut-filename	      M-x ... RET
  Cut filename at current point.
thing-cut-sentence	      M-x ... RET
  Cut sentence around point.
thing-replace-line	      M-x ... RET
  Replace current line with kill ring
thing-replace-list	      M-x ... RET
  Replace list around point with the content of kill ring.
thing-replace-page	      M-x ... RET
  Replace page around point with the content of currnt line.
thing-replace-sexp	      M-x ... RET
  Replace sexp at current point with the content of kill-ring.
thing-replace-word	      M-x ... RET
  Replace words at point with kill ring.
tramp-unload-tramp	      M-x ... RET
  Discard Tramp from loading remote files.
vc-revision-other-window      <menu-bar> <tools> <vc> <vc-revision-other-window>
  Visit revision REV of the current file in another window.
vc-root-version-diff	      M-x ... RET
  Report diffs between REV1 and REV2 revisions of the whole tree.
view-order-manuals	      <menu-bar> <help-menu> <more-manuals> <order-emacs-manuals>, <help> RET, <f1> RET
  Display information on how to buy printed copies of Emacs manuals.
wisent-grammar-mode	      M-x ... RET
  Major mode for editing Wisent grammars.
xref-revert-buffer	      M-x ... RET
  Refresh the search results in the current buffer.
xwidget-webkit-browse-url     M-x ... RET
  Ask xwidget-webkit to browse URL.
5x5-crack-xor-mutate	      M-x ... RET
  Attempt to crack 5x5 by xoring the current and best solution.
article-fill-long-lines	      M-x ... RET
  Fill lines that are wider than the window width or `fill-column'.
bookmark-insert-location      M-x ... RET
  Insert the name of the file associated with BOOKMARK-NAME.
calc-grab-sum-across	      M-x ... RET
  Parse a rectangle as a matrix of numbers and sum its rows.
calendar-set-date-style	      M-x ... RET
  Set the style of calendar and diary dates to STYLE (a symbol).
citre-peek-chain-forward      M-x ... RET
  Move forward in the currently browsed chain.
clipboard-kill-ring-save      M-x ... RET
  Copy region to kill ring, and save in the GUI's clipboard.
customize-changed-options     M-x ... RET
  Customize all settings whose meanings have changed in Emacs itself.
describe-input-method	      <menu-bar> <options> <mule> <describe-input-method>, <menu-bar> <help-menu> <describe> <describe-input-method>, <help> C-\, <help> I, <f1> C-\, <f1> I
  Describe input method INPUT-METHOD.
describe-repeat-maps	      M-x ... RET
  Describe mappings of commands repeatable by symbol property
  `repeat-map'.
diff-lisp-diff-a-and-b	      M-x ... RET
  Compare current region with region from
  `diff-lisp-mark-selected-text-as-a'.
dired-async-do-rename	      M-x ... RET
  Run ‘dired-do-rename’ asynchronously.
edit-named-kbd-macro	      M-x ... RET
  Edit a keyboard macro which has been given a name by
  `name-last-kbd-macro'.
execute-extended-command      M-x, <menu>, <execute>
  Read a command name, then read the arguments and call the command.
exit-recursive-edit	      C-M-c
  Exit from the innermost recursive edit or minibuffer.
fill-individual-paragraphs    M-x ... RET
  Fill paragraphs of uniform indentation within the region.
gnus-child-no-server	      M-x ... RET
  Read network news as a child, without connecting to the local
  server.
gnus-group-list-flush	      M-x ... RET
  Flush groups from the current selection.
gnus-slave-no-server	      M-x ... RET
  Read network news as a child, without connecting to the local
  server.
gnus-summary-hide-thread      M-x ... RET
  Hide thread subtrees.
gnus-summary-idna-message     M-x ... RET
  Decode IDNA encoded domain names in the current articles.
help-fns-edit-mode-done	      M-x ... RET
  Update the value of the variable being edited and kill the edit
  buffer.
ibuffer-mark-for-delete	      M-x ... RET
  Mark for deletion the buffers in the region, or ARG buffers.
list-multisession-values      M-x ... RET
  List all values in the "multisession" database.
magit-copy-buffer-revision    M-x ... RET
  Save the revision of the current buffer for later use.
make-variable-buffer-local    M-x ... RET
  Make VARIABLE become buffer-local whenever it is set.
makefile-gmake-mode	      M-x ... RET
  An adapted `makefile-mode' that knows about gmake.
makefile-imake-mode	      M-x ... RET
  An adapted `makefile-mode' that knows about imake.
markdown-insert-wiki-link     M-x ... RET
  Insert a wiki link of the form [[WikiLink]].
newsticker-start-ticker	      M-x ... RET
  Start newsticker's ticker (but not the news retrieval).
org-babel-execute-buffer      M-x ... RET
  Execute source code blocks in a buffer.
org-beamer-export-as-latex    M-x ... RET
  Export current buffer as a Beamer buffer.
org-beamer-export-to-latex    M-x ... RET
  Export current buffer as a Beamer presentation (tex).
org-revert-all-org-buffers    M-x ... RET
  Revert all Org buffers.
org-transpose-element	      M-x ... RET
  Transpose current and previous elements, keeping blank lines
  between.
previous-line-completion      M-x ... RET
  Move to the item on the previous line in the completion list.
quail-set-keyboard-layout     M-x ... RET
  Set the current keyboard layout to the same as keyboard KBD-TYPE.
view-echo-area-messages	      <help> e, <f1> e
  View the log of recent echo-area messages: the `*Messages*' buffer.
Custom-reset-current	      M-x ... RET
  Reset all edited settings in the buffer to show their current
  values.
View-scroll-line-forward      M-x ... RET
  Scroll forward one line (or prefix LINES lines) in View mode.
async-byte-compile-file	      M-x ... RET
  Byte compile Lisp code FILE asynchronously.
bibtex-beginning-of-entry     M-x ... RET
  Move to beginning of BibTeX entry (beginning of line).
bibtex-remove-delimiters      M-x ... RET
  Remove "" or {} around current BibTeX field text.
calendar-backward-month	      M-x ... RET
  Move the cursor backward by ARG months.
calendar-cursor-holidays      M-x ... RET
  Find holidays for the date specified by the cursor in the calendar
  window.
delete-trailing-whitespace    M-x ... RET
  Delete trailing whitespace between START and END.
doc-view-presentation-mode    M-x ... RET
  Minor mode used while in presentation mode.
  Value: nil
ediff-toggle-multiframe	      <menu-bar> <tools> <compare> <ediff-misc> <emultiframe>
  Switch from multiframe display to single-frame display and back.
ethio-sera-to-fidel-buffer    M-x ... RET
  Convert the current buffer from SERA to FIDEL.
ethio-sera-to-fidel-region    M-x ... RET
  Convert the characters in region from SERA to FIDEL.
flyspell-auto-correct-word    M-x ... RET
  Correct the current word.
  Value: nil
gnus-article-date-user	      M-x ... RET
  Run `article-date-user' in the article buffer.
gnus-article-decode-HZ	      M-x ... RET
  Run `article-decode-HZ' in the article buffer.
gnus-article-emphasize	      M-x ... RET
  Run `article-emphasize' in the article buffer.
gnus-article-push-button      M-x ... RET
  Check text under the mouse pointer for a callback function.
gnus-article-remove-cr	      M-x ... RET
  Run `article-remove-cr' in the article buffer.
gnus-article-wash-html	      M-x ... RET
  Run `article-wash-html' in the article buffer.
gnus-group-make-nnir-group    M-x ... RET
  Read an nnselect group based on a search.
gnus-summary-mark-as-spam     M-x ... RET
  Mark N articles forward as spam.
gnus-summary-show-thread      M-x ... RET
  Show thread subtrees.
hi-lock-face-phrase-buffer    M-x ... RET
  Set face of each match of phrase REGEXP to FACE.
image-dired-display-thumbs    M-x ... RET
  Display thumbnails of all marked files, in
  `image-dired-thumbnail-buffer'.
japanese-katakana-region      M-x ... RET
  Convert Japanese `hiragana' chars in the region to `katakana' chars.
kmacro-end-or-call-macro      <f4>
  End kbd macro if currently being defined; else call last kbd macro.
markdown-narrow-to-page	      M-x ... RET
  Make text outside current top level section invisible.
message-insert-screenshot     M-x ... RET
  Take a screenshot and insert in the current buffer.
org-babel-lob-ingest	      M-x ... RET
  Add all named source blocks defined in FILE to
  `org-babel-library-of-babel'.
org-bibtex-read-file	      M-x ... RET
  Read FILE with `org-bibtex-read-buffer'.
org-eww-copy-for-org-mode     M-x ... RET
  Copy current buffer content or active region with `org-mode' style
  links.
org-persist-write:version     M-x ... RET
  Do nothing and return nil.
org-preview-latex-fragment    M-x ... RET
  Toggle preview of the LaTeX fragment at point.
org-texinfo-export-to-info    M-x ... RET
  Export current buffer to Texinfo then process through to INFO.
origami-forward-fold	      M-x ... RET
  Move point to the beginning of the first fold in the BUFFER after
  POINT.
outline-toggle-children	      M-x ... RET
  Show or hide the current subtree depending on its current state.
shell-highlight-undef-mode    M-x ... RET
  Highlight undefined shell commands and aliases.
  Value: nil
skeleton-pair-insert-maybe    M-x ... RET
  Insert the character you type ARG times.
tramp-crypt-add-directory     M-x ... RET
  Mark remote directory NAME for encryption.
with-editor-export-editor     M-x ... RET
  Teach subsequent commands to use current Emacs instance as editor.
abbrev-edit-save-buffer	      M-x ... RET
  Save all the user-level abbrev definitions in current buffer.
calendar-end-of-year	      M-x ... RET
  Move the cursor forward ARG year beginnings.
cl-font-lock-built-in-mode    M-x ... RET
  Highlight built-in functions, variables, and types in `lisp-mode'.
  Value: nil
close-display-connection      M-x ... RET
  Close the connection to a display, deleting all its associated
  frames.
doc-view-previous-page	      M-x ... RET
  Browse ARG pages backward.
edebug-eval-top-level-form    M-x ... RET
  Evaluate the top level form point is in, stepping through with
  Edebug.
electric-indent-local-mode    M-x ... RET
  Toggle `electric-indent-mode' only in this buffer.
electric-layout-local-mode    M-x ... RET
  Toggle `electric-layout-mode' only in this buffer.
ethio-fidel-to-sera-buffer    M-x ... RET
  Convert all the FIDEL characters in the current buffer to the SERA
  format.
ethio-fidel-to-sera-region    M-x ... RET
  Convert all the FIDEL characters in the region to the SERA format.
gnus-group-find-new-groups    M-x ... RET
  Search for new groups and add them.
highlight-parentheses-mode    M-x ... RET
  Minor mode to highlight the surrounding parentheses.
  Value: t
horizontal-scroll-bar-mode    <menu-bar> <options> <showhide> <showhide-scroll-bar> <horizontal>
  Toggle horizontal scroll bars on all frames (Horizontal Scroll Bar
  mode).
  Value: nil
ibuffer-mark-by-mode	      M-x ... RET
  Mark all buffers whose major mode equals MODE.
ido-find-file-in-dir	      M-x ... RET
  Switch to another file starting from DIR.
indent-rigidly-right	      M-x ... RET
  Indent all lines between BEG and END rightward by one space.
kmacro-call-ring-2nd	      M-x ... RET
  Execute second keyboard macro in macro ring.
kmacro-end-and-call-macro     M-x ... RET
  Call last keyboard macro, ending it first if currently being
  defined.
magit-gitignore-in-subdir     M-x ... RET
  Add the Git ignore RULE to a ".gitignore" file in DIRECTORY.
magit-sparse-checkout-add     M-x ... RET
  Add DIRECTORIES to the working tree.
markdown-live-preview-mode    M-x ... RET
  Toggle native previewing on save for a specific markdown file.
  Value: nil
message-kill-to-signature     M-x ... RET
  Kill all text up to the signature.
mouse-wheel-text-scale	      C-<wheel-up>, C-<wheel-down>, C-<mouse-4>, C-<mouse-5>
  Adjust font size of the default face according to EVENT.
org-bibtex-read-buffer	      M-x ... RET
  Read all bibtex entries in BUFFER and save to `org-bibtex-entries'.
org-edit-latex-environment    M-x ... RET
  Edit LaTeX environment at point.
org-id-update-id-locations    M-x ... RET
  Scan relevant files for IDs.
org-table-header-line-mode    M-x ... RET
  Display the first row of the table at point in the header line.
  Value: nil
read--expression-try-read     M-x ... RET
  Try to read an Emacs Lisp expression in the minibuffer.
read-extended-command-mode    M-x ... RET
  Minor mode used for completion in `read-extended-command'.
  Value: nil
set-justification-center      M-x ... RET
  Make paragraphs in the region centered.
set-keyboard-coding-system    <menu-bar> <options> <mule> <set-various-coding-system> <set-keyboard-coding-system>
  Set coding system for keyboard input on TERMINAL to CODING-SYSTEM.
set-terminal-coding-system    <menu-bar> <options> <mule> <set-various-coding-system> <set-terminal-coding-system>
  Set coding system of terminal output to CODING-SYSTEM.
shell-backward-command	      M-x ... RET
  Move backward across ARG shell command(s).  Does not cross lines.
shr-mouse-browse-url	      M-x ... RET
  Browse the URL under the mouse cursor.
tab-bar-select-tab-by-name    M-x ... RET
  Switch to the tab by NAME.
trace-function-background     <menu-bar> <emacs-lisp> <Tracing> <Trace Function Quietly...>
  Trace calls to function FUNCTION, quietly.
vc-revert-buffer-internal     M-x ... RET
  Revert buffer, keeping point and mark where user expects them.
xah-replace-invisible-char    M-x ... RET
  Query replace some invisible Unicode chars.
View-search-regexp-forward    M-x ... RET
  Search forward for first (or prefix Nth) occurrence of REGEXP in
  View mode.
bibtex-skip-to-valid-entry    M-x ... RET
  Move point to beginning of the next valid BibTeX entry.
checkdoc-ispell-comments      M-x ... RET
  Check the style and spelling of the current buffer's comments.
checkdoc-ispell-continue      M-x ... RET
  Check the style and spelling of the current buffer after point.
copy-rectangle-to-register    M-x ... RET
  Copy rectangular region into register REGISTER.
cursor-face-highlight-mode    M-x ... RET
  When enabled, highlight text that has `cursor-face' property near
  point.
  Value: nil
dired-do-byte-compile	      M-x ... RET
  Byte compile marked (or next ARG) Emacs Lisp files.
fill-nonuniform-paragraphs    M-x ... RET
  Fill paragraphs within the region, allowing varying indentation
  within each.
gnus-summary-prev-thread      M-x ... RET
  Go to the same level previous N'th thread.
gnus-summary-print-article    M-x ... RET
  Generate and print a PostScript image of the process-marked (mail)
  articles.
gnus-treat-from-gravatar      M-x ... RET
  Display gravatar in the From header.
  Value: nil
imenu-add-menubar-index	      M-x ... RET
  Add an Imenu "Index" entry on the menu bar for the current buffer.
magit-remote-unset-head	      M-x ... RET
  Unset the local representation of REMOTE's default branch.
magit-sparse-checkout-set     M-x ... RET
  Restrict working tree to DIRECTORIES.
markdown-demote-list-item     M-x ... RET
  Indent (or demote) the current list item.
markdown-table-transpose      M-x ... RET
  Transpose table at point.
minibuffer-force-complete     M-x ... RET
  Complete the minibuffer to an exact match.
mode-line-change-eol	      M-x ... RET
  Cycle through the various possible kinds of end-of-line styles.
org-babel-load-in-session     M-x ... RET
  Load the body of the current source-code block.
org-escape-code-in-region     M-x ... RET
  Escape lines between BEG and END.
org-fold-hide-block-toggle    M-x ... RET
  Toggle the visibility of the current block.
org-table-fedit-scroll-down   M-x ... RET
  (not documented)
org-update-checkbox-count     M-x ... RET
  Update the checkbox statistics in the current section.
package-menu-mark-upgrades    M-x ... RET
  Mark all upgradable packages in the Package Menu.
package-menu-quick-help	      M-x ... RET
  Show short key binding help for `package-menu-mode'.
pp-macroexpand-last-sexp      M-x ... RET
  Run `pp-macroexpand-expression' on sexp before point.
redirect-debugging-output     M-x ... RET
  Redirect debugging output (stderr stream) to file FILE.
set-face-background-pixmap    M-x ... RET
  Change the stipple pixmap of face FACE to STIPPLE.
spam-report-process-queue     M-x ... RET
  Report all queued requests from `spam-report-requests-file'.
tab-bar-close-other-tabs      M-x ... RET
  Close all tabs on the selected frame, except the tab TAB-NUMBER.
tab-bar-history-forward	      M-x ... RET
  Cancel restoration of the previous window configuration.
  Value: #s(hash-table size 65 test eql rehash-size 1.5 rehash-threshold 0.8125 data …
tab-bar-move-tab-backward     M-x ... RET
  Move the current tab ARG positions to the left.
ucs-normalize-NFD-region      M-x ... RET
  Decompose the region between FROM and TO according to the Unicode
  NFD.
ucs-normalize-NFKC-region     M-x ... RET
  Compose the region between FROM and TO according to the Unicode
  NFKC.
window-toggle-side-windows    M-x ... RET
  Toggle display of side windows on specified FRAME.
c-guess-buffer-no-install     M-x ... RET
  Guess the style on the whole current buffer; don't install it.
calendar-end-of-week	      M-x ... RET
  Move the cursor forward ARG calendar-week-start-day+6's.
comint-bol-or-process-mark    M-x ... RET
  Move point to beginning of line (after prompt) or to the process
  mark.
delimit-columns-rectangle     M-x ... RET
  Prettify all columns in a text rectangle.
dired-do-hardlink-regexp      M-x ... RET
  Hardlink selected files whose names match REGEXP to NEWNAME.
display-buffer-other-frame    M-x ... RET
  Display buffer BUFFER preferably in another frame.
file-notify-handle-event      M-x ... RET
  Handle a file system monitoring event, coming from backends.
gnus-article-press-button     M-x ... RET
  Check text at point for a callback function.
gnus-group-make-help-group    M-x ... RET
  Create the Gnus documentation group.
gnus-mime-button-menu	      M-x ... RET
  Construct a context-sensitive menu of MIME commands.
  Value: (keymap "MIME Part" (Toggle\ Display menu-item "Toggle Display" gnus-article…
gnus-summary-goto-article     M-x ... RET
  Fetch ARTICLE (article number or Message-ID) and display it if it
  exists.
gnus-summary-goto-subject     M-x ... RET
  Go to the subject line of ARTICLE.
gnus-summary-prev-subject     M-x ... RET
  Go to previous N'th summary line.
gnus-summary-save-newsrc      M-x ... RET
  Save the current number of read/marked articles in the dribble
  buffer.
gnus-treat-mail-gravatar      M-x ... RET
  Display gravatars in the Cc and To headers.
  Value: nil
isearch-toggle-case-fold      M-x ... RET
  Toggle case-fold searching on or off.
kmacro-delete-ring-head	      M-x ... RET
  Delete current macro from keyboard macro ring.
magit-clone-shallow-since     M-x ... RET
  Create a shallow clone of REPOSITORY in DIRECTORY.
mail-abbrev-end-of-buffer     M-x ... RET
  Expand a mail abbrev before point, then move to the end of the
  buffer.
org-check-dates-range	      M-x ... RET
  Check for deadlines/scheduled entries between START-DATE and
  END-DATE.
org-clock-remove-overlays     M-x ... RET
  Remove the occur highlights from the buffer.
org-next-visible-heading      M-x ... RET
  Move to the next visible heading line.
org-w3m-copy-for-org-mode     M-x ... RET
  Copy current buffer content or active region with Org style links.
reset-language-environment    M-x ... RET
  Reset multilingual environment of Emacs to the default status.
rmail-summary-by-senders      M-x ... RET
  Display a summary of all messages whose "From" field matches
  SENDERS.
scroll-bar-scroll-down	      M-x ... RET
  Scroll the window's top line down to the location of the scroll bar
  click.
shadow-define-regexp-group    M-x ... RET
  Make each of a group of files be shared between hosts.
Buffer-menu-not-modified      M-x ... RET
  Mark the buffer on this line as unmodified (no changes to save).
Info-last-menu-item	      M-x ... RET
  Go to the node of the previous menu item.
Info-mouse-scroll-down	      M-x ... RET
  Scroll one screenful backward in Info, using the mouse.
Info-next-menu-item	      M-x ... RET
  Go to the node of the next menu item.
View-kill-and-leave	      M-x ... RET
  Quit View mode, kill current buffer and return to previous buffer.
apply-macro-to-region-lines   M-x ... RET
  Apply last keyboard macro to all lines in the region.
apropos-next-symbol	      M-x ... RET
  Move cursor down to the next symbol in an `apropos-mode' buffer.
back-to-indentation	      M-m
  Move point to the first non-whitespace character on this line.
bibtex-InCollection	      M-x ... RET
  Insert a template for a @InCollection entry; see also
  `bibtex-entry'.
bibtex-MVCollection	      M-x ... RET
  Insert a template for a @MVCollection entry; see also
  `bibtex-entry'.
bibtex-ispell-entry	      M-x ... RET
  Check BibTeX entry for spelling errors.
blink-matching-open	      M-x ... RET
  Momentarily highlight the beginning of the sexp before point.
bln-backward-half-b	      M-x ... RET
  This function is used in combination with `bln-forward-half-b' to
  provide binary line navigation in the buffer (see `bln-mode').
bln-backward-half-v	      M-x ... RET
  This function is used in combination with `bln-forward-half' to
  provide binary line navigation (see `bln-mode').
cal-tex-cursor-week-monday    M-x ... RET
  Make a one page LaTeX calendar for one week, showing hours of the
  day.
calendar-goto-today	      M-x ... RET
  Reposition the calendar window so the current date is visible.
citre-peek-next-tag	      M-x ... RET
  Peek the next tag in tag list.
citre-peek-prev-tag	      M-x ... RET
  Peek the previous tag in tag list.
comint-clear-buffer	      M-x ... RET
  Clear the comint buffer.
corfu-info-location	      M-x ... RET
  Show location of current candidate.
debugger-eval-expression      M-x ... RET
  Eval an expression, in an environment like that outside the
  debugger.
delete-completion-window      M-x ... RET
  Delete the completion list window.
describe-prefix-bindings      M-x ... RET
  Describe the bindings of the prefix used to reach this command.
diary-islamic-insert-entry    M-x ... RET
  Insert a diary entry.
doc-view-first-page	      M-x ... RET
  View the first page.
doc-view-search-backward      M-x ... RET
  Call `doc-view-search' for backward search.
edebug-top-level-nonstop      M-x ... RET
  Set mode to Go-nonstop, and exit to top-level.
ediff-show-registry	      <menu-bar> <tools> <compare> <ediff-misc> <eregistry>
  Display Ediff's registry.
emacs-news-view-mode	      M-x ... RET
  Major mode for viewing the Emacs NEWS file.
emoji-zoom-decrease	      M-x ... RET
  Decrease the size of the character under point.
epa-dired-do-verify	      M-x ... RET
  Verify marked files.
epa-import-keys-region	      <menu-bar> <tools> <encryption-decryption> <import-keys-region>
  Import keys from the region.
epkg-describe-package	      M-x ... RET
  Display the full documentation of PACKAGE.
eww-bookmark-browse	      M-x ... RET
  Browse the bookmark under point in eww.
eww-toggle-checkbox	      M-x ... RET
  Toggle the value of the checkbox under point.
faceup-clean-buffer	      M-x ... RET
  Remove faceup markup from buffer.
gif-screencast-stop	      M-x ... RET
  Stop recording and compile GIF.
gnus-article-edit-done	      M-x ... RET
  Update the article edits and exit.
gnus-draft-reminder	      M-x ... RET
  Reminder user if there are unsent drafts.
gnus-group-unsubscribe	      M-x ... RET
  Unsubscribe the current group.
grugru-edit-no-save	      M-x ... RET
  Same as `grugru-edit' except it never save data.
help-at-pt-cancel-timer	      M-x ... RET
  Cancel any timer set by `help-at-pt-set-timer'.
ibuffer-bury-buffer	      M-x ... RET
  Bury the buffer on this line.
image-reverse-speed	      M-x ... RET
  Reverse the animation of the current image.
isearch-tmm-menubar	      M-x ... RET
  Run `tmm-menubar' while `isearch-mode' is enabled.
iso-cvt-define-menu	      M-x ... RET
  Add submenus to the File menu, to convert to and from various
  formats.
kmacro-edit-lossage	      M-x ... RET
  Edit most recent 300 keystrokes as a keyboard macro.
link-hint-open-link	      M-x ... RET
  Use avy to open a visible link.
markdown-back-to-heading      M-x ... RET
  Move to previous heading line, or beg of this line if it's a
  heading.
markdown-insert-header-dwim   M-x ... RET
  Insert or replace header markup.
markdown-kill-block	      M-x ... RET
  Kill visible code block, list item, or blockquote and add it to
  `kill-ring'.
markdown-narrow-to-block      M-x ... RET
  Make text outside current block invisible.
markdown-outline-up	      M-x ... RET
  Move to previous list item, when in a list, or previous heading.
message-caesar-region	      M-x ... RET
  Caesar rotate region B to E by N, default 13, for decrypting
  netnews.
message-kill-buffer	      M-x ... RET
  Kill the current buffer.
message-widen-reply	      M-x ... RET
  Widen the reply to include maximum recipients.
mml-secure-sign-pgp	      M-x ... RET
  Add MML tags to PGP sign this MML part.
multi-isearch-files-regexp    M-x ... RET
  Start multi-buffer regexp Isearch on a list of FILES.
newsticker-treeview	      M-x ... RET
  Start newsticker treeview.
ns-spi-service-call	      M-x ... RET
  Respond to a service request.
org-babel-initiate-session    M-x ... RET
  Initiate session for current code block.
org-cancel-repeater	      M-x ... RET
  Cancel a repeater by setting its numeric value to zero.
org-change-tag-in-region      M-x ... RET
  Add or remove TAG for each entry in the region.
org-decrypt-entries	      M-x ... RET
  Decrypt all entries in the current buffer.
org-encrypt-entries	      M-x ... RET
  Encrypt all top-level entries in the current buffer.
org-feed-update-all	      M-x ... RET
  Get inbox items from all feeds in `org-feed-alist'.
org-first-sibling-p	      M-x ... RET
  Is this heading the first child of its parents?
org-fold-hide-entry	      M-x ... RET
  Hide the body directly following this heading.
org-narrow-to-block	      M-x ... RET
  Narrow buffer to the current block.
org-property-action	      M-x ... RET
  Do an action on properties.
org-table-field-info	      M-x ... RET
  Show info about the current field, and highlight any reference at
  point.
org-table-show-reference      M-x ... RET
  Show the location/value of the $ expression at point.
osx-dictionary-quit	      M-x ... RET
  Quit osx-dictionary: reselect previously selected buffer.
outline-hide-leaves	      M-x ... RET
  Hide the body after this heading and at deeper levels.
pixel-scroll-precision-mode   M-x ... RET
  Toggle pixel scrolling.
  Value: nil
posframe-delete-all	      M-x ... RET
  Delete all posframe frames and buffers.
pr-toggle-landscape	      M-x ... RET
  Toggle landscape.
ps-spool-buffer-with-faces    M-x ... RET
  Generate and spool a PostScript image of the buffer.
python-sort-imports	      M-x ... RET
  Sort Python imports in the current buffer.
rxt-toggle-elisp-rx	      M-x ... RET
  Toggle the regexp near point between Elisp string and rx syntax.
siri-shortcuts-edit	      M-x ... RET
  Edit a Shortcut with the given NAME.
siri-shortcuts-open	      M-x ... RET
  Edit a Shortcut with the given NAME.
thai-compose-buffer	      M-x ... RET
  Compose Thai characters in the current buffer.
thing-cut-paragraph	      M-x ... RET
  Cut current paragraph around the point
thing-replace-defun	      M-x ... RET
  Replace function around point with the content of kill ring.
thing-replace-email	      M-x ... RET
  Replace email at current point with the content kill ring.
vc-git-region-history-mode    M-x ... RET
  Major mode to browse Git's "log -p" output.
view-emacs-problems	      <menu-bar> <help-menu> <emacs-known-problems>, <help> C-p, <f1> C-p
  Display info on known problems with Emacs and possible workarounds.
yas-describe-tables	      M-x ... RET
  Display snippets for each table.
yas/describe-tables	      M-x ... RET
  Display snippets for each table.
calendar-sunrise-sunset	      M-x ... RET
  Local time of sunrise and sunset for date under cursor.
compile-mouse-goto-error      M-x ... RET
  Visit the source for the error message at point.
completion-help-at-point      M-x ... RET
  Display the completions on the text around point.
dictionary-match-words	      M-x ... RET
  Search PATTERN in current default dictionary using default strategy.
dired-async-do-symlink	      M-x ... RET
  Run ‘dired-do-symlink’ asynchronously.
ebrowse-tags-query-replace    M-x ... RET
  Query replace FROM with TO in all files of a class tree.
ediff-directory-revisions     <menu-bar> <tools> <compare> <ediff-dir-revision>
  Run Ediff on a directory, DIR1, comparing its files with their
  revisions.
gnus-article-add-buttons      M-x ... RET
  Find external references in the article and make buttons of them.
gnus-group-catchup-current    M-x ... RET
  Mark all unread articles in the current newsgroup as read.
gnus-summary-copy-article     M-x ... RET
  Copy the current article to some other group.
gnus-summary-first-subject    M-x ... RET
  Go to the first subject satisfying any non-nil constraint.
isearch-toggle-char-fold      M-x ... RET
  Toggle char-fold searching on or off.
magit-remote-prune-refspecs   M-x ... RET
  Remove stale refspecs for REMOTE.
makefile-makepp-mode	      M-x ... RET
  An adapted `makefile-mode' that knows about makepp.
mouse-drag-and-drop-region    M-x ... RET
  Move text in the region to point where mouse is dragged to.
  Value: nil
org-babel-execute-subtree     M-x ... RET
  Execute source code blocks in a subtree.
org-check-after-date	      M-x ... RET
  Check if there are deadlines or scheduled entries after date D.
org-icalendar-export-to-ics   M-x ... RET
  Export current buffer to an iCalendar file.
org-move-subtree-down	      M-x ... RET
  Move the current subtree down past ARG headlines of the same level.
org-replace-buffer-contents   M-x ... RET
  Replace accessible portion of current buffer with that of SOURCE.
org-table-move-column	      M-x ... RET
  Move the current column to the right.  With arg LEFT, move to the
  left.
paragraph-indent-text-mode    M-x ... RET
  Major mode for editing text, with leading spaces starting a
  paragraph.
ps-spool-region-with-faces    M-x ... RET
  Generate a PostScript image of the region and spool locally.
save-buffers-kill-terminal    <menu-bar> <file> <exit-emacs>
  Offer to save each buffer, then kill the current connection.
sqlite-mode-open-file	      M-x ... RET
  Browse the contents of an sqlite file.
calendar-end-of-month	      M-x ... RET
  Move the cursor forward ARG month ends.
check-declare-directory	      M-x ... RET
  Check veracity of all `declare-function' statements under directory
  ROOT.
citre-edit-tags-file-recipe   M-x ... RET
  Edit the recipe of TAGSFILE.
citre-peek-chain-backward     M-x ... RET
  Move backward in the currently browsed chain.
compose-mail-other-frame      M-x ... RET
  Like \[compose-mail], but edit the outgoing message in another
  frame.
describe-coding-system	      <menu-bar> <options> <mule> <describe-coding-system>, <menu-bar> <help-menu> <describe> <describe-coding-system>
  Display information about CODING-SYSTEM.
ebrowse-tags-loop-continue    M-x ... RET
  Repeat last operation on files in tree.
elisp-byte-compile-buffer     M-x ... RET
  Byte compile the current buffer, but don't write a file.
gnus-article-date-local	      M-x ... RET
  Run `article-date-local' in the article buffer.
gnus-article-treat-date	      M-x ... RET
  Run `article-treat-date' in the article buffer.
gnus-group-delete-articles    M-x ... RET
  Delete all articles in the current group.
gnus-group-kill-level	      M-x ... RET
  Kill all groups that is on a certain LEVEL.
httpd-serve-directory	      M-x ... RET
  Start the web server with given `directory' as `httpd-root'.
ibuffer-kill-filter-group     M-x ... RET
  Kill the filter group named NAME.
ido-find-file-other-frame     M-x ... RET
  Switch to another file and show it in another frame.
info-xref-check-all-custom    M-x ... RET
  Check info references in all customize groups and variables.
ispell-buffer-with-debug      M-x ... RET
  `ispell-buffer' with some output sent to `ispell-debug-buffer'.
magit-diff-edit-hunk-commit   M-x ... RET
  From a hunk, edit the respective commit and visit the file.
magit-log-all-branches	      M-x ... RET
  Show log for all local and remote branches and `HEAD'.
message-caesar-buffer-body    M-x ... RET
  Caesar rotate all letters in the current buffer by 13 places.
occur-symbol-at-mouse	      M-x ... RET
  Display an occur buffer for the symbol at EVENT.
org-babel-execute-src-block   M-x ... RET
  Execute the current source code block and return the result.
org-table-follow-field-mode   M-x ... RET
  Minor mode to make the table field editor window follow the cursor.
  Value: nil
org-table-previous-field      M-x ... RET
  Go to the previous field in the table.
org-tree-to-indirect-buffer   M-x ... RET
  Create indirect buffer and narrow it to current subtree.
paragraph-indent-minor-mode   M-x ... RET
  Minor mode for editing text, with leading spaces starting a
  paragraph.
  Value: nil
ps-print-buffer-with-faces    <menu-bar> <file> <print> <ps-print-buffer-faces>
  Generate and print a PostScript image of the buffer.
ps-print-region-with-faces    <menu-bar> <file> <print> <ps-print-region-faces>
  Generate and print a PostScript image of the region.
rmail-next-labeled-message    M-x ... RET
  Show next message with one of the labels LABELS.
shortdoc-previous-section     M-x ... RET
  Move point to the previous section.
tab-bar-rename-tab-by-name    M-x ... RET
  Rename the tab named TAB-NAME to NEW-NAME.
with-editor-shell-command     M-x ... RET
  Like `shell-command' or `with-editor-async-shell-command'.
abort-recursive-edit	      C-]
  Abort the command that requested this recursive edit or minibuffer
  input.
async-bytecomp-package-mode   M-x ... RET
  Byte compile asynchronously packages installed with package.el.
  Value: nil
calendar-coptic-goto-date     M-x ... RET
  Move cursor to Coptic date DATE.
calendar-iso-goto-date	      M-x ... RET
  Move cursor to ISO DATE; echo ISO date unless NOECHO is non-nil.
citre-peek-copy-clue-link     M-x ... RET
  Copy currently browsed line in the peek window as a clue link.
dired-copy-filename-as-kill   M-x ... RET
  Copy names of marked (or next ARG) files into the kill ring.
ethio-sera-to-fidel-marker    M-x ... RET
  Convert regions surrounded by "<sera>" and "</sera>" from SERA to
  FIDEL.
global-centered-cursor-mode   M-x ... RET
  Toggle Centered-Cursor mode in all buffers.
  Value: nil
gnus-group-make-web-group     M-x ... RET
  Create an ephemeral nnweb group.
gnus-summary-delete-article   M-x ... RET
  Delete the N next (mail) articles.
gnus-summary-prev-article     M-x ... RET
  Select the article before the current one.
indian-compose-region	      M-x ... RET
  Compose the region according to `composition-function-table'.
mail-fill-yanked-message      M-x ... RET
  Fill the paragraphs of a message yanked into this one.
markdown-blockquote-region    M-x ... RET
  Blockquote the region.
markdown-electric-backquote   M-x ... RET
  Insert a backquote.
markdown-outdent-or-delete    M-x ... RET
  Handle BACKSPACE by cycling through indentation points.
markdown-table-next-row	      M-x ... RET
  Go to the next row (same column) in the table.
markdown-toggle-wiki-links    M-x ... RET
  Toggle support for wiki links.
org-babel-expand-src-block    M-x ... RET
  Expand the current source code block.
org-babel-remove-result	      M-x ... RET
  Remove the result of the current source block.
org-drag-line-forward	      M-x ... RET
  Drag the line at point ARG lines forward.
org-edit-fixed-width-region   M-x ... RET
  Edit the fixed-width ASCII drawing at point.
package-install-from-buffer   M-x ... RET
  Install a package from the current buffer.
tab-bar-move-window-to-tab    M-x ... RET
  Move the selected window to a new tab.
tab-bar-switch-to-next-tab    M-x ... RET
  Switch to ARGth next tab.
Info-mouse-follow-link	      M-x ... RET
  Follow a link where you click.
View-search-regexp-backward   M-x ... RET
  Search backward for first (or prefix Nth) occurrence of REGEXP in
  View mode.
article-display-x-face	      M-x ... RET
  Look for an X-Face header and display it if present.
avy-kill-ring-save-region     M-x ... RET
  Select two lines and save the region between them to the kill ring.
bibtex-copy-field-as-kill     M-x ... RET
  Copy the BibTeX field at point to `bibtex-field-kill-ring'.
calendar-french-goto-date     M-x ... RET
  Move cursor to French Revolutionary date DATE.
citre-update-this-tags-file   M-x ... RET
  Update the currently used tags file.
comint-previous-input	      M-x ... RET
  Cycle backwards through input history, saving input.
edebug-Continue-fast-mode     M-x ... RET
  Trace with no wait at each step.
explain-pause-log-to-socket   M-x ... RET
  Log the event stream to a UNIX file socket, FILE-SOCKET. If
  FILE-SOCKET is nil,
gnus-article-highlight-some   M-x ... RET
  Highlight current article.
gnus-mouse-pick-group	      M-x ... RET
  Enter the group under the mouse pointer.
gnus-summary-limit-to-age     M-x ... RET
  Limit the summary buffer to articles that are older than (or equal)
  AGE days.
gnus-summary-sort-by-date     M-x ... RET
  Sort the summary buffer by date.
highlight-compare-with-file   M-x ... RET
  Compare this buffer with a file, and highlight differences.
magit-gitignore-in-topdir     M-x ... RET
  Add the Git ignore RULE to the top-level ".gitignore" file.
magit-log-matching-tags	      M-x ... RET
  Show log for all tags matching PATTERN and `HEAD'.
markdown-promote-list-item    M-x ... RET
  Unindent (or promote) the current list item.
message-display-abbrev	      M-x ... RET
  Display the next possible abbrev for the text before point.
mode-line-bury-buffer	      M-x ... RET
  Like `bury-buffer', but temporarily select EVENT's window.
mode-line-next-buffer	      M-x ... RET
  Like `next-buffer', but temporarily select EVENT's window.
org-fold-hide-drawer-toggle   M-x ... RET
  Toggle the visibility of the current drawer.
popwin:popup-last-buffer      M-x ... RET
  Show the last popup buffer with the same configuration.
switch-to-buffer-other-tab    M-x ... RET
  Switch to buffer BUFFER-OR-NAME in another tab.
time-stamp-toggle-active      M-x ... RET
  Toggle `time-stamp-active', setting whether \[time-stamp] updates a
  buffer.
timeclock-mode-line-display   M-x ... RET
  Toggle display of the amount of time left today in the mode line.
  Value: nil
ucs-normalize-NFKD-region     M-x ... RET
  Decompose the region between FROM and TO according to the Unicode
  NFKD.
yas-expand-from-trigger-key   M-x ... RET
  Expand a snippet before point.
yas/expand-from-trigger-key   M-x ... RET
  Expand a snippet before point.
add-dir-local-variable	      M-x ... RET
  Add directory-local VARIABLE with its VALUE and MODE to
  .dir-locals.el.
align-newline-and-indent      M-x ... RET
  A replacement function for `newline-and-indent', aligning as it
  goes.
calendar-goto-day-of-year     M-x ... RET
  Move cursor to YEAR, DAY number; echo DAY/YEAR unless NOECHO is
  non-nil.
comint-next-matching-input    M-x ... RET
  Search forwards through input history for match for REGEXP.
dired-flag-auto-save-files    M-x ... RET
  Flag for deletion files whose names suggest they are auto save
  files.
dired-sort-toggle-or-edit     M-x ... RET
  Toggle sorting by date, and refresh the Dired buffer.
electric-pair-delete-pair     M-x ... RET
  When between adjacent paired delimiters, delete both of them.
gnus-mime-view-all-parts      M-x ... RET
  View all the MIME parts.
ibuffer-save-filter-groups    M-x ... RET
  Save all active filter groups GROUPS as NAME.
isearch-beginning-of-buffer   M-x ... RET
  Go to the first occurrence of the current search string.
json-pretty-print-ordered     M-x ... RET
  Pretty-print the region with object keys ordered.
magit-add-change-log-entry    M-x ... RET
  Find change log file and add date entry and item for current change.
magit-find-file-other-frame   M-x ... RET
  View FILE from REV, in another frame.
magit-shell-command-topdir    M-x ... RET
  Execute COMMAND asynchronously; display output.
markdown-end-of-text-block    M-x ... RET
  Move forward to next beginning of a plain text block.
markdown-toggle-url-hiding    M-x ... RET
  Toggle the display or hiding of URLs.
message-add-archive-header    M-x ... RET
  Insert "X-No-Archive: Yes" in the header and a note in the body.
mouse-appearance-menu	      M-x ... RET
  Show a menu for changing the default face in the current buffer.
mouse-minor-mode-menu	      M-x ... RET
  Show minor-mode menu for EVENT on minor modes area of the mode line.
mouse-tear-off-window	      M-x ... RET
  Delete the selected window, and create a new frame displaying its
  buffer.
ns-open-file-select-line      M-x ... RET
  Open a buffer containing the file `ns-input-file'.
org-force-self-insert	      M-x ... RET
  Needed to enforce self-insert under remapping.
org-link-open-from-string     M-x ... RET
  Open a link in the string S, as if it was in Org mode.
org-open-link-from-string     M-x ... RET
  Open a link in the string S, as if it was in Org mode.
org-table-move-cell-down      M-x ... RET
  Move a single cell down in a table.
org-table-move-cell-left      M-x ... RET
  Move a single cell left in a table.
rfc-mode-next-section	      M-x ... RET
  Move point to Nth next section (default 1).
symbol-overlay-remove-all     M-x ... RET
  Remove all highlighted symbols in the buffer.
tab-bar-switch-to-last-tab    M-x ... RET
  Switch to the last tab or ARGth tab from the end of the tab bar.
tab-bar-switch-to-prev-tab    M-x ... RET
  Switch to ARGth previous tab.
tibetan-decompose-buffer      M-x ... RET
  Decomposes Tibetan characters in the buffer into their components.
xref--transient-buffer-mode   M-x ... RET
  Major mode derived from `xref--xref-buffer-mode' by
  `define-derived-mode'.
Buffer-menu-1-window	      M-x ... RET
  Select this line's buffer, alone, in full frame.
Buffer-menu-2-window	      M-x ... RET
  Select this line's buffer, with previous buffer in second window.
Info-history-forward	      M-x ... RET
  Go forward in the history of visited nodes.
  Value: nil
ace-pinyin-jump-char	      M-x ... RET
  AceJump with pinyin by QUERY-CHAR.
article-unsplit-urls	      M-x ... RET
  Remove the newlines that some other mailers insert into URLs.
backtrace-multi-line	      M-x ... RET
  Show the top level s-expression at point on multiple lines with
  indentation.
bibtex-InProceedings	      M-x ... RET
  Insert a template for a @InProceedings entry; see also
  `bibtex-entry'.
bibtex-MVProceedings	      M-x ... RET
  Insert a template for a @MVProceedings entry; see also
  `bibtex-entry'.
bibtex-MastersThesis	      M-x ... RET
  Insert a template for a @MastersThesis entry; see also
  `bibtex-entry'.
bibtex-print-help-message     M-x ... RET
  Print helpful information about current FIELD in current BibTeX
  entry.
citre-peek-next-line	      M-x ... RET
  Scroll to the next line in a peek window.
citre-peek-prev-line	      M-x ... RET
  Scroll to the previous line in a peek window.
citre-peek-reference	      M-x ... RET
  Peek the definitions of the symbol at point.
comint-restore-input	      M-x ... RET
  Restore unfinished input.
corfu-quick-complete	      M-x ... RET
  Complete candidate using quick keys.
customize-customized	      M-x ... RET
  Customize all options and faces set in this session but not saved.
delete-whitespace-rectangle   M-x ... RET
  Delete all whitespace following a specified column in each line.
describe-gnu-project	      <menu-bar> <help-menu> <about-gnu-project>, <help> g, <f1> g
  Browse online information on the GNU project.
describe-no-warranty	      <menu-bar> <help-menu> <describe-no-warranty>, <help> C-w, <f1> C-w
  Display info on all the kinds of warranty Emacs does NOT have.
dired-mode-mark-menu	      M-x ... RET
  Mark menu for Dired mode.
  Value: (keymap "Mark" (Toggle\ Marks menu-item "Toggle Marks" dired-toggle-marks :h…
doc-view-clear-cache	      M-x ... RET
  Delete the whole cache (`doc-view-cache-directory').
doc-view-dired-cache	      M-x ... RET
  Open `dired' in `doc-view-cache-directory'.
doc-view-scale-reset	      M-x ... RET
  Reset the document size/zoom level to the initial one.
elisp-demos-add-demo	      M-x ... RET
  Add demo for SYMBOL.
emacs-lisp-mode-menu	      M-x ... RET
  Menu for Emacs Lisp mode.
  Value: (keymap "Emacs-Lisp" (Indent\ Line menu-item "Indent Line" lisp-indent-line)…
epa-dired-do-decrypt	      M-x ... RET
  Decrypt marked files.
epa-dired-do-encrypt	      M-x ... RET
  Encrypt marked files.
ethio-fidel-to-sera-marker    M-x ... RET
  Convert the regions surrounded by "<sera>" and "</sera>" from FIDEL
  to SERA.
eudc-try-bbdb-insert	      M-x ... RET
  Call `eudc-insert-record-at-point-into-bbdb' if on a record.
eww-buffer-show-next	      M-x ... RET
  Move to next eww buffer in the list and display it.
eww-switch-to-buffer	      M-x ... RET
  Prompt for an EWW buffer to display in the selected window.
expand-jump-to-next-slot      M-x ... RET
  Move the cursor to the next slot in the last abbrev expansion.
find-library-other-frame      M-x ... RET
  Find the Emacs Lisp source of LIBRARY in another frame.
finder-list-keywords	      M-x ... RET
  Display descriptions of the keywords in the Finder buffer.
git-gutter:statistic	      M-x ... RET
  Return statistic unstaged hunks in current buffer.
gnus-group-list-all-groups    M-x ... RET
  List all newsgroups with level ARG or lower.
gnus-group-mark-regexp	      M-x ... RET
  Mark all groups that match some regexp.
gnus-stop-date-timer	      M-x ... RET
  Stop the Date timer.
gnus-summary-prepare	      M-x ... RET
  Generate the summary buffer.
gnus-summary-read-document    M-x ... RET
  Open a new group based on the current article(s).
gnus-summary-respool-article  M-x ... RET
  Respool the current article.
gnus-url-button-menu	      M-x ... RET
  URL button menu.
  Value: (keymap "Url Button" (Copy\ URL\ to\ kill\ ring menu-item "Copy URL to kill …
ibuffer-forward-next-marked   M-x ... RET
  Move forward by COUNT marked buffers (default 1).
ibuffer-toggle-sorting-mode   M-x ... RET
  Toggle the current sorting mode.
ilog-minor-mode-menu	      M-x ... RET
  Menu used when `ilog-log-buffer-mode' is active.
  Value: (keymap "ILog" (Toggle\ view menu-item "Toggle view" ilog-toggle-view) (Togg…
image-decrease-speed	      M-x ... RET
  Decrease the speed of current animated image by a factor of 2.
image-dired-tag-files	      M-x ... RET
  Tag marked file(s) in Dired.  With prefix ARG, tag file at point.
image-increase-speed	      M-x ... RET
  Increase the speed of current animated image by a factor of 2.
isearch-describe-key	      M-x ... RET
  Display documentation of the function invoked by isearch key.
isearch-menu-bar-map	      M-x ... RET
  Menu for `isearch-mode'.
  Value: (keymap "Isearch" (Cancel\ search menu-item "Cancel search" isearch-cancel :…
isearch-ring-advance	      M-x ... RET
  Advance to the next search string in the ring.
isearch-ring-retreat	      M-x ... RET
  Retreat to the previous search string in the ring.
kmacro-view-ring-2nd	      M-x ... RET
  Display the second macro in the keyboard macro ring.
magit-project-status	      M-x ... RET
  Run `magit-status' in the current project's root.
magit-sequencer-skip	      M-x ... RET
  Skip the stopped at commit during a cherry-pick or revert sequence.
markdown-end-of-list	      M-x ... RET
  Move point to end of list at point, if any.
markdown-forward-paragraph    M-x ... RET
  Move forward to the next end of a paragraph.
message-change-subject	      M-x ... RET
  Ask for NEW-SUBJECT header, append (was: <Old Subject>).
message-goto-subject	      M-x ... RET
  Move point to the Subject header.
message-goto-summary	      M-x ... RET
  Move point to the Summary header.
message-kill-address	      M-x ... RET
  Kill the address under point.
message-sort-headers	      M-x ... RET
  Sort headers of the current message according to
  `message-header-format-alist'.
mml-unsecure-message	      M-x ... RET
  Remove security related MML tags from message.
my-clean-aspell-dict	      M-x ... RET
  Clean ~/.aspell.pws (dictionary used by aspell).
newsticker-plainview	      M-x ... RET
  Start newsticker plainview.
newsticker-show-news	      M-x ... RET
  Start reading news.  You may want to bind this to a key.
nnmail-split-history	      M-x ... RET
  Generate an overview of where the last mail split put articles.
  Value: nil
org-babel-mark-block	      M-x ... RET
  Mark current source block.
org-calendar-goto-agenda      M-x ... RET
  Compute the Org agenda for the calendar date displayed at the
  cursor.
org-roam-dailies-goto-date    M-x ... RET
  Find the daily-note for a date using the calendar, creating it if
  necessary.
org-roam-diagnostics	      M-x ... RET
  Collect and print info for `org-roam' issues.
org-shiftcontrolleft	      M-x ... RET
  Switch to previous TODO set.
org-table-fedit-menu	      M-x ... RET
  Org Edit Formulas Menu.
  Value: (keymap "Edit-Formulas" (Finish\ and\ Install menu-item "Finish and Install"…
origami-show-only-node	      M-x ... RET
  Close all fold nodes in BUFFER except for those necessary to make
  POINT visible.  Very useful for quickly collapsing everything in the
  buffer other
outline-next-heading	      M-x ... RET
  Move to the next (possibly invisible) heading line.
popwin:stick-popup-window     M-x ... RET
  Stick the currently shown popup window.
quail-update-leim-list-file   M-x ... RET
  Update entries for Quail packages in `LEIM' list file in directory
  DIRNAME.
self-insert-and-exit	      M-x ... RET
  Terminate minibuffer input.
srecode-template-mode	      M-x ... RET
  Major-mode for writing SRecode macros.
switch-to-minibuffer	      M-x ... RET
  Select the minibuffer window.
symbol-overlay-count	      M-x ... RET
  Show count of symbol at point.
tab-switcher-execute	      M-x ... RET
  Delete window configurations marked with
  \<tab-switcher-mode-map>\[tab-switcher-delete] commands.
testcover-this-defun	      M-x ... RET
  Start coverage on function under point.
text-property-search-forward  M-x ... RET
  Search for the next region of text where PREDICATE is true.
thing-copy-region-or-line     M-x ... RET
  Copy content of the current region or line.
thing-cut-whitespace	      M-x ... RET
  Cut whitespace around point.
thing-replace-number	      M-x ... RET
  Replace number at point with kill ring.
thing-replace-symbol	      M-x ... RET
  Replace symbol around point with kill ring.
tibetan-compose-region	      M-x ... RET
  Compose Tibetan text the region BEG and END.
tramp-compat-set-file-modes   M-x ... RET
  Set mode bits of file named FILENAME to MODE (an integer).
view-emacs-debugging	      <help> C-d, <f1> C-d
  Display info on how to debug Emacs problems.
yas--minor-mode-menu	      M-x ... RET
  Menu used when `yas-minor-mode' is active.
  Value: menu-function-17
compilation-previous-error    M-x ... RET
  Move point to the previous error in the compilation buffer.
compilation-shell-minor-mode  M-x ... RET
  Toggle Compilation Shell minor mode.
  Value: nil
dired-isearch-filenames-mode  M-x ... RET
  Toggle file names searching on or off.
  Value: nil
forward-to-indentation	      M-x ... RET
  Move forward ARG lines and position at first nonblank character.
free-keys-change-buffer	      M-x ... RET
  Change the buffer for which the bindings are displayed to
gnus-article-show-images      M-x ... RET
  Show any images that are in the HTML-rendered article buffer.
gnus-group-make-doc-group     M-x ... RET
  Create a group that uses a single file as the source.
gnus-group-make-rss-group     M-x ... RET
  Given a URL, discover if there is an RSS feed.
gnus-group-make-search-group  M-x ... RET
  Make a group based on a search.
gnus-summary-pop-article      M-x ... RET
  Pop one article off the history and go to the previous.
gnus-summary-tick-above	      M-x ... RET
  Tick all articles with score above SCORE.
isearch-yank-word-or-char     M-x ... RET
  Pull next character or word from buffer into search string.
latin1-display-ucs-per-lynx   M-x ... RET
  Set up Latin-1/ASCII display for Unicode characters.
  Value: nil
magit-gitignore-in-gitdir     M-x ... RET
  Add the Git ignore RULE to "$GIT_DIR/info/exclude".
magit-wip-before-change-mode  M-x ... RET
  Commit to work-in-progress refs before certain destructive changes.
  Value: nil
makefile-bsdmake-mode	      M-x ... RET
  An adapted `makefile-mode' that knows about BSD make.
markdown-table-insert-row     M-x ... RET
  Insert a new row above the row at point into the table.
message-check-recipients      M-x ... RET
  Warn before composing or sending a mail to an invalid address.
message-news-other-frame      M-x ... RET
  Start editing a news article to be sent.
mml-secure-message-encrypt    M-x ... RET
  Add MML tag to encrypt the entire message.
org-calendar-select-mouse     M-x ... RET
  Return to `org-read-date' with the date currently selected.
org-check-before-date	      M-x ... RET
  Check if there are deadlines or scheduled entries before date D.
org-feed-show-raw-feed	      M-x ... RET
  Show the raw feed buffer of a feed.
org-find-file-at-mouse	      M-x ... RET
  Open file link or URL at mouse.
org-return-and-maybe-indent   M-x ... RET
  Goto next table row, or insert a newline, maybe indented.
origami-open-all-nodes	      M-x ... RET
  Recursively open every fold node in BUFFER.
package-menu-toggle-hiding    M-x ... RET
  In Package Menu, toggle visibility of obsolete available packages.
previous-history-element      M-x ... RET
  Puts previous element of the minibuffer history in the minibuffer.
recenter-current-error	      M-x ... RET
  Recenter the current displayed error in the `next-error' buffer.
scroll-other-window-down      ESC <prior>, C-M-S-v, M-<prior>
  Scroll next window downward LINES lines; or near full screen if no
  ARG.
switch-to-buffer-other-frame  M-x ... RET
  Switch to buffer BUFFER-OR-NAME in another frame.
tab-bar-close-group-tabs      M-x ... RET
  Close all tabs that belong to GROUP-NAME on the selected frame.
tab-bar-close-tab-by-name     M-x ... RET
  Close the tab given its NAME.
cal-tex-cursor-filofax-week   M-x ... RET
  One-week-at-a-glance Filofax style calendar for week indicated by
  cursor.
calendar-chinese-goto-date    M-x ... RET
  Move cursor to Chinese date DATE.
calendar-persian-goto-date    M-x ... RET
  Move cursor to Persian date DATE.
copy-rectangle-as-kill	      M-x ... RET
  Copy the region-rectangle and save it as the last killed one.
dired-async-do-hardlink	      M-x ... RET
  Run ‘dired-do-hardlink’ asynchronously.
dired-do-relsymlink-regexp    M-x ... RET
  RelSymlink all marked files containing REGEXP to NEWNAME.
dired-jump-other-window	      M-x ... RET
  Like \[dired-jump] (`dired-jump') but in other window.
dired-make-relative-symlink   M-x ... RET
  Make a symbolic link (pointing to FILE1) in FILE2.
doc-view-fit-page-to-window   M-x ... RET
  Fit the image to the window.
edebug-toggle-save-windows    M-x ... RET
  Toggle the saving and restoring of windows.
epkg-list-packages-of-type    M-x ... RET
  Display a list of all packages of a certain type.
ethio-insert-ethio-space      M-x ... RET
  Insert the Ethiopic word delimiter (the colon-like character).
global-auto-composition-mode  M-x ... RET
  Toggle Auto Composition mode in all buffers.
gnus-article-date-lapsed      M-x ... RET
  Run `article-date-lapsed' in the article buffer.
gnus-summary-kill-below	      M-x ... RET
  Mark articles with score below SCORE as read.
hi-lock-face-symbol-at-point  M-x ... RET
  Highlight each instance of the symbol at point.
ibuffer-do-toggle-read-only   M-x ... RET
  Toggle read only status in marked buffers.
image-mode-wallpaper-set      M-x ... RET
  Set the desktop background to the current image.
kmacro-edit-macro-repeat      M-x ... RET
  Edit last keyboard macro.
magit-bundle-list-heads	      M-x ... RET
  List the refs in FILE.
next-error-follow-minor-mode  M-x ... RET
  Minor mode for compilation, occur and diff modes.
  Value: nil
shadow-define-literal-group   M-x ... RET
  Declare a single file to be shared between sites.
shrink-window-horizontally    M-x ... RET
  Make selected window DELTA columns narrower.
toggle-text-mode-auto-fill    M-x ... RET
  Toggle whether to use Auto Fill in Text mode and related modes.
Buffer-menu-multi-occur	      M-x ... RET
  Show all lines in marked buffers containing a match for a regexp.
abbrev-suggest-show-report    M-x ... RET
  Show a buffer with the list of abbrevs you could have used.
compose-mail-other-window     M-x ... RET
  Like \[compose-mail], but edit the outgoing message in another
  window.
customize-face-other-window   M-x ... RET
  Show customization buffer for face FACE in other window.
dash-register-info-lookup     M-x ... RET
  Register the Dash Info manual with `info-lookup-symbol'.
describe-character-set	      M-x ... RET
  Display information about built-in character set CHARSET.
diary-insert-block-entry      M-x ... RET
  Insert a block diary entry for the days between the point and marked
  date.
diff-hl-show-hunk-mouse-mode  M-x ... RET
  Enables the margin and fringe to show a posframe/popup with vc diffs
  when clicked.
dired-do-async-shell-command  M-x ... RET
  Run a shell command COMMAND on the marked files asynchronously.
edebug-eval-print-last-sexp   M-x ... RET
  Evaluate sexp before point in outside environment; insert value.
epkg-list-matching-packages   M-x ... RET
  Display a list of packages whose name or summary matches PATTERN.
global-prettify-symbols-mode  M-x ... RET
  Toggle Prettify-Symbols mode in all buffers.
  Value: nil
gnus-group-next-unread-group  M-x ... RET
  Go to next N'th unread newsgroup.
gnus-summary-caesar-message   M-x ... RET
  Caesar rotate the current article by 13.
gnus-summary-toggle-header    M-x ... RET
  Show the headers if they are hidden, or hide them if they are shown.
ido-find-file-other-window    M-x ... RET
  Switch to another file and show it in another window.
magit-worktree-checkout	      M-x ... RET
  Checkout BRANCH in a new worktree at PATH.
message-insert-expires	      M-x ... RET
  Insert the Expires header.  Expiry in DAYS days.
minibuffer-complete-and-exit  M-x ... RET
  Exit if the minibuffer contains a valid completion.
org-babel-switch-to-session   M-x ... RET
  Switch to the session of the current code block.
org-convert-to-odd-levels     M-x ... RET
  Convert an Org file with all levels allowed to one with odd levels.
org-drag-line-backward	      M-x ... RET
  Drag the line at point ARG lines backward.
org-list-checkbox-radio-mode  M-x ... RET
  When turned on, use list checkboxes as radio buttons.
  Value: nil
org-remove-inline-images      M-x ... RET
  Remove inline display of images.
org-remove-occur-highlights   M-x ... RET
  Remove the occur highlights from the buffer.
org-set-property-and-value    M-x ... RET
  Allow to set [PROPERTY]: [value] direction from prompt.
org-toggle-sticky-agenda      M-x ... RET
  Toggle `org-agenda-sticky'.
orgtbl-self-insert-command    M-x ... RET
  Like `self-insert-command', use overwrite-mode for whitespace in
  tables.
package-menu-filter-by-name   M-x ... RET
  Filter the "*Packages*" buffer by NAME regexp.
pr-despool-using-ghostscript  M-x ... RET
  Print spooled PostScript using ghostscript.
quail-show-keyboard-layout    M-x ... RET
  Show the physical layout of the keyboard type KEYBOARD-TYPE.
rmail-sort-by-correspondent   M-x ... RET
  Sort messages of current Rmail buffer by other correspondent.
rmail-summary-by-recipients   M-x ... RET
  Display a summary of all messages with the given RECIPIENTS.
sis-global-cursor-color-mode  M-x ... RET
  Automaticly change cursor color according to input source.
  Value: t
undigestify-rmail-message     M-x ... RET
  Break up a digest message into its constituent messages.
xref-select-and-show-xref     M-x ... RET
  Move point to the button and show the xref definition.
bibtex-copy-summary-as-kill   M-x ... RET
  Push summary of current BibTeX entry to kill ring.
company-quickhelp-local-mode  M-x ... RET
  Provides documentation popups for `company-mode' using `pos-tip'.
compilation-previous-file     M-x ... RET
  Move point to the previous error for a different file than the
  current one.
delete-minibuffer-contents    M-x ... RET
  Delete all user input in a minibuffer.
diary-hebrew-insert-entry     M-x ... RET
  Insert a diary entry for the Hebrew date at point.
end-of-buffer-other-window    ESC <end>, M-<end>
  Move point to the end of the buffer in the other window.
gnus-group-transpose-groups   M-x ... RET
  Move the current newsgroup up N places.
gnus-mime-replace-part	      M-x ... RET
  Replace MIME part under point with an external body.
gnus-summary-edit-wash	      M-x ... RET
  Perform editing command KEY in the article buffer.
gnus-summary-sort-by-score    M-x ... RET
  Sort the summary buffer by score.
image-transform-set-smoothing M-x ... RET
  (not documented)
magit-assume-unchanged	      M-x ... RET
  Call "git update-index --assume-unchanged -- FILE".
magit-clone-shallow-exclude   M-x ... RET
  Create a shallow clone of REPOSITORY in DIRECTORY.
magit-gitignore-on-system     M-x ... RET
  Add the Git ignore RULE to the file specified by
  `core.excludesFile'.
magit-no-skip-worktree	      M-x ... RET
  Call "git update-index --no-skip-worktree -- FILE".
minibuffer-next-completion    M-x ... RET
  Move to the next item in its completions window from the minibuffer.
org-hide-block-toggle-maybe   M-x ... RET
  Toggle visibility of block at point.
org-unescape-code-in-region   M-x ... RET
  Un-escape lines between BEG and END.
pcre-query-replace-regexp     M-x ... RET
  Perform `query-replace-regexp' using PCRE syntax.
popwin:close-popup-window     M-x ... RET
  Close the popup window and restore to the previous window
  configuration.
project-query-replace-regexp  M-x ... RET
  Query-replace REGEXP in all the files of the project.
todo-filtered-items-mode      M-x ... RET
  Mode for displaying and reprioritizing top priority Todo.
toggle-save-place-globally    <menu-bar> <options> <save-place>
  Toggle whether to visit files of previous session when restarting
  Emacs.
tree-sitter-query-builder     M-x ... RET
  Provide means for developers to write and test tree-sitter queries.
tree-widget-button-click      M-x ... RET
  Move to the position clicked on, and if it is a button, invoke it.
use-package-statistics-mode   M-x ... RET
  Show current statistics gathered about `use-package' declarations.
windmove-default-keybindings  M-x ... RET
  Set up keybindings for `windmove'.
Info-copy-current-node-name   M-x ... RET
  Put the name of the current Info node into the kill ring.
checkdoc-message-interactive  M-x ... RET
  Interactively check the current buffer for message string errors.
dired-find-alternate-file     M-x ... RET
  In Dired, visit file or directory on current line via
  `find-alternate-file'.
gnus-article-copy-part	      M-x ... RET
  Copy MIME part N, which is the numerical prefix.
gnus-article-encrypt-body     M-x ... RET
  Encrypt the article body.
gnus-article-pipe-part	      M-x ... RET
  Pipe MIME part N, which is the numerical prefix.
gnus-article-save-part	      M-x ... RET
  Save MIME part N, which is the numerical prefix.
gnus-group-edit-global-kill   M-x ... RET
  Edit the global kill file.
gnus-summary-limit-to-marks   M-x ... RET
  Limit the summary buffer to articles that are marked with MARKS
  (e.g. "DK").
gnus-summary-mark-above	      M-x ... RET
  Mark articles with score over SCORE with MARK.
help-fns-edit-mode-cancel     M-x ... RET
  Kill the edit buffer and cancel editing of the value.
ispell-comments-and-strings   <menu-bar> <tools> <spell> <ispell-comments-and-strings>, <menu-bar> <edit> <spell> <ispell-comments-and-strings>
  Check comments and strings in the current buffer for spelling
  errors.
kmacro-cycle-ring-previous    M-x ... RET
  Move to previous keyboard macro in keyboard macro ring.
magit-find-file-other-window  M-x ... RET
  View FILE from REV, in another window.
magit-subtree-add-commit      M-x ... RET
  Add COMMIT as a new subtree at PREFIX.
mail-abbrev-insert-alias      M-x ... RET
  Prompt for and insert a mail alias.
markdown-table-move-column    M-x ... RET
  Move table column at point to the right.
mouse-drag-top-left-corner    <top-left-corner> <down-mouse-1>
  Drag top left corner of a frame with the mouse.
occur-mode-goto-occurrence    M-x ... RET
  Go to the occurrence specified by EVENT, a mouse click.
project-other-tab-command     M-x ... RET
  Run project command, displaying resultant buffer in a new tab.
widget-move-and-invoke	      M-x ... RET
  Move to where you click, and if it is an active field, invoke it.
Buffer-menu-mode-menu	      M-x ... RET
  Menu for `Buffer-menu-mode' buffers.
  Value: (keymap "Buffer-Menu" (Mark menu-item "Mark" Buffer-menu-mark :help "Mark bu…
View-scroll-line-backward     M-x ... RET
  Scroll backward one line (or prefix LINES lines) in View mode.
View-scroll-page-backward     M-x ... RET
  Scroll "page size" or prefix LINES lines backward in View mode.
article-de-base64-unreadable  M-x ... RET
  Translate a base64 article.
avy-goto-char-in-line	      M-x ... RET
  Ace-pinyn replacement of `avy-goto-char-in-line'.
backtrace-goto-source	      M-x ... RET
  If its location is known, jump to the source code for the frame at
  point.
backtrace-single-line	      M-x ... RET
  Show the top level s-expression at point on one line.
bibtex-SuppCollection	      M-x ... RET
  Insert a template for a @SuppCollection entry; see also
  `bibtex-entry'.
bibtex-SuppPeriodical	      M-x ... RET
  Insert a template for a @SuppPeriodical entry; see also
  `bibtex-entry'.
bookmark-bmenu-search	      M-x ... RET
  Incremental search of bookmarks, hiding the non-matches as we go.
cal-tex-cursor-filofax-daily  M-x ... RET
  Day-per-page Filofax style calendar for week indicated by cursor.
color-rg-rerun-regexp	      M-x ... RET
  Re-search as regexp.
delete-other-windows-internal M-x ... RET
  Make WINDOW fill its frame.
describe-distribution	      <menu-bar> <help-menu> <getting-new-versions>, <help> C-o, <f1> C-o
  Display info on how to obtain the latest version of GNU Emacs.
doc-view-presentation	      M-x ... RET
  Put Doc-View in presentation mode.
edit-abbrevs-redefine	      M-x ... RET
  Redefine abbrevs according to current buffer contents.
elisp-demos-find-demo	      M-x ... RET
  Find the demo of the SYMBOL.
elisp-depmap-graphviz	      M-x ... RET
  Make a very basic dot file representation of all the top level
  definitions in a project, and their references.
elisp-refs-next-match	      M-x ... RET
  Move to the next search result in the Refs buffer.
elisp-refs-prev-match	      M-x ... RET
  Move to the previous search result in the Refs buffer.
emacs-lisp-compilation-mode   M-x ... RET
  The variant of `compilation-mode' used for emacs-lisp compilation
  buffers.
eww-beginning-of-text	      M-x ... RET
  Move to the start of the input field.
eww-previous-bookmark	      M-x ... RET
  Go to the previous bookmark in the list.
explain-pause-log-off	      M-x ... RET
  Turn off logging of the event stream.
find-library-other-window     M-x ... RET
  Find the Emacs Lisp source of LIBRARY in another window.
git-gutter:stage-hunk	      M-x ... RET
  Stage this hunk like 'git add -p'.
gnus-delay-send-queue	      M-x ... RET
  Send all the delayed messages that are due now.
gnus-group-list-all-matching  M-x ... RET
  List all groups that match REGEXP.
gnus-summary-mark-below	      M-x ... RET
  Mark articles with score less than SCORE with MARK.
  Value: 0
gnus-summary-sort-by-marks    M-x ... RET
  Sort the summary buffer by article marks.
hi-lock-find-patterns	      M-s h f
  Add patterns from the current buffer to the list of hi-lock
  patterns.
ibuffer-do-kill-lines	      M-x ... RET
  Hide all of the currently marked lines.
ibuffer-negate-filter	      M-x ... RET
  Negate the sense of the top filter in the current buffer.
ibuffer-switch-format	      M-x ... RET
  Switch the current display format.
image-flip-vertically	      M-x ... RET
  Vertically flip the image under point.
image-transform-reset	      M-x ... RET
  Display the current image with the default (initial) size and
  rotation.
indent-comment-buffer	      M-x ... RET
  Indent comment of buffer.
indent-comment-region	      M-x ... RET
  Indent region.
isearch-complete-edit	      M-x ... RET
  Same as `isearch-complete' except in the minibuffer.
isearch-describe-bindings     M-x ... RET
  Show a list of all keys defined in Isearch mode, and their
  definitions.
isearch-describe-mode	      M-x ... RET
  Display documentation of Isearch mode.
isearch-help-for-help	      M-x ... RET
  Display Isearch help menu.
isearch-toggle-regexp	      M-x ... RET
  Toggle regexp searching on or off.
magit-list-submodules	      M-x ... RET
  Display a list of the current repository's submodules.
mail-mail-followup-to	      M-x ... RET
  Move point to end of Mail-Followup-To field, creating it if
  necessary.
markdown-complete-at-point    M-x ... RET
  Complete markup of object near point.
markdown-insert-blockquote    M-x ... RET
  Start a blockquote section (or blockquote the region).
markdown-kill-outline	      M-x ... RET
  Kill visible heading and add it to `kill-ring'.
markdown-outline-next	      M-x ... RET
  Move to next list item, when in a list, or next visible heading.
markdown-show-version	      M-x ... RET
  Show the version number in the minibuffer.
menu-bar-options-save	      <menu-bar> <options> <save>
  Save current values of Options menu items using Custom.
menu-bar-read-lispref	      <menu-bar> <help-menu> <more-manuals> <emacs-lisp-reference>
  Display the Emacs Lisp Reference manual in Info mode.
message-goto-keywords	      M-x ... RET
  Move point to the Keywords header.
message-goto-reply-to	      M-x ... RET
  Move point to the Reply-To header.
minibuffer-choose-completion  M-x ... RET
  Run `choose-completion' from the minibuffer in its completions
  window.
mml-secure-sign-smime	      M-x ... RET
  Add MML tags to S/MIME sign this MML part.
mode-line-unbury-buffer	      M-x ... RET
  Call `unbury-buffer' in this window.
next-window-any-frame	      s-'
  Select the next window, regardless of which frame it is on.
org-fold-hide-subtree	      M-x ... RET
  Hide everything after this heading at deeper levels.
org-fold-show-subtree	      M-x ... RET
  Show everything after this heading at deeper levels.
org-footnote-goto-definition  M-x ... RET
  Move point to the definition of the footnote LABEL.
org-list-make-subtree	      M-x ... RET
  Convert the plain list at point into a subtree.
org-narrow-to-element	      M-x ... RET
  Narrow buffer to current element.
org-roam-dailies-goto-today   M-x ... RET
  Find the daily-note for today, creating it if necessary.
org-shiftcontrolright	      M-x ... RET
  Switch to next TODO set.
org-table-blank-field	      M-x ... RET
  Blank the current table field or active region.
org-table-fedit-abort	      M-x ... RET
  Abort editing formulas, without installing the changes.
org-table-move-cell-right     M-x ... RET
  Move a single cell right in a table.
org-table-move-row-up	      M-x ... RET
  Move table row up.
org-toggle-ordered-property   M-x ... RET
  Toggle the ORDERED property of the current entry.
org-toggle-radio-button	      M-x ... RET
  Toggle off all checkboxes and toggle on the one at point.
orgtbl-toggle-comment	      M-x ... RET
  Comment or uncomment the orgtbl at point.
outline-next-visible-heading  M-x ... RET
  Move to the next visible heading line.
outline-show-branches	      M-x ... RET
  Show all subheadings of this heading, but not their bodies.
package-vc-update-all	      M-x ... RET
  Attempt to update all installed VC packages.
popwin:find-file-tail	      M-x ... RET
  Edit file FILENAME with popup window by `popwin:popup-buffer-tail'.
popwin:universal-display      M-x ... RET
  Call the following command interactively with letting
  `popwin:special-display-config' be
  `popwin:universal-display-config'.
pr-toggle-file-duplex	      M-x ... RET
  Toggle duplex for PostScript file.
pr-toggle-ghostscript	      M-x ... RET
  Toggle printing using ghostscript.
pr-toggle-upside-down	      M-x ... RET
  Toggle upside-down.
project-shell-command	      M-x ... RET
  Run `shell-command' in the current project's root directory.
quickrun-compile-only	      M-x ... RET
  Exec only compilation.
quickrun-replace-region	      M-x ... RET
  Run commands with specified region and replace.
rfc-mode-forward-page	      M-x ... RET
  Scroll to the next page of the current buffer.
rfc-mode-reload-index	      M-x ... RET
  Reload the RFC document index from its original file.
search-emacs-glossary	      <menu-bar> <help-menu> <search-documentation> <emacs-terminology>
  Display the Glossary node of the Emacs manual in Info mode.
siri-shortcuts-create	      M-x ... RET
  Open the Shortcuts editor to create a new shortcut.
switch-to-completions	      M-x ... RET
  Select the completion list window.
symbol-overlay-rename	      M-x ... RET
  Rename symbol at point on all its occurrences.
thing-cut-parentheses	      M-x ... RET
  Cut content in match parentheses.
thing-cut-to-line-end	      M-x ... RET
  Cut content from current point to line end.
vc-git-stash-snapshot	      M-x ... RET
  Create a stash with the current tree state.
wdired-change-to-wdired-mode  M-x ... RET
  Put a Dired buffer in Writable Dired (WDired) mode.
woman-dired-find-file	      M-x ... RET
  In dired, run the WoMan man-page browser on this file.
yas-exit-all-snippets	      M-x ... RET
  Exit all snippets.
yas/exit-all-snippets	      M-x ... RET
  Exit all snippets.
Info-history-back-menu	      M-x ... RET
  Pop up the menu with a list of previously visited Info nodes.
avy-org-goto-heading-timer    M-x ... RET
  Read one or many characters and jump to matching Org headings.
backward-to-indentation	      M-x ... RET
  Move backward ARG lines and position at first nonblank character.
cal-tex-cursor-week2-summary  M-x ... RET
  Make a two page LaTeX calendar for one week, with optional diary
  entries.
calendar-print-other-dates    M-x ... RET
  Show dates on other calendars for date under the cursor.
customize-apropos-faces	      <menu-bar> <options> <customize> <customize-apropos-faces>
  Customize all loaded faces matching REGEXP.
diary-insert-cyclic-entry     M-x ... RET
  Insert a cyclic diary entry starting at the date given by point.
epkg-list-keyworded-packages  M-x ... RET
  Display a list of packages that have KEYWORD set.
eww-set-character-encoding    M-x ... RET
  Set character encoding to CHARSET.
find-function-other-frame     M-x ... RET
  Find, in another frame, the definition of FUNCTION near point.
find-variable-other-frame     M-x ... RET
  Find, in another frame, the definition of VARIABLE near point.
gnus-article-delete-part      M-x ... RET
  Delete MIME part N and add some information about the removed part.
gnus-group-kill-all-zombies   M-x ... RET
  Kill all zombie newsgroups.
gnus-mime-view-part-as-type   M-x ... RET
  Choose a MIME media type, and view the part as such.
gnus-summary-expand-window    M-x ... RET
  Make the summary buffer take up the entire Emacs frame.
gnus-summary-first-article    M-x ... RET
  Select the first article.
gnus-summary-sort-by-chars    M-x ... RET
  Sort the summary buffer by article length.
gnus-summary-toggle-threads   M-x ... RET
  Toggle showing conversation threads.
kmacro-call-ring-2nd-repeat   M-x ... RET
  Execute second keyboard macro in macro ring.
magit-ediff-show-commit	      M-x ... RET
  Show changes introduced by COMMIT using Ediff.
makefile-automake-mode	      M-x ... RET
  An adapted `makefile-mode' that knows about automake.
markdown-kill-thing-at-point  M-x ... RET
  Kill thing at point and add important text, without markup, to kill
  ring.
message-news-other-window     M-x ... RET
  Start editing a news article to be sent.
org-koma-letter-export-to-pdf M-x ... RET
  Export current buffer as a KOMA Scrlttr2 letter (pdf).
org-table-hline-and-move      M-x ... RET
  Insert a hline and move to the row below that line.
org-table-toggle-column-width M-x ... RET
  Shrink or expand current column in an Org table.
org-texinfo-export-to-texinfo M-x ... RET
  Export current buffer to a Texinfo file.
origami-close-all-nodes	      M-x ... RET
  Recursively close every fold node in BUFFER.
outline-move-subtree-up	      M-x ... RET
  Move the current subtree up past ARG headlines of the same level.
scroll-bar-horizontal-drag    M-x ... RET
  Scroll the window horizontally by dragging the scroll bar slider.
tabulated-list-col-sort	      M-x ... RET
  Sort Tabulated List entries by the column of the mouse click E.
windmove-display-new-tab      M-x ... RET
  Display the next buffer in a new tab.
advertised-widget-backward    M-x ... RET
  Move point to the previous field or button.
cal-tex-cursor-filofax-2week  M-x ... RET
  Two-weeks-at-a-glance Filofax style calendar for week cursor is in.
cal-tex-cursor-filofax-year   M-x ... RET
  Make a Filofax one page yearly calendar of year indicated by cursor.
copyright-update-directory    M-x ... RET
  Update copyright notice for all files in DIRECTORY matching MATCH.
diary-insert-weekly-entry     M-x ... RET
  Insert a weekly diary entry for the day of the week indicated by
  point.
diary-insert-yearly-entry     M-x ... RET
  Insert an annual diary entry for the day of the year indicated by
  point.
emerge-files-with-ancestor    M-x ... RET
  Run Emerge on two files, giving another file as the ancestor.
enlarge-window-horizontally   M-x ... RET
  Make selected window DELTA columns wider.
gnus-article-date-english     M-x ... RET
  Run `article-date-english' in the article buffer.
gnus-article-date-iso8601     M-x ... RET
  Run `article-date-iso8601' in the article buffer.
gnus-article-display-face     M-x ... RET
  Run `article-display-face' in the article buffer.
gnus-article-hide-headers     M-x ... RET
  Run `article-hide-headers' in the article buffer.
gnus-article-replace-part     M-x ... RET
  Replace MIME part N with an external body.
gnus-article-strip-banner     M-x ... RET
  Run `article-strip-banner' in the article buffer.
gnus-article-unsplit-urls     M-x ... RET
  Run `article-unsplit-urls' in the article buffer.
gnus-summary-limit-to-score   M-x ... RET
  Limit to articles with score at or above SCORE.
gnus-summary-sort-by-author   M-x ... RET
  Sort the summary buffer by author name alphabetically.
gnus-summary-sort-by-extra    M-x ... RET
  Sort the summary buffer using an extra header.
gnus-summary-sort-by-lines    M-x ... RET
  Sort the summary buffer by the number of lines.
magit-rebase-interactive      M-x ... RET
  Start an interactive rebase sequence.
magit-stash-format-patch      M-x ... RET
  Create a patch from STASH
mail-quote-printable-region   M-x ... RET
  Convert the region to the "quoted printable" Q encoding.
markdown-backward-paragraph   M-x ... RET
  Move the point to the start of the current paragraph.
markdown-toggle-gfm-checkbox  M-x ... RET
  Toggle GFM checkbox at point.
message-newline-and-reformat  M-x ... RET
  Insert four newlines, and then reformat if inside quoted text.
multi-isearch-buffers-regexp  M-x ... RET
  Start multi-buffer regexp Isearch on a list of BUFFERS.
org-attach-dired-to-subtree   M-x ... RET
  Attach FILES marked or current file in `dired' to subtree in other
  window.
org-babel-lob-execute-maybe   M-x ... RET
  Execute a Library of Babel source block, if appropriate.
org-md-convert-region-to-md   M-x ... RET
  Assume the current region has Org syntax, and convert it to
  Markdown.
origami-forward-toggle-node   M-x ... RET
  Like `origami-toggle-node' but search forward in BUFFER for a fold
  node.
restart-emacs-start-new-emacs M-x ... RET
  Start a new instance of Emacs.
set-buffer-file-coding-system <menu-bar> <options> <mule> <set-various-coding-system> <set-buffer-file-coding-system>
  Set the file coding-system of the current buffer to CODING-SYSTEM.
set-clipboard-coding-system   M-x ... RET
  Make CODING-SYSTEM used for communicating with other X clients.
set-selection-coding-system   <menu-bar> <options> <mule> <set-various-coding-system> <set-selection-coding-system>
  Make CODING-SYSTEM used for communicating with other X clients.
switch-to-buffer-other-window M-x ... RET
  Select the buffer specified by BUFFER-OR-NAME in another window.
table-split-cell-vertically   M-x ... RET
  Split current cell vertically.
terminal-focus-reporting-mode M-x ... RET
  Minor mode for terminal focus reporting integration.
  Value: nil
Info-goto-emacs-command-node  <menu-bar> <help-menu> <search-documentation> <lookup-command-in-manual>
  Go to the Info node in the Emacs manual for command COMMAND.
browse-url-with-browser-kind  M-x ... RET
  Browse URL with a browser of the given browser KIND.
comment-or-uncomment-region   M-x ... RET
  Call `comment-region', unless the region only consists of comments,
epkg-list-packages-by-author  M-x ... RET
  Display a list of packages authored or maintained by AUTHOR.
global-highlight-changes-mode M-x ... RET
  Toggle Highlight-Changes mode in all buffers.
  Value: nil
gnus-article-describe-key     M-x ... RET
  Display documentation of the function invoked by KEY.
gnus-group-quick-select-group M-x ... RET
  Select the GROUP "quickly".
gnus-mime-action-on-part      M-x ... RET
  Do something with the MIME attachment at (point).
gnus-summary-mark-as-dormant  M-x ... RET
  Mark N articles forward as dormant.
gnus-summary-respool-trace    M-x ... RET
  Trace where the respool algorithm would put this article.
isearch-query-replace-regexp  M-x ... RET
  Start `query-replace-regexp' with string to replace from last search
  string.
magit-diff-while-committing   M-x ... RET
  While committing, show the changes that are about to be committed.
magit-rebase-edit-commit      M-x ... RET
  Edit a single older commit using rebase.
markdown-table-convert-region M-x ... RET
  Convert region from BEGIN to END to table with SEPARATOR.
minibuffer-complete-history   M-x ... RET
  Complete the minibuffer history as far as possible.
strokes-do-complex-stroke     M-x ... RET
  Read a complex stroke from the user and then execute its command.
toggle-indicate-empty-lines   <menu-bar> <options> <showhide> <showhide-fringe> <indicate-empty-lines>
  Toggle whether to indicate trailing empty lines in fringe, globally.
article-de-quoted-unreadable  M-x ... RET
  Translate a quoted-printable-encoded article.
article-hide-boring-headers   M-x ... RET
  Toggle hiding of headers that aren't very interesting.
backward-delete-char-untabify DEL
  Delete characters backward, changing tabs into spaces.
calendar-ethiopic-goto-date   M-x ... RET
  Move cursor to Ethiopic date THEDATE.
checkdoc-ispell-interactive   M-x ... RET
  Check the style and spelling of the current buffer interactively.
citre-peek-paste-clue-link    M-x ... RET
  Paste currently browsed line in the peek window as a clue link.
dired-do-query-replace-regexp M-x ... RET
  Do `query-replace-regexp' of FROM with TO, on all marked files.
ebrowse-tags-complete-symbol  M-x ... RET
  Perform completion on the C++ symbol preceding point.
epa-decrypt-armor-in-region   M-x ... RET
  Decrypt OpenPGP armors in the current region between START and END.
font-lock-fontify-buffer      M-x ... RET
  Fontify the current buffer the way the function `font-lock-mode'
  would.
gnus-group-toggle-subscription M-x ... RET
  (not documented)
gnus-group-unmark-group	      M-x ... RET
  Remove the mark from the current group.
gnus-read-ephemeral-bug-group M-x ... RET
  Browse bug reports with IDS in an ephemeral group.
gnus-summary-clear-above      M-x ... RET
  Clear all marks from articles with score above SCORE.
gnus-summary-sort-by-random   M-x ... RET
  Randomize the order in the summary buffer.
gnus-summary-verbose-headers  M-x ... RET
  Toggle permanent full header display.
ibuffer-mouse-popup-menu      M-x ... RET
  Display a menu of operations.
image-dired-mark-tagged-files M-x ... RET
  Use REGEXP to mark files with matching tag.
magit-log-move-to-parent      M-x ... RET
  Move to the Nth parent of the current commit.
mail-unquote-printable-region M-x ... RET
  Undo the "quoted printable" encoding in buffer from BEG to END.
markdown-live-preview-export  M-x ... RET
  Export to XHTML using `markdown-export'.
markdown-outdent-region	      M-x ... RET
  Call `markdown-indent-region' on region from BEG to END with prefix.
org-decrease-number-at-point  M-x ... RET
  Decrement the number at point.
org-increase-number-at-point  M-x ... RET
  Increment the number at point.
org-publish-current-project   M-x ... RET
  Publish the project associated with the current file.
org-table-beginning-of-field  M-x ... RET
  Move to the beginning of the current table field.
outline-forward-same-level    M-x ... RET
  Move forward to the ARG'th subheading at same level as this one.
package-menu-filter-by-status M-x ... RET
  Filter the "*Packages*" buffer by STATUS.
popwin:display-last-buffer    M-x ... RET
  Show the last popup buffer with the same configuration.
profiler-report-expand-entry  M-x ... RET
  Expand entry at point.
profiler-report-find-entry    M-x ... RET
  Find entry at point.
revert-buffer-with-fine-grain M-x ... RET
  Revert buffer preserving markers, overlays, etc.
widget-key-sequence-read-event M-x ... RET
  (not documented)
acm-complete-quick-access     M-x ... RET
  Insert a candidate visible on a ROW matched by a quick-access key
  binding.
calendar-bahai-goto-date      M-x ... RET
  Move cursor to Bahá’í date DATE; echo Bahá’í date unless NOECHO is
  non-nil.
checkdoc-eval-current-buffer  M-x ... RET
  Evaluate and check documentation for the current buffer.
company-graphviz-dot-backend  M-x ... RET
  Company backend for `graphviz-dot-mode'.
epa-file-name-regexp-update   M-x ... RET
  Update `file-name-handler-alist' after configuring outside Custom.
git-gutter:previous-diff      M-x ... RET
  Move to previous diff hunk
git-gutter:previous-hunk      M-x ... RET
  Move to previous diff hunk
gnus-activate-all-groups      M-x ... RET
  Activate absolutely all groups.
gnus-group-make-group-simple  M-x ... RET
  Add a new newsgroup.
ibuffer-copy-filename-as-kill M-x ... RET
  Copy filenames of marked (or next ARG) buffers into the kill ring.
magit-branch-and-checkout     M-x ... RET
  Create and checkout BRANCH at branch or revision START-POINT.
magit-rebase-autosquash	      M-x ... RET
  Combine squash and fixup commits with their intended targets.
markdown-move-list-item-up    M-x ... RET
  Move the current list item up in the list when possible.
menu-bar-enable-clipboard     M-x ... RET
  Make CUT, PASTE and COPY (keys and menu bar items) use the
  clipboard.
message-mail-other-frame      M-x ... RET
  Like `message-mail' command, but display mail buffer in another
  frame.
next-matching-history-element M-x ... RET
  Find the next history element that matches REGEXP.
org-html-htmlize-generate-css M-x ... RET
  Create the CSS for all font definitions in the current Emacs
  session.
org-table-rotate-recalc-marks M-x ... RET
  Rotate the recalculation mark in the first column.
package-menu-mark-delete      M-x ... RET
  Mark a package for deletion and move to the next line.
package-menu-mark-unmark      M-x ... RET
  Clear any marks on a package and move to the next line.
recentf-open-most-recent-file M-x ... RET
  Open the Nth most recent file.
shr-maybe-probe-and-copy-url  M-x ... RET
  Copy the URL under point to the kill ring.
strokes-describe-stroke	      M-x ... RET
  Displays the command which STROKE maps to, reading STROKE
  interactively.
typescript-ts-base-mode	      M-x ... RET
  Major mode for editing TypeScript.
Buffer-menu-unmark-all	      M-x ... RET
  Cancel all requested operations on buffers.
ace-pinyin-goto-word-0	      M-x ... RET
  Ace-pinyin replacement of `avy-goto-word-0'.
ace-pinyin-goto-word-1	      M-x ... RET
  Ace-pinyin replacement of `avy-goto-word-1'.
ace-pinyin-jump-char-2	      M-x ... RET
  Ace-pinyin replacement of `avy-goto-char-2'.
align-unhighlight-rule	      M-x ... RET
  Remove any highlighting that was added by `align-highlight-rule'.
amread-pause-or-resume	      M-x ... RET
  Pause or resume amread.
bibtex-ispell-abstract	      M-x ... RET
  Check abstract of BibTeX entry for spelling errors.
bibtex-narrow-to-entry	      M-x ... RET
  Narrow buffer to current BibTeX entry.
browse-url-button-copy	      M-x ... RET
  Copy the URL under point.
citre-peek-next-branch	      M-x ... RET
  Switch to the next branch under current symbol.
citre-peek-prev-branch	      M-x ... RET
  Switch to the previous branch under current symbol.
color-rg-rerun-literal	      M-x ... RET
  Re-search as literal.
cperl-perldoc-at-point	      M-x ... RET
  Run a `perldoc' on the word around point.
diff-hl-show-hunk-next	      M-x ... RET
  Go to next hunk/change and show it.
dired-mode-regexp-menu	      M-x ... RET
  Regexp menu for Dired mode.
  Value: (keymap "Regexp" (Mark\ Containing... menu-item "Mark Containing..." dired-m…
dired-mode-subdir-menu	      M-x ... RET
  Subdir menu for Dired mode.
  Value: (keymap "Subdir" (Insert\ This\ Subdir menu-item "Insert This Subdir" dired-…
dired-unmark-all-marks	      M-x ... RET
  Remove all marks from all files in the Dired buffer.
edebug-next-breakpoint	      M-x ... RET
  Move point to the next breakpoint, or first if none past point.
edebug-previous-result	      M-x ... RET
  Print the previous result.
  Value: nil
edebug-visit-eval-list	      M-x ... RET
  Switch to the evaluation list buffer "*edebug*".
elisp-enable-lexical-binding  M-x ... RET
  Make the current buffer use `lexical-binding'.
elisp-refs-visit-match	      M-x ... RET
  Goto the search result at point.
epa-key-list-mode-menu	      M-x ... RET
  Menu for `epa-key-list-mode'.
  Value: (keymap "Keys" (Export\ Keys menu-item "Export Keys" epa-export-keys :help "…
ethio-tex-to-fidel-buffer     M-x ... RET
  Convert fidel-tex commands in the current buffer into FIDEL chars.
eww-open-in-new-buffer	      M-x ... RET
  Fetch link at point in a new EWW buffer.
find-file-read-only-other-tab M-x ... RET
  Edit file FILENAME, in another tab, but don't allow changes.
find-function-at-point	      M-x ... RET
  Find directly the function at point in the other window.
find-variable-at-point	      M-x ... RET
  Find directly the variable at point in the other window.
git-gutter:end-of-hunk	      M-x ... RET
  Move to end of current diff hunk
git-gutter:revert-hunk	      M-x ... RET
  Revert current hunk.
gnus-agent-batch-fetch	      M-x ... RET
  Start Gnus and fetch session.
gnus-article-edit-exit	      M-x ... RET
  Exit the article editing without updating.
gnus-close-all-servers	      M-x ... RET
  Close all servers.
gnus-group-describe-group     M-x ... RET
  Display a description of the current newsgroup.
gnus-group-expire-articles    M-x ... RET
  Expire all expirable articles in the current newsgroup.
gnus-group-list-active	      M-x ... RET
  List all groups that are available from the server(s).
gnus-start-draft-setup	      M-x ... RET
  Make sure the draft group exists.
gnus-symbolic-argument	      M-x ... RET
  Read a symbolic argument and a command, and then execute command.
hashcash-insert-payment	      M-x ... RET
  Insert X-Payment and X-Hashcash headers with a payment for ARG.
help-fns-edit-variable	      M-x ... RET
  Edit the variable under point.
help-for-help-internal	      M-x ... RET
  Help command.
help-goto-lispref-info	      M-x ... RET
  View the Emacs Lisp manual *info* node of the current help item.
ibuffer-invert-sorting	      M-x ... RET
  Toggle whether or not sorting is in reverse order.
ibuffer-mark-by-locked	      M-x ... RET
  Mark all locked buffers.
ibuffer-mode-mark-menu	      M-x ... RET
  Mark menu for `ibuffer'.
  Value: (keymap "Mark" (Toggle\ marks menu-item "Toggle marks" ibuffer-toggle-marks …
ibuffer-mode-view-menu	      M-x ... RET
  View menu for `ibuffer'.
  Value: (keymap "View" (View\ this\ buffer menu-item "View this buffer" ibuffer-visi…
ispell-hunspell-add-multi-dic M-x ... RET
  Add DICT of the form "DICT1,DICT2,..." to `ispell-dictionary-alist'.
kill-buffer-and-window	      M-x ... RET
  Kill the current buffer and delete the selected window.
markdown-follow-link-at-point M-x ... RET
  Open the non-wiki link at point or EVENT.
menu-bar-no-scroll-bar	      <menu-bar> <options> <showhide> <showhide-scroll-bar> <none>
  Turn off scroll bars.
message-insert-headers	      M-x ... RET
  Generate the headers for the article.
message-mark-inserted-region  M-x ... RET
  Mark some region in the current article with enclosing tags.
mml-secure-encrypt-pgp	      M-x ... RET
  Add MML tags to PGP encrypt this MML part.
mode-line-other-buffer	      M-x ... RET
  Switch to the most recently selected buffer other than the current
  one.
mouse-drag-top-right-corner   <top-right-corner> <down-mouse-1>
  Drag top right corner of a frame with the mouse.
next-multiframe-window	      M-x ... RET
  Select the next window, regardless of which frame it is on.
normal-erase-is-backspace-mode M-x ... RET
  Toggle the Erase and Delete mode of the Backspace and Delete keys.
ns-delete-working-text	      M-x ... RET
  Delete working text and clear `ns-working-overlay'.
org-babel-previous-src-block  M-x ... RET
  Jump to the previous source block.
org-fold-show-branches	      M-x ... RET
  Show all subheadings of this heading, but not their bodies.
org-footnote-normalize	      M-x ... RET
  Turn every footnote in buffer into a numbered one.
org-insert-todo-subheading    M-x ... RET
  Insert a new subheading with TODO keyword or checkbox and demote it.
org-occur-in-agenda-files     M-x ... RET
  Call `multi-occur' with buffers for all agenda files.
org-roam-ui-sync-theme	      M-x ... RET
  Sync your current Emacs theme with org-roam-ui.
org-speed-command-help	      M-x ... RET
  Show the available speed commands.
org-table-fedit-ref-up	      M-x ... RET
  Shift the reference at point one row/hline up.
outline-insert-heading	      M-x ... RET
  Insert a new heading at same depth at point.
package-menu-filter-marked    M-x ... RET
  Filter "*Packages*" buffer by non-empty upgrade mark.
package-menu-mode-menu	      M-x ... RET
  Menu for `package-menu-mode'.
  Value: (keymap "Package" (Describe\ Package menu-item "Describe Package" package-me…
pr-toggle-header-frame	      M-x ... RET
  Toggle printing header frame.
rfc-mode-backward-page	      M-x ... RET
  Scroll to the previous page of the current buffer.
rxt-convert-to-strings	      M-x ... RET
  Convert regex at point to RX syntax. Chooses Emacs or PCRE syntax by
  major mode.
select-tags-table-quit	      M-x ... RET
  Kill the buffer and delete the selected window.
select-tags-table-select      M-x ... RET
  Select the tags table named on this line.
set-file-name-coding-system   <menu-bar> <options> <mule> <set-various-coding-system> <set-file-name-coding-system>
  Set coding system for decoding and encoding file names to
  CODING-SYSTEM.
shell-narrow-to-prompt	      M-x ... RET
  Narrow buffer to the command line (and any following command output)
  at point.
sis--inline-deactivate	      M-x ... RET
  Deactivate the inline region overlay.
tab-bar-undo-close-tab	      M-x ... RET
  Restore the most recently closed tab.
tabulated-list-next-column    M-x ... RET
  Go to the start of the next column after point on the current line.
tags-reset-tags-tables	      M-x ... RET
  Reset tags state to cancel effect of any previous
  \[visit-tags-table] or \[find-tag].
thing-cut-whole-buffer	      M-x ... RET
  Cut content of the current buffer.
thing-replace-filename	      M-x ... RET
  Replace filename at current point with kill ring.
thing-replace-sentence	      M-x ... RET
  Replace sentence around point with the content of currnt line.
url-setup-privacy-info	      M-x ... RET
  Setup variables that expose info about you and your system.
vc-msg-sdk-quit-window	      M-x ... RET
  Quit window.
view-external-packages	      <menu-bar> <help-menu> <external-packages>, <help> C-e, <f1> C-e
  Display info on where to get more Emacs packages.
widget-narrow-to-field	      M-x ... RET
  Narrow to field.
xref-next-line-no-show	      M-x ... RET
  Move to the next xref but don't display its source.
xref-prev-line-no-show	      M-x ... RET
  Move to the previous xref but don't display its source.
yas-expand-from-keymap	      M-x ... RET
  Expand/run snippets from keymaps, possibly falling back to original
  binding.
yas/expand-from-keymap	      M-x ... RET
  Directly expand some snippets, searching `yas--direct-keymaps'.
Buffer-menu-delete-backwards  M-x ... RET
  Mark the buffer on this Buffer Menu line for deletion, and move up.
Buffer-menu-mouse-select      M-x ... RET
  Select the buffer whose line you click on.
avy-goto-word-or-subword-1    M-x ... RET
  Forward to `avy-goto-subword-1' or `avy-goto-word-1'.
color-rg-customized-search    M-x ... RET
  Rerun rg with customized arguments. This function will give
comint-delchar-or-maybe-eof   M-x ... RET
  Delete ARG characters forward or send an EOF to subprocess.
customize-apropos-groups      M-x ... RET
  Customize all loaded groups matching REGEXP.
customize-apropos-options     <menu-bar> <options> <customize> <customize-apropos-options>
  Customize all loaded customizable options matching REGEXP.
elisp-eval-region-or-buffer   M-x ... RET
  Evaluate the forms in the active region or the whole current buffer.
ethio-fidel-to-tex-buffer     M-x ... RET
  Convert each FIDEL characters in the current buffer into a fidel-tex
  command.
file-cache-add-directory-list M-x ... RET
  Add DIRECTORIES (a list of directory names) to the file cache.
find-function-other-window    M-x ... RET
  Find, in another window, the definition of FUNCTION near point.
find-variable-other-window    M-x ... RET
  Find, in another window, the definition of VARIABLE near point.
flyspell-deplacement-command  M-x ... RET
  Set COMMAND that implement cursor movements, for Flyspell.
gnus-group-delete-groups      M-x ... RET
  Delete the current group.  Only meaningful with editable groups.
gnus-group-edit-local-kill    M-x ... RET
  Edit a local kill file.
gnus-mailing-list-insinuate   M-x ... RET
  Setup group parameters from List-Post header.
gnus-summary-execute-command  M-x ... RET
  Search forward for an article whose HEADER matches REGEXP and
  execute COMMAND.
gnus-summary-put-mark-as-read M-x ... RET
  Mark the current article as read.
gnus-summary-sort-by-number   M-x ... RET
  Sort the summary buffer by article number.
image-dired-show-all-from-dir M-x ... RET
  Make a thumbnail buffer for all images in DIR and display it.
image-mouse-decrease-size     M-x ... RET
  Decrease the image size using the mouse.
image-mouse-increase-size     M-x ... RET
  Increase the image size using the mouse.
isearch-yank-symbol-or-char   M-x ... RET
  Pull next character or symbol from buffer into search string.
minibuffer-depth-indicate-mode M-x ... RET
  Toggle Minibuffer Depth Indication mode.
  Value: nil
org-advertized-archive-subtree M-x ... RET
  Move the current subtree to the archive.
org-insert-structure-template M-x ... RET
  Insert a block structure of the type #+begin_foo/#+end_foo.
org-roam-dailies-capture-date M-x ... RET
  Create an entry in the daily-note for a date using the calendar.
project-forget-projects-under M-x ... RET
  Forget all known projects below a directory DIR.
shell-command-with-editor-mode M-x ... RET
  Teach `shell-command' to use current Emacs instance as editor.
  Value: nil
avy-kill-ring-save-whole-line M-x ... RET
  Select line and save the whole selected line as if killed, but don’t
  kill it.
comint-redirect-send-command  M-x ... RET
  Send COMMAND to process in current buffer, with output to
  OUTPUT-BUFFER.
diary-insert-monthly-entry    M-x ... RET
  Insert a monthly diary entry for the day of the month indicated by
  point.
global-whitespace-newline-mode M-x ... RET
  Toggle global newline visualization (Global Whitespace Newline
  mode).
  Value: nil
gnus-cache-generate-active    M-x ... RET
  Generate the cache active file.
gnus-group-check-bogus-groups M-x ... RET
  Check bogus newsgroups.
gnus-group-prev-unread-group  M-x ... RET
  Go to previous N'th unread newsgroup.
gnus-summary-hide-all-threads M-x ... RET
  Hide all thread subtrees.
highlight-changes-visible-mode M-x ... RET
  Toggle visibility of highlighting due to Highlight Changes mode.
markdown-toggle-markup-hiding M-x ... RET
  Toggle the display or hiding of markup.
mpv-insert-playback-position  M-x ... RET
  Insert the current playback position at point.
next-line-or-history-element  M-x ... RET
  Move cursor vertically down ARG lines, or to the next history
  element.
org-archive-subtree-default   M-x ... RET
  Archive the current subtree with the default command.
org-beamer-select-environment M-x ... RET
  Select the environment to be used by beamer for this entry.
org-compute-property-at-point M-x ... RET
  Compute the property at point.
project-or-external-find-file M-x ... RET
  Visit a file (with completion) in the current project or external
  roots.
project-other-frame-command   M-x ... RET
  Run project command, displaying resultant buffer in another frame.
checkdoc-ispell-message-text  M-x ... RET
  Check the style and spelling of message text interactively.
comint-forward-matching-input M-x ... RET
  Search forward through buffer for input fields that match REGEXP.
custom-theme-visit-theme      M-x ... RET
  Set up a Custom buffer to edit custom theme THEME.
global-origami-indicators-mode M-x ... RET
  Toggle Origami-Indicators mode in all buffers.
  Value: nil
gnus-article-date-original    M-x ... RET
  Run `article-date-original' in the article buffer.
gnus-group-expunge-group      M-x ... RET
  Expunge deleted articles in current nnimap GROUP.
gnus-summary-reparent-thread  M-x ... RET
  Make the current article child of the marked (or previous) article.
icomplete-forward-completions M-x ... RET
  Step forward completions by one entry.
isearch-toggle-lax-whitespace M-x ... RET
  Toggle lax-whitespace searching on or off.
magit-sparse-checkout-disable M-x ... RET
  Convert sparse checkout to full checkout.
markdown-insert-gfm-checkbox  M-x ... RET
  Add GFM checkbox at point.
markdown-insert-gfm-code-block M-x ... RET
  Insert GFM code block for language LANG.
message-fill-yanked-message   M-x ... RET
  Fill the paragraphs of a message yanked into this one.
minibuffer-complete-defaults  M-x ... RET
  Complete minibuffer defaults as far as possible.
mouse-secondary-save-then-kill M-<mouse-3>
  Set the secondary selection and save it to the kill ring.
org-timer-pause-or-continue   M-x ... RET
  Pause or continue the relative or countdown timer.
pr-ps-buffer-using-ghostscript M-x ... RET
  Print buffer using PostScript through ghostscript.
pyim-cregexp-convert-at-point M-x ... RET
  将光标前的字符串按拼音的规则转换为一个搜索中文的 regexp.
shell-dynamic-complete-command M-x ... RET
  Dynamically complete the command at point.
tab-bar-switch-to-recent-tab  M-x ... RET
  Switch to ARGth most recently visited tab.
autothemer-generate-templates M-x ... RET
  Autogenerate customizations for unthemed faces (optionally by
  REGEXP).
calendar-hebrew-goto-date     M-x ... RET
  Move cursor to Hebrew DATE; echo Hebrew date unless NOECHO is
  non-nil.
calendar-julian-goto-date     M-x ... RET
  Move cursor to Julian DATE; echo Julian date unless NOECHO is
  non-nil.
comint-get-next-from-history  M-x ... RET
  After fetching a line from input history, this fetches the following
  line.
exec-path-from-shell-copy-env M-x ... RET
  Set the environment variable $NAME from the user's shell.
gnus-article-jump-to-part     M-x ... RET
  Jump to MIME part N.
gnus-group-best-unread-group  M-x ... RET
  Go to the group with the highest level.
gnus-outlook-deuglify-article M-x ... RET
  Full deuglify of broken Outlook (Express) articles.
gnus-summary-button-forward   M-x ... RET
  Move point to the next field or button in the article.
gnus-summary-set-bookmark     M-x ... RET
  Set a bookmark in current article.
gnus-summary-widget-forward   M-x ... RET
  Move point to the next field or button in the article.
highlight-changes-rotate-faces M-x ... RET
  "Age" changes if in Highlight Changes mode and the changes are
  visible.
image-transform-set-percent   M-x ... RET
  Prompt for a percentage, and resize the current image to that size.
japanese-hiragana-region      M-x ... RET
  Convert Japanese `katakana' chars in the region to `hiragana' chars.
lsp-bridge-call-hierarchy-mode M-x ... RET
  Major mode derived from `text-mode' by `define-derived-mode'.
magit-dired-am-apply-patches  M-x ... RET
  In Dired, apply the marked (or next ARG) files as patches.
magit-sparse-checkout-reapply M-x ... RET
  Reapply the sparse checkout rules to the working tree.
mail-source-report-new-mail   M-x ... RET
  Toggle whether to report when new mail is available.
  Value: nil
markdown-forward-same-level   M-x ... RET
  Move forward to the ARG'th heading at same level as this one.
markdown-table-forward-cell   M-x ... RET
  Go to the next cell in the table.
org-babel-examplify-region    M-x ... RET
  Comment out region using the inline `==' or `: ' org example quote.
org-babel-examplize-region    M-x ... RET
  Comment out region using the inline `==' or `: ' org example quote.
outline-backward-same-level   M-x ... RET
  Move backward to the ARG'th subheading at same level as this one.
package-menu-filter-by-archive M-x ... RET
  Filter the "*Packages*" buffer by ARCHIVE.
package-menu-filter-by-keyword M-x ... RET
  Filter the "*Packages*" buffer by KEYWORD.
package-menu-filter-by-version M-x ... RET
  Filter the "*Packages*" buffer by VERSION and PREDICATE.
search-forward-help-for-help  <help> C-s, <f1> C-s
  Search forward in the help-for-help window.
toggle-save-desktop-globally  <menu-bar> <options> <save-desktop>
  Toggle whether to visit desktop of previous session when restarting
  Emacs.
toggle-uniquify-buffer-names  <menu-bar> <options> <uniquify>
  Toggle whether to uniquify buffer names by adding parent directory
  names.
View-scroll-half-page-forward M-x ... RET
  Scroll forward a "half page" (or prefix LINES) lines in View mode.
citre-global-update-database  M-x ... RET
  Update the gtags database in use.
custom-toggle-hide-all-widgets M-x ... RET
  Hide or show details of all customizable settings in a Custom
  buffer.
ebrowse-tags-search-member-use M-x ... RET
  Search for call sites of a member.
edebug-remove-instrumentation M-x ... RET
  Remove Edebug instrumentation from FUNCTIONS.
gnus-group-add-to-virtual     M-x ... RET
  Add the current group to a virtual group.
gnus-summary-limit-to-address M-x ... RET
  Limit the summary buffer to articles with the given ADDRESS.
gnus-summary-save-article-file M-x ... RET
  Append the current article to a file.
gnus-summary-sort-by-subject  M-x ... RET
  Sort the summary buffer by subject alphabetically.  `Re:'s are
  ignored.
ibuffer-backwards-next-marked M-x ... RET
  Move backwards by COUNT marked buffers (default 1).
isearch-forward-thing-at-point M-s M-.
  Do incremental search forward for the "thing" found near point.
  Value: (region url symbol sexp)
magit-bundle-create-tracked   M-x ... RET
  Create and track a new bundle.
message-idna-to-ascii-rhs     M-x ... RET
  Possibly IDNA encode non-ASCII domain names in From:, To: and Cc:
  headers.
message-mail-other-window     M-x ... RET
  Like `message-mail' command, but display mail buffer in another
  window.
mouse-drag-region-rectangle   C-M-<down-mouse-1>
  Set the region to the rectangle that the mouse is dragged over.
move-to-window-line-top-bottom M-r
  Position point relative to window.
org-beginning-of-item-list    M-x ... RET
  Go to the beginning item of the current list or sublist.
org-convert-to-oddeven-levels M-x ... RET
  Convert an Org file with only odd levels to one with odd/even
  levels.
org-previous-visible-heading  M-x ... RET
  Move to the previous visible heading.
origami-toggle-all-nodes      M-x ... RET
  Toggle all fold nodes in the buffer recursively open or recursively
  closed.
package-menu-describe-package M-x ... RET
  Describe the current package.
text-property-search-backward M-x ... RET
  Search for the previous region of text whose PROPERTY matches VALUE.
5x5-crack-mutating-best	      M-x ... RET
  Attempt to crack 5x5 by mutating the best solution.
Buffer-menu-this-window	      M-x ... RET
  Select this line's buffer in this window.
abbrev-edit-save-to-file      M-x ... RET
  Save to FILE all the user-level abbrev definitions in current
  buffer.
apropos-previous-symbol	      M-x ... RET
  Move cursor back to the last symbol in an `apropos-mode' buffer.
article-emojize-symbols	      M-x ... RET
  Display symbols (that have an emoji version) as emojis.
article-treat-non-ascii	      M-x ... RET
  Translate many Unicode characters into their ASCII equivalents.
avy-org-refile-as-child	      M-x ... RET
  Refile current heading as first child of heading selected with
  `avy.'
backtrace-forward-frame	      M-x ... RET
  Move forward to the beginning of the next frame.
benchmark-init/activate	      M-x ... RET
  Activate benchmark-init and start collecting data.
cal-tex-cursor-month-landscape M-x ... RET
  Make a LaTeX calendar buffer for the month the cursor is on.
calendar-goto-info-node	      M-x ... RET
  Go to the info node for the calendar.
calendar-iso-print-date	      M-x ... RET
  Show equivalent ISO date for the date under the cursor.
citre-peek-load-session	      M-x ... RET
  Load a peek session.
comint-interrupt-subjob	      M-x ... RET
  Interrupt the current subjob.
comint-redirect-cleanup	      M-x ... RET
  End a Comint redirection.  See `comint-redirect-send-command'.
comint-set-process-mark	      M-x ... RET
  Set the process mark at point.
debugger-list-functions	      M-x ... RET
  Display a list of all the functions now set to debug on entry.
diff-kill-applied-hunks	      M-x ... RET
  Kill all hunks that have already been applied starting at point.
dired-do-isearch-regexp	      M-x ... RET
  Search for a regexp through all marked files using Isearch.
dired-isearch-filenames	      M-x ... RET
  Search for a string using Isearch only in file names in the Dired
  buffer.
  Value: nil
dired-mode-operate-menu	      M-x ... RET
  Operate menu for Dired mode.
  Value: (keymap "Operate" (Copy\ to... menu-item "Copy to..." dired-do-copy :help "C…
doc-view-toggle-display	      M-x ... RET
  Toggle between editing a document as text or viewing it.
edebug-delete-eval-item	      M-x ... RET
  Delete the item under point and redisplay.
edebug-pop-to-backtrace	      M-x ... RET
  Display the current backtrace in a `backtrace-mode' window.
edebug-unset-breakpoint	      M-x ... RET
  Clear the breakpoint of nearest sexp.
edebug-update-eval-list	      M-x ... RET
  Replace the evaluation list with the sexps now in the eval buffer.
elisp-demos-for-helpful	      M-x ... RET
  Find a demo for the current `helpful' buffer.
emacs-build-description	      M-x ... RET
  Insert a description of the current Emacs build in the current
  buffer.
emacs-lisp-byte-compile	      <menu-bar> <emacs-lisp> <Byte-compile This File>
  Byte compile the file containing the current buffer.
ert-run-tests-interactively   M-x ... RET
  Run the tests specified by SELECTOR and display the results in a
  buffer.
faceup-render-view-buffer     M-x ... RET
  Convert BUFFER containing Faceup markup to a new buffer and display
  it.
gnus-article-inline-part      M-x ... RET
  Inline MIME part N, which is the numerical prefix.
gnus-group-force-update	      M-x ... RET
  Update `.newsrc' file.
gnus-group-list-zombies	      M-x ... RET
  List all zombie newsgroups in the group buffer.
gnus-summary-isearch-article  M-x ... RET
  Do incremental search forward on the current article.
gnus-summary-morse-message    M-x ... RET
  Morse decode the current article.
gnus-summary-pipe-message     M-x ... RET
  Pipe the current article through PROGRAM.
gnus-summary-top-thread	      M-x ... RET
  Go to the top of the thread.
helpful-visit-reference	      M-x ... RET
  Go to the reference at point.
htmlfontify-copy-and-link-dir M-x ... RET
  Trawl SRCDIR and write fontified-and-hyperlinked output in DSTDIR.
ibuffer-mark-by-content-regexp M-x ... RET
  Mark all buffers whose content matches REGEXP.
image-flip-horizontally	      M-x ... RET
  Horizontally flip the image under point.
isearch-toggle-color-rg	      M-x ... RET
  toggle `color-rg' in isearch-mode.
lsp-bridge-kill-process	      M-x ... RET
  Stop LSP-Bridge process and kill all LSP-Bridge buffers.
magit-run-gitk-branches	      M-x ... RET
  Run `gitk --branches' in the current repository.
mairix-use-saved-search	      M-x ... RET
  Use a saved search for querying Mairix.
markdown-demote-subtree	      M-x ... RET
  Demote the current subtree of ATX headings.
markdown-insert-strike-through M-x ... RET
  Insert markup to make a region or word strikethrough.
markdown-kill-ring-save	      M-x ... RET
  Run Markdown on file and store output in the kill ring.
menu-bar-read-lispintro	      <menu-bar> <help-menu> <more-manuals> <emacs-lisp-intro>
  Display the Introduction to Emacs Lisp Programming in Info mode.
menu-find-file-existing	      <menu-bar> <file> <open-file>
  Edit the existing file FILENAME.
message-goto-newsgroups	      M-x ... RET
  Move point to the Newsgroups header.
message-insinuate-rmail	      M-x ... RET
  Let RMAIL use message to forward.
message-reduce-to-to-cc	      M-x ... RET
  Replace contents of To: header with contents of Cc: or Bcc: header.
mml-secure-sign-pgpauto	      M-x ... RET
  Add MML tags to PGP-auto sign this MML part.
mml-secure-sign-pgpmime	      M-x ... RET
  Add MML tags to PGP/MIME sign this MML part.
org-babel-view-src-block-info M-x ... RET
  Display information on the current source block.
org-cdlatex-underscore-caret  M-x ... RET
  Execute `cdlatex-sub-superscript' in LaTeX fragments.
org-edit-latex-fragment	      M-x ... RET
  Edit LaTeX fragment at point.
org-fold-hide-block-all	      M-x ... RET
  Fold all blocks in the current buffer.
org-table-delete-column	      M-x ... RET
  Delete a column from the table.
org-table-edit-formulas	      M-x ... RET
  Edit the formulas of the current table in a separate buffer.
org-table-fedit-line-up	      M-x ... RET
  Move cursor one line up in the window showing the table.
org-table-insert-column	      M-x ... RET
  Insert a new column into the table.
org-table-move-row-down	      M-x ... RET
  Move table row down.
org-toggle-link-display	      M-x ... RET
  Toggle the literal or descriptive display of links in current
  buffer.
org-update-statistics-cookies M-x ... RET
  Update the statistics cookie, either from TODO or from checkboxes.
package-menu-mark-install     M-x ... RET
  Mark a package for installation and move to the next line.
pp-macroexpand-expression     M-x ... RET
  Macroexpand EXPRESSION and pretty-print its value.
quickrun-select-default	      M-x ... RET
  Update the default.
quoted-printable-decode-region M-x ... RET
  Decode quoted-printable in the region between FROM and TO, per RFC
  2045.
recentf-edit-list-validate    M-x ... RET
  Process the recent list when the edit list dialog is committed.
recentf-open-more-files	      M-x ... RET
  Show a dialog to open a recent file that is not in the menu.
scroll-bar-set-window-start   M-x ... RET
  Set the window start according to where the scroll bar is dragged.
siri-shortcuts-open-app	      M-x ... RET
  Open the Shortcuts app.
tab-bar-move-tab-to-group     M-x ... RET
  Relocate TAB (by default, the current tab) closer to its group.
thing-replace-paragraph	      M-x ... RET
  Replace current paragraph around the point with the content of kill
  ring.
timeclock-when-to-leave-string M-x ... RET
  Return a string representing the end of today's workday.
turn-on-ace-pinyin-mode	      M-x ... RET
  Turn on `ace-pinyin-mode'.
turn-on-gnus-dired-mode	      M-x ... RET
  Convenience method to turn on `gnus-dired-mode'.
vc-find-conflicted-file	      M-x ... RET
  Visit the next conflicted file in the current project.
viet-decode-viqr-buffer	      M-x ... RET
  Convert `VIQR' mnemonics of the current buffer to Vietnamese
  characters.
viet-encode-viqr-buffer	      M-x ... RET
  Convert Vietnamese characters of the current buffer to `VIQR'
  mnemonics.
windmove-swap-states-up	      M-x ... RET
  Swap the states with the window above from the current one.
xref-quit-and-goto-xref	      M-x ... RET
  Quit *xref* buffer, then jump to xref on current line.
backtrace-expand-ellipsis     M-x ... RET
  Expand display of the elided form at BUTTON.
backtrace-help-follow-symbol  M-x ... RET
  Follow cross-reference at POS, defaulting to point.
backtrace-toggle-print-circle M-x ... RET
  Toggle `print-circle' for the backtrace frame at point.
backtrace-toggle-print-gensym M-x ... RET
  Toggle `print-gensym' for the backtrace frame at point.
cal-tex-cursor-year-landscape M-x ... RET
  Make a buffer with LaTeX commands for the year cursor is on.
delete-dir-local-variable     M-x ... RET
  Delete all MODE settings of file-local VARIABLE from .dir-locals.el.
edebug-cancel-edebug-on-entry M-x ... RET
  Cause Edebug to not stop when FUNCTION is called.
elisp-depmap-graphviz-digraph M-x ... RET
  Make a dot file representation of all definitions and references.
epkg-find-file-other-frame    M-x ... RET
  Visit a file in a submodule of `epkg-repository' in another frame.
explain-pause-top-auto-refresh M-x ... RET
  Turn on or off auto-refresh for the BUFFER, or the current buffer if
file-cache-minibuffer-complete M-x ... RET
  Complete a filename in the minibuffer using a preloaded cache.
global-dictionary-tooltip-mode M-x ... RET
  Enable/disable `dictionary-tooltip-mode' for all buffers.
gnus-summary-catchup-and-exit M-x ... RET
  Mark all unread articles in this group as read, then exit.
gnus-summary-catchup-to-here  M-x ... RET
  Mark all unticked articles before the current one as read.
gnus-summary-limit-to-author  M-x ... RET
  Limit the summary buffer to articles that have authors that match a
  regexp.
gnus-summary-limit-to-bodies  M-x ... RET
  Limit the summary buffer to articles that have bodies that match
  MATCH.
gnus-summary-rescan-group     M-x ... RET
  Exit the newsgroup, ask for new articles, and select the newsgroup.
my/eval-current-elisp-func    M-x ... RET
   eval-last-sexp 当前光标处的 function
org-koma-letter-export-as-latex	M-x ... RET
  Export current buffer as a KOMA Scrlttr2 letter.
org-koma-letter-export-to-latex	M-x ... RET
  Export current buffer as a KOMA Scrlttr2 letter (tex).
org-roam-dailies-capture-today M-x ... RET
  Create an entry in the daily-note for today.
outline-move-subtree-down     M-x ... RET
  Move the current subtree down past ARG headlines of the same level.
scroll-bar-toolkit-scroll     <vertical-scroll-bar> <down-mouse-1>
  Handle event EVENT on vertical scroll bar.
table-split-cell-horizontally M-x ... RET
  Split current cell horizontally.
Buffer-menu-toggle-read-only  M-x ... RET
  Toggle read-only status of buffer on this line.
citre-ctags-cmd-buf-add-lang  M-x ... RET
  Insert a language in the command editing buffer.
delete-file-local-variable    M-x ... RET
  Delete all settings of file-local VARIABLE from the Local Variables
  list.
ediff-merge-files-with-ancestor	<menu-bar> <tools> <ediff-merge> <ediff-merge-files-with-ancestor>
  Merge two files with ancestor.
font-lock-after-fontify-buffer M-x ... RET
  Do nothing and return nil.
gnus-summary-respool-query    M-x ... RET
  Query where the respool algorithm would put this article.
gnus-summary-save-article-mail M-x ... RET
  Append the current article to a Unix mail box file.
gnus-treat-newsgroups-picon   M-x ... RET
  Display picons in the Newsgroups and Followup-To headers.
  Value: nil
highlight-lines-matching-regexp	M-s h l
  Highlight all lines that match REGEXP using FACE.
ido-switch-buffer-other-frame M-x ... RET
  Switch to another buffer and show it in another frame.
magit-log-trace-definition    M-x ... RET
  Show log for the definition at point.
make-frame-on-current-monitor M-x ... RET
  Make a frame on the currently selected monitor.
markdown-next-visible-heading M-x ... RET
  Move to the next visible heading line of any level.
org-agenda-list-stuck-projects M-x ... RET
  Create agenda view for projects that are stuck.
org-archive-to-archive-sibling M-x ... RET
  Archive the current heading by moving it under the archive sibling.
org-delete-property-globally  M-x ... RET
  Remove PROPERTY globally, from all entries.
package-list-packages-no-fetch M-x ... RET
  Display a list of packages.
pr-ps-mode-using-ghostscript  M-x ... RET
  Print major mode using PostScript through ghostscript.
profiler-report-toggle-entry  M-x ... RET
  Expand entry at point if the tree is collapsed, otherwise collapse.
project-other-window-command  M-x ... RET
  Run project command, displaying resultant buffer in another window.
thing-copy-to-line-beginning  M-x ... RET
  Copy content from current point tot line beginning.
widget-browse-other-window    M-x ... RET
  Show widget browser for WIDGET in other window.
windmove-display-new-frame    M-x ... RET
  Display the next buffer in a new frame.
with-editor-async-shell-command	M-x ... RET
  Like `async-shell-command' but with `$EDITOR' set.
Info-mouse-follow-nearest-node M-x ... RET
  \<Info-mode-map>Follow a node reference near point.
bookmark-jump-other-frame     M-x ... RET
  Jump to BOOKMARK in another frame.  See `bookmark-jump' for more.
comint-backward-matching-input M-x ... RET
  Search backward through buffer for input fields that match REGEXP.
duplicate-line-or-region-above M-x ... RET
  Duplicate current line or region above.
gnus-article-decode-charset   M-x ... RET
  Run `article-decode-charset' in the article buffer.
gnus-article-display-x-face   M-x ... RET
  Run `article-display-x-face' in the article buffer.
gnus-article-hide-signature   M-x ... RET
  Run `article-hide-signature' in the article buffer.
gnus-group-make-directory-group	M-x ... RET
  Create an nndir group.
gnus-group-read-ephemeral-group	M-x ... RET
  Read GROUP from METHOD as an ephemeral group.
gnus-summary-reparent-children M-x ... RET
  Make PARENT the parent of CHILDREN.
graphviz-turn-on-live-preview M-x ... RET
  Turn on live preview.
highlight-confusing-reorderings	M-x ... RET
  Highlight text in region that might be bidi-reordered in suspicious
  ways.
magit-commit-instant-fixup    M-x ... RET
  Create a fixup commit targeting COMMIT and instantly rebase.
magit-rebase-remove-commit    M-x ... RET
  Remove a single older commit using rebase.
magit-rebase-reword-commit    M-x ... RET
  Reword a single older commit using rebase.
minibuffer-beginning-of-buffer M-x ... RET
  Move to the logical beginning of the minibuffer.
mode-line-toggle-modified     M-x ... RET
  Toggle the buffer-modified flag from the mode-line.
org-cdlatex-environment-indent M-x ... RET
  Execute `cdlatex-environment' and indent the inserted environment.
org-roam-dailies-goto-tomorrow M-x ... RET
  Find the daily-note for tomorrow, creating it if necessary.
rmail-previous-labeled-message M-x ... RET
  Show previous message with one of the labels LABELS.
toggle-horizontal-scroll-bar  M-x ... RET
  Toggle whether or not the selected frame has horizontal scroll bars.
tramp-cleanup-all-connections M-x ... RET
  Flush all Tramp internal objects.
calendar-islamic-goto-date    M-x ... RET
  Move cursor to Islamic DATE; echo Islamic date unless NOECHO is
  non-nil.
comint-append-output-to-file  M-x ... RET
  Append output from interpreter since last input to FILENAME.
diary-chinese-insert-entry    M-x ... RET
  Insert a diary entry for the Chinese date at point.
doc-view-fit-window-to-page   M-x ... RET
  Resize selected window so it just fits the current page.
emerge-buffers-with-ancestor  M-x ... RET
  Run Emerge on two buffers, giving another buffer as the ancestor.
gnus-cloud-download-all-data  M-x ... RET
  Download the Gnus Cloud data and install it.
gnus-summary-describe-group   M-x ... RET
  Describe the current newsgroup.
gnus-summary-limit-to-replied M-x ... RET
  Limit the summary buffer to replied articles.
gnus-summary-limit-to-unread  M-x ... RET
  Limit the summary buffer to articles that are not marked as read.
gnus-summary-mark-as-expirable M-x ... RET
  Mark N articles forward as expirable.
gnus-summary-refer-article    M-x ... RET
  Fetch an article specified by MESSAGE-ID.
ibuffer-mouse-toggle-mark     M-x ... RET
  Toggle the marked status of the buffer chosen with the mouse.
icomplete-backward-completions M-x ... RET
  Step backward completions by one entry.
magit-do-async-shell-command  M-x ... RET
  Open FILE with `dired-do-async-shell-command'.
magit-no-assume-unchanged     M-x ... RET
  Call "git update-index --no-assume-unchanged -- FILE".
markdown-backward-same-level  M-x ... RET
  Move backward to the ARG'th heading at same level as this one.
markdown-follow-thing-at-point M-x ... RET
  Follow thing at point if possible, such as a reference link or wiki
  link.
minor-mode-menu-from-indicator M-x ... RET
  Show menu for minor mode specified by INDICATOR.
mode-line-previous-buffer     M-x ... RET
  Like `previous-buffer', but temporarily select EVENT's window.
network-connection-to-service M-x ... RET
  Open a network connection to SERVICE on HOST.
org-babel-pop-to-session-maybe M-x ... RET
  Conditionally pop to a session.
org-forward-heading-same-level M-x ... RET
  Move forward to the ARG'th subheading at same level as this one.
rmail-set-remote-password     M-x ... RET
  Set PASSWORD to be used for retrieving mail from a POP or IMAP
  server.
tab-switcher-mouse-select     M-x ... RET
  Select the window configuration whose line you click on.
vc-git-log-edit-toggle-amend  M-x ... RET
  Toggle whether this will amend the previous commit.
Buffer-menu-toggle-files-only M-x ... RET
  Toggle whether the current `buffer-menu' displays only file buffers.
Buffer-menu-unmark-all-buffers M-x ... RET
  Cancel a requested operation on all buffers.
avy-goto-whitespace-end-above M-x ... RET
  Jump to the end of a whitespace sequence between point and window
  end.
avy-goto-whitespace-end-below M-x ... RET
  Jump to the end of a whitespace sequence between window start and
  point.
comint-insert-previous-argument	M-x ... RET
  Insert the INDEXth argument from the previous Comint command-line at
  point.
comint-previous-matching-input M-x ... RET
  Search backwards through input history for match for REGEXP.
diff-delete-trailing-whitespace	M-x ... RET
  Remove trailing whitespace from lines modified in this diff.
doc-view-set-slice-using-mouse M-x ... RET
  Set the slice of the images that should be displayed.
global-whitespace-toggle-options M-x ... RET
  Toggle global `whitespace-mode' options.
gnus-summary-button-backward  M-x ... RET
  Move point to the previous field or button in the article.
gnus-summary-limit-to-extra   M-x ... RET
  Limit the summary buffer to articles that match an `extra' header.
gnus-summary-widget-backward  M-x ... RET
  Move point to the previous field or button in the article.
ibuffer-yank-filter-group     M-x ... RET
  Yank the last killed filter group before group named NAME.
image-transform-set-scale     M-x ... RET
  Prompt for a number, and resize the current image by that amount.
isearch-forward-symbol-at-point	<menu-bar> <edit> <i-search> <isearch-forward-symbol-at-point>, M-s .
  Do incremental search forward for a symbol found near point.
magit-log-matching-branches   M-x ... RET
  Show log for all branches matching PATTERN and `HEAD'.
message-delete-not-region     M-x ... RET
  Delete everything in the body of the current message outside of the
  region.
minibuffer-completion-help    M-x ... RET
  Display a list of possible completions of the current minibuffer
  contents.
mode-line-minor-mode-help     M-x ... RET
  Describe minor mode for EVENT on minor modes area of the mode line.
origami-recursively-toggle-node	M-x ... RET
  Cycle a fold node between recursively closed, open and recursively
  open
rfc-mode-previous-section     M-x ... RET
  Move point to Nth previous section (default 1).
url-cookie-parse-file-netscape M-x ... RET
  Load cookies from FILENAME in Netscape/Mozilla format.
url-gateway-nslookup-host     M-x ... RET
  Attempt to resolve the given HOST using nslookup if possible.
use-package-reset-statistics  M-x ... RET
  Reset statistics for `use-package'.
Buffer-menu-other-window      M-x ... RET
  Select this line's buffer in other window, leaving buffer menu
  visible.
Helper-describe-bindings      M-x ... RET
  Describe local key bindings of current mode.
article-treat-overstrike      M-x ... RET
  Translate overstrikes into bold text.
article-verify-x-pgp-sig      M-x ... RET
  Verify X-PGP-Sig.
backtrace-backward-frame      M-x ... RET
  Move backward to the start of a stack frame.
browse-url-of-dired-file      M-x ... RET
  In Dired, ask a WWW browser to display the file named on this line.
calendar-mayan-next-haab-date M-x ... RET
  Move cursor to next instance of Mayan HAAB-DATE.
calendar-not-implemented      M-x ... RET
  Not implemented.
citre-ace-peek-reference      M-x ... RET
  Peek the references of a symbol on screen using ace jump.
citre-peek-delete-branch      M-x ... RET
  Delete the first branch in currently browsed def node.
corfu-info-documentation      M-x ... RET
  Show documentation of current candidate.
customize-option-other-window M-x ... RET
  Customize SYMBOL, which must be a user option.
dbus-monitor-goto-serial      M-x ... RET
  Goto D-Bus message with the same serial number.
describe-char-fold-equivalences	M-x ... RET
  Display characters equivalent to CHAR under character-folding.
diary-view-other-diary-entries M-x ... RET
  Prepare and display buffer of diary entries from an alternative
  diary file.
dired-flag-garbage-files      M-x ... RET
  Flag for deletion all files that match `dired-garbage-files-regexp'.
dired-get-file-for-visit      M-x ... RET
  Get the current line's file name, with an error if file does not
  exist.
doc-view-minor-mode-menu      M-x ... RET
  Menu for Doc View minor mode.
  Value: (keymap "DocView (edit)" (Toggle\ edit/display menu-item "Toggle edit/displa…
edebug-step-through-mode      M-x ... RET
  Proceed to next stop point.
edebug-toggle-save-all-windows M-x ... RET
  Toggle the saving and restoring of all windows.
edebug-unset-breakpoints      M-x ... RET
  Unset all the breakpoints in the current form.
elpl-completion-at-point      M-x ... RET
  Function for ‘completion-at-point-functions’ in ‘elpl-mode’.
eww-buffer-show-previous      M-x ... RET
  Move to previous eww buffer in the list and display it.
gnus-article-copy-string      M-x ... RET
  Copy the string in the button to the kill ring.
gnus-article-describe-bindings M-x ... RET
  Show a list of all defined keys, and their definitions.
gnus-group-clear-dribble      M-x ... RET
  Clear all information from the dribble buffer.
gnus-registry-initialize      M-x ... RET
  Initialize the Gnus registry.
gnus-summary-limit-to-subject M-x ... RET
  Limit the summary buffer to articles that have subjects that match a
  regexp.
ibuffer-add-saved-filters     M-x ... RET
  Add saved filters from `ibuffer-saved-filters' to this buffer's
  filters.
ibuffer-exchange-filters      M-x ... RET
  Exchange the top two filters on the stack in this buffer.
ibuffer-mark-old-buffers      M-x ... RET
  Mark buffers which have not been viewed in `ibuffer-old-time' hours.
ibuffer-pop-filter-group      M-x ... RET
  Remove the first filter group.
ibuffer-unmark-all-marks      M-x ... RET
  Remove all marks from all marked buffers in Ibuffer.
ibuffer-visit-tags-table      M-x ... RET
  Visit the tags table in the buffer on this line.  See
  `visit-tags-table'.
icomplete-force-complete      M-x ... RET
  Complete the icomplete minibuffer.
ido-display-buffer-other-frame M-x ... RET
  Display a buffer preferably in another frame.
image-toggle-hex-display      M-x ... RET
  Toggle between image and hex display.
image-transform-fit-both      M-x ... RET
  Scale the current image down to fit in the current window.
image-transform-original      M-x ... RET
  Display the current image with the original (actual) size and
  rotation.
isearch-yank-x-selection      M-x ... RET
  Pull current X selection into search string.
link-hint-copy-all-links      M-x ... RET
  Copy all visible links.
link-hint-open-all-links      M-x ... RET
  Open all visible links.
magit-fetch-all-no-prune      M-x ... RET
  Fetch from all remotes.
magit-sequencer-continue      M-x ... RET
  Resume the current cherry-pick or revert sequence.
mark-beginning-of-buffer      M-x ... RET
  Set mark at the beginning of the buffer.
markdown-complete-buffer      M-x ... RET
  Complete markup for all objects in the current buffer.
markdown-edit-code-block      M-x ... RET
  Edit Markdown code block in an indirect buffer.
markdown-footnote-return      M-x ... RET
  Return from a footnote to its footnote number in the main text.
markdown-insert-foldable-block M-x ... RET
  Insert details disclosure element to make content foldable.
markdown-insert-footnote      M-x ... RET
  Insert footnote with a new number and move point to footnote
  definition.
markdown-move-list-item-down  M-x ... RET
  Move the current list item down in the list when possible.
markdown-move-subtree-up      M-x ... RET
  Move the current subtree of ATX headings up.
markdown-remove-inline-images M-x ... RET
  Remove inline image overlays from image links in the buffer.
menu-bar-left-scroll-bar      <menu-bar> <options> <showhide> <showhide-scroll-bar> <left>
  Display scroll bars on the left of each window.
message-goto-followup-to      M-x ... RET
  Move point to the Followup-To header.
minibuffer-electric-default-mode M-x ... RET
  Toggle Minibuffer Electric Default mode.
  Value: nil
mml-secure-encrypt-smime      M-x ... RET
  Add MML tags to S/MIME encrypt this MML part.
mouse-drag-bottom-left-corner <bottom-left-corner> <down-mouse-1>
  Drag bottom left corner of a frame with the mouse.
mouse-split-window-vertically <vertical-scroll-bar> C-<mouse-2>, <vertical-line> C-<mouse-2>, <right-divider> C-<mouse-2>
  Select Emacs window mouse is on, then split it vertically in half.
ns-open-file-using-panel      s-o
  Pop up open-file panel, and load the result in a buffer.
org-babel-open-src-block-result	M-x ... RET
  Open results of source block at point.
org-cycle-force-archived      M-x ... RET
  Cycle subtree even if it is archived.
org-drag-element-forward      M-x ... RET
  Move forward element at point.
org-duration-set-regexps      M-x ... RET
  Set duration related regexps.
org-dynamic-block-insert-dblock	M-x ... RET
  Insert a dynamic block of type TYPE.
org-edit-inline-src-code      M-x ... RET
  Edit inline source code at point.
org-force-cycle-archived      M-x ... RET
  Cycle subtree even if it is archived.
org-roam-extract-subtree      M-x ... RET
  Convert current subtree at point to a node, and extract it into a
  new file.
org-save-all-org-buffers      M-x ... RET
  Save all Org buffers without user confirmation.
org-table-current-column      M-x ... RET
  Return current column number.
org-table-fedit-ref-down      M-x ... RET
  Shift the reference at point one row/hline down.
org-table-fedit-ref-left      M-x ... RET
  Shift the reference at point one field to the left.
osx-dictionary-read-word      M-x ... RET
  Open current searched `word' in Dictionary.app.
osx-dictionary-recompile      M-x ... RET
  Create or replace the `osx-dictionary-cli' executable using the
  latest code.
outline-previous-heading      M-x ... RET
  Move to the previous (possibly invisible) heading line.
popwin:popup-buffer-tail      M-x ... RET
  Same as `popwin:popup-buffer' except that the buffer will be
  `recenter'ed at the bottom.
pr-toggle-file-landscape      M-x ... RET
  Toggle landscape for PostScript file.
project-remember-projects-under	M-x ... RET
  Index all projects below a directory DIR.
pyim-convert-string-at-point  M-x ... RET
  将光标前的用户输入的字符串转换为中文.
read-aloud-change-engine      M-x ... RET
  Select another TTS engine.
siri-shortcuts-run-async      M-x ... RET
  Run a macOS Shortcut with a given NAME without waiting for
  completion.
symbol-overlay-echo-mark      M-x ... RET
  Jump back to the mark.
symbol-overlay-jump-last      M-x ... RET
  Jump to the last location.
symbol-overlay-jump-next      M-x ... RET
  Jump to the next location of symbol at point.
symbol-overlay-jump-prev      M-x ... RET
  Jump to the previous location of symbol at point.
thing-cut-region-or-line      M-x ... RET
  Cut content of the current region or line.
thing-replace-whitespace      M-x ... RET
  Replace whitespace around point with the content of currnt line.
tramp-setup-debug-buffer      M-x ... RET
  Function to setup debug buffers.
turn-off-ace-pinyin-mode      M-x ... RET
  Turn off `ace-pinyin-mode'.
xeft--find-file-at-point      M-x ... RET
  View file at point.
yas-skip-and-clear-field      M-x ... RET
  Clears unmodified FIELD if at field start, skips to next tab.
checkdoc-ispell-current-buffer M-x ... RET
  Check the style and spelling of the current buffer.
diary-bahai-insert-yearly-entry	M-x ... RET
  Insert an annual diary entry.
doc-view-search-next-match    M-x ... RET
  Go to the ARGth next matching page.
duplicate-line-or-region-below M-x ... RET
  Duplicate current line or region below.
epkg-find-file-other-window   M-x ... RET
  Visit a file in a submodule of `epkg-repository' in another window.
expand-jump-to-previous-slot  M-x ... RET
  Move the cursor to the previous slot in the last abbrev expansion.
gnus-group-enter-directory    M-x ... RET
  Enter an ephemeral nneething group.
gnus-summary-import-article   M-x ... RET
  Import an arbitrary file into a mail newsgroup.
gnus-summary-save-article-rmail	M-x ... RET
  Append the current article to an rmail file.
graphviz-turn-off-live-preview M-x ... RET
  Turn off live preview.
ibuffer-mouse-visit-buffer    M-x ... RET
  Visit the buffer chosen with the mouse.
next-complete-history-element M-x ... RET
  Get next history element that completes the minibuffer before the
  point.
org--forward-paragraph-once   M-x ... RET
  Move forward to end of paragraph or equivalent, once.
org-agenda-set-restriction-lock	M-x ... RET
  Set restriction lock for agenda to current subtree or file.
org-babel-goto-named-result   M-x ... RET
  Go to a named result.
python-import-symbol-at-point M-x ... RET
  Add an import statement for the symbol at point to the current
  buffer.
Info-history-forward-menu     M-x ... RET
  Pop up the menu with a list of Info nodes visited with
  `Info-history-back'.
cancel-debug-on-variable-change	M-x ... RET
  Undo effect of \[debug-on-variable-change] on VARIABLE.
comint-dynamic-complete-filename M-x ... RET
  Dynamically complete the filename at point.
dired-do-find-regexp-and-replace M-x ... RET
  Replace matches of FROM with TO, in all marked files.
gnus-summary-enter-digest-group	M-x ... RET
  Enter an nndoc group based on the current article.
gnus-summary-kill-same-subject M-x ... RET
  Mark articles which has the same subject as read.
gnus-summary-put-mark-as-ticked	M-x ... RET
  Mark the current article as ticked.
gnus-summary-put-mark-as-unread	M-x ... RET
  Mark the current article as unread.
gnus-summary-sort-by-recipient M-x ... RET
  Sort the summary buffer by recipient name alphabetically.
ido-switch-buffer-other-window M-x ... RET
  Switch to another buffer and show it in another window.
magit-ediff-show-working-tree M-x ... RET
  Show changes between `HEAD' and working tree using Ediff.
mouse-delete-other-windows    <mode-line> <mouse-2>
  Delete all windows except the one you click on.
multi-occur-in-matching-buffers	M-x ... RET
  Show all lines matching REGEXP in buffers specified by BUFREGEXP.
nnml-generate-nov-databases   M-x ... RET
  Generate NOV databases in all nnml directories.
org-clock-update-time-maybe   M-x ... RET
  If this is a CLOCK line, update it and return t.
org-roam-update-org-id-locations M-x ... RET
  Scan Org-roam files to update `org-id' related state.
repeat-matching-complex-command	M-x ... RET
  Edit and re-evaluate complex command with name matching PATTERN.
shell-copy-environment-variable	M-x ... RET
  Copy the environment variable VARIABLE from the subshell to Emacs.
article-hide-list-identifiers M-x ... RET
  Remove list identifiers from the Subject header.
bookmark-jump-other-window    M-x ... RET
  Jump to BOOKMARK in another window.  See `bookmark-jump' for more.
calendar-astro-goto-day-number M-x ... RET
  Move cursor to astronomical (Julian) DAYNUMBER.
calendar-beginning-of-year    M-x ... RET
  Move the cursor backward ARG year beginnings.
ediff-merge-directory-revisions	<menu-bar> <tools> <ediff-merge> <ediff-merge-dir-revisions>
  Run Ediff on a directory, DIR1, merging its files with their
  revisions.
elisp-depend-print-dependencies	M-x ... RET
  Print library dependencies of the current buffer.
explain-pause-profile-clear   M-x ... RET
  Clear the profiling data. Note that this does not clear profiles
  already visible
global-display-line-numbers-mode <menu-bar> <options> <showhide> <display-line-numbers> <global>
  Toggle Display-Line-Numbers mode in all buffers.
  Value: t
global-word-wrap-whitespace-mode M-x ... RET
  Toggle Word-Wrap-Whitespace mode in all buffers.
  Value: nil
gnus-article-browse-html-article M-x ... RET
  View "text/html" parts of the current article with a WWW browser.
gnus-article-emojize-symbols  M-x ... RET
  Run `article-emojize-symbols' in the article buffer.
gnus-article-fill-long-lines  M-x ... RET
  Run `article-fill-long-lines' in the article buffer.
gnus-article-treat-non-ascii  M-x ... RET
  Run `article-treat-non-ascii' in the article buffer.
gnus-group-nnimap-edit-acl    M-x ... RET
  Edit the Access Control List of current nnimap GROUP.
gnus-read-ephemeral-gmane-group	M-x ... RET
  Read articles from Gmane group GROUP as an ephemeral group.
gnus-summary-exit-no-update   M-x ... RET
  Quit reading current newsgroup without updating read article info.
gnus-summary-limit-to-headers M-x ... RET
  Limit the summary buffer to articles that have headers that match
  MATCH.
gnus-summary-write-article-file	M-x ... RET
  Write the current article to a file, deleting the previous file.
magit-commit-instant-squash   M-x ... RET
  Create a squash commit targeting COMMIT and instantly rebase.
markdown-live-preview-re-export	M-x ... RET
  Re-export the current live previewed content.
mode-line-toggle-read-only    M-x ... RET
  Like toggling `read-only-mode', for the mode-line.
org-babel-insert-header-arg   M-x ... RET
  Insert a header argument selecting from lists of common args and
  values.
org-roam-dailies-goto-yesterday	M-x ... RET
  Find the daily-note for yesterday, creating it if necessary.
origami-open-node-recursively M-x ... RET
  Open the fold node and all of its children at POINT in BUFFER.
toggle-tab-bar-mode-from-frame <menu-bar> <options> <showhide> <showhide-tab-bar>
  Toggle tab bar on or off, based on the status of the current frame.
calendar-beginning-of-week    M-x ... RET
  Move the cursor back ARG calendar-week-start-day's.
calendar-hebrew-list-yahrzeits M-x ... RET
  List Yahrzeit dates for *Gregorian* DEATH-DATE from START-YEAR to
  END-YEAR.
doc-view-scroll-up-or-next-page	M-x ... RET
  Scroll page up ARG lines if possible, else goto next page.
epa-import-armor-in-region    M-x ... RET
  Import keys in the OpenPGP armor format in the current region from
  START to END.
exec-path-from-shell-initialize	M-x ... RET
  Initialize environment from the user's shell.
gnus-group-browse-foreign-server M-x ... RET
  Browse a foreign news server.
gnus-mime-save-part-and-strip M-x ... RET
  Save the MIME part under point then replace it with an external
  body.
gnus-summary-clear-mark-forward	M-x ... RET
  Clear marks from N articles forward.
gnus-summary-sort-by-original M-x ... RET
  Sort the summary buffer using the default sorting method.
hashcash-insert-payment-async M-x ... RET
  Insert X-Payment and X-Hashcash headers with a payment for ARG
image-dired-dired-display-image	M-x ... RET
  Display current image file.
image-transform-set-rotation  M-x ... RET
  Prompt for an angle ROTATION, and rotate the image by that amount.
magit-commit-absorb-modules   M-x ... RET
  Spread modified modules across recent commits.
minibuffer-previous-completion M-x ... RET
  Move to the previous item in its completions window from the
  minibuffer.
nnheader-cancel-function-timers	M-x ... RET
  Cancel all timers which would run FUNCTION.
org-babel-remove-inline-result M-x ... RET
  Remove the result of the current inline-src-block or babel call.
org-occur-link-in-agenda-files M-x ... RET
  Create a link and search for it in the agendas.
org-table-create-with-table.el M-x ... RET
  Use the table.el package to insert a new table.
tab-bar-mouse-context-menu    M-x ... RET
  Pop up the context menu for the tab on which you click.
xref-find-references-at-mouse M-x ... RET
  Find references to the identifier at or around mouse click.
frame-configuration-to-register	M-x ... RET
  Store the window configuration of all frames in register REGISTER.
gnus-group-sort-groups-by-rank M-x ... RET
  Sort the group buffer by group rank.
gnus-kill-sticky-article-buffer	M-x ... RET
  Kill the given sticky article BUFFER.
gnus-summary-article-posted-p M-x ... RET
  Say whether the current (mail) article is available from news as
  well.
gnus-summary-crosspost-article M-x ... RET
  Crosspost the current article to some other group.
mairix-search-from-this-article	M-x ... RET
  Search messages from sender of the current article.
markdown-display-inline-images M-x ... RET
  Add inline image overlays to image links in the buffer.
markdown-insert-header-atx-2  M-x ... RET
  Insert a level two atx-style (hash mark) header.
mml-secure-message-encrypt-pgp M-x ... RET
  Add MML tag to encrypt and sign the entire message.
org-agenda-prepare-buffers    M-x ... RET
  Create buffers for all agenda files, protect archived trees and
  comments.
revert-buffer-with-coding-system <menu-bar> <options> <mule> <set-various-coding-system> <revert-buffer-with-coding-system>
  Visit the current buffer's file again using coding system
  CODING-SYSTEM.
Buffer-menu-backup-unmark     M-x ... RET
  Move up and cancel all requested operations on buffer on line above.
View-scroll-to-buffer-end     M-x ... RET
  Scroll backward or forward so that buffer end is at last line of
  window.
View-search-last-regexp-forward	M-x ... RET
  Search forward for first (or prefix Nth) instance of last regexp in
  View mode.
ace-pinyin-goto-subword-0     M-x ... RET
  Ace-pinyin replacement of `avy-goto-subword-0'.
ace-pinyin-goto-subword-1     M-x ... RET
  Ace-pinyin replacement of `avy-goto-subword-1'.
article-decode-mime-words     M-x ... RET
  Decode all MIME-encoded words in the article.
article-normalize-headers     M-x ... RET
  Make all header lines 40 characters long.
article-strip-blank-lines     M-x ... RET
  Strip leading, trailing and multiple blank lines.
beginning-of-defun-comments   M-x ... RET
  Move to the beginning of ARGth defun, including comments.
bibtex-copy-entry-as-kill     M-x ... RET
  Copy the entire enclosing BibTeX entry to `bibtex-entry-kill-ring'.
blink-search-kill-process     M-x ... RET
  Stop Blink-Search process and kill all Blink-Search buffers.
calendar-bahai-print-date     M-x ... RET
  Show the Bahá’í calendar equivalent of the selected date.
calendar-mayan-print-date     M-x ... RET
  Show the Mayan long count, tzolkin, and haab equivalents of date.
checkdoc-package-keywords     M-x ... RET
  Find package keywords that aren't in `finder-known-keywords'.
color-rg-rerun-change-dir     M-x ... RET
  Rerun last command but prompt for new dir.
color-rg-rerun-in-project     M-x ... RET
  Rerun last command in project root.
compilation-display-error     M-x ... RET
  Display the source for current error in another window.
customize-save-customized     M-x ... RET
  Save all user options which have been set in this session.
debugger-record-expression    M-x ... RET
  Display a variable's value and record it in `*Backtrace-record*'
  buffer.
delimit-columns-customize     M-x ... RET
  Customize the `columns' group.
diff-hl-inline-popup-hide     M-x ... RET
  Hide the current inline popup.
dired-mode-immediate-menu     M-x ... RET
  Immediate menu for Dired mode.
  Value: (keymap "Immediate" (Edit\ File\ Names menu-item "Edit File Names" wdired-ch…
epa-verify-cleartext-in-region M-x ... RET
  Verify OpenPGP cleartext signed messages in current region from
  START to END.
ert-kill-all-test-buffers     M-x ... RET
  Kill all test buffers that are still live.
flyspell-pre-command-hook     M-x ... RET
  Save the current buffer and point for Flyspell's post-command hook.
gnus-article-show-summary     M-x ... RET
  Reconfigure windows to show summary buffer.
gnus-article-toggle-fonts     M-x ... RET
  Toggle the use of proportional fonts for HTML articles.
gnus-group-catchup-current-all M-x ... RET
  Mark all articles in current newsgroup as read.
gnus-group-read-init-file     M-x ... RET
  Read the Gnus elisp init file.
gnus-mime-view-part-internally M-x ... RET
  View the MIME part under point with an internal viewer.
gnus-summary-limit-to-articles M-x ... RET
  Limit the summary buffer to the next N articles.
gnus-summary-limit-to-recipient	M-x ... RET
  Limit the summary buffer to articles with the given RECIPIENT.
ibuffer-mark-help-buffers     M-x ... RET
  Mark buffers whose major mode is in variable
  `ibuffer-help-buffer-modes'.
ibuffer-mode-groups-popup     M-x ... RET
  Menu for `ibuffer'.
  Value: (keymap "Filter Groups" (Create\ filter\ group\ from\ current\ filters... me…
ibuffer-mode-operate-menu     M-x ... RET
  Operate menu for `ibuffer'.
  Value: (keymap "Operate" (View menu-item "View" ibuffer-do-view) (View\ \(separate\…
ibuffer-recompile-formats     M-x ... RET
  Recompile `ibuffer-formats'.
image-dired-display-thumb     M-x ... RET
  Shorthand for `image-dired-display-thumbs' with prefix argument.
mail-decode-encoded-word-region	M-x ... RET
  Decode MIME-encoded words in region between START and END.
markdown-insert-header-atx-1  M-x ... RET
  Insert a first level atx-style (hash mark) header.
markdown-insert-header-atx-3  M-x ... RET
  Insert a level three atx-style (hash mark) header.
markdown-insert-header-atx-4  M-x ... RET
  Insert a level four atx-style (hash mark) header.
markdown-insert-header-atx-5  M-x ... RET
  Insert a level five atx-style (hash mark) header.
markdown-insert-header-atx-6  M-x ... RET
  Insert a sixth level atx-style (hash mark) header.
markdown-outline-previous     M-x ... RET
  Move to previous list item, when in a list, or previous visible
  heading.
markdown-table-delete-row     M-x ... RET
  Delete row or horizontal line at point from the table.
menu-bar-right-scroll-bar     <menu-bar> <options> <showhide> <showhide-scroll-bar> <right>
  Display scroll bars on the right of each window.
message-goto-distribution     M-x ... RET
  Move point to the Distribution header.
message-insert-newsgroups     M-x ... RET
  Insert the Newsgroups header from the article being replied to.
message-insert-wide-reply     M-x ... RET
  Insert To and Cc headers as if you were doing a wide reply.
mouse-drag-bottom-right-corner <bottom-right-corner> <down-mouse-1>
  Drag bottom right corner of a frame with the mouse.
mouse-wheel-global-text-scale C-M-<wheel-up>, C-M-<wheel-down>, C-M-<mouse-4>, C-M-<mouse-5>
  Increase or decrease the global font size according to the EVENT.
ns-write-file-using-panel     s-S
  Pop up save-file panel, and save buffer in resulting name.
org-babel-check-src-block     M-x ... RET
  Check for misspelled header arguments in the current code block.
org-babel-result-hide-all     M-x ... RET
  Fold all results in the current buffer.
org-clone-subtree-with-time-shift M-x ... RET
  Clone the task (subtree) at point N times.
org-columns-insert-dblock     M-x ... RET
  Create a dynamic block capturing a column view table.
org-create-customize-menu     M-x ... RET
  Create a full customization menu for Org mode, insert it into the
  menu.
org-drag-element-backward     M-x ... RET
  Move backward element at point.
org-element-update-syntax     M-x ... RET
  Update parser internals.
org-entities-create-table     M-x ... RET
  Create an Org mode table with all entities.
org-insert-columns-dblock     M-x ... RET
  Create a dynamic block capturing a column view table.
org-table-fedit-line-down     M-x ... RET
  Move cursor one line down in the window showing the table.
org-table-fedit-ref-right     M-x ... RET
  Shift the reference at point one field to the right.
org-toggle-timestamp-type     M-x ... RET
  Toggle the type (<active> or [inactive]) of a time stamp.
orgtbl-insert-radio-table     M-x ... RET
  Insert a radio table template appropriate for this major mode.
package-menu-clear-filter     M-x ... RET
  Clear any filter currently applied to the "*Packages*" buffer.
previous-window-any-frame     M-x ... RET
  Select the previous window, regardless of which frame it is on.
reveal-in-folder-at-point     M-x ... RET
  Reveal the current file in folder at point.
smtpmail-send-queued-mail     M-x ... RET
  Send mail that was queued as a result of setting
  `smtpmail-queue-mail'.
standard-themes-load-dark     M-x ... RET
  Load `standard-dark' and run `standard-themes-post-load-hook'.
strokes-load-user-strokes     M-x ... RET
  Load user-defined strokes from file named by `strokes-file'.
subed-word-data-load-from-file M-x ... RET
  Load word-level timing from FILE.
symbol-overlay-jump-first     M-x ... RET
  Jump to the first location.
thing-replace-parentheses     M-x ... RET
  Replace content in match parentheses with the content of currnt
  line.
tramp-cleanup-all-buffers     M-x ... RET
  Kill all remote buffers.
vc-git-stash-pop-at-point     M-x ... RET
  Pop the stash at point.
windmove-swap-states-down     M-x ... RET
  Swap the states with the window below from the current one.
windmove-swap-states-left     M-x ... RET
  Swap the states with the window on the left from the current one.
yas-deactivate-extra-mode     M-x ... RET
  Deactivates the snippets for the given `mode' in the buffer.
yas-direct-keymaps-reload     M-x ... RET
  Force reload the direct keybinding for active snippet tables.
yas/direct-keymaps-reload     M-x ... RET
  Force reload the direct keybinding for active snippet tables.
diary-bahai-insert-monthly-entry M-x ... RET
  Insert a monthly diary entry.
diary-hebrew-insert-yearly-entry M-x ... RET
  Insert an annual diary entry.
ediff-merge-buffers-with-ancestor <menu-bar> <tools> <ediff-merge> <ediff-merge-buffers-with-ancestor>
  Merge buffers with ancestor.
electric-newline-and-maybe-indent C-j
  Insert a newline.
gnus-summary-make-search-group M-x ... RET
  Search a group from the summary buffer.
gnus-summary-mark-as-processable M-x ... RET
  Set the process mark on the next N articles.
ibuffer-do-view-horizontally  M-x ... RET
  As `ibuffer-do-view', but split windows horizontally.
ibuffer-mark-by-name-regexp   M-x ... RET
  Mark all buffers whose name matches REGEXP.
message-cross-post-followup-to M-x ... RET
  Crossposts message and set Followup-To to TARGET-GROUP.
mpv-seek-to-position-at-point M-x ... RET
  Jump to playback position as inserted by
  `mpv-insert-playback-position'.
org--backward-paragraph-once  M-x ... RET
  Move backward to start of paragraph or equivalent, once.
org-src-associate-babel-session	M-x ... RET
  Associate edit buffer with comint session.
package-vc-install-from-checkout M-x ... RET
  Set up the package NAME in DIR by linking it into the ELPA
  directory.
pr-ps-region-using-ghostscript M-x ... RET
  Print region using PostScript through ghostscript.
reindent-then-newline-and-indent M-x ... RET
  Reindent current line, insert newline, then indent the new line.
doc-view-next-line-or-next-page	M-x ... RET
  Scroll upward by ARG lines if possible, else goto next page.
find-alternate-file-other-window M-x ... RET
  Find file FILENAME as a replacement for the file in the next window.
find-file-read-only-other-frame	M-x ... RET
  Edit file FILENAME in another frame but don't allow changes.
font-lock-after-unfontify-buffer M-x ... RET
  Do nothing and return nil.
gnus-summary-display-buttonized	M-x ... RET
  Display the current article buffer fully MIME-buttonized.
gnus-summary-edit-article-done M-x ... RET
  Make edits to the current article permanent.
gnus-summary-put-mark-as-dormant M-x ... RET
  Mark the current article as dormant.
ibuffer-forward-filter-group  M-x ... RET
  Move point forwards by COUNT filtering groups.
ibuffer-mark-by-mode-regexp   M-x ... RET
  Mark all buffers whose major mode matches REGEXP.
mail-source-delete-old-incoming	M-x ... RET
  Remove incoming files older than AGE days.
markdown-reference-goto-link  M-x ... RET
  Jump to the location of the first use of REFERENCE.
org-babel-hide-result-toggle  M-x ... RET
  Toggle the visibility of the current result.
org-babel-load-in-session-maybe	M-x ... RET
  Conditionally load a source block in a session.
org-html-convert-region-to-html	M-x ... RET
  Assume the current region has Org syntax, and convert it to HTML.
outline-previous-visible-heading M-x ... RET
  Move to the previous heading line.
pr-ps-directory-using-ghostscript M-x ... RET
  Print directory using PostScript through ghostscript.
Info-goto-emacs-key-command-node <menu-bar> <help-menu> <search-documentation> <lookup-key-in-manual>, <help> K, <f1> K
  Go to the node in the Emacs manual which describes the command bound
  to KEY.
calendar-mayan-next-round-date M-x ... RET
  Move cursor to next instance of Mayan TZOLKIN-DATE HAAB-DATE
  combination.
global-highlight-parentheses-mode M-x ... RET
  Toggle Highlight-Parentheses mode in all buffers.
  Value: t
gnus-article-treat-overstrike M-x ... RET
  Run `article-treat-overstrike' in the article buffer.
gnus-article-verify-x-pgp-sig M-x ... RET
  Run `article-verify-x-pgp-sig' in the article buffer.
gnus-summary-catchup-from-here M-x ... RET
  Mark all unticked articles after (and including) the current one as
  read.
gnus-summary-limit-exclude-marks M-x ... RET
  Exclude articles that are marked with MARKS (e.g. "DK").
magit-sparse-checkout-enable  M-x ... RET
  Convert the working tree to a sparse checkout.
message-remove-blank-cited-lines M-x ... RET
  Remove cited lines containing only blanks.
mml-secure-message-sign-encrypt	M-x ... RET
  Add MML tag to sign and encrypt the entire message.
nnfolder-generate-active-file M-x ... RET
  Look for mbox folders in the nnfolder directory and make them into
  groups.
origami-close-node-recursively M-x ... RET
  Close the fold node and all of its children at POINT in BUFFER.
pcomplete-expand-and-complete M-x ... RET
  Expand the textual value of the current argument.
profiler-report-write-profile M-x ... RET
  Write the current profile into file FILENAME.
rfc2047-encode-message-header M-x ... RET
  Encode the message header according to
  `rfc2047-header-encoding-alist'.
tab-switcher-delete-backwards M-x ... RET
  Mark window configuration on this line to be deleted by
  \<tab-switcher-mode-map>\[tab-switcher-execute] command.
toggle-menu-bar-mode-from-frame	<menu-bar> <options> <showhide> <menu-bar-mode>
  Toggle display of the menu bar.
windmove-display-same-window  M-x ... RET
  Display the next buffer in the same window.
article-date-combined-lapsed  M-x ... RET
  Convert the current article date to time lapsed since it was sent.
calendar-beginning-of-month   M-x ... RET
  Move the cursor backward ARG month beginnings.
delete-other-windows-vertically	M-x ... RET
  Delete the windows in the same column with WINDOW, but not WINDOW
  itself.
ebrowse-back-in-position-stack M-x ... RET
  Move backward in the position stack.
emerge-revisions-with-ancestor M-x ... RET
  Emerge two RCS revisions of a file, with another revision as
  ancestor.
find-lisp-find-dired-filter   M-x ... RET
  Change the filter on a `find-lisp-find-dired' buffer to REGEXP.
gnus-article-treat-dumbquotes M-x ... RET
  Run `article-treat-smartquotes' in the article buffer.
gnus-summary-insert-old-articles M-x ... RET
  Insert all old articles in this group.
gnus-summary-refer-references M-x ... RET
  Fetch all articles mentioned in the References header.
ibuffer-copy-buffername-as-kill	M-x ... RET
  Copy buffer names of marked (or next ARG) buffers into the kill
  ring.
magit-bundle-update-tracked   M-x ... RET
  Update a bundle that is being tracked using TAG.
org-insert-last-stored-link   M-x ... RET
  Insert the last link stored in `org-stored-links'.
org-odt-export-as-odf-and-open M-x ... RET
  Export LaTeX fragment as OpenDocument formula and immediately open
  it.
org-roam-buffer-display-dedicated M-x ... RET
  Launch NODE dedicated Org-roam buffer.
set-buffer-process-coding-system <menu-bar> <options> <mule> <set-various-coding-system> <set-buffer-process-coding-system>
  Set coding systems for the process associated with the current
  buffer.
shell-dynamic-complete-filename	M-x ... RET
  Dynamically complete the filename at point.
xref-find-definitions-at-mouse M-x ... RET
  Find the definition of identifier at or around mouse click.
gnus-fetch-group-other-frame  M-x ... RET
  Pop up a frame and enter GROUP.
gnus-group-edit-group-method  M-x ... RET
  Edit the select method of GROUP.
nonincremental-search-forward <menu-bar> <edit> <search> <search-forward>
  Read a string and search for it nonincrementally.
org-roam-dailies-capture-tomorrow M-x ... RET
  Create an entry in the daily-note for tomorrow.
org-table-calc-current-TBLFM  M-x ... RET
  Apply the #+TBLFM in the line at point to the table.
previous-matching-history-element M-x ... RET
  Find the previous history element that matches REGEXP.
sis--prefix-override-handler  C-c, C-x, C-h
  Prefix key handler with ARG.
tabulated-list-previous-column M-x ... RET
  Go to the start of the column point is in on the current line.
5x5-crack-mutating-current    M-x ... RET
  Attempt to crack 5x5 by mutating the current solution.
View-scroll-half-page-backward M-x ... RET
  Scroll backward a "half page" (or prefix LINES) lines in View mode.
View-search-last-regexp-backward M-x ... RET
  Search backward for first (or prefix Nth) instance of last regexp in
  View mode.
abridge-diff-enable-hiding    M-x ... RET
  Enable abridged text hiding.
abridge-diff-toggle-hiding    M-x ... RET
  Toggle abridged text hiding.
activate-transient-input-method	<menu-bar> <options> <mule> <activate-transient-input-method>
  Select and enable a transient input method for the current buffer.
amread-voice-reader-toggle    M-x ... RET
  Toggle text voice reading.
article-verify-cancel-lock    M-x ... RET
  Verify Cancel-Lock header.
cal-menu-global-mouse-menu    M-x ... RET
  Menu bound to a mouse event, not specific to the mouse-click
  location.
  Value: (keymap "Calendar" (Scroll\ forward menu-item "Scroll forward" calendar-scro…
calendar-coptic-print-date    M-x ... RET
  Show the Coptic calendar equivalent of the selected date.
calendar-count-days-region    M-x ... RET
  Count the number of days (inclusive) between point and the mark.
calendar-french-print-date    M-x ... RET
  Show the French Revolutionary calendar equivalent of the selected
  date.
calendar-hebrew-print-date    M-x ... RET
  Show the Hebrew calendar equivalent of the date under the cursor.
calendar-julian-print-date    M-x ... RET
  Show the Julian calendar equivalent of the date under the cursor.
calendar-print-day-of-year    M-x ... RET
  Show day number in year/days remaining in year for date under the
  cursor.
citre-ctags-cmd-buf-cancel    M-x ... RET
  Quit the command editing.
citre-ctags-cmd-buf-commit    M-x ... RET
  Commit in the command editing buffer.
citre-peek-delete-branches    M-x ... RET
  Delete all branchs in currently browsed def node.
citre-peek-next-definition    M-x ... RET
  Peek the next tag in tag list.
citre-peek-prev-definition    M-x ... RET
  Peek the previous tag in tag list.
color-rg-rerun-toggle-case    M-x ... RET
  Rerun last search with toggled case sensitivity setting.
color-rg-rerun-toggle-node    M-x ... RET
  Rerun last search with toggled '--no-node' flag.
comint-show-maximum-output    M-x ... RET
  Put the end of the buffer at the bottom of the window.
customize-variable-other-window	M-x ... RET
  Customize SYMBOL, which must be a user option.
describe-buffer-case-table    M-x ... RET
  Describe the case table of the current buffer.
diary-insert-anniversary-entry M-x ... RET
  Insert an anniversary diary entry for the date given by point.
diff-hl-show-hunk-previous    M-x ... RET
  Go to previous hunk/change and show it.
dired-advertised-find-file    M-x ... RET
  In Dired, visit the file or directory named on this line.
doc-view-presentation-exit    M-x ... RET
  Leave Doc-View's presentation mode.
file-notify-rm-all-watches    M-x ... RET
  Remove all existing file notification watches from Emacs.
flyspell-post-command-hook    M-x ... RET
  The `post-command-hook' used by flyspell to check a word on-the-fly.
gnus-article-highlight-signature M-x ... RET
  Highlight the signature in an article.
gnus-article-outlook-unwrap-lines M-x ... RET
  Unwrap lines that appear to be wrapped citation lines.
gnus-article-refer-article    M-x ... RET
  Read article specified by message-id around point.
gnus-article-remove-images    M-x ... RET
  Remove all images from the article buffer.
gnus-cloud-upload-all-data    M-x ... RET
  Upload all data (newsrc and files) to the Gnus Cloud.
gnus-group-make-useful-group  M-x ... RET
  Create one of the groups described in `gnus-useful-groups'.
gnus-group-set-current-level  M-x ... RET
  Set the level of the next N groups to LEVEL.
gnus-group-sort-groups-by-level	M-x ... RET
  Sort the group buffer by group level.
gnus-group-sort-groups-by-score	M-x ... RET
  Sort the group buffer by group score.
gnus-summary-best-unread-article M-x ... RET
  Select the unread article with the highest score.
gnus-summary-expire-thread    M-x ... RET
  Mark articles under current thread as expired.
goto-line-preview-relative    M-x ... RET
  Preview goto line relative.
ibuffer-do-sort-by-recency    M-x ... RET
  Sort the buffers by how recently they've been used.
ibuffer-do-toggle-modified    M-x ... RET
  Toggle modification flag of marked buffers.
ibuffer-mark-dired-buffers    M-x ... RET
  Mark all `dired' buffers.
icomplete-fido-delete-char    M-x ... RET
  Delete char or maybe call `dired', like `ido-mode'.
iconify-or-deiconify-frame    M-x ... RET
  Iconify the selected frame, or deiconify if it's currently an icon.
lisp-interaction-mode-menu    M-x ... RET
  Menu for Lisp Interaction mode.
  Value: (keymap "Lisp-Interaction" (Complete\ Lisp\ Symbol menu-item "Complete Lisp …
list-packages-and-versions    M-x ... RET
  Returns a list of all installed packages and their versions
lsp-bridge-find-def-return    M-x ... RET
  Pop off lsp-bridge-mark-ring and jump to the top location.
lsp-bridge-install-tabnine    M-x ... RET
  Install TabNine binary into `tabnine-bridge-binaries-folder'.
lsp-bridge-restart-process    M-x ... RET
  Stop and restart LSP-Bridge process.
mail-abbrev-complete-alias    M-x ... RET
  Perform completion on alias preceding point.
mairix-edit-saved-searches    M-x ... RET
  Edit current mairix searches.
markdown-beginning-of-list    M-x ... RET
  Move point to beginning of list at point, if any.
markdown-beginning-of-text-block M-x ... RET
  Move backward to previous beginning of a plain text block.
markdown-move-subtree-down    M-x ... RET
  Move the current subtree of ATX headings down.
markdown-narrow-to-subtree    M-x ... RET
  Narrow buffer to the current subtree.
markdown-reload-extensions    M-x ... RET
  Check settings, update font-lock keywords and hooks, and re-fontify
  buffer.
markdown-table-move-row-up    M-x ... RET
  Move table row at point up.
menu-bar-no-window-divider    <menu-bar> <options> <showhide> <showhide-window-divider> <no-divider>
  Do not display window dividers.
message-widen-and-recenter    M-x ... RET
  Widen the buffer and go to the start.
mml-secure-encrypt-pgpmime    M-x ... RET
  Add MML tags to PGP/MIME encrypt this MML part.
mouse-split-window-horizontally	<mode-line> C-<mouse-2>, <horizontal-scroll-bar> C-<mouse-2>, <bottom-divider> C-<mouse-2>
  Select Emacs window mouse is on, then split it horizontally in half.
org-babel-expand-src-block-maybe M-x ... RET
  Conditionally expand a source block.
org-bibtex-import-from-file   M-x ... RET
  Read bibtex entries from FILE and insert as Org headlines after
  point.
org-fold-show-hidden-entry    M-x ... RET
  Show an entry where even the heading is hidden.
org-footnote-renumber-fn:N    M-x ... RET
  Order numbered footnotes into a sequence in the document.
org-table-move-column-left    M-x ... RET
  Move column to the left.
org-toggle-pretty-entities    M-x ... RET
  Toggle the composition display of entities as UTF8 characters.
outline-show-only-headings    M-x ... RET
  Show only headings.
package-menu-backup-unmark    M-x ... RET
  Back up one line and clear any marks on that package.
package-quickstart-refresh    M-x ... RET
  (Re)Generate the `package-quickstart-file'.
popwin:select-popup-window    M-x ... RET
  Select the currently shown popup window.
previous-multiframe-window    M-x ... RET
  Select the previous window, regardless of which frame it is on.
profiler-report-next-entry    M-x ... RET
  Move cursor to next entry.
project-or-external-find-regexp	M-x ... RET
  Find all matches for REGEXP in the project roots or external roots.
rmail-edit-current-message    M-x ... RET
  Edit the contents of this message.
standard-themes-load-light    M-x ... RET
  Load `standard-light' and run `standard-themes-post-load-hook'.
symbol-overlay-save-symbol    M-x ... RET
  Copy symbol at point.
tab-switcher-backup-unmark    M-x ... RET
  Move up one line and cancel requested operations on window
  configuration there.
thing-replace-whole-buffer    M-x ... RET
  Replace the current buffer with the content.
timeclock-workday-elapsed-string M-x ... RET
  Return a string representing the amount of time worked today.
use-package-jump-to-package-form M-x ... RET
  Attempt to find and jump to the `use-package' form that loaded
  PACKAGE.
vc-git-stash-show-at-point    M-x ... RET
  Show the stash at point.
windmove-swap-states-right    M-x ... RET
  Swap the states with the window on the right from the current one.
window-configuration-to-register M-x ... RET
  Store the window configuration of the selected frame in register
  REGISTER.
yas-load-snippet-buffer-and-close M-x ... RET
  Load and save the snippet, then `quit-window' if saved.
add-file-local-variable-prop-line M-x ... RET
  Add file-local VARIABLE with its VALUE to the -*- line.
customize-group-other-window  M-x ... RET
  Customize GROUP, which must be a customization group, in another
  window.
diary-hebrew-insert-monthly-entry M-x ... RET
  Insert a monthly diary entry.
diary-islamic-insert-yearly-entry M-x ... RET
  Insert an annual diary entry.
display-fill-column-indicator-mode M-x ... RET
  Toggle display of `fill-column' indicator.
gnus-article-reply-with-original M-x ... RET
  Start composing a reply mail to the current message.
gnus-summary-expire-articles  M-x ... RET
  Expire all articles that are marked as expirable in the current
  group.
gnus-summary-remove-bookmark  M-x ... RET
  Remove the bookmark from the current article.
gnus-summary-tick-article-forward M-x ... RET
  Tick N articles forwards.
minibuffer-scroll-up-command  M-x ... RET
  Run `scroll-up-command' from the minibuffer in its original window.
org-backward-heading-same-level	M-x ... RET
  Move backward to the ARG'th subheading at same level as this one.
pr-ps-file-using-ghostscript  M-x ... RET
  Print PostScript file FILENAME using ghostscript.
yas-next-field-or-maybe-expand M-x ... RET
  Try to expand a snippet at a key before point.
yas/next-field-or-maybe-expand M-x ... RET
  Try to expand a snippet at a key before point.
comint-replace-by-expanded-history M-x ... RET
  Expand input command history references before point.
eww-browse-with-external-browser M-x ... RET
  Browse the current URL with an external browser.
find-file-read-only-other-window M-x ... RET
  Edit file FILENAME in another window but don't allow changes.
git-gutter:set-start-revision M-x ... RET
  Set start revision. If `start-rev' is nil or empty string then reset
gnus-article-button-next-page M-x ... RET
  Go to the next page.
gnus-article-button-prev-page M-x ... RET
  Go to the prev page.
gnus-mime-view-part-as-charset M-x ... RET
  Insert the MIME part under point into the current buffer using the
gnus-summary-edit-global-kill M-x ... RET
  Edit the "global" kill file.
gnus-summary-mark-region-as-read M-x ... RET
  Mark all unread articles between point and mark as read.
gnus-summary-refer-parent-article M-x ... RET
  Refer parent article N times.
previous-line-or-history-element M-x ... RET
  Move cursor vertically up ARG lines, or to the previous history
  element.
calendar-scroll-left-three-months M-x ... RET
  Scroll the displayed calendar window left by 3*ARG months.
compilation-next-error-function	M-x ... RET
  Advance to the next error message and visit the file where the error
  was.
gnus-summary-clear-mark-backward M-x ... RET
  Clear marks from N articles backward.
gnus-summary-mark-as-read-forward M-x ... RET
  Mark N articles as read forwards.
gnus-summary-wake-up-the-dead M-x ... RET
  Wake up the dead summary buffer.
ibuffer-backward-filter-group M-x ... RET
  Move point backwards by COUNT filtering groups.
ibuffer-jump-to-filter-group  M-x ... RET
  Move point to the filter group whose name is NAME.
magit-display-repository-buffer	M-x ... RET
  Display a Magit buffer belonging to the current Git repository.
standard-display-cyrillic-translit M-x ... RET
  Display a Cyrillic buffer using a transliteration.
symbol-overlay-jump-to-definition M-x ... RET
  Jump to the definition of symbol at point.
toggle-enable-multibyte-characters M-x ... RET
  Change whether this buffer uses multibyte characters.
calendar-mouse-view-diary-entries M-x ... RET
  Pop up menu of diary entries for mouse-selected date.
eldoc-print-current-symbol-info	M-x ... RET
  Document thing at point.
electric-indent-just-newline  M-x ... RET
  Insert just a newline, without any auto-indentation.
forms-find-file-other-window  M-x ... RET
  Visit a file in Forms mode in other window.
gnus-article-decode-mime-words M-x ... RET
  Run `article-decode-mime-words' in the article buffer.
gnus-article-normalize-headers M-x ... RET
  Run `article-normalize-headers' in the article buffer.
gnus-article-strip-blank-lines M-x ... RET
  Run `article-strip-blank-lines' in the article buffer.
gnus-article-treat-smartquotes M-x ... RET
  Run `article-treat-smartquotes' in the article buffer.
gnus-article-view-part-as-type M-x ... RET
  Choose a MIME media type, and view part N as such.
gnus-group-universal-argument M-x ... RET
  Perform any command on all groups according to the process/prefix
  convention.
gnus-kill-sticky-article-buffers M-x ... RET
  Kill all sticky article buffers.
gnus-summary-expire-articles-now M-x ... RET
  Expunge all expirable articles in the current group.
gnus-summary-limit-include-thread M-x ... RET
  Display all hidden articles belonging to thread ID.
gnus-summary-prev-page-or-article M-x ... RET
  Show previous page of selected article.
gnus-summary-sort-by-newsgroups	M-x ... RET
  Sort the summary buffer by newsgroups alphabetically.
ibuffer-mouse-filter-by-mode  M-x ... RET
  Enable or disable filtering by the major mode chosen via mouse.
beginning-of-buffer-other-window ESC <home>, ESC <begin>, M-<home>, M-<begin>
  Move point to the beginning of the buffer in the other window.
browse-url-default-haiku-browser M-x ... RET
  Browse URL with the system default browser.
dired-mark-files-containing-regexp M-x ... RET
  Mark all files with contents containing REGEXP for use in later
  commands.
ethio-input-special-character M-x ... RET
  This function is deprecated.
ibuffer-delete-saved-filters  M-x ... RET
  Delete saved filters with NAME from `ibuffer-saved-filters'.
mairix-search-thread-this-article M-x ... RET
  Search thread for the current article.
mml-secure-message-encrypt-smime M-x ... RET
  Add MML tag to encrypt and sign the entire message.
package-install-selected-packages M-x ... RET
  Ensure packages in `package-selected-packages' are installed.
project-display-buffer-other-frame M-x ... RET
  Display BUFFER-OR-NAME preferably in another frame.
translate-open-reference-file M-x ... RET
  Prompt to open the file FILENAME and set it as the reference buffer.
ucs-normalize-HFS-NFC-region  M-x ... RET
  Normalize region between FROM and TO by Unicode NFC and Mac OS's HFS
  Plus.
ucs-normalize-HFS-NFD-region  M-x ... RET
  Normalize region between FROM and TO by Unicode NFD and Mac OS's HFS
  Plus.
Buffer-menu-isearch-buffers   M-x ... RET
  Search for a string through all marked buffers using Isearch.
abridge-diff-disable-hiding   M-x ... RET
  Disable abridged text hiding.
article-strip-leading-space   M-x ... RET
  Remove all white space from the beginning of the lines in the
  article.
cal-menu-context-mouse-menu   M-x ... RET
  Pop up mouse menu for selected date in the calendar window.
  Value: menu-function-22
calendar-chinese-print-date   M-x ... RET
  Show the Chinese date equivalents of date.
calendar-islamic-print-date   M-x ... RET
  Show the Islamic calendar equivalent of the date under the cursor.
calendar-mayan-next-tzolkin-date M-x ... RET
  Move cursor to next instance of Mayan TZOLKIN-DATE.
calendar-persian-print-date   M-x ... RET
  Show the Persian calendar equivalent of the selected date.
calendar-sunrise-sunset-month M-x ... RET
  Local time of sunrise and sunset for month under cursor or at EVENT.
color-rg-rerun-change-globs   M-x ... RET
  Rerun last search but prompt for new files.
describe-language-environment <help> L, <f1> L
  Describe how Emacs supports language environment LANGUAGE-NAME.
desktop-save-in-desktop-dir   M-x ... RET
  Save the desktop in directory `desktop-dirname'.
diff-hl-reset-reference-rev   M-x ... RET
  Reset the reference revision globally to the most recent one.
diff-ignore-whitespace-hunk   M-x ... RET
  Re-diff the current hunk, ignoring whitespace differences.
dired-reset-subdir-switches   M-x ... RET
  Set `dired-switches-alist' to nil and revert dired buffer.
edebug-set-conditional-breakpoint M-x ... RET
  Set a conditional breakpoint at nearest sexp.
edit-tab-stops-note-changes   M-x ... RET
  Put edited tab stops into effect.
elisp-depend-insert-comment   M-x ... RET
  Insert a block of `sym' statements into an elisp file.
elisp-depend-insert-require   M-x ... RET
  Insert a block of (require sym) or 'autoload statements into an
  elisp file.
gif-screencast-toggle-pause   M-x ... RET
  Toggle recording of the GIF.
gnus-article-goto-next-page   M-x ... RET
  Show the next page of the article.
gnus-article-goto-prev-page   M-x ... RET
  Show the previous page of the article.
gnus-article-read-summary-keys M-x ... RET
  Read a summary buffer key sequence and execute it from the article
  buffer.
gnus-group-describe-briefly   M-x ... RET
  Give a one line description of the group mode commands.
gnus-group-sort-selected-groups	M-x ... RET
  Sort the process/prefixed groups.
gnus-summary-catchup-all-and-exit M-x ... RET
  Mark all articles in this newsgroup as read, and then exit.
gnus-summary-create-article   M-x ... RET
  Create an article in a mail newsgroup.
gnus-summary-end-of-article   M-x ... RET
  Scroll to the end of the article.
goto-line-preview-goto-line   M-x ... RET
  Preview goto line.
ibuffer-clear-filter-groups   M-x ... RET
  Remove all filter groups.
ibuffer-do-view-other-frame   M-x ... RET
  View each of the marked buffers in a separate frame.
ibuffer-toggle-filter-group   M-x ... RET
  Toggle the display status of the filter group on this line.
image-dired-setup-dired-keybindings M-x ... RET
  Setup easy-to-use keybindings for Image-Dired in Dired mode.
isearch-toggle-input-method   M-x ... RET
  Toggle input method in interactive search.
isearch-yank-from-kill-ring   M-x ... RET
  Read a string from the `kill-ring' and append it to the search
  string.
markdown-export-and-preview   M-x ... RET
  Export to XHTML using `markdown-export' and browse the resulting
  file.
markdown-follow-wiki-link-at-point M-x ... RET
  Find Wiki Link at point.
markdown-footnote-goto-text   M-x ... RET
  Jump to the text of the footnote at point.
minibuffer-hide-completions   M-x ... RET
  Get rid of an out-of-date *Completions* buffer.
mml-secure-message-sign-pgp   M-x ... RET
  Add MML tag to encrypt/sign the entire message.
org-babel-describe-bindings   M-x ... RET
  Describe all keybindings behind `org-babel-key-prefix'.
org-babel-execute-src-block-maybe M-x ... RET
  Conditionally execute a source block.
org-columns-remove-overlays   M-x ... RET
  Remove all currently active column overlays.
org-edit-footnote-reference   M-x ... RET
  Edit definition of footnote reference at point.
org-export-insert-default-template M-x ... RET
  Insert all export keywords with default values at beginning of line.
org-redisplay-inline-images   M-x ... RET
  Assure display of inline images and refresh them.
org-refile-goto-last-stored   M-x ... RET
  Go to the location where the last refile was stored.
org-roam-dailies-capture-yesterday M-x ... RET
  Create an entry in the daily-note for yesteday.
org-table-fedit-lisp-indent   M-x ... RET
  Pretty-print and re-indent Lisp expressions in the Formula Editor.
org-table-move-column-right   M-x ... RET
  Move column to the right.
osx-dictionary-search-input   M-x ... RET
  Search input word and display result with buffer.
outline-apply-default-state   M-x ... RET
  Apply the outline state defined by `outline-default-state'.
process-menu-delete-process   M-x ... RET
  Kill process at point in a `list-processes' buffer.
project-async-shell-command   M-x ... RET
  Run `async-shell-command' in the current project's root directory.
siri-shortcuts-gallery-open   M-x ... RET
  Open the Shortcuts Gallery.
thing-cut-to-line-beginning   M-x ... RET
  Cut content from current point to line beginning.
url-cookie-setup-save-timer   M-x ... RET
  Reset the cookie saver timer.
vc-git-stash-apply-at-point   M-x ... RET
  Apply the stash at point.
xref-show-location-at-point   M-x ... RET
  Display the source of xref at point in the appropriate window, if
  any.
diary-islamic-insert-monthly-entry M-x ... RET
  Insert a monthly diary entry.
execute-extended-command-cycle M-x ... RET
  Choose the next version of the extended command predicates.
gnus-article-treat-unfold-headers M-x ... RET
  Unfold folded message headers.
gnus-group-make-empty-virtual M-x ... RET
  Create a new, fresh, empty virtual group.
gnus-mime-view-part-externally M-x ... RET
  View the MIME part under point with an external viewer.
indian-2-column-to-ucs-region M-x ... RET
  Convert old Emacs Devanagari characters to UCS.
json-pretty-print-buffer-ordered M-x ... RET
  Pretty-print current buffer with object keys ordered.
org-icalendar-combine-agenda-files M-x ... RET
  Combine all agenda files into a single iCalendar file.
org-icalendar-export-agenda-files M-x ... RET
  Export all agenda files to iCalendar files.
org-update-radio-target-regexp M-x ... RET
  Find all radio targets in this file and update the regular
  expression.
toggle-tool-bar-mode-from-frame	<menu-bar> <options> <showhide> <showhide-tool-bar>
  Toggle tool bar on or off, based on the status of the current frame.
add-change-log-entry-other-window M-x ... RET
  Find change log file in other window and add entry and item.
edebug-temp-display-freq-count M-x ... RET
  Temporarily display the frequency count data for the current
  definition.
gnus-summary-limit-to-singletons M-x ... RET
  Limit the summary buffer to articles that aren't part on any thread.
gnus-summary-put-mark-as-expirable M-x ... RET
  Mark the current article as expirable.
gnus-summary-put-mark-as-read-next M-x ... RET
  Mark the current article as readnext.
markdown-previous-visible-heading M-x ... RET
  Move to the previous visible heading line of any level.
org-latex-convert-region-to-latex M-x ... RET
  Assume the current region has Org syntax, and convert it to LaTeX.
org-table-transpose-table-at-point M-x ... RET
  Transpose Org table at point and eliminate hlines.
pangu-spacing-space-current-buffer M-x ... RET
  Space current buffer.
timeclock-workday-remaining-string M-x ... RET
  Return a string representing the amount of time left today.
Buffer-menu-switch-other-window	M-x ... RET
  Make the other window select this line's buffer.
calendar-scroll-right-three-months M-x ... RET
  Scroll the displayed calendar window right by 3*ARG months.
edir-merge-revisions-with-ancestor M-x ... RET
  Run Ediff on DIR1 and merge its files with their revisions and
  ancestors.
global-diff-hl-show-hunk-mouse-mode M-x ... RET
  Toggle Diff-Hl-Show-Hunk-Mouse mode in all buffers.
  Value: nil
gnus-summary-put-mark-as-read-prev M-x ... RET
  Mark the current article as readprevious.
gnus-summary-repair-multipart M-x ... RET
  Add a Content-Type header to a multipart article without one.
mail-encode-encoded-word-buffer	M-x ... RET
  Encode the message header according to
  `rfc2047-header-encoding-alist'.
nndiary-generate-nov-databases M-x ... RET
  Generate NOV databases in all nndiary directories.
package-menu-filter-by-description M-x ... RET
  Filter the "*Packages*" buffer by DESCRIPTION regexp.
scroll-bar-maybe-set-window-start M-x ... RET
  Set the window start according to where the scroll bar is dragged.
gnus-article-verify-cancel-lock	M-x ... RET
  Run `article-verify-cancel-lock' in the article buffer.
gnus-group-describe-all-groups M-x ... RET
  Pop up a buffer with descriptions of all newsgroups.
gnus-group-sort-groups-by-method M-x ... RET
  Sort the group buffer alphabetically by back end name.
gnus-group-sort-groups-by-server M-x ... RET
  Sort the group buffer alphabetically by server name.
gnus-group-sort-groups-by-unread M-x ... RET
  Sort the group buffer by number of unread articles.
ibuffer-mark-for-delete-backwards M-x ... RET
  Mark for deletion the ARG previous buffers.
ignore-preserving-kill-region <mouse-movement>
  Like `ignore', but don't overwrite `last-event' if it's
  `kill-region'.
nonincremental-search-backward <menu-bar> <edit> <search> <search-backward>
  Read a string and search backward for it nonincrementally.
shrink-window-if-larger-than-buffer M-x ... RET
  Shrink height of WINDOW if its buffer doesn't need so many lines.
windmove-delete-default-keybindings M-x ... RET
  Set up keybindings for directional window deletion.
browse-url-default-macosx-browser M-x ... RET
  Invoke the macOS system's default Web browser.
emacs-lisp-native-compile-and-load M-x ... RET
  Native-compile synchronously the current file (if it has changed).
flyspell-correct-word-before-point M-x ... RET
  Pop up a menu of possible corrections for misspelled word before
  point.
gnus-group-get-new-news-this-group M-x ... RET
  Check for newly arrived news in the current group (and the N-1 next
  groups).
gnus-read-ephemeral-gmane-group-url M-x ... RET
  Create an ephemeral Gmane group from URL.
gnus-summary-show-all-expunged M-x ... RET
  Display all the hidden articles that were expunged for low scores.
gnus-summary-tick-article-backward M-x ... RET
  Tick N articles backwards.
hi-lock-write-interactive-patterns M-s h w
  Write interactively added patterns, if any, into buffer at point.
set-next-selection-coding-system <menu-bar> <options> <mule> <set-various-coding-system> <set-next-selection-coding-system>
  Use CODING-SYSTEM for next communication with other window system
  clients.
Buffer-menu-visit-tags-table  M-x ... RET
  Visit the tags table in the buffer on this line.  See
  `visit-tags-table'.
Info-search-case-sensitively  M-x ... RET
  Search for a regexp case-sensitively.
ace-pinyin-jump-char-in-line  M-x ... RET
  Ace-pinyn replacement of `avy-goto-char-in-line'.
article-capitalize-sentences  M-x ... RET
  Capitalize the first word in each sentence.
article-strip-trailing-space  M-x ... RET
  Remove all white space from the end of the lines in the article.
article-treat-ansi-sequences  M-x ... RET
  Translate ANSI SGR control sequences into overlays or extents.
blink-search-restart-process  M-x ... RET
  Stop and restart Blink-Search process.
calendar-ethiopic-print-date  M-x ... RET
  Show the Ethiopic calendar equivalent of the selected date.
citre-global-create-database  M-x ... RET
  Create gtags database.
citre-peek-through-reference  M-x ... RET
  Peek through a symbol in current peek window for references.
color-rg-replace-all-matches  M-x ... RET
  Replace all matched results.
color-rg-rerun-in-parent-dir  M-x ... RET
  Rerun last command in parent directory.
color-rg-rerun-toggle-ignore  M-x ... RET
  Rerun last search with toggled '--no-ignore' flag.
comint-replace-by-expanded-filename M-x ... RET
  Dynamically expand and complete the filename at point.
comment-or-uncomment-region+  M-x ... RET
  This function is to comment or uncomment a line or a region.
dictionary-lookup-definition  M-x ... RET
  Unconditionally lookup the word at point.
dired-find-file-other-window  M-x ... RET
  In Dired, visit this file or directory in another window.
dired-number-of-marked-files  M-x ... RET
  Display the number and total size of the marked files.
doc-view-fit-width-to-window  M-x ... RET
  Fit the image width to the window width.
duplicate-line-above-comment  M-x ... RET
  Duplicate current line above, and comment current line.
duplicate-line-below-comment  M-x ... RET
  Duplicate current line below, and comment current line.
ebrowse-electric-choose-tree  M-x ... RET
  Return a buffer containing a tree or nil if no tree found or
  canceled.
ebrowse-tags-find-definition  M-x ... RET
  Find definition of member at point.
ebrowse-tags-view-definition  M-x ... RET
  View definition of member at point.
gif-screencast-start-or-stop  <f9>
  Start a screencast or stop it if already screencasting.
gnus-article-edit-full-stops  M-x ... RET
  Interactively repair spacing at end of sentences.
gnus-group-enter-server-mode  M-x ... RET
  Jump to the server buffer.
gnus-group-expire-all-groups  M-x ... RET
  Expire all expirable articles in all newsgroups.
gnus-group-unmark-all-groups  M-x ... RET
  Unmark all groups.
gnus-mime-security-button-menu M-x ... RET
  Construct a context-sensitive menu of security commands.
  Value: (keymap "Security Part" (Show\ Detail menu-item "Show Detail" gnus-article-p…
gnus-mime-security-pipe-part  M-x ... RET
  Pipe the security part under point to a process.
gnus-mime-security-save-part  M-x ... RET
  Save the security part under point.
gnus-summary-edit-local-kill  M-x ... RET
  Edit a local kill file applied to the current newsgroup.
gnus-summary-edit-parameters  M-x ... RET
  Edit the group parameters of the current group.
gnus-summary-limit-to-unseen  M-x ... RET
  Limit to unseen articles.
gnus-summary-save-article-body-file M-x ... RET
  Append the current article body to a file.
gnus-summary-unmark-as-processable M-x ... RET
  Remove the process mark from the next N articles.
ibuffer-mark-special-buffers  M-x ... RET
  Mark all buffers whose name begins and ends with `*'.
ibuffer-mark-unsaved-buffers  M-x ... RET
  Mark all modified buffers that have an associated file.
icomplete-vertical-goto-last  M-x ... RET
  Go to last completions entry when `icomplete-scroll' is non-nil.
image-transform-fit-to-width  M-x ... RET
  Fit the current image to the width of the current window.
indent-relative-first-indent-point M-x ... RET
  Indent the current line like the previous nonblank line.
link-hint-copy-link-at-point  M-x ... RET
  Copy the link with the highest priority at the point.
link-hint-open-link-at-point  M-x ... RET
  Open the link with the highest priority at the point.
markdown-table-backward-cell  M-x ... RET
  Go to the previous cell in the table.
markdown-table-delete-column  M-x ... RET
  Delete column at point from table.
markdown-table-insert-column  M-x ... RET
  Insert a new table column.
markdown-table-move-row-down  M-x ... RET
  Move table row at point down.
move-past-close-and-reindent  M-)
  Move past next `)', delete indentation before it, then indent after
  it.
org-babel-exp-process-buffer  M-x ... RET
  Execute all Babel blocks in current buffer.
org-babel-goto-named-src-block M-x ... RET
  Go to a named source-code block.
org-babel-tangle-jump-to-org  M-x ... RET
  Jump from a tangled code file to the related Org mode file.
org-toggle-narrow-to-subtree  M-x ... RET
  Narrow to the subtree at point or widen a narrowed buffer.
popwin:switch-to-last-buffer  M-x ... RET
  Switch to the last popup buffer.
quickrun-compile-only-select  M-x ... RET
  Run commands after selecting the backend.
reveal-in-folder-this-buffer  M-x ... RET
  Reveal the current buffer in folder.
symbol-overlay-query-replace  M-x ... RET
  Query replace symbol at point.
thing-replace-region-or-line  M-x ... RET
  Replace the current region or line with the content.
url-history-setup-save-timer  M-x ... RET
  Reset the history list timer.
vc-git-stash-delete-at-point  M-x ... RET
  Delete the stash at point.
with-editor-export-hg-editor  M-x ... RET
  Like `with-editor-export-editor' but always set `$HG_EDITOR'.
calendar-mayan-previous-haab-date M-x ... RET
  Move cursor to previous instance of Mayan HAAB-DATE.
ediff-merge-revisions-with-ancestor <menu-bar> <tools> <ediff-merge> <ediff-merge-revisions-with-ancestor>
  Run Ediff by merging two revisions of a file with a common ancestor.
elisp-last-sexp-toggle-display M-x ... RET
  Toggle between abbreviated and unabbreviated printed
  representations.
execute-extended-command-for-buffer M-X
  Query user for a command relevant for the current mode, and then
  execute it.
gnus-article-toggle-truncate-lines M-x ... RET
  Toggle whether to fold or truncate long lines in article the buffer.
markdown-insert-header-setext-1	M-x ... RET
  Insert a setext-style (underlined) first-level header.
markdown-insert-header-setext-2	M-x ... RET
  Insert a setext-style (underlined) second-level header.
minibuffer-recenter-top-bottom M-x ... RET
  Run `recenter-top-bottom' from the minibuffer in its original
  window.
minibuffer-scroll-down-command M-x ... RET
  Run `scroll-down-command' from the minibuffer in its original
  window.
minibuffer-scroll-other-window M-x ... RET
  Run `scroll-other-window' from the minibuffer in its original
  window.
origami-indicators-click-fringe	M-x ... RET
  EVENT click on fringe.
standard-display-by-replacement-char M-x ... RET
  Produce code to display characters between FROM and TO using REPL.
youtube-sub-extractor-subtitles-mode M-x ... RET
  Minor mode for youtube-sub-extractor.
  Value: nil
compilation-set-skip-threshold M-x ... RET
  Switch the `compilation-skip-threshold' level.
doc-view-search-previous-match M-x ... RET
  Go to the ARGth previous matching page.
edebug-toggle-save-selected-window M-x ... RET
  Toggle the saving and restoring of the selected window.
gnus-summary-first-unread-article M-x ... RET
  Select the first unread article.
gnus-summary-mark-as-read-backward M-x ... RET
  Mark the N articles as read backwards.
gnus-summary-next-unseen-article M-x ... RET
  Select the next unseen article.
icomplete-force-complete-and-exit M-x ... RET
  Complete the minibuffer with the longest possible match and exit.
kmacro-start-macro-or-insert-counter <f3>
  Record subsequent keyboard input, defining a keyboard macro.
org-property-next-allowed-value	M-x ... RET
  Switch to the next allowed value for this property.
previous-complete-history-element M-x ... RET
  Get previous history element that completes the minibuffer before
  the point.
standard-themes-preview-colors M-x ... RET
  Preview palette of the Standard THEME of choice.
describe-minor-mode-from-indicator M-x ... RET
  Display documentation of a minor mode specified by INDICATOR.
ibuffer-decompose-filter-group M-x ... RET
  Decompose the filter group GROUP into active filters.
ebrowse-forward-in-position-stack M-x ... RET
  Move forward in the position stack.
find-function-on-key-other-frame M-x ... RET
  Find, in the other frame, the function that KEY invokes.
gnus-article-hide-boring-headers M-x ... RET
  Run `article-hide-boring-headers' in the article buffer.
gnus-article-strip-leading-space M-x ... RET
  Run `article-strip-leading-space' in the article buffer.
gnus-summary-show-complete-article M-x ... RET
  Show a complete version of the current article.
org-roam-ui-add-to-local-graph M-x ... RET
  Add current node to the local graph. If not in local mode, open
  local-graph for this node.
calendar-scroll-toolkit-scroll M-x ... RET
  Function to scroll the calendar after a toolkit scroll-bar click.
gnus-article-describe-key-briefly M-x ... RET
  Display documentation of the function invoked by KEY.
gnus-article-save-part-and-strip M-x ... RET
  Save MIME part N and replace it with an external body.
gnus-group-description-apropos M-x ... RET
  List all newsgroups that have names or descriptions that match
  REGEXP.
gnus-group-visible-select-group	M-x ... RET
  Select the current group without hiding any articles.
ibuffer-delete-saved-filter-groups M-x ... RET
  Delete saved filter groups with NAME.
mml-secure-message-encrypt-pgpauto M-x ... RET
  Add MML tag to encrypt and sign the entire message.
mml-secure-message-encrypt-pgpmime M-x ... RET
  Add MML tag to encrypt and sign the entire message.
shell-replace-by-expanded-directory M-x ... RET
  Expand directory stack reference before point.
windmove-display-default-keybindings M-x ... RET
  Set up keybindings for directional buffer display.
yas-skip-and-clear-or-delete-char M-x ... RET
  Clears unmodified field if at field start, skips to next tab.
yas/skip-and-clear-or-delete-char M-x ... RET
  Clears unmodified field if at field start, skips to next tab.
Buffer-menu-view-other-window M-x ... RET
  View this line's buffer in View mode in another window.
ansi-color-for-comint-mode-on M-x ... RET
  Set `ansi-color-for-comint-mode' to t.
article-strip-all-blank-lines M-x ... RET
  Strip all blank lines.
article-strip-headers-in-body M-x ... RET
  Strip offensive headers from bodies.
auth-source-forget-all-cached M-x ... RET
  Forget all cached auth-source data.
avy-transpose-lines-in-region M-x ... RET
  Transpose lines in the active region.
browse-url-default-windows-browser M-x ... RET
  Invoke the MS-Windows system's default Web browser.
company-tng-configure-default M-x ... RET
  Applies the default configuration to enable company-tng.
describe-personal-keybindings M-x ... RET
  Display all the personal keybindings defined by `bind-key'.
dictionary-popup-matching-words	M-x ... RET
  Display entries matching WORD or the current word if not given.
dns-mode-soa-increment-serial M-x ... RET
  Locate SOA record and increment the serial field.
doc-view-fit-height-to-window M-x ... RET
  Fit the image height to the window height.
doc-view-kill-proc-and-buffer M-x ... RET
  Kill the current buffer.
ebrowse-tags-find-declaration M-x ... RET
  Find declaration of member at point.
ebrowse-tags-view-declaration M-x ... RET
  View declaration of member at point.
elisp-depmap-makesummarytable M-x ... RET
  Make a summary org table of variables and references to them.
file-cache-add-directory-recursively M-x ... RET
  Add DIR and any subdirectories to the file-cache.
git-gutter:update-all-windows M-x ... RET
  Update git-gutter information for all visible buffers.
gnus-article-describe-briefly M-x ... RET
  Describe article mode commands briefly.
gnus-group-first-unread-group M-x ... RET
  Go to the first group with unread articles.
gnus-summary-describe-briefly M-x ... RET
  Describe summary mode commands briefly.
gnus-summary-limit-include-dormant M-x ... RET
  Display all the hidden articles that are marked as dormant.
gnus-summary-rethread-current M-x ... RET
  Rethread the thread the current article is part of.
gnus-summary-show-all-headers M-x ... RET
  Make all header lines visible.
gnus-summary-show-all-threads M-x ... RET
  Show all threads.
gnus-summary-show-raw-article M-x ... RET
  Show the raw article without any article massaging functions being
  run.
grep-find-toggle-abbreviation M-x ... RET
  Toggle showing the hidden part of rgrep/lgrep/zrgrep command line.
highlight-changes-next-change M-x ... RET
  Move to the beginning of the next change, if in Highlight Changes
  mode.
highlight-changes-remove-highlight M-x ... RET
  Remove the change face from the region between BEG and END.
ibuffer-mark-modified-buffers M-x ... RET
  Mark all modified buffers.
ibuffer-visit-buffer-1-window M-x ... RET
  Visit the buffer on this line, and delete other windows.
icomplete-fido-backward-updir M-x ... RET
  Delete char before or go up directory, like `ido-mode'.
icomplete-vertical-goto-first M-x ... RET
  Go to first completions entry when `icomplete-scroll' is non-nil.
image-transform-fit-to-height M-x ... RET
  Fit the current image to the height of the current window.
image-transform-fit-to-window M-x ... RET
  Fit the current image to the height and width of the current window.
link-hint-copy-multiple-links M-x ... RET
  Use avy to copy multiple visible links at once to the kill ring.
link-hint-open-multiple-links M-x ... RET
  Use avy to open multiple visible links at once.
lsp-bridge-toggle-sdcv-helper M-x ... RET
  Toggle sdcv helper.
markdown-cleanup-list-numbers M-x ... RET
  Update the numbering of ordered lists.
markdown-insert-header-setext-dwim M-x ... RET
  Insert or replace header markup, with preference for setext.
markdown-toggle-inline-images M-x ... RET
  Toggle inline image overlays in the buffer.
menu-bar-right-window-divider <menu-bar> <options> <showhide> <showhide-window-divider> <right-only>
  Display dividers only on the right of each window.
message-goto-mail-followup-to M-x ... RET
  Move point to the Mail-Followup-To header.
message-insert-importance-low M-x ... RET
  Insert header to mark message as unimportant.
mml-secure-message-sign-smime M-x ... RET
  Add MML tag to encrypt/sign the entire message.
occur-mode-display-occurrence M-x ... RET
  Display in another window the occurrence the current line describes.
org-babel-goto-src-block-head M-x ... RET
  Go to the beginning of the current code block.
org-table-overlay-coordinates M-x ... RET
  Add overlays to the table at point, to show row/column coordinates.
  Value: nil
osx-dictionary-search-pointer M-x ... RET
  Search word around and display result with buffer.
profiler-report-compare-profile	M-x ... RET
  Compare the current profile with another.
siri-shortcuts-gallery-search M-x ... RET
  Search the Gallery with the given QUERY.
symbol-overlay-switch-forward M-x ... RET
  Switch forward to another symbol.
tramp-cleanup-this-connection M-x ... RET
  Flush all connection related objects of the current buffer's
  connection.
with-editor-export-git-editor M-x ... RET
  Like `with-editor-export-editor' but always set `$GIT_EDITOR'.
xwidget-webkit-search-forward M-x ... RET
  Search forward of `text'
gnus-article-followup-with-original M-x ... RET
  Compose a followup to the current article.
gnus-summary-select-article-buffer M-x ... RET
  Reconfigure windows to show the article buffer.
org-bibtex-create-in-current-entry M-x ... RET
  Add bibliographical data to the current entry.
checkdoc-ispell-message-interactive M-x ... RET
  Check the style and spelling of message text interactively.
ff-find-other-file-other-window	M-x ... RET
  Visit the file you point at in another window.
gnus-article-view-part-as-charset M-x ... RET
  View MIME part N using a specified charset.
gnus-read-ephemeral-emacs-bug-group M-x ... RET
  Browse Emacs bug reports with IDS in an ephemeral group.
gnus-summary-put-mark-as-ticked-next M-x ... RET
  Mark the current article as tickednext.
gnus-summary-put-mark-as-unread-next M-x ... RET
  Mark the current article as unreadnext.
isearch-yank-char-in-minibuffer	M-x ... RET
  Pull next character from buffer into end of search string in
  minibuffer.
org-src-do-key-sequence-at-code-block M-x ... RET
  Execute key sequence at code block in the source Org buffer.
calendar-mayan-previous-round-date M-x ... RET
  Move to previous instance of Mayan TZOLKIN-DATE HAAB-DATE
  combination.
follow-delete-other-windows-and-split M-x ... RET
  Create two side by side windows and enter Follow mode.
gnus-group-select-group-ephemerally M-x ... RET
  Select the current group without doing any processing whatsoever.
gnus-summary-put-mark-as-ticked-prev M-x ... RET
  Mark the current article as tickedprevious.
gnus-summary-put-mark-as-unread-prev M-x ... RET
  Mark the current article as unreadprevious.
ibuffer-filters-to-filter-group	M-x ... RET
  Make the current filters into a filtering group.
indent-rigidly-left-to-tab-stop	M-x ... RET
  Indent all lines between BEG and END leftward to a tab stop.
org-speedbar-set-agenda-restriction M-x ... RET
  Restrict future agenda commands to the location at point in
  speedbar.
org-timer-change-times-in-region M-x ... RET
  Change all h:mm:ss time in region by a DELTA.
calendar-mayan-goto-long-count-date M-x ... RET
  Move cursor to Mayan long count DATE.
describe-minor-mode-from-symbol	M-x ... RET
  Display documentation of a minor mode given as a symbol, SYMBOL.
find-function-on-key-other-window M-x ... RET
  Find, in the other window, the function that KEY invokes.
gnus-article-capitalize-sentences M-x ... RET
  Run `article-capitalize-sentences' in the article buffer.
gnus-article-date-combined-lapsed M-x ... RET
  Run `article-date-combined-lapsed' in the article buffer.
gnus-article-de-base64-unreadable M-x ... RET
  Run `article-de-base64-unreadable' in the article buffer.
gnus-article-de-quoted-unreadable M-x ... RET
  Run `article-de-quoted-unreadable' in the article buffer.
gnus-article-decode-encoded-words M-x ... RET
  Run `article-decode-encoded-words' in the article buffer.
gnus-article-strip-trailing-space M-x ... RET
  Run `article-strip-trailing-space' in the article buffer.
gnus-article-treat-ansi-sequences M-x ... RET
  Run `article-treat-ansi-sequences' in the article buffer.
gnus-group-sort-groups-by-alphabet M-x ... RET
  Sort the group buffer alphabetically by group name.
gnus-summary-first-unread-subject M-x ... RET
  Place the point on the subject line of the first unread article.
gnus-summary-first-unseen-subject M-x ... RET
  Place the point on the subject line of the first unseen article.
gnus-summary-reselect-current-group M-x ... RET
  Exit and then reselect the current newsgroup.
gnus-summary-write-article-body-file M-x ... RET
  Write the current article body to a file, deleting the previous
  file.
grugru-find-function-integration-mode M-x ... RET
  `describe-function' can find functions defined by
  `grugru-define-function'.
  Value: nil
org-babel-remove-result-one-or-many M-x ... RET
  Remove the result of the current source block.
profiler-find-profile-other-frame M-x ... RET
  Open profile FILENAME.
youtube-sub-extractor-copy-ts-link M-x ... RET
  Construct a video url at specific timestamp.
doc-view-set-slice-from-bounding-box M-x ... RET
  Set the slice from the document's BoundingBox information.
origami-forward-fold-same-level	M-x ... RET
  Move point to the beginning of the next fold in the buffer that is a
  sibling of the fold the point is currently in.
shr-mouse-browse-url-new-window	M-x ... RET
  Browse the URL under the mouse cursor in a new window.
tabulated-list-widen-current-column M-x ... RET
  Widen the current tabulated-list column by N chars.
ansi-color-for-comint-mode-off M-x ... RET
  Set `ansi-color-for-comint-mode' to nil.
citre-peek-move-current-def-up M-x ... RET
  Move the current tag up.
citre-peek-move-current-tag-up M-x ... RET
  Move the current tag up.
comint-dynamic-list-input-ring M-x ... RET
  Display a list of recent inputs entered into the current buffer.
copy-dir-locals-to-file-locals M-x ... RET
  Copy directory-local variables to the Local Variables list.
copy-file-locals-to-dir-locals M-x ... RET
  Copy file-local variables to .dir-locals.el.
describe-current-coding-system M-x ... RET
  Display coding systems currently used, in detail.
describe-current-display-table <menu-bar> <help-menu> <describe> <describe-current-display-table>
  Describe the display table in use in the selected window and buffer.
dired-isearch-filenames-regexp M-x ... RET
  Search for a regexp using Isearch only in file names in the Dired
  buffer.
ebrowse-electric-position-menu M-x ... RET
  List positions in the position stack in an electric buffer.
ediff-merge-directories-with-ancestor <menu-bar> <tools> <ediff-merge> <ediff-merge-directories-with-ancestor>
  Merge files in DIR1 and DIR2 using files in ANCESTOR-DIR as
  ancestors.
eww-toggle-paragraph-direction M-x ... RET
  Cycle the paragraph direction between left-to-right, right-to-left
  and auto.
gnus-article-highlight-headers M-x ... RET
  Highlight article headers as specified by `gnus-header-face-alist'.
gnus-group-edit-group-parameters M-x ... RET
  Edit the group parameters of GROUP.
gnus-summary-goto-last-article M-x ... RET
  Go to the previously read article.
gnus-summary-kill-process-mark M-x ... RET
  Push the current set of process marked articles on the stack and
  unmark.
gnus-summary-next-same-subject M-x ... RET
  Select next article which has the same subject as current one.
gnus-summary-prev-same-subject M-x ... RET
  Select previous article which has the same subject as current one.
gnus-summary-save-process-mark M-x ... RET
  Push the current set of process marked articles on the stack.
gnus-summary-universal-argument	M-x ... RET
  Perform any operation on all articles that are process/prefixed.
gnus-summary-yank-process-mark M-x ... RET
  Pop the last process mark state off the stack and restore it.
ibuffer-mark-read-only-buffers M-x ... RET
  Mark all read-only buffers.
ibuffer-switch-to-saved-filters	M-x ... RET
  Set this buffer's filters to filters with NAME from
  `ibuffer-saved-filters'.
ido-find-file-read-only-other-frame M-x ... RET
  Edit file read-only in other frame with name obtained via
  minibuffer.
isearch-help-for-help-internal M-x ... RET
  Help command.
isearch-transient-input-method M-x ... RET
  Activate transient input method in interactive search.
kmacro-end-or-call-macro-repeat	M-x ... RET
  As `kmacro-end-or-call-macro' but allow repeat without repeating
  prefix.
menu-bar-bottom-window-divider <menu-bar> <options> <showhide> <showhide-window-divider> <bottom-only>
  Display dividers only at the bottom of each window.
message-insert-importance-high M-x ... RET
  Insert header to mark message as important.
org-bibtex-export-to-kill-ring M-x ... RET
  Export current headline to kill ring as bibtex entry.
org-kill-note-or-show-branches M-x ... RET
  Abort storing current note, or show just branches.
org-toggle-time-stamp-overlays M-x ... RET
  Toggle the use of custom time stamp formats.
package-menu-filter-upgradable M-x ... RET
  Filter "*Packages*" buffer to show only upgradable packages.
pkg-dm-rebuild-dependency-list M-x ... RET
  Rebuild dependency graph and save to list.
profiler-report-ascending-sort M-x ... RET
  Sort calltree view in ascending order.
profiler-report-collapse-entry M-x ... RET
  Collapse entry at point.
profiler-report-describe-entry M-x ... RET
  Describe entry at point.
profiler-report-previous-entry M-x ... RET
  Move cursor to previous entry.
project-forget-zombie-projects M-x ... RET
  Forget all known projects that don't exist any more.
shortdoc-copy-function-as-kill M-x ... RET
  Copy name of the function near point into the kill ring.
strokes-compose-complex-stroke M-x ... RET
  Read a complex stroke and insert its glyph into the current buffer.
symbol-overlay-switch-backward M-x ... RET
  Switch backward to another symbol.
symbol-overlay-toggle-in-scope M-x ... RET
  Toggle overlays to be showed in buffer or only in scope.
universal-coding-system-argument <menu-bar> <options> <mule> <set-various-coding-system> <universal-coding-system-argument>
  Execute an I/O command using the specified CODING-SYSTEM.
vc-git-log-edit-toggle-signoff M-x ... RET
  Toggle whether to add the "Signed-off-by" line at the end of commit
  message.
xref-quit-and-pop-marker-stack M-x ... RET
  Quit *xref* buffer, then pop the xref marker stack.
yas-describe-table-by-namehash M-x ... RET
  Display snippet tables by NAMEHASH.
feedmail-run-the-queue-global-prompt M-x ... RET
  Like `feedmail-run-the-queue', but with a global confirmation
  prompt.
gnus-summary-search-article-forward M-x ... RET
  Search for an article containing REGEXP forward.
ibuffer-visit-buffer-other-window M-x ... RET
  Visit the buffer on this line in another window.
browse-kill-ring-default-keybindings M-x ... RET
  Set up M-y (`yank-pop') so that it can invoke `browse-kill-ring'.
describe-current-coding-system-briefly <menu-bar> <help-menu> <describe> <describe-coding-system-briefly>
  Display coding systems currently used in a brief format in echo
  area.
gnus-group-read-ephemeral-search-group M-x ... RET
  Read an nnselect group based on a search.
gnus-read-ephemeral-debian-bug-group M-x ... RET
  Browse Debian bug reports with IDS in an ephemeral group.
ibuffer-mark-by-file-name-regexp M-x ... RET
  Mark all buffers whose file name matches REGEXP.
translate-select-reference-buffer M-x ... RET
  Prompt to select the reference buffer BUF.
xref-find-references-and-replace M-x ... RET
  Replace all references to identifier FROM with TO.
clone-indirect-buffer-other-window M-x ... RET
  Like `clone-indirect-buffer' but display in another window.
gnus-summary-next-unread-subject M-x ... RET
  Go to next N'th unread summary line.
gnus-summary-put-mark-as-dormant-next M-x ... RET
  Mark the current article as dormantnext.
gnus-summary-put-mark-as-dormant-prev M-x ... RET
  Mark the current article as dormantprevious.
indent-rigidly-right-to-tab-stop M-x ... RET
  Indent all lines between BEG and END rightward to a tab stop.
View-revert-buffer-scroll-page-forward M-x ... RET
  Scroll forward, reverting buffer if needed, in View mode.
gnus-article-hide-list-identifiers M-x ... RET
  Run `article-hide-list-identifiers' in the article buffer.
gnus-article-strip-all-blank-lines M-x ... RET
  Run `article-strip-all-blank-lines' in the article buffer.
gnus-article-strip-headers-in-body M-x ... RET
  Run `article-strip-headers-in-body' in the article buffer.
gnus-summary-prev-unread-subject M-x ... RET
  Go to previous N'th unread summary line.
ido-find-alternate-file-other-window M-x ... RET
  Find file as a replacement for the file in the next window.
message-insert-or-toggle-importance M-x ... RET
  Insert a "Importance: high" header, or cycle through the header
  values.
nonincremental-re-search-forward <menu-bar> <edit> <search> <re-search-forward>
  Read a regular expression and search for it nonincrementally.
orgtbl-create-or-convert-from-region M-x ... RET
  Create table or convert region to table, if no conflicting binding.
profiler-find-profile-other-window M-x ... RET
  Open profile FILENAME.
doc-view-scroll-down-or-previous-page M-x ... RET
  Scroll page down ARG lines if possible, else goto previous page.
flyspell-auto-correct-previous-word M-x ... RET
  Auto correct the first misspelled word that occurs before point.
tabulated-list-narrow-current-column M-x ... RET
  Narrow the current tabulated list column by N chars.
View-scroll-page-forward-set-page-size M-x ... RET
  Scroll forward LINES lines in View mode, setting the "page size".
amread-voice-reader-read-buffer	M-x ... RET
  Read current buffer text without timer highlight updating.
calendar-astro-print-day-number	M-x ... RET
  Show astronomical (Julian) day number after noon UTC on cursor date.
citre-ctags-cmd-buf-add-dir-or-file M-x ... RET
  Insert a directory or file in the command editing buffer.
comint-history-isearch-backward	M-x ... RET
  Search for a string backward in input history using Isearch.
comint-next-matching-input-from-input M-x ... RET
  Search forwards through input history for match for current input.
delete-selection-repeat-replace-region M-x ... RET
  Repeat replacing text of highlighted region with typed text.
diary-chinese-insert-yearly-entry M-x ... RET
  Insert a yearly diary entry for the Chinese date at point.
doc-view-center-page-vertically	M-x ... RET
  Center page vertically when page is wider than window.
edebug-set-global-break-condition M-x ... RET
  Set `edebug-global-break-condition' to EXPRESSION.
gnus-article-treat-fold-headers	M-x ... RET
  Fold message headers.
gnus-summary-stop-page-breaking	M-x ... RET
  Stop page breaking in the current article.
howdoi			      M-x ... RET
  (not documented)
ido-find-file-read-only-other-window M-x ... RET
  Edit file read-only in other window with name obtained via
  minibuffer.
image-dired-dired-comment-files	M-x ... RET
  Add comment to current or marked files in Dired.
isearch-forward-exit-minibuffer	M-x ... RET
  Resume isearching forward from the minibuffer that edits the search
  string.
isearch-reverse-exit-minibuffer	M-x ... RET
  Resume isearching backward from the minibuffer that edits the search
  string.
markdown-table-move-column-left	M-x ... RET
  Move table column at point to the left.
message-toggle-image-thumbnails	M-x ... RET
  For any included image files, insert a thumbnail of that image.
mml-secure-message-sign-pgpauto	M-x ... RET
  Add MML tag to encrypt/sign the entire message.
mml-secure-message-sign-pgpmime	M-x ... RET
  Add MML tag to encrypt/sign the entire message.
my/eval-current-elisp-func-only	M-x ... RET
   eval-last-sexp 当前光标处的 function 
org-roam-dailies-find-directory	M-x ... RET
  Find and open `org-roam-dailies-directory'.
org-table-fedit-toggle-ref-type	M-x ... RET
  Convert all references in the buffer from B3 to @3$2 and back.
org-table-iterate-buffer-tables	M-x ... RET
  Iterate all tables in the buffer, to converge inter-table
  dependencies.
origami-backward-fold-same-level M-x ... RET
  Move point to the beginning of the previous fold in the buffer that
  is a sibling of the fold the point is currently in.
profiler-report-descending-sort	M-x ... RET
  Sort calltree view in descending order.
profiler-report-render-calltree	M-x ... RET
  Render calltree view.
calendar-mayan-previous-tzolkin-date M-x ... RET
  Move cursor to previous instance of Mayan TZOLKIN-DATE.
color-rg-rerun-change-exclude-files M-x ... RET
  Rerun last search but prompt for new files which will NOT be
  searched.
feedmail-run-the-queue-no-prompts M-x ... RET
  Like `feedmail-run-the-queue', but suppress confirmation prompts.
find-lisp-find-dired-other-window M-x ... RET
  Same as `find-lisp-find-dired', but use another window.
gnus-group-sort-groups-by-real-name M-x ... RET
  Sort the group buffer alphabetically by real (unprefixed) group
  name.
xref-find-definitions-other-frame M-x ... RET
  Like `xref-find-definitions' but switch to the other frame.
image-dired-dired-toggle-marked-thumbs M-x ... RET
  Toggle thumbnails in front of file names in the Dired buffer.
minibuffer-default-add-shell-commands M-x ... RET
  Return a list of all commands associated with the current file.
org-element-cache-hash-show-statistics M-x ... RET
  Display efficiency of O(1) query cache for
  `org-element--cache-find'.
org-texinfo-convert-region-to-texinfo M-x ... RET
  Assume the current region has Org syntax, and convert it to Texinfo.
file-cache-add-directory-using-find M-x ... RET
  Use the `find' command to add files to the file cache.
org-insert-heading-respect-content M-x ... RET
  Insert heading with `org-insert-heading-respect-content' set to t.
  Value: nil
read-char-from-minibuffer-insert-other M-x ... RET
  Reject a disallowed character typed into the minibuffer.
youtube-sub-extractor-browse-ts-link M-x ... RET
  Browse video url at specific timestamp.
flyspell-auto-correct-previous-hook M-x ... RET
  Hook to track successive calls to
  `flyspell-auto-correct-previous-word'.
gnus-article-wide-reply-with-original M-x ... RET
  Start composing a wide reply mail to the current message.
gnus-cache-generate-nov-databases M-x ... RET
  Generate NOV files recursively starting in DIR.
magit-switch-to-repository-buffer M-x ... RET
  Switch to a Magit buffer belonging to the current Git repository.
dired-mouse-find-file-other-frame M-x ... RET
  In Dired, visit the file or directory name you click on in another
  frame.
ibuffer-interactive-filter-by-mode M-x ... RET
  Enable or disable filtering by the major mode at point.
ibuffer-mouse-toggle-filter-group M-x ... RET
  Toggle the display status of the filter group chosen with the mouse.
nonincremental-re-search-backward <menu-bar> <edit> <search> <re-search-backward>
  Read a regular expression and search backward for it
  nonincrementally.
uniquify-rationalize-file-buffer-names M-x ... RET
  Make file buffer names unique by adding segments from file name.
calendar-exchange-point-and-mark M-x ... RET
  Exchange the current cursor position with the marked date.
citre-peek-move-current-def-down M-x ... RET
  Move the current tag down.
citre-peek-move-current-tag-down M-x ... RET
  Move the current tag down.
diary-chinese-insert-monthly-entry M-x ... RET
  Insert a monthly diary entry for the Chinese date at point.
edebug-toggle-disable-breakpoint M-x ... RET
  Toggle whether the breakpoint near point is disabled.
emacs-lisp-byte-compile-and-load <menu-bar> <emacs-lisp> <Byte-compile and Load>
  Byte-compile the current file (if it has changed), then load
  compiled code.
emacs-lisp-compilation-recompile M-x ... RET
  Recompile the previously byte-compiled file.
eudc-insert-record-at-point-into-bbdb M-x ... RET
  Insert record at point into the BBDB database.
gnus-article-add-buttons-to-head M-x ... RET
  Add buttons to the head of the article.
gnus-article-treat-body-boundary M-x ... RET
  Place a boundary line at the end of the headers.
gnus-insert-random-x-face-header M-x ... RET
  Insert a random X-Face header from `gnus-x-face-directory'.
gnus-summary-best-unread-subject M-x ... RET
  Select the unread subject with the highest score.
gnus-summary-insert-new-articles M-x ... RET
  Insert all new articles in this group.
gnus-summary-mark-article-as-replied M-x ... RET
  Mark ARTICLE as replied to and update the summary line.
gnus-summary-next-unread-article M-x ... RET
  Select unread article after current one.
gnus-summary-prev-unread-article M-x ... RET
  Select unread article before current one.
gnus-summary-prev-unseen-article M-x ... RET
  Select the previous unseen article.
help-with-tutorial-spec-language <menu-bar> <help-menu> <emacs-tutorial-language-specific>
  Use the Emacs tutorial, specifying which language you want.
ibuffer-mark-dissociated-buffers M-x ... RET
  Mark all buffers whose associated file does not exist.
ibuffer-visit-buffer-other-frame M-x ... RET
  Visit the buffer on this line in another frame.
ilog-toggle-display-buffer-names M-x ... RET
  Toggle display of buffers in log buffer.
image-transform-reset-to-initial M-x ... RET
  Display the current image with the default (initial) size and
  rotation.
markdown-outline-next-same-level M-x ... RET
  Move to next list item or heading of same level.
markdown-table-move-column-right M-x ... RET
  Move table column at point to the right.
menu-bar-showhide-fringe-ind-box <menu-bar> <options> <showhide> <showhide-fringe> <showhide-fringe-ind> <box>
  Display top and bottom indicators in opposite fringes.
org-ascii-convert-region-to-utf8 M-x ... RET
  Assume region has Org syntax, and convert it to UTF-8.
org-insert-heading-after-current M-x ... RET
  Insert a new heading with same level as current, after current
  subtree.
org-reset-checkbox-state-subtree M-x ... RET
  Reset all checkboxes in an entry subtree.
org-table-create-or-convert-from-region	M-x ... RET
  Convert region to table, or create an empty table.
org-table-maybe-recalculate-line M-x ... RET
  Recompute the current line if marked for it, and if we haven't just
  done it.
project-execute-extended-command M-x ... RET
  Execute an extended command in project root.
sis--inline-fly-check-deactivate M-x ... RET
  Check whether to deactivate the inline region overlay.
symbol-overlay-isearch-literally M-x ... RET
  Isearch symbol at point literally.
vc-git-log-edit-toggle-no-verify M-x ... RET
  Toggle whether to bypass the pre-commit and commit-msg hooks.
wucuo-spell-check-visible-region M-x ... RET
  Spell check visible region in current buffer.
describe-random-interactive-function M-x ... RET
  Show the documentation for a random interactive function.
gnus-article-view-part-externally M-x ... RET
  View MIME part N externally, which is the numerical prefix.
gnus-summary-sort-by-most-recent-date M-x ... RET
  Sort the summary buffer by most recent date.
xref-find-definitions-other-window <menu-bar> <edit> <goto> <xref-find-otherw>
  Like `xref-find-definitions' but switch to the other window.
org-property-previous-allowed-value M-x ... RET
  Switch to the next allowed value for this property.
file-cache-add-directory-using-locate M-x ... RET
  Use the `locate' command to add files to the file cache.
gnus-article-outlook-rearrange-citation	M-x ... RET
  Repair broken citations.
gnus-summary-put-mark-as-expirable-next	M-x ... RET
  Mark the current article as expirablenext.
gnus-group-sort-selected-groups-by-rank	M-x ... RET
  Sort the group buffer by group rank.
gnus-summary-put-mark-as-expirable-prev	M-x ... RET
  Mark the current article as expirableprevious.
ibuffer-switch-to-saved-filter-groups M-x ... RET
  Set this buffer's filter groups to saved version with NAME.
magit-add-change-log-entry-other-window	M-x ... RET
  Find change log file in other window and add entry and item.
message-cross-post-followup-to-header M-x ... RET
  Mangles FollowUp-To and Newsgroups header to point to TARGET-GROUP.
windmove-swap-states-default-keybindings M-x ... RET
  Set up keybindings for directional window swap states.
youtube-sub-extractor-extract-subs M-x ... RET
  For a given YouTube vid VIDEO-URL, extract subtitles and open them
  in a buffer.
comint-redirect-send-command-to-process	M-x ... RET
  Send COMMAND to PROCESS, with output to OUTPUT-BUFFER.
gnus-summary-limit-include-expunged M-x ... RET
  Display all the hidden articles that were expunged for low scores.
org-babel-do-key-sequence-in-edit-buffer M-x ... RET
  Read key sequence and execute the command in edit buffer.
read-char-from-minibuffer-insert-char M-x ... RET
  Insert the character you type into the minibuffer and exit
  minibuffer.
ansi-color-for-comint-mode-filter M-x ... RET
  Set `ansi-color-for-comint-mode' to symbol `filter'.
article-remove-leading-whitespace M-x ... RET
  Remove excessive whitespace from all headers.
article-strip-leading-blank-lines M-x ... RET
  Remove all blank lines from the beginning of the article.
citre-peek-make-current-tag-first M-x ... RET
  Put the current tag node in the first position.
delete-file-local-variable-prop-line M-x ... RET
  Delete all settings of file-local VARIABLE from the -*- line.
diff-lisp-mark-selected-text-as-a M-x ... RET
  Select a region to compare.
dired-mouse-find-file-other-window M-x ... RET
  In Dired, visit the file or directory name you click on in another
  window.
doc-view-center-page-horizontally M-x ... RET
  Center page horizontally when page is wider than window.
find-lisp-find-dired-subdirectories M-x ... RET
  Find all subdirectories of DIR.
gnus-article-outlook-repair-attribution	M-x ... RET
  Repair a broken attribution line.
gnus-bug		      M-x ... RET
  (not documented)
gnus-summary-beginning-of-article M-x ... RET
  Scroll the article back to the beginning.
gnus-summary-customize-parameters M-x ... RET
  Customize the group parameters of the current group.
highlight-changes-previous-change M-x ... RET
  Move to the beginning of the previous change, if in Highlight
  Changes mode.
ibuffer-do-kill-on-deletion-marks M-x ... RET
  Kill buffers marked for deletion as with `kill-this-buffer'.
ibuffer-set-filter-groups-by-mode M-x ... RET
  Set the current filter groups to filter by mode.
image-dired-display-thumbs-append M-x ... RET
  Append thumbnails to `image-dired-thumbnail-buffer'.
image-dired-jump-thumbnail-buffer M-x ... RET
  Jump to thumbnail buffer.
image-mode-copy-file-name-as-kill M-x ... RET
  Push the currently visited file name onto the kill ring.
image-transform-reset-to-original M-x ... RET
  Display the current image with the original (actual) size and
  rotation.
ispell-comment-or-string-at-point M-x ... RET
  Check the comment or string containing point for spelling errors.
magit-am		      M-x ... RET
  (not documented)
menu-bar--visual-line-mode-enable <menu-bar> <options> <line-wrapping> <word-wrap>
  Enable visual line mode.
menu-bar-showhide-fringe-ind-left <menu-bar> <options> <showhide> <showhide-fringe> <showhide-fringe-ind> <left>
  Display buffer boundaries and arrows in the left fringe.
menu-bar-showhide-fringe-ind-none <menu-bar> <options> <showhide> <showhide-fringe> <showhide-fringe-ind> <none>
  Do not display any buffer boundary indicators.
menu-bar-window-divider-customize <menu-bar> <options> <showhide> <showhide-window-divider> <customize>
  Show customization buffer for `window-divider' group.
mml-menu		      M-x ... RET
  (not documented)
  Value: (keymap "Attachments" (Attach\ File... menu-item "Attach File..." mml-attach…
org-ascii-convert-region-to-ascii M-x ... RET
  Assume region has Org syntax, and convert it to plain ASCII.
org-table-toggle-formula-debugger M-x ... RET
  Toggle the formula debugger in tables.
sis-prefix-override-buffer-enable M-x ... RET
  Disable prefix override in current buffer.
autothemer-generate-templates-filtered M-x ... RET
  Autogenerate customizations for unthemed faces matching REGEXP.
gnus-group-next-unread-group-same-level	M-x ... RET
  Go to next N'th unread newsgroup on the same level.
doc-view-previous-line-or-previous-page	M-x ... RET
  Scroll downward by ARG lines if possible, else goto previous page.
flyspell-check-previous-highlighted-word M-x ... RET
  Correct the closest previous word that is highlighted as misspelled.
isearch-highlight-lines-matching-regexp	M-x ... RET
  Exit Isearch mode and call `highlight-lines-matching-regexp'.
minibuffer-scroll-other-window-down M-x ... RET
  Run `scroll-other-window-down' from the minibuffer in its original
  window.
origami-open-node-recursively-till-depth M-x ... RET
  Opens childs from the node at POINT up to the given DEPTH, and
  collapses all other.
global-display-fill-column-indicator-mode M-x ... RET
  Toggle Display-Fill-Column-Indicator mode in all buffers.
  Value: t
gnus-group-sort-selected-groups-by-level M-x ... RET
  Sort the group buffer by group level.
gnus-group-sort-selected-groups-by-score M-x ... RET
  Sort the group buffer by group score.
markdown-check-change-for-wiki-link M-x ... RET
  Check region between FROM and TO for wiki links and re-fontify as
  needed.
org-roam-ui-remove-from-local-graph M-x ... RET
  Remove current node from the local graph. If not in local mode, open
  local-graph for this node.
diary-chinese-insert-anniversary-entry M-x ... RET
  Insert an anniversary diary entry for the Chinese date at point.
gnus-group-toggle-subscription-at-point	M-x ... RET
  Toggle subscription of the current group.
Buffer-menu-isearch-buffers-regexp M-x ... RET
  Search for a regexp through all marked buffers using Isearch.
Info-exit		      M-x ... RET
  (not documented)
acm-complete-or-expand-yas-snippet M-x ... RET
  Do complete or expand yasnippet, you need binding this funtion to
  `<tab>' in `yas-keymap'.
article-strip-multiple-blank-lines M-x ... RET
  Replace consecutive blank lines with one empty line.
benchmark-init/show-durations-tree M-x ... RET
  Show durations in call-tree.
comint-redirect-remove-redirection M-x ... RET
  End a Comint redirection.  See `comint-redirect-send-command'.
elisp-flymake--batch-compile-for-flymake M-x ... RET
  Helper for `elisp-flymake-byte-compile'.
gnus-article-treat-fold-newsgroups M-x ... RET
  Fold the Newsgroups and Followup-To message headers.
gnus-check-duplicate-killed-groups M-x ... RET
  Remove duplicates from the list of killed groups.
gnus-group-prev-unread-group-same-level	M-x ... RET
  Go to next N'th unread newsgroup on the same level.
gnus-summary-better-unread-subject M-x ... RET
  Select the first unread subject that has a score over the default
  score.
gnus-summary-edit-article-postpone M-x ... RET
  Exit the article editing without updating.
gnus-summary-limit-exclude-dormant M-x ... RET
  Hide all dormant articles.
html2text		      M-x ... RET
  (not documented)
image-dired-dired-display-external M-x ... RET
  Display file at point using an external viewer.
ispell-complete-word-interior-frag <menu-bar> <tools> <spell> <ispell-complete-word-interior-frag>, <menu-bar> <edit> <spell> <ispell-complete-word-interior-frag>
  Completes word matching character sequence inside a word.
magit-log		      M-x ... RET
  (not documented)
magit-run		      M-x ... RET
  (not documented)
magit-tag		      M-x ... RET
  (not documented)
markdown-fontify-buffer-wiki-links M-x ... RET
  Refontify all wiki links in the buffer.
markdown-reference-goto-definition M-x ... RET
  Jump to the definition of the reference at point or create it.
menu-bar-showhide-fringe-ind-mixed <menu-bar> <options> <showhide> <showhide-fringe> <showhide-fringe-ind> <mixed>
  Display top and bottom indicators in opposite fringes, arrows in
  right.
menu-bar-showhide-fringe-ind-right <menu-bar> <options> <showhide> <showhide-fringe> <showhide-fringe-ind> <right>
  Display buffer boundaries and arrows in the right fringe.
minibuffer-force-complete-and-exit M-x ... RET
  Complete the minibuffer with first of the matches and exit.
my/eval-current-elisp-func-and-run M-x ... RET
   eval-last-sexp 当前光标处的 function 并运行
org-babel-hide-result-toggle-maybe M-x ... RET
  Toggle visibility of result at point.
org-babel-switch-to-session-with-code M-x ... RET
  Switch to code buffer and display session.
org-table-fedit-toggle-coordinates M-x ... RET
  Toggle the display of coordinates in the referenced table.
osx-dictionary-open-dictionary.app M-x ... RET
  Open current searched `word' in Dictionary.app.
popwin:original-pop-to-last-buffer M-x ... RET
  Call `pop-to-buffer' for the last popup buffer without special
  displaying.
sis-prefix-override-buffer-disable M-x ... RET
  Disable prefix override in current buffer.
yas-about		      <menu-bar> <yasnippet> <About>
  (not documented)
yas/about		      M-x ... RET
  (not documented)
gnus-summary-open-group-with-article M-x ... RET
  Open a group containing the article with the given MESSAGE-ID.
gnus-summary-search-article-backward M-x ... RET
  Search for an article containing REGEXP backward.
image-dired-dired-edit-comment-and-tags	M-x ... RET
  Edit comment and tags of current or marked image files.
scroll-bar-toolkit-horizontal-scroll <horizontal-scroll-bar> <down-mouse-1>
  Handle event EVENT on horizontal scroll bar.
type-break-guesstimate-keystroke-threshold M-x ... RET
  Guess values for the minimum/maximum keystroke threshold for typing
  breaks.
gnus-summary-put-mark-as-read-next-unread M-x ... RET
  Mark the current article as readnext unread.
gnus-mime-buttonize-attachments-in-header M-x ... RET
  Show attachments as buttons in the end of the header of an article.
gnus-summary-limit-mark-excluded-as-read M-x ... RET
  Mark all unread excluded articles as read.
gnus-summary-put-mark-as-read-prev-unread M-x ... RET
  Mark the current article as readprevious unread.
gnus-summary-sort-by-most-recent-number	M-x ... RET
  Sort the summary buffer by most recent article number.
markdown-live-preview-switch-to-output M-x ... RET
  Turn on `markdown-live-preview-mode' and switch to output buffer.
markdown-unfontify-region-wiki-links M-x ... RET
  Remove wiki link faces from the region specified by FROM and TO.
Electric-command-history-redo-expression M-x ... RET
  Edit current history line in minibuffer and execute result.
View-scroll-page-backward-set-page-size	M-x ... RET
  Scroll backward prefix LINES lines in View mode, setting the "page
  size".
gnus-article-remove-leading-whitespace M-x ... RET
  Run `article-remove-leading-whitespace' in the article buffer.
gnus-article-strip-leading-blank-lines M-x ... RET
  Run `article-strip-leading-blank-lines' in the article buffer.
org-footnote-goto-previous-reference M-x ... RET
  Find the first closest (to point) reference of footnote with label
  LABEL.
2C-command		      <f2>
  (not documented)
article-remove-trailing-blank-lines M-x ... RET
  Remove all trailing blank lines from the article.
comint-previous-matching-input-from-input M-x ... RET
  Search backwards through input history for match for current input.
compilation-goto-in-progress-buffer M-x ... RET
  Switch to the compilation buffer.
edit-indirect-display-active-buffer M-x ... RET
  Display the active edit-indirect buffer of the region the point is
  in.
elisp-demos-help-find-demo-at-point M-x ... RET
  Find the demo at point.
elisp-refs-visit-match-other-window M-x ... RET
  Goto the search result at point, opening in another window.
gnus-summary-insert-ticked-articles M-x ... RET
  Insert ticked articles for this group into the current buffer.
gnus-summary-make-group-from-search M-x ... RET
  Make a persistent group from the current ephemeral search group.
gnus-summary-simplify-subject-query M-x ... RET
  Query where the respool algorithm would put this article.
gnus-summary-unmark-all-processable M-x ... RET
  Remove the process mark from all articles.
magit-diff		      M-x ... RET
  (not documented)
magit-pull		      M-x ... RET
  (not documented)
magit-push		      M-x ... RET
  (not documented)
org-table-recalculate-buffer-tables M-x ... RET
  Recalculate all tables in the current buffer.
osx-dictionary-search-word-at-point M-x ... RET
  Search word around and display result with buffer.
popwin:original-display-last-buffer M-x ... RET
  Call `display-buffer' for the last popup buffer without special
  displaying.
sis--inline-ret-check-to-deactivate M-x ... RET
  Deactivate the inline region overlay.
gnus-article-interactively-view-part M-x ... RET
  View MIME part N interactively, which is the numerical prefix.
gnus-group-sort-selected-groups-by-unread M-x ... RET
  Sort the group buffer by number of unread articles.
gnus-summary-catchup-and-goto-next-group M-x ... RET
  Mark all articles in this group as read and select the next group.
ff-mouse-find-other-file-other-window M-x ... RET
  Visit the file you point at in another window.
gnus-group-sort-selected-groups-by-method M-x ... RET
  Sort the group buffer alphabetically by back end name.
gnus-summary-catchup-and-goto-prev-group M-x ... RET
  Mark all articles in this group as read and select the previous
  group.
package-menu-filter-by-name-or-description M-x ... RET
  Filter the "*Packages*" buffer by NAME-OR-DESCRIPTION regexp.
shell-dynamic-complete-environment-variable M-x ... RET
  Dynamically complete the environment variable at point.
diff-add-change-log-entries-other-window M-x ... RET
  Iterate through the current diff and create ChangeLog entries.
image-dired-dired-with-window-configuration M-x ... RET
  Open directory DIR and create a default window configuration.
dictionary-mouse-popup-matching-words M-x ... RET
  Display entries matching the word at the cursor retrieved using
  EVENT.
gnus-article-strip-multiple-blank-lines	M-x ... RET
  Run `article-strip-multiple-blank-lines' in the article buffer.
gnus-summary-insert-dormant-articles M-x ... RET
  Insert all the dormant articles for this group into the current
  buffer.
ibuffer-mark-compressed-file-buffers M-x ... RET
  Mark buffers whose associated file is compressed.
magit-blame		      M-x ... RET
  (not documented)
magit-clone		      M-x ... RET
  (not documented)
magit-fetch		      M-x ... RET
  (not documented)
magit-merge		      M-x ... RET
  (not documented)
magit-notes		      M-x ... RET
  (not documented)
magit-patch		      M-x ... RET
  (not documented)
magit-reset		      M-x ... RET
  (not documented)
magit-stash		      M-x ... RET
  (not documented)
mairix-edit-saved-searches-customize M-x ... RET
  Edit the list of saved searches in a customization buffer.
markdown-outline-previous-same-level M-x ... RET
  Move to previous list item or heading of same level.
menu-bar--toggle-truncate-long-lines <menu-bar> <options> <line-wrapping> <truncate>
  Toggle long lines mode.
minibuffer-insert-file-name-at-point M-x ... RET
  Get a file name at point in original buffer and insert it to
  minibuffer.
my/siri-ocr		      M-x ... RET
  (not documented)
nonincremental-repeat-search-forward <menu-bar> <edit> <search> <repeat-search-fwd>
  Search forward for the previous search string or regexp.
org-table-toggle-coordinate-overlays M-x ... RET
  Toggle the display of Row/Column numbers in tables.
package-vc-install-selected-packages M-x ... RET
  Ensure packages specified in `package-vc-selected-packages' are
  installed.
gnus-summary-kill-same-subject-and-select M-x ... RET
  Mark articles which has the same subject as read, and then select
  the next.
gnus-summary-put-mark-as-ticked-next-unread M-x ... RET
  Mark the current article as tickednext unread.
gnus-summary-put-mark-as-ticked-prev-unread M-x ... RET
  Mark the current article as tickedprevious unread.
gnus-summary-put-mark-as-unread-next-unread M-x ... RET
  Mark the current article as unreadnext unread.
gnus-summary-put-mark-as-unread-prev-unread M-x ... RET
  Mark the current article as unreadprevious unread.
acm-complete		      M-x ... RET
  (not documented)
comint-dynamic-list-input-ring-select M-x ... RET
  Choose the input history entry that point is in or next to.
edebug-backtrace-hide-instrumentation M-x ... RET
  Hide Edebug's instrumentation in an Edebug Backtrace buffer.
edebug-backtrace-show-instrumentation M-x ... RET
  Show Edebug's instrumentation in an Edebug Backtrace buffer.
gnus-article-outlook-deuglify-article M-x ... RET
  Deuglify broken Outlook (Express) articles and redisplay.
gnus-article-remove-trailing-blank-lines M-x ... RET
  Run `article-remove-trailing-blank-lines' in the article buffer.
gnus-article-treat-suspicious-headers M-x ... RET
  Mark suspicious headers.
gnus-summary-force-verify-and-decrypt M-x ... RET
  Display buttons for signed/encrypted parts and verify/decrypt them.
gnus-summary-next-unread-same-subject M-x ... RET
  Select next unread article which has the same subject as current
  one.
gnus-summary-prev-unread-same-subject M-x ... RET
  Select previous unread article which has the same subject as current
  one.
isearch-toggle-specified-input-method M-x ... RET
  Select an input method and turn it on in interactive search.
magit-bisect		      M-x ... RET
  (not documented)
magit-branch		      M-x ... RET
  (not documented)
magit-bundle		      M-x ... RET
  (not documented)
magit-commit		      M-x ... RET
  (not documented)
magit-rebase		      M-x ... RET
  (not documented)
magit-remote		      M-x ... RET
  (not documented)
magit-revert		      M-x ... RET
  (not documented)
mairix-widget-search-based-on-article M-x ... RET
  Create mairix query based on current article using widgets.
menu-bar--wrap-long-lines-window-edge <menu-bar> <options> <line-wrapping> <window-wrap>
  Wrap long lines at window edge.
nonincremental-repeat-search-backward <menu-bar> <edit> <search> <repeat-search-back>
  Search backward for the previous search string or regexp.
org-capture-import-remember-templates M-x ... RET
  Set `org-capture-templates' to be similar to
  `org-remember-templates'.
read-expression-switch-to-completions M-x ... RET
  Select the completion list window while reading an expression.
calendar-mouse-view-other-diary-entries	M-x ... RET
  Pop up menu of diary entries from alternative file on mouse-selected
  date.
gnus-group-sort-selected-groups-by-alphabet M-x ... RET
  Sort the group buffer alphabetically by group name.
org-insert-todo-heading-respect-content	M-x ... RET
  Insert TODO heading with `org-insert-heading-respect-content' set to
  t.
dired-minibuffer-default-add-shell-commands M-x ... RET
  Return a list of all commands associated with current dired files.
minibuffer-default-add-dired-shell-commands M-x ... RET
  Return a list of all commands associated with current dired files.
gnus-summary-put-mark-as-dormant-next-unread M-x ... RET
  Mark the current article as dormantnext unread.
amread-voice-reader-switch-language-voice M-x ... RET
  Switch voice reader LANGUAGE or voice.
gnus-summary-put-mark-as-dormant-prev-unread M-x ... RET
  Mark the current article as dormantprevious unread.
color-rg-back		      M-x ... RET
  (not documented)
color-rg-quit		      M-x ... RET
  (not documented)
comint-history-isearch-backward-regexp M-x ... RET
  Search for a regular expression backward in input history using
  Isearch.
gnus-group-clear-data-on-native-groups M-x ... RET
  Clear all marks and read ranges from all native groups.
gnus-msg-mail		      M-x ... RET
  (not documented)
gnus-summary-toggle-display-buttonized M-x ... RET
  Toggle the buttonizing of the article buffer.
magit-subtree		      M-x ... RET
  (not documented)
menu-bar-showhide-fringe-ind-customize <menu-bar> <options> <showhide> <showhide-fringe> <showhide-fringe-ind> <customize>
  Show customization buffer for `indicate-buffer-boundaries'.
rfc-mode-quit		      M-x ... RET
  (not documented)
standard-themes-preview-colors-current M-x ... RET
  Call `standard-themes-preview-colors' for the current Standard
  theme.
gnus-group-sort-selected-groups-by-real-name M-x ... RET
  Sort the group buffer alphabetically by real group name.
ediff-merge-directory-revisions-with-ancestor <menu-bar> <tools> <ediff-merge> <ediff-merge-dir-revisions-with-ancestor>
  Run Ediff on DIR1 and merge its files with their revisions and
  ancestors.
markdown-toggle-fontify-code-blocks-natively M-x ... RET
  Toggle the native fontification of code blocks.
message-generate-unsubscribed-mail-followup-to M-x ... RET
  Insert a reasonable MFT header in a post to an unsubscribed list.
message-insert-disposition-notification-to M-x ... RET
  Request a disposition notification (return receipt) to this message.
gnus-summary-first-unseen-or-unread-subject M-x ... RET
  Place the point on the subject line of the first unseen and unread
  article.
benchmark-init/show-durations-tabulated	M-x ... RET
  Show the benchmark results in a sorted table.
gnus-pick-mode		      M-x ... RET
  (not documented)
gnus-summary-limit-to-display-predicate	M-x ... RET
  Limit the summary buffer to the predicated in the `display' group
  parameter.
hide-sublevels		      M-x ... RET
  (not documented)
magit-dispatch		      M-x ... RET
  (not documented)
magit-shortlog		      M-x ... RET
  (not documented)
magit-worktree		      M-x ... RET
  (not documented)
mail-insert-disposition-notification-to	M-x ... RET
  Insert a Disposition-Notification-To header, if it doesn't already
  exist.
menu-bar-showhide-fringe-menu-customize	<menu-bar> <options> <showhide> <showhide-fringe> <customize>
  Show customization buffer for `fringe-mode'.
my-side-buffer		      M-x ... RET
  (not documented)
occur-mode-goto-occurrence-other-window	M-x ... RET
  Go to the occurrence the current line describes, in another window.
open-init-file		      M-x ... RET
  (not documented)
org-toggle-custom-properties-visibility	M-x ... RET
  Display or hide properties in `org-custom-properties'.
package-menu-mark-obsolete-for-deletion	M-x ... RET
  Mark all obsolete packages for deletion.
wucuo-create-aspell-personal-dictionary	M-x ... RET
  Create aspell personal dictionary which is utf-8 encoded plain text
  file.
yas/minor-mode		      M-x ... RET
  (not documented)
  Value: t
gnus-summary-put-mark-as-expirable-next-unread M-x ... RET
  Mark the current article as expirablenext unread.
gnus-summary-put-mark-as-expirable-prev-unread M-x ... RET
  Mark the current article as expirableprevious unread.
blink-search-do		      M-x ... RET
  (not documented)
comint-dynamic-list-filename-completions M-x ... RET
  Display a list of possible completions for the filename at point.
copy-dir-locals-to-file-locals-prop-line M-x ... RET
  Copy directory-local variables to the -*- line.
ebrowse-tags-find-definition-other-frame M-x ... RET
  Find definition of member at point in other frame.
ebrowse-tags-view-definition-other-frame M-x ... RET
  View definition of member at point in other frame.
ediff-customize		      <menu-bar> <tools> <compare> <ediff-misc> <ediff-cust>
  (not documented)
epa-file-enable		      M-x ... RET
  (not documented)
find-lisp-find-dired-subdirs-other-window M-x ... RET
  Same as `find-lisp-find-dired-subdirectories', but use another
  window.
gnus-demon-init		      M-x ... RET
  (not documented)
gnus-draft-mode		      M-x ... RET
  (not documented)
gnus-group-mail		      M-x ... RET
  (not documented)
gnus-group-news		      M-x ... RET
  (not documented)
gnus-topic-mode		      M-x ... RET
  (not documented)
lisp-eval-defun		      M-x ... RET
  (not documented)
magit-gitignore		      M-x ... RET
  (not documented)
magit-show-refs		      M-x ... RET
  (not documented)
magit-submodule		      M-x ... RET
  (not documented)
menu-bar--display-line-numbers-mode-none <menu-bar> <options> <showhide> <display-line-numbers> <none>
  Disable line numbers.
menu-bar-bottom-and-right-window-divider <menu-bar> <options> <showhide> <showhide-window-divider> <bottom-and-right>
  Display dividers on the bottom and right of each window.
org-set-visibility-according-to-property M-x ... RET
  Switch subtree visibility according to VISIBILITY property.
profiler-report-render-reversed-calltree M-x ... RET
  Render reversed calltree view.
reftex-citation		      M-x ... RET
  (not documented)
org-archive-subtree-default-with-confirmation M-x ... RET
  Archive the current subtree with the default command.
message-make-html-message-with-image-files M-x ... RET
  Make a message containing the current dired-marked image files.
ebrowse-tags-find-declaration-other-frame M-x ... RET
  Find definition of member at point in other frame.
ebrowse-tags-find-definition-other-window M-x ... RET
  Find definition of member at point in other window.
ebrowse-tags-view-definition-other-window M-x ... RET
  View definition of member at point in other window.
epa-file-disable	      M-x ... RET
  (not documented)
gnus-binary-mode	      M-x ... RET
  (not documented)
gnus-score-close	      M-x ... RET
  (not documented)
gnus-uu-mark-all	      M-x ... RET
  (not documented)
magit-blame-echo	      M-x ... RET
  (not documented)
magit-stash-push	      M-x ... RET
  (not documented)
ns-do-hide-emacs	      s-h
  (not documented)
wucuo-create-hunspell-personal-dictionary M-x ... RET
  Create hunspell personal dictionary which is utf-8 encoded plain
  text file.
achievements-init	      M-x ... RET
  (not documented)
achievements-mode	      M-x ... RET
  (not documented)
  Value: t
acm-doc-scroll-up	      M-x ... RET
  (not documented)
blink-search-copy	      M-x ... RET
  (not documented)
blink-search-quit	      M-x ... RET
  (not documented)
color-rg-unfilter	      M-x ... RET
  (not documented)
ebrowse-tags-find-declaration-other-window M-x ... RET
  Find declaration of member at point in other window.
gnus-button-reply	      M-x ... RET
  (not documented)
gnus-demon-cancel	      M-x ... RET
  (not documented)
gnus-summary-repeat-search-article-forward M-x ... RET
  Repeat the previous search forwards.
gnus-uu-decode-uu	      M-x ... RET
  (not documented)
gnus-uu-mark-over	      M-x ... RET
  (not documented)
gnus-uu-post-news	      M-x ... RET
  (not documented)
ibuffer-visit-buffer-other-window-noselect M-x ... RET
  Visit the buffer on this line in another window, but don't select
  it.
lsp-bridge-rename	      M-x ... RET
  (not documented)
magit-cherry-pick	      M-x ... RET
  (not documented)
magit-log-refresh	      M-x ... RET
  (not documented)
magit-patch-apply	      M-x ... RET
  (not documented)
menu-bar--display-line-numbers-mode-visual <menu-bar> <options> <showhide> <display-line-numbers> <visual>
  Turn on visual line number mode.
my/open-in-vscode	      M-x ... RET
  (not documented)
my/siri-translate	      M-x ... RET
  (not documented)
ns-do-hide-others	      M-s-h, s-H
  (not documented)
table-unrecognize	      M-x ... RET
  (not documented)
yas-abort-snippet	      M-x ... RET
  (not documented)
yas/abort-snippet	      M-x ... RET
  (not documented)
gnus-summary-limit-include-matching-articles M-x ... RET
  Display all the hidden articles that have HEADERs that match REGEXP.
acm-icon-fetch-all	      M-x ... RET
  (not documented)
gnus-summary-repeat-search-article-backward M-x ... RET
  Repeat the previous search backwards.
gnus-summary-reply	      M-x ... RET
  (not documented)
magit-diff-refresh	      M-x ... RET
  (not documented)
magit-patch-create	      M-x ... RET
  (not documented)
ns-paste-secondary	      s-y
  (not documented)
yas/load-directory	      M-x ... RET
  (not documented)
magit-switch-to-repository-buffer-other-frame M-x ... RET
  Switch to a Magit buffer belonging to the current Git repository.
acm-doc-scroll-down	      M-x ... RET
  (not documented)
blink-search-action	      M-x ... RET
  (not documented)
blink-search-parent	      M-x ... RET
  (not documented)
edmacro-finish-edit	      M-x ... RET
  (not documented)
gnus-summary-limit-exclude-childless-dormant M-x ... RET
  Hide all dormant articles that have no children.
gnus-uu-decode-save	      M-x ... RET
  (not documented)
gnus-uu-decode-yenc	      M-x ... RET
  (not documented)
gnus-uu-mark-buffer	      M-x ... RET
  (not documented)
gnus-uu-mark-region	      M-x ... RET
  (not documented)
gnus-uu-mark-series	      M-x ... RET
  (not documented)
gnus-uu-mark-sparse	      M-x ... RET
  (not documented)
gnus-uu-mark-thread	      M-x ... RET
  (not documented)
lsp-bridge-find-def	      M-x ... RET
  (not documented)
lsp-bridge-ref-quit	      M-x ... RET
  (not documented)
magit-blame-removal	      M-x ... RET
  (not documented)
magit-blame-reverse	      M-x ... RET
  (not documented)
magit-bundle-import	      M-x ... RET
  (not documented)
magit-commit-absorb	      M-x ... RET
  (not documented)
magit-fetch-modules	      M-x ... RET
  (not documented)
magit-file-dispatch	      M-x ... RET
  (not documented)
magit-git-mergetool	      M-x ... RET
  (not documented)
magit-submodule-add	      M-x ... RET
  (not documented)
menu-bar--display-line-numbers-mode-absolute <menu-bar> <options> <showhide> <display-line-numbers> <absolute>
  Turn on absolute line number mode.
menu-bar--display-line-numbers-mode-relative <menu-bar> <options> <showhide> <display-line-numbers> <relative>
  Turn on relative line number mode.
menu-bar-showhide-fringe-menu-customize-left <menu-bar> <options> <showhide> <showhide-fringe> <left>
  Display fringes only on the left of each window.
my/open-in-obsidian	      M-x ... RET
  (not documented)
ns-put-working-text	      M-x ... RET
  (not documented)
magit-switch-to-repository-buffer-other-window M-x ... RET
  Switch to a Magit buffer belonging to the current Git repository.
blink-search-preview	      M-x ... RET
  (not documented)
color-rg-delete-line	      M-x ... RET
  (not documented)
company-manual-begin	      M-x ... RET
  (not documented)
find-orphan-function	      M-x ... RET
  (not documented)
git-gutter:mark-hunk	      M-x ... RET
  (not documented)
gnus-group-customize	      M-x ... RET
  (not documented)
gnus-group-post-news	      M-x ... RET
  (not documented)
gnus-score-customize	      M-x ... RET
  (not documented)
lsp-bridge-find-impl	      M-x ... RET
  (not documented)
magit-blame-addition	      M-x ... RET
  (not documented)
magit-push-to-remote	      M-x ... RET
  (not documented)
magit-subtree-export	      M-x ... RET
  (not documented)
magit-subtree-import	      M-x ... RET
  (not documented)
menu-bar-showhide-fringe-menu-customize-reset <menu-bar> <options> <showhide> <showhide-fringe> <default>
  Reset the fringe mode: display fringes on both sides of a window.
menu-bar-showhide-fringe-menu-customize-right <menu-bar> <options> <showhide> <showhide-fringe> <right>
  Display fringes only on the right of each window.
my/go-to-random-line	      M-x ... RET
  (not documented)
open-early-init-file	      M-x ... RET
  (not documented)
package-menu-refresh	      M-x ... RET
  (not documented)
secrets-show-secrets	      M-x ... RET
  (not documented)
whois-reverse-lookup	      M-x ... RET
  (not documented)
blink-search-continue	      M-x ... RET
  (not documented)
blink-search-quick-do	      M-x ... RET
  (not documented)
color-rg-recover-line	      M-x ... RET
  (not documented)
color-rg-search-input	      M-x ... RET
  (not documented)
diff-lisp-set-a-and-b	      s-/
  (not documented)
  Value: nil
doc-view-show-tooltip	      M-x ... RET
  (not documented)
gnus-group-send-queue	      M-x ... RET
  (not documented)
gnus-pick-line-number	      M-x ... RET
  (not documented)
gnus-summary-followup	      M-x ... RET
  (not documented)
gnus-uu-decode-binhex	      M-x ... RET
  (not documented)
gnus-uu-decode-unshar	      M-x ... RET
  (not documented)
gnus-uu-unmark-region	      M-x ... RET
  (not documented)
gnus-uu-unmark-thread	      M-x ... RET
  (not documented)
magit-push-implicitly	      M-x ... RET
  (not documented)
magit-sparse-checkout	      M-x ... RET
  (not documented)
ns-unput-working-text	      M-x ... RET
  (not documented)
org-cycle-set-visibility-according-to-property M-x ... RET
  Switch subtree visibility according to VISIBILITY property.
color-rg-apply-changed	      M-x ... RET
  (not documented)
color-rg-search-symbol	      M-x ... RET
  (not documented)
doc-view-revert-buffer	      M-x ... RET
  (not documented)
global-lsp-bridge-mode	      M-x ... RET
  (not documented)
gnus-score-flush-cache	      M-x ... RET
  (not documented)
gnus-summary-post-news	      M-x ... RET
  (not documented)
gnus-summary-set-score	      M-x ... RET
  (not documented)
gnus-uu-decode-uu-view	      M-x ... RET
  (not documented)
gnus-uu-mark-by-regexp	      M-x ... RET
  (not documented)
lsp-bridge-code-action	      M-x ... RET
  (not documented)
lsp-bridge-code-format	      M-x ... RET
  (not documented)
magit-branch-configure	      M-x ... RET
  (not documented)
magit-commit-autofixup	      M-x ... RET
  (not documented)
magit-remote-configure	      M-x ... RET
  (not documented)
magit-submodule-update	      M-x ... RET
  (not documented)
menu-bar-select-buffer	      M-x ... RET
  (not documented)
menu-bar-showhide-fringe-menu-customize-disable	<menu-bar> <options> <showhide> <showhide-fringe> <none>
  Do not display window fringes.
ns-do-emacs-info-panel	      M-x ... RET
  (not documented)
table-unrecognize-cell	      M-x ... RET
  (not documented)
color-rg-jump-next-file	      M-x ... RET
  (not documented)
color-rg-jump-prev-file	      M-x ... RET
  (not documented)
color-rg-recover-buffer	      M-x ... RET
  (not documented)
color-rg-search-project	      M-x ... RET
  (not documented)
gnus-summary-save-in-vm	      M-x ... RET
  (not documented)
gnus-summary-wide-reply	      M-x ... RET
  (not documented)
lsp-bridge-profile-dump	      M-x ... RET
  (not documented)
lsp-bridge-ref-unfilter	      M-x ... RET
  (not documented)
message-mode-field-menu	      M-x ... RET
  (not documented)
  Value: (keymap "Field" (To menu-item "To" message-goto-to) (From menu-item "From" m…
org-babel-execute-maybe	      M-x ... RET
  (not documented)
table-unrecognize-table	      M-x ... RET
  (not documented)
yas/load-snippet-buffer	      M-x ... RET
  (not documented)
gnus-cache-enter-article      M-x ... RET
  (not documented)
gnus-score-delta-default      M-x ... RET
  (not documented)
gnus-summary-lower-score      M-x ... RET
  (not documented)
gnus-summary-raise-score      M-x ... RET
  (not documented)
gnus-uu-decode-save-view      M-x ... RET
  (not documented)
gnus-uu-unmark-by-regexp      M-x ... RET
  (not documented)
lsp-bridge-ref-open-file      M-x ... RET
  (not documented)
magit-pull-from-upstream      M-x ... RET
  (not documented)
magit-submodule-populate      M-x ... RET
  (not documented)
magit-submodule-register      M-x ... RET
  (not documented)
make-directory-autoloads      M-x ... RET
  (not documented)
menu-bar-showhide-tool-bar-menu-customize-disable M-x ... RET
  Do not display tool bars.
my/color-rg-search-input      M-x ... RET
  (not documented)
blink-search-preview-next     M-x ... RET
  (not documented)
blink-search-preview-prev     M-x ... RET
  (not documented)
color-rg-delete-all-lines     M-x ... RET
  (not documented)
gnus-cache-remove-article     M-x ... RET
  (not documented)
gnus-score-edit-all-score     M-x ... RET
  (not documented)
gnus-summary-lower-thread     M-x ... RET
  (not documented)
gnus-summary-mail-forward     M-x ... RET
  (not documented)
gnus-summary-post-forward     M-x ... RET
  (not documented)
gnus-summary-raise-thread     M-x ... RET
  (not documented)
gnus-uu-decode-postscript     M-x ... RET
  (not documented)
magit-fetch-from-upstream     M-x ... RET
  (not documented)
reftex-index-phrases-mode     M-x ... RET
  (not documented)
reftex-isearch-minor-mode     M-x ... RET
  (not documented)
color-rg-beginning-of-line    M-x ... RET
  (not documented)
color-rg-jump-next-keyword    M-x ... RET
  (not documented)
color-rg-jump-prev-keyword    M-x ... RET
  (not documented)
gnus-article-hide-citation    M-x ... RET
  (not documented)
gnus-score-followup-thread    M-x ... RET
  (not documented)
gnus-summary-current-score    M-x ... RET
  (not documented)
gnus-uu-decode-binhex-view    M-x ... RET
  (not documented)
gnus-uu-decode-unshar-view    M-x ... RET
  (not documented)
gnus-uu-decode-uu-and-save    M-x ... RET
  (not documented)
gnus-uu-invert-processable    M-x ... RET
  (not documented)
lsp-bridge-diagnostic-copy    M-x ... RET
  (not documented)
lsp-bridge-diagnostic-list    M-x ... RET
  (not documented)
lsp-bridge-find-references    M-x ... RET
  (not documented)
lsp-bridge-ref-delete-line    M-x ... RET
  (not documented)
magit-pull-from-pushremote    M-x ... RET
  (not documented)
magit-rebase-onto-upstream    M-x ... RET
  (not documented)
magit-submodule-unpopulate    M-x ... RET
  (not documented)
update-directory-autoloads    M-x ... RET
  (not documented)
blink-search-icon-fetch-all   M-x ... RET
  (not documented)
color-rg-filter-match-files   M-x ... RET
  (not documented)
color-rg-open-file-and-stay   M-x ... RET
  (not documented)
gnus-possibly-score-headers   M-x ... RET
  (not documented)
gnus-score-followup-article   M-x ... RET
  (not documented)
gnus-sieve-article-add-rule   M-x ... RET
  (not documented)
gnus-summary-cancel-article   M-x ... RET
  (not documented)
gnus-summary-increase-score   M-x ... RET
  (not documented)
gnus-summary-resend-message   M-x ... RET
  (not documented)
gnus-summary-save-in-folder   M-x ... RET
  (not documented)
gnus-uu-digest-mail-forward   M-x ... RET
  (not documented)
gnus-uu-digest-post-forward   M-x ... RET
  (not documented)
lsp-bridge-ref-recover-line   M-x ... RET
  (not documented)
lsp-bridge-tabnine-complete   M-x ... RET
  (not documented)
magit-fetch-from-pushremote   M-x ... RET
  (not documented)
magit-submodule-synchronize   M-x ... RET
  (not documented)
menu-bar-showhide-tool-bar-menu-customize-enable-top M-x ... RET
  Display tool bars on the top side.
message-simplify-recipients   M-x ... RET
  (not documented)
my/emacs-package-dependency   M-x ... RET
  (not documented)
ns-copy-including-secondary   <menu-bar> <edit> <copy>
  (not documented)
org-setup-comments-handling   M-x ... RET
  (not documented)
color-rg-insert-current-line  M-x ... RET
  (not documented)
color-rg-switch-to-edit-mode  M-x ... RET
  (not documented)
color-rg-switch-to-view-mode  M-x ... RET
  (not documented)
gnus-summary-save-article-vm  M-x ... RET
  (not documented)
lsp-bridge-diagnostic-ignore  M-x ... RET
  (not documented)
lsp-bridge-install-omnisharp  M-x ... RET
  (not documented)
lsp-bridge-ref-apply-changed  M-x ... RET
  (not documented)
magit-rebase-onto-pushremote  M-x ... RET
  (not documented)
menu-bar-showhide-tool-bar-menu-customize-enable-left M-x ... RET
  Display tool bars on the left side.
ns-do-show-character-palette  C-s-SPC
  (not documented)
color-rg-filter-match-results M-x ... RET
  (not documented)
color-rg-search-project-rails M-x ... RET
  (not documented)
git-gutter:start-update-timer M-x ... RET
  (not documented)
gnus-summary-followup-to-mail M-x ... RET
  (not documented)
lsp-bridge-ref-jump-next-file M-x ... RET
  (not documented)
lsp-bridge-ref-jump-prev-file M-x ... RET
  (not documented)
lsp-bridge-ref-recover-buffer M-x ... RET
  (not documented)
menu-bar-showhide-tool-bar-menu-customize-enable-right M-x ... RET
  Display tool bars on the right side.
achievements-list-achievements M-x ... RET
  (not documented)
color-rg-filter-mismatch-files M-x ... RET
  (not documented)
find-orphan-function-in-buffer M-x ... RET
  (not documented)
git-gutter:cancel-update-timer M-x ... RET
  (not documented)
gnus-summary-mail-other-window M-x ... RET
  (not documented)
gnus-summary-news-other-window M-x ... RET
  (not documented)
gnus-summary-supersede-article M-x ... RET
  (not documented)
gnus-summary-toggle-truncation M-x ... RET
  (not documented)
gnus-uu-decode-postscript-view M-x ... RET
  (not documented)
gnus-uu-decode-unshar-and-save M-x ... RET
  (not documented)
lsp-bridge-call-hierarchy-next M-x ... RET
  (not documented)
lsp-bridge-call-hierarchy-prev M-x ... RET
  (not documented)
lsp-bridge-call-hierarchy-quit M-x ... RET
  (not documented)
lsp-bridge-popup-complete-menu M-x ... RET
  (not documented)
lsp-bridge-popup-documentation M-x ... RET
  (not documented)
magit-push-current-to-upstream M-x ... RET
  (not documented)
menu-bar-showhide-tool-bar-menu-customize-enable-bottom	M-x ... RET
  Display tool bars on the bottom side.
org-clock-toggle-auto-clockout M-x ... RET
  (not documented)
org-require-autoloaded-modules M-x ... RET
  (not documented)
gnus-article-fill-cited-article	M-x ... RET
  (not documented)
gnus-article-highlight-citation	M-x ... RET
  (not documented)
gnus-summary-lower-same-subject	M-x ... RET
  (not documented)
gnus-summary-raise-same-subject	M-x ... RET
  (not documented)
gnus-uu-decode-uu-and-save-view	M-x ... RET
  (not documented)
lsp-bridge-diagnostic-jump-next	M-x ... RET
  (not documented)
lsp-bridge-diagnostic-jump-prev	M-x ... RET
  (not documented)
lsp-bridge-ref-delete-all-lines	M-x ... RET
  (not documented)
lsp-bridge-signature-help-fetch	M-x ... RET
  (not documented)
read-regexp-toggle-case-folding	M-x ... RET
  (not documented)
recentf-open-most-recent-file-0	M-x ... RET
  (not documented)
recentf-open-most-recent-file-1	M-x ... RET
  (not documented)
recentf-open-most-recent-file-2	M-x ... RET
  (not documented)
recentf-open-most-recent-file-3	M-x ... RET
  (not documented)
recentf-open-most-recent-file-4	M-x ... RET
  (not documented)
recentf-open-most-recent-file-5	M-x ... RET
  (not documented)
recentf-open-most-recent-file-6	M-x ... RET
  (not documented)
recentf-open-most-recent-file-7	M-x ... RET
  (not documented)
recentf-open-most-recent-file-8	M-x ... RET
  (not documented)
recentf-open-most-recent-file-9	M-x ... RET
  (not documented)
blink-search-backend-select-next M-x ... RET
  (not documented)
blink-search-backend-select-prev M-x ... RET
  (not documented)
color-rg-filter-mismatch-results M-x ... RET
  (not documented)
color-rg-search-input-in-project M-x ... RET
  (not documented)
color-rg-search-symbol-with-type M-x ... RET
  (not documented)
gnus-article-hide-citation-maybe M-x ... RET
  (not documented)
gnus-summary-reply-with-original M-x ... RET
  (not documented)
gnus-summary-resend-bounced-mail M-x ... RET
  (not documented)
lsp-bridge-call-hierarchy--popup M-x ... RET
  (not documented)
lsp-bridge-call-hierarchy-select M-x ... RET
  (not documented)
lsp-bridge-find-def-other-window M-x ... RET
  (not documented)
lsp-bridge-ref-beginning-of-line M-x ... RET
  (not documented)
lsp-bridge-ref-jump-next-keyword M-x ... RET
  (not documented)
lsp-bridge-ref-jump-prev-keyword M-x ... RET
  (not documented)
magit-push-current-to-pushremote M-x ... RET
  (not documented)
color-rg-remove-line-from-results M-x ... RET
  (not documented)
color-rg-search-project-with-type M-x ... RET
  (not documented)
color-rg-search-symbol-in-project M-x ... RET
  (not documented)
find-orphan-function-in-directory M-x ... RET
  (not documented)
gnus-article-edit-mode-field-menu M-x ... RET
  (not documented)
  Value: (keymap "Field" (Fetch\ To menu-item "Fetch To" message-insert-to) (Fetch\ N…
gnus-summary-limit-include-cached M-x ... RET
  (not documented)
lsp-bridge-code-action-popup-quit M-x ... RET
  (not documented)
lsp-bridge-find-impl-other-window M-x ... RET
  (not documented)
lsp-bridge-ref-filter-match-files M-x ... RET
  (not documented)
lsp-bridge-ref-open-file-and-stay M-x ... RET
  (not documented)
lsp-bridge-workspace-list-symbols M-x ... RET
  (not documented)
blink-search-candidate-select-next M-x ... RET
  (not documented)
blink-search-candidate-select-prev M-x ... RET
  (not documented)
edebug-install-read-eval-functions M-x ... RET
  (not documented)
gnus-article-fill-cited-long-lines M-x ... RET
  (not documented)
gnus-uu-decode-postscript-and-save M-x ... RET
  (not documented)
lsp-bridge-incoming-call-hierarchy M-x ... RET
  (not documented)
lsp-bridge-outgoing-call-hierarchy M-x ... RET
  (not documented)
lsp-bridge-ref-insert-current-line M-x ... RET
  (not documented)
lsp-bridge-ref-replace-all-matches M-x ... RET
  (not documented)
lsp-bridge-ref-switch-to-edit-mode M-x ... RET
  (not documented)
lsp-bridge-ref-switch-to-view-mode M-x ... RET
  (not documented)
gnus-article-read-summary-send-keys M-x ... RET
  (not documented)
gnus-summary-followup-with-original M-x ... RET
  (not documented)
gnus-summary-insert-cached-articles M-x ... RET
  (not documented)
gnus-uu-decode-unshar-and-save-view M-x ... RET
  (not documented)
lsp-bridge-code-action-popup-select M-x ... RET
  (not documented)
lsp-bridge-ref-filter-match-results M-x ... RET
  (not documented)
edebug-uninstall-read-eval-functions M-x ... RET
  (not documented)
lsp-bridge-ref-filter-mismatch-files M-x ... RET
  (not documented)
color-rg-search-input-in-current-file M-x ... RET
  (not documented)
gnus-summary-wide-reply-with-original M-x ... RET
  (not documented)
color-rg-search-symbol-in-current-file M-x ... RET
  (not documented)
isearch-nonincremental-exit-minibuffer M-x ... RET
  (not documented)
lsp-bridge-ref-filter-mismatch-results M-x ... RET
  (not documented)
color-rg-search-project-rails-with-type	M-x ... RET
  (not documented)
gnus-article-hide-citation-in-followups	M-x ... RET
  (not documented)
gnus-uu-decode-postscript-and-save-view	M-x ... RET
  (not documented)
lsp-bridge-ref-remove-line-from-results	M-x ... RET
  (not documented)
blink-search-candidate-group-select-next M-x ... RET
  (not documented)
blink-search-candidate-group-select-prev M-x ... RET
  (not documented)
lsp-bridge-popup-documentation-scroll-up M-x ... RET
  (not documented)
pangu-spacing-org-mode-at-special-region M-x ... RET
  (not documented)
dired-name-filter-only-show-matched-lines M-x ... RET
  (not documented)
gnus-summary-lower-same-subject-and-select M-x ... RET
  (not documented)
gnus-summary-raise-same-subject-and-select M-x ... RET
  (not documented)
lsp-bridge-popup-documentation-scroll-down M-x ... RET
  (not documented)
gnus-summary-followup-to-mail-with-original M-x ... RET
  (not documented)
